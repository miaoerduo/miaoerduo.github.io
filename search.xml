<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>随笔</title>
    <url>/2020/04/12/2020-04-11/</url>
    <content><![CDATA[<p>这是我花了差不多2天完成的图。期间在女票的建议下，对各个地方的比例都做了大量的修改，才得到目前的效果。</p>
<p><img data-src="http://cdn.miaoerduo.com/images/illustration/2020-04-11.png" alt="随笔" /></p>
<p>这个手是实在太难画了。光影的效果，目前还需要再练习。接下来的一段时间，期待生活会发生一些改变。</p>
]]></content>
      <categories>
        <category>插画</category>
      </categories>
  </entry>
  <entry>
    <title>《来自新世界》</title>
    <url>/2020/04/08/%E8%AF%BB%E4%B9%A6%E7%AC%94%E8%AE%B0-%E6%9D%A5%E8%87%AA%E6%96%B0%E4%B8%96%E7%95%8C/</url>
    <content><![CDATA[<p>看完之前的《如何有效阅读一本书》之后，我就很想把这本《来自新世界》再看一遍。现在每天上下班通勤时间接近3h，正好用来看看书。</p>
<p>第一次看这本书也是在Kindle上。忘记是什么原因了，似乎是豆瓣上看到了这本书的评分还挺高，内容什么的并没有看。等到翻开的时候，发觉这是本偏古风的书，主人翁和一群小伙伴一起快乐的成长。随后又提到咒术，发现这居然是本带有法术的书。等到后来看到拟蓑白之后，咦，这个剧情真不简单。居然是本描写未来的科幻书。</p>
<p>后来没花几天就读完了。强大而又备受限制的咒术，各怀鬼胎的化鼠，变态发育的气球狗……各式各样的生物被平淡无奇的点出，早已习以为常的存在，他们才是故事的原住民，我们才是访客。</p>
<p>最后人类面临浩劫，而敌人却恰恰是对人类唯唯诺诺的化鼠一行。主人公等人最终在另一波化鼠的协助下在遗迹寻找最后的救赎。当然最终是人类战胜了化鼠。</p>
<p>在整个故事里面，最令我感觉意外的就是心里暗示的存在。不能伤害同类，这既是一种保护，同时也是最可怕的枷锁。</p>
<p>现在是我第二遍读这本书，着实发现了很多不一样的东西。</p>
<p>本书的真实性。在第二遍阅读的时候，我其实已经知道了大人会通过各种手段抹杀存在和记忆。那么本书中所有的重要决定似乎都是女主做出的。而这，究竟是不是他自己做出的呢。</p>
<p>在小学毕业进入全人班的时候，长老会试探孩子们的同理心。第一次看的时候感觉这个场景稍微有点奇怪，第二遍的时候才恍然大悟，这原来是在进行筛选。同时，不断的有人消失但是大家却毫无知觉，这个虽然在第一遍的时候就觉得有问题，第二遍的时候才能真切的感受到小孩子被不当做人类看待的现实。</p>
<p><del>过了几天</del></p>
<p>终于看到了抓拟蓑白的地方了。小女主通过虎蛱蟹的帮助（这种螃蟹体型堪比人了，蓑白的大小也有小孩那么大），终于抓到了拟蓑白，然后从中问到了这个世界的真相。世界在千年前就基本上毁灭了。由于部分人得到的超能力，其中有个小男孩利用超能力作了很多恶，激起了全世界普通人对能力者的反击，导致能力者数目锐减。后来能力者中拥有了堪比核弹的超能力，将普通人上击垮。社会演变成多种能力者的部落。</p>
<p>掠夺者因暴躁而消亡，独裁者因残酷额湮灭。幸存者将人类自降为动物，然后使用动物的基因和行为学来约束人类本身。这时候最可怕的是任何一个能力者都拥有威力巨大的咒力，只要有一个人心术不正，或者一瞬间的暴戾，都会给社会带来巨大的无法挽回的损失。这也是小说一直暗示的对学生们进行严苛选择的原因——抹杀一切破坏的种子。</p>
]]></content>
      <categories>
        <category>读书笔记</category>
      </categories>
  </entry>
  <entry>
    <title>要开始记笔记啦——《如何有效阅读一本书》</title>
    <url>/2020/04/02/it-is-time-to-take-notes/</url>
    <content><![CDATA[<p>最近读了《如何有效阅读一本书》。书中有一个观点，让我觉得可以一试。</p>
<p>记笔记可以不那么正规，可以天马行空，可以只记录比如“我觉得这个挺有意思”，“好像很有道理”之类的无营养的内容。也可以和书的内容完全无关。总之记录这一行为本身，就已经是不错的一件事情了。</p>
<p>做笔记，可以让我们对所阅读的书加深印象，不仅仅是对书的内容更好的掌握，也能最低限度的让我们更容易回想起，阅读这本书的感受，为啥要读这本书等等。让我们在阅读中留下一些痕迹。而且在若干年后，等我们在翻开旧笔记的时候，也会觉得很有趣，这份趣味就是过去送给现在的礼物。</p>
<p>所以，我打算最近看书的时候开始记录一下，阅读时的感受、想法等等。</p>
<p>首先要记录的就是看这本书的想法了。我觉得作者是个有些上年纪的老爷子，他介绍的很多方法，对于现代的我们来说过于古老，比如要看报纸的介绍，查百科全书，都让人觉得是上个世纪的东西。然而，书中给了一个2013年这个日期，有让人觉得似乎也没有那么遥远。可能是时代变化太快了吧。</p>
<p>之前看过一些新闻，说是现在的实体书店已经很难存活了。比如西单的图书馆似乎都关了，或者就转型成了网红打卡图书馆、咖啡馆，其卖点早已不是书籍。所以还是时代变了。</p>
<p>另外，对于日本（本书的作者来自日本），我最喜欢的是他们的动漫。日本文化中似乎很自然的捕捉和放大生活中的点点细节，从而让人有深的感悟。本书也是这样，但作为类似工具书的存在，细节太多反而让人找不到重点。</p>
<p>这里，最大的收获不在于书的内容，而是让我有了试图记笔记的念头。</p>
<p>对了，今天学习了github的<code>actions</code>功能，以后可以直接提交hexo的md文件，然后直接白嫖微软的服务器了！私有仓库每个月有2000 minutes的时长，而每次部署只需要44s，看起来就是无限长的时间啦。</p>
<p>还有一个发现是<code>GitNote</code>这个软件，一个记事本软件，在各pc平台都有，使用git做文件后台。但是试用了一下，感觉配色太白了，然后文件的创建位置啥的都不直白，莫名奇妙的退出然后不知道咋进入。感觉不是很好用。我好想也自己写一个笔记软件。或许可以学习一下<code>electron</code>？</p>
<p>所以这个博客以后就增加了新的模块——我的读书笔记~</p>
]]></content>
      <categories>
        <category>读书笔记</category>
      </categories>
  </entry>
  <entry>
    <title>hexo的小技巧</title>
    <url>/2020/02/28/hexo-trick/</url>
    <content><![CDATA[<p>从WordPress转到Hexo没多久。这里记录一下遇到的问题和解决方案。</p>
<a id="more"></a>
<h2 id="一部署失败-jekyll-build失败">一、部署失败 Jekyll Build失败</h2>
<p>在Github上，部署之后，报了Jekyll build的错误。页面虽然上传到github了，但是实际的网页却没有变化。</p>
<p>查了很久，发现是需要先删除本地的<code>.deploy_git</code>。再继续<code>hexo clean &amp;&amp; hexo generate &amp;&amp; hexo deploy</code>才行。</p>
<h2 id="二latex">二、Latex</h2>
<p>在Next主题，使用mathjax的话，最好使用pandoc这个渲染引擎。他对Latex支持最好，并且不会出现<code>_</code>，<code>*</code>的误识别。</p>
<p>pandoc这个软件需要单独安装，npm也需要安装相关的软件。所以是要安装两个东西。</p>
<h2 id="三next主题下图片的小标题显示不正确">三、Next主题下，图片的小标题显示不正确</h2>
<figure class="highlight md"><table><tr><td class="code"><pre><span class="line">![<span class="string">tag text</span>](<span class="link">image url</span>)</span><br></pre></td></tr></table></figure>
<p>这中写法，<code>tag text</code>会显示在图像的居中正下方，看起来很好看。但是在pandoc引擎下，该效果就消失。原因是pandoc和图像标题的标签改掉了。需要在<code>root/_config.yml</code>和<code>root/themes/next/_config.xml</code>两个文件中均加上：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">pandoc:</span></span><br><span class="line">  <span class="attr">extensions:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="string">'-implicit_figures'</span></span><br></pre></td></tr></table></figure>
<p>在两个文件都加上是因为，<code>hexo server</code>和<code>hexo deploy</code>似乎会读取不同的配置，总之都加上肯定没错。</p>
<h2 id="四在首页隐藏文章">四、在首页隐藏文章</h2>
<p>使用<code>hexo-generator-index2</code>插件。</p>
<p>安装:</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm install hexo-generator-index2 -save</span><br></pre></td></tr></table></figure>
<p>在<code>root/_config.yml</code>中加上:</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">index2_include_index:</span> <span class="literal">true</span></span><br><span class="line"><span class="attr">index2_generator:</span></span><br><span class="line">  <span class="attr">per_page:</span> <span class="number">8</span></span><br><span class="line">  <span class="attr">order_by:</span> <span class="string">-date</span></span><br><span class="line">  <span class="attr">exclude:</span></span><br><span class="line">    <span class="bullet">-</span> <span class="string">tag</span> <span class="string">hide</span></span><br><span class="line">    <span class="bullet">-</span> <span class="string">category</span> <span class="string">hide</span></span><br></pre></td></tr></table></figure>
<p>以后tags里面有<code>hide</code>，或者类别为<code>hide</code>的文章都不会显示在首页了。</p>
<h2 id="五中英文之间有空格">五、中英文之间有空格</h2>
<p>我们文章中间，中英文之间有一点空白，会显得更好看。一般我都是自己手动加上空格，然后发现有个插件 <code>pangu</code>。<code>Next</code>主题自己就集成了。</p>
<p>把他的<code>_config.yml</code>中开关打开：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><span class="line"><span class="attr">pangu:</span> <span class="literal">true</span></span><br></pre></td></tr></table></figure>
]]></content>
  </entry>
  <entry>
    <title>C程序优雅地调用Lua?一篇博客带你全面了解lua_State</title>
    <url>/2020/02/26/lua-state-tutorial/</url>
    <content><![CDATA[<h2 id="一前言">一、前言</h2>
<p>Lua是一种使用十分方便的脚本语言，同时又能C/C++无缝的粘合在一起，在速度上也比很多其他的脚本语言快了一个数量级，在使用LuaJit的情况下，速度甚至可以与C语言比肩。同时Lua支持模块的热更新，在游戏等行业受到广泛的应用。</p>
<p>同时Lua虚拟机十分的轻量，在占用极少内存和依赖的情况下，可以方便的嵌入到我们的项目中。</p>
<p>本文中，主要介绍了在C/C++项目中，使用Lua虚拟机的技巧，着重介绍Lua_State的原理和使用。简单的说就是用C去调用Lua。另一个技术是用Lua调用C库，可以为Lua程序加速，这个在以后的文章中再介绍。</p>
<a id="more"></a>
<p>本文中使用的LuaJit作为我们Lua脚本的执行引擎。对应的Lua的版本是Lua5.1。</p>
<h2 id="二c调用lua的案例">二、C调用Lua的案例</h2>
<p>在下面的程序中，我们的Lua脚本中，使用一个全局变量<code>version</code>表示程序的版本，定义一个<code>add</code>函数，负责计算两个数组的和。之后通过在C程序中加载Lua虚拟机，打印版本号和调用<code>add</code>函数。</p>
<p>下面是对应的C++程序：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">extern</span> <span class="string">"C"</span> &#123;</span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;lualib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;lauxlib.h&gt;</span></span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">const</span> <span class="keyword">char</span> *lua_script = R<span class="string">"(</span></span><br><span class="line"><span class="string">version = '1.0.0'</span></span><br><span class="line"><span class="string">function add(a, b)</span></span><br><span class="line"><span class="string">    return a + b;</span></span><br><span class="line"><span class="string">end</span></span><br><span class="line"><span class="string">)"</span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    lua_State *L = luaL_newstate();</span><br><span class="line">    <span class="keyword">if</span> (L == <span class="literal">NULL</span>) &#123;</span><br><span class="line">        <span class="built_in">std</span>::<span class="built_in">cerr</span> &lt;&lt; <span class="string">"create state failed!"</span> &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">-1</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (luaL_dostring(L, lua_script) != <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="built_in">std</span>::<span class="built_in">cerr</span> &lt;&lt; <span class="string">"cannot run lua script!"</span> &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">-1</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// get global variable</span></span><br><span class="line">    lua_getglobal(L, <span class="string">"version"</span>);</span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">char</span> *version = lua_tostring(L, <span class="number">-1</span>);</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">"version: "</span> &lt;&lt; version &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">    lua_pop(L, <span class="number">1</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// call global function</span></span><br><span class="line">    lua_getglobal(L, <span class="string">"add"</span>);</span><br><span class="line">    <span class="keyword">int</span> a = <span class="number">100</span>;</span><br><span class="line">    <span class="keyword">int</span> b = <span class="number">200</span>;</span><br><span class="line">    lua_pushnumber(L, a);</span><br><span class="line">    lua_pushnumber(L, b);</span><br><span class="line">    <span class="keyword">if</span> (lua_pcall(L, <span class="number">2</span>, <span class="number">1</span>, <span class="number">0</span>) != <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="built_in">std</span>::<span class="built_in">cerr</span> &lt;&lt; <span class="string">"call func failed, got error: "</span> &lt;&lt; lua_tostring(L, <span class="number">-1</span>) &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">        lua_pop(L, <span class="number">1</span>);</span><br><span class="line">        <span class="built_in">exit</span>(<span class="number">-1</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">int</span> sum = lua_tonumber(L, <span class="number">-1</span>);</span><br><span class="line">    lua_pop(L, <span class="number">1</span>);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; a &lt;&lt; <span class="string">" + "</span> &lt;&lt; b &lt;&lt; <span class="string">" = "</span> &lt;&lt; sum &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">    lua_close(L);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>编译和运行，我这里用的luajit，所以链接的库可能略有不同，使用lua的话，使用<code>-llua</code>：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># compile</span></span><br><span class="line">g++ lua_sample.cpp -o lua_sample -I /path/to/lua/include/ -L /path/to/lua/lib -lluajit-5.1 -std=c++11 -o lua_sample</span><br><span class="line"><span class="comment"># run</span></span><br><span class="line">LD_LIBRARY_PATH=/path/to/lua/lib ./lua_sample</span><br></pre></td></tr></table></figure>
<p>就可以得到如下的输出：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">version: 1.0.0</span><br><span class="line">100 + 200 &#x3D; 300</span><br></pre></td></tr></table></figure>
<h2 id="三交互与栈">三、交互与栈</h2>
<p>C与Lua的交互，是通过lua_State和相关的函数接口来实现的（<code>lua_getglobal</code>, <code>lua_pushnumber</code>等）。但是具体的值的获取和函数调用，其实是利用了一套栈机制。</p>
<p>C与Lua的交互，本质上是对Lua栈做相关的操作。<code>lua_pushxxx</code>是将数据压入栈中，而<code>lua_pop</code>就是出栈操作。<code>lua_toxxx</code>是将栈中对应位置的数据转换成我们期望的数据类型，而输出。</p>
<p>下面我们按情况分别介绍。</p>
<h2 id="四全局变量">四、全局变量</h2>
<h3 id="全局的基本类型数据">1. 全局的基本类型数据</h3>
<p>获取和设置全局变量，是一个重要的功能。</p>
<p>例子中的是获取全局变量的实例。我们现在知道所谓的交互都是堆栈做操作。<code>lua_getglobal</code>的作用就是把输入的变量名对应的数据放入栈顶。</p>
<p><img data-src="http://cdn.miaoerduo.com/images/lua_getglobal.png" alt="lua_getglobal" /></p>
<p>之后，我们只需要根据栈中的位置，获取对应的数据即可。比如上图中，<code>version</code>就在栈顶的位置，索引号为101。由于我们一般都是对栈顶做操作，使用负数的索引会更方便。<code>lua_tostring(L, -1)</code>就可以把索引号为<code>-1</code>的数据（即栈顶的数据）转换成<code>string</code>类型输出。</p>
<p>在数据使用结束后，我们需要将数据pop出栈（<code>lua_pop(L, 1)</code>出栈一个数据），不然这个栈的大小会越来越大，导致最终程序内存很大，但是又不是因为内存泄漏，难以用valgrind等程序查到问题。</p>
<p>那么如何设置一个全局变量呢？</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">lua_pushstring(L, <span class="string">"hello world"</span>);</span><br><span class="line">lua_setglobal(L, <span class="string">"greet"</span>);</span><br></pre></td></tr></table></figure>
<p>通过<code>lua_pushxxx</code>，在栈中push一个数据，之后使用<code>lua_setglobal</code>即可。<code>lua_setglobal</code>的功能是，pop出<strong>栈顶</strong>的数据，并使用给定的名字把它设置为全局变量。这样，我们在<code>lua</code>虚拟机就新建了名字为<code>greet</code>，值为字符串<code>"hello world"</code>的全局变量了。由于该操作本身就会pop出栈顶数据，我们随后就不需要再自行pop了。</p>
<h3 id="全局table">2. 全局Table</h3>
<p>Table的创建和读取，比一般变量要稍微复杂一点。主要使用<code>lua_newtable</code>或<code>lua_createtable</code>创建Table。之后使用<code>lua_settable</code>设置Table的键值对，<code>lua_gettable</code>去读取对应的值。</p>
<p>下面是一个完整的Demo。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">extern</span> <span class="string">"C"</span> &#123;</span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;lualib.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;lauxlib.h&gt;</span></span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    lua_State *L = luaL_newstate();</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 创建table</span></span><br><span class="line">    lua_newtable(L); <span class="comment">// t = &#123;&#125; 创建没有名字的table，这里用t代替</span></span><br><span class="line">                        <span class="comment">// 也可以使用lua_createtable创建，该函数可以指定预分配的大小</span></span><br><span class="line">    lua_pushstring(L, <span class="string">"key1"</span>);</span><br><span class="line">    lua_pushstring(L, <span class="string">"val1"</span>);</span><br><span class="line">    lua_settable(L, <span class="number">-3</span>); <span class="comment">// t["key1"] = "val1"</span></span><br><span class="line"></span><br><span class="line">    lua_pushinteger(L, <span class="number">1</span>);</span><br><span class="line">    lua_pushstring(L, <span class="string">"val2"</span>);</span><br><span class="line">    lua_settable(L, <span class="number">-3</span>); <span class="comment">// t[1] = "val2"</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// setfield 可以稍微简化创建的操作，但是key只能是字符串</span></span><br><span class="line">    lua_pushstring(L, <span class="string">"val3"</span>);</span><br><span class="line">    lua_setfield(L, <span class="number">-2</span>, <span class="string">"key3"</span>); <span class="comment">// t["key3"] = "val3"</span></span><br><span class="line"></span><br><span class="line">    lua_setglobal(L, <span class="string">"my_table"</span>); <span class="comment">// 给这个匿名的table一个名字</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 读取table</span></span><br><span class="line">    lua_getglobal(L, <span class="string">"my_table"</span>);</span><br><span class="line"></span><br><span class="line">    lua_pushstring(L, <span class="string">"key1"</span>);</span><br><span class="line">    lua_gettable(L, <span class="number">-2</span>);</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">"v1: "</span> &lt;&lt; lua_tostring(L, <span class="number">-1</span>) &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">    lua_pop(L, <span class="number">1</span>);</span><br><span class="line"></span><br><span class="line">    lua_pushinteger(L, <span class="number">1</span>);</span><br><span class="line">    lua_gettable(L, <span class="number">-2</span>);</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">"v2: "</span> &lt;&lt; lua_tostring(L, <span class="number">-1</span>) &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">    lua_pop(L, <span class="number">1</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// getfield可以简化</span></span><br><span class="line">    lua_getfield(L, <span class="number">-1</span>, <span class="string">"key3"</span>);</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">"v3: "</span> &lt;&lt; lua_tostring(L, <span class="number">-1</span>) &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">    lua_pop(L, <span class="number">1</span>);</span><br><span class="line"></span><br><span class="line">    lua_pop(L, <span class="number">1</span>); <span class="comment">// pop table</span></span><br><span class="line"></span><br><span class="line">    lua_close(L);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>为了方便理解，这里我绘制了一个栈空间的示意图：</p>
<p><img data-src="http://cdn.miaoerduo.com/images/lua_table.png" alt="lua table" /></p>
<p>首先是table的创建，使用<code>lua_newtable</code>可以创建一个table对象，并且在栈顶。之后我们将键、值分别push进栈，使用<code>lua_settable</code>，就可以将键值出栈，并设置进table中。这个过程也可以使用<code>lua_setfield</code>函数进行简化，在demo里面有写。最后我们使用<code>lua_setglobal</code>给栈顶的数据设置一个名字，并且出栈。这样，就创建好了一个Table。</p>
<p>读取的操作也类似。首先，我们使用<code>lua_getglobal</code>将对应的table放入栈顶。之后push想要获取的key值，最后使用<code>lua_gettable</code>，该函数在pop出key值后，会获取key值对应的value，并放入栈顶。这样就可以完成table的读取了。<code>lua_gettable</code>可以通过<code>lua_getfield</code>来简化操作。demo中也给出了例子。</p>
<h2 id="五函数调用">五、函数调用</h2>
<p>在前面，我们知道了变量的读取和设置。本节将介绍函数的调用。我们知道对于一个函数而言，最重要的有三个部分：</p>
<ol type="1">
<li>函数入口</li>
<li>参数</li>
<li>返回值</li>
</ol>
<p>这是第一个例子的节选。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">lua_getglobal(L, <span class="string">"add"</span>);</span><br><span class="line"><span class="keyword">int</span> a = <span class="number">100</span>;</span><br><span class="line"><span class="keyword">int</span> b = <span class="number">200</span>;</span><br><span class="line">lua_pushnumber(L, a);</span><br><span class="line">lua_pushnumber(L, b);</span><br><span class="line"><span class="keyword">if</span> (lua_pcall(L, <span class="number">2</span>, <span class="number">1</span>, <span class="number">0</span>) != <span class="number">0</span>) &#123;</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cerr</span> &lt;&lt; <span class="string">"call func failed, got error: "</span> &lt;&lt; lua_tostring(L, <span class="number">-1</span>) &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">    lua_pop(L, <span class="number">1</span>);</span><br><span class="line">    <span class="built_in">exit</span>(<span class="number">-1</span>);</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">int</span> sum = lua_tonumber(L, <span class="number">-1</span>);</span><br><span class="line">lua_pop(L, <span class="number">1</span>);</span><br></pre></td></tr></table></figure>
<p>调用示意图：</p>
<p><img data-src="http://cdn.miaoerduo.com/images/lua_function_call.png" alt="lua function call" /></p>
<p>首选，我们使用<code>lua_getglobal</code>将函数入口入栈。之后使用<code>lua_pushxxx</code>向栈中push入参数。之后我们调用该函数，可以使用的函数有<code>lua_call</code>和<code>lua_pcall</code>。</p>
<p><code>lua_pcall</code>表示<code>protected mode</code>。简单理解，<code>lua_call</code>没有返回值，我们不能知道函数调用的状态。而<code>lua_pcall</code>有返回值，并且支持设定错误处理函数。速度上<code>lua_call</code>更快一点。我这边只使用了<code>lua_pcall</code>最简单的用法，简单的检查一下返回值。</p>
<p>在函数调用完成后，函数入口和参数都均会出栈，如果调用没出现问题的话，返回值会被依次入栈。当一个函数有多个返回值，它的第一个返回值会首先入栈，因此栈顶是最终的返回值。如果调用出错的话，错误信息会放在栈顶。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">lua_call</span> <span class="params">(lua_State *L, <span class="keyword">int</span> nargs, <span class="keyword">int</span> nresults)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">lua_pcall</span> <span class="params">(lua_State *L, <span class="keyword">int</span> nargs, <span class="keyword">int</span> nresults, <span class="keyword">int</span> errfunc)</span></span>;</span><br></pre></td></tr></table></figure>
<p><code>nargs</code>表示函数的参数数目，<code>nresults</code>表示返回值的数目。<code>errfunc</code>是异常处理函数在栈中的位置，如果填写0的话，出错时，<code>lua_pcall</code>会把错误信息直接放在栈顶。<code>lua_pcall</code>的返回值表示是否出错，返回0则表示一切运行正常。</p>
<p>我们例子中的<code>lua_pcall(L, 2, 1, 0)</code>的意思就很明确了：帮忙调用一个函数，它的输入参数有2个，返回值有1个，如果程序出错的话，麻烦把错误信息放栈顶，谢谢。</p>
<p>函数返回值，我们通过<code>lua_toxxx(L, index)</code>来进行获取。该函数会把栈中对应<code>index</code>的值，转换成<code>xxx</code>的类型返回。这里的内存是由lua维护，我们在C中一般不需要释放的。</p>
<p>那么Lua支持哪些类型的数据呢？</p>
<p>建议使用这个参考文档：<span class="exturl" data-url="aHR0cHM6Ly9wZ2wueW95by5vcmcvbHVhaS9pL18=" title="https://pgl.yoyo.org/luai/i/_">https://pgl.yoyo.org/luai/i/_<i class="fa fa-external-link"></i></span></p>
<p>入栈<code>lua_pushxxx</code>:</p>
<ul>
<li>lua_pushboolean</li>
<li>lua_pushcclosure</li>
<li>lua_pushcfunction</li>
<li>lua_pushfstring</li>
<li>lua_pushinteger</li>
<li>lua_pushlightuserdata</li>
<li>lua_pushliteral</li>
<li>lua_pushlstring</li>
<li>lua_pushnil</li>
<li>lua_pushnumber</li>
<li>lua_pushstring</li>
<li>lua_pushthread</li>
<li>lua_pushvalue</li>
<li>lua_pushvfstring</li>
</ul>
<p>取值<code>lua_toxxx</code>:</p>
<ul>
<li>lua_toboolean</li>
<li>lua_tocfunction</li>
<li>lua_tointeger</li>
<li>lua_tolstring</li>
<li>lua_tonumber</li>
<li>lua_topointer</li>
<li>lua_tostring</li>
<li>lua_tothread</li>
<li>lua_touserdat</li>
</ul>
<p>这里我碰到了需要传递指针的情况，这里就使用lightuserdata类型。我们可以把它看成<code>void *</code>。</p>
<p>对于更复杂的自定义数据类型，需要使用<code>lua_newuserdata</code>来创建。不过C和Lua交互，能用指针还是用指针吧，这样会方便很多，也符合C程序的特点。</p>
<h2 id="六package-loader">六、Package Loader</h2>
<p>对于一段Lua的程序字符串，我们怎么把它加载进Lua虚拟机并且编译成可以操作的变量和函数呢？Lua提供了若干个函数，可以将数据装载进Lua。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">luaL_loadbuffer</span> <span class="params">(lua_State *L,</span></span></span><br><span class="line"><span class="function"><span class="params">                     <span class="keyword">const</span> <span class="keyword">char</span> *buff,</span></span></span><br><span class="line"><span class="function"><span class="params">                     <span class="keyword">size_t</span> sz,</span></span></span><br><span class="line"><span class="function"><span class="params">                     <span class="keyword">const</span> <span class="keyword">char</span> *name)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">luaL_loadfile</span> <span class="params">(lua_State *L, <span class="keyword">const</span> <span class="keyword">char</span> *filename)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">luaL_loadstring</span> <span class="params">(lua_State *L, <span class="keyword">const</span> <span class="keyword">char</span> *s)</span></span>;</span><br></pre></td></tr></table></figure>
<p>这几个函数的功能都是把数据加载进Lua虚拟机，<code>luaL_loadbuffer</code>是加载一段数据，并且给个命名，用于debug等用处。<code>luaL_loadfile</code>是从文件中读取。<code>luaL_loadstring</code>是读取字符串。返回值不为0的话，表示加载出错。这三个函数最终都是调用了<code>lua_load</code>进行代码编译，并将编译后的代码放与栈顶。</p>
<p>上述的函数只是完成程序文本的加载，编译，只有在执行之后才能变成虚拟机中的变量和函数。由于代码已经在栈顶了，我们也没有需要传入的参数。因此只需要随后调用<code>lua_pcall(L, 0, LUA_MULTRET, 0)</code>即可。这里的<code>LUA_MULTRET</code>表示多个返回值。Lua会帮我们自动处理这些返回值。</p>
<p>因此，我们常见的加载Lua代码的逻辑是：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (luaL_loadstring(L, str) || lua_pcall(L, <span class="number">0</span>, LUA_MULTRET, <span class="number">0</span>)) &#123;</span><br><span class="line">    <span class="comment">// load fail or pcall fail</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>Lua中提供了一套简单的函数，和前面的写法等价：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">luaL_dofile</span> <span class="params">(lua_State *L, <span class="keyword">const</span> <span class="keyword">char</span> *filename)</span></span>;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">luaL_dostring</span> <span class="params">(lua_State *L, <span class="keyword">const</span> <span class="keyword">char</span> *str)</span></span>;</span><br></pre></td></tr></table></figure>
<p>所以在最开始的例子中，使用了<code>luaL_dostring</code>这个函数。</p>
<p>在Lua程序中，我们使用<code>require</code>关键字去加载模块，在写入<code>local my_module = require("my_module")</code>之后，Lua会从<code>package.path</code>和<code>package.cpath</code>依次去查找。</p>
<p>但是存在这么一种情况。我们的产品中，有许多的Lua模块，这些模块一起被打包和加密，只能通过特定接口去获取对应的数据，而且为了安全，数据必须在内存中，不能写入文件。这种情况下，Lua的默认行为就不能很好的加载我们的这些模块，这时候该怎么办？</p>
<p>这里就用到了Package Loader。Lua支持注册我们自己的包加载器。在Lua中，包的加载并不是严格的去根据文件名去查找，而是将包名传递给package loaders，让他们自行处理。Lua中可以有多种package loaders，在<code>require</code>时会依次调用<code>package.loaders</code>中的loader去加载，如果都没有加载成功，才会报错。</p>
<p>这样我们就可以注册一个自己的package loader，完成自定义的模块数据的加载。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">my_package_loader</span><span class="params">(lua_State *L)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">char</span> *required_module_name = lua_tostring(L, <span class="number">-1</span>);</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">string</span> data = user_defined_load(required_module_name);</span><br><span class="line">    <span class="keyword">if</span> (luaL_loadbuffer(L, data.c_str(), data.size(), required_module_name) != <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="built_in">std</span>::<span class="built_in">cerr</span> &lt;&lt; <span class="string">"error load: "</span> &lt;&lt; lua_tostring(L, <span class="number">-1</span>) &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">        lua_pop(L, <span class="number">1</span>);</span><br><span class="line">        <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    lua_State *L = luaL_newstate();</span><br><span class="line">    luaL_openlibs(L); <span class="comment">// 要装载系统库，比如math、table等。</span></span><br><span class="line">    lua_register(L, <span class="string">"my_loader"</span>, my_package_loader);</span><br><span class="line">    luaL_dostring(L, <span class="string">"table.insert(package.loaders, 2, my_loader)\n"</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// do whatever you enjoy</span></span><br><span class="line"></span><br><span class="line">    lua_close(L);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这里，我们在C/C++程序中，编写了package loader，lua中对loader的定义就是<code>int xxx(lua_State *L)</code>。之后我们使用<code>lua_register</code>，该函数可以将C函数注册为Lua的函数。之后，通过<code>"table.insert(package.loaders, 2, my_loader)</code>语句，将自定义的<code>my_loader</code>插入进<code>package.loaders</code>数组中。这样就完成的loader的注册了。由此，我们就可以封装数据读取的接口<code>user_defined_load</code>，从而完成内存中的Lua package的加载了。</p>
<h2 id="七沙盒环境">七、沙盒环境</h2>
<p>我们有很多时候，希望用户可以编写自己的插件，以增强软件的功能。比如VS Code就支持自己编写插件。但是这个插件，我们又需要较为严格的权限管理。比如，不能访问本地文件，删除文件等等。那么有什么比较合适的办法呢？</p>
<p>其实十分简单，Lua的全局函数都是存放在<code>_G</code>这个表里面的。由于虚拟机是由程序创建和提供的，我们只需要在虚拟机创建的时候，手动的禁用掉一些函数就可以了。</p>
<p>比如：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">static</span> <span class="keyword">const</span> <span class="keyword">char</span> *sandbox_env = <span class="string">"_G.os.execute = nil\n"</span></span><br><span class="line">                                 <span class="string">"_G.os.exit = nil\n"</span></span><br><span class="line">                                 <span class="string">"_G.os.remove = nil\n"</span></span><br><span class="line">                                 <span class="string">"_G.os.rename = nil\n"</span></span><br><span class="line">                                 <span class="string">"_G.io = nil\n"</span></span><br><span class="line">                                 <span class="string">"_G.debug = nil\n"</span>;</span><br><span class="line"></span><br><span class="line">LuaL_dostring(L, sandbox_env);</span><br></pre></td></tr></table></figure>
<p>这样就可以禁用掉<code>execute</code>、<code>exit</code>等函数了。所以针对你期望的沙盒环境，可以选择性的开放相关的函数即可。</p>
<h2 id="八结束语">八、结束语</h2>
<p>至此，我们学习完了C调用Lua的相关知识，相信对于常见的功能需求都可以很好的满足了，针对个别的特殊的功能，还需要通过文档等进行学习。有时间，我会再整理一篇Lua调用C的文章。</p>
<p><strong>转载</strong>请注明出处。</p>
]]></content>
      <categories>
        <category>C++</category>
        <category>Lua</category>
      </categories>
  </entry>
  <entry>
    <title>20200202</title>
    <url>/2020/02/02/20200202/</url>
    <content><![CDATA[<p>今天是 <strong>20200202</strong> 这个对称的日子，明天也是年后上班的第一天，也是远程办公的一天。今天就画上了一幅。</p>
<p><img data-src="http://cdn.miaoerduo.com/images/illustration/2020-02-02.png" /></p>
<p><strong>2020</strong> 是艰难的一年，伊朗、科比以及最近无比艰难的肺炎。希望世界和平吧。</p>
]]></content>
      <categories>
        <category>插画</category>
      </categories>
  </entry>
  <entry>
    <title>国庆快乐</title>
    <url>/2019/10/01/2019-09-30/</url>
    <content><![CDATA[<p>这个是参考了一张Saber的插画。之后绘画的时候是一点都没有再看原图。和官方的Saber比，脸是不太一样。</p>
<p><img data-src="http://cdn.miaoerduo.com/images/illustration/2019-09-30-saber.png" /></p>
]]></content>
      <categories>
        <category>插画</category>
      </categories>
  </entry>
  <entry>
    <title>因为懒得被画头发而带上帽子的灰度少女</title>
    <url>/2019/07/31/2019-07-31/</url>
    <content><![CDATA[<p><img data-src="http://cdn.miaoerduo.com/images/illustration/2019-07-31.jpg" /></p>
]]></content>
      <categories>
        <category>插画</category>
      </categories>
  </entry>
  <entry>
    <title>随笔</title>
    <url>/2019/07/15/2019-07-15/</url>
    <content><![CDATA[<p><img data-src="http://cdn.miaoerduo.com/images/illustration/2019-07-15.jpg" /></p>
<p>画了两天 = =，感觉整体上没有什么太违和的地方，但是细节的地方需要更好的处理。</p>
]]></content>
      <categories>
        <category>插画</category>
      </categories>
      <tags>
        <tag>hide</tag>
      </tags>
  </entry>
  <entry>
    <title>随笔</title>
    <url>/2019/06/21/2019-06-21/</url>
    <content><![CDATA[<p>啧啧，买了个单手键盘，现在终于可以一边使用键盘，一边使用数位板了！开心！</p>
<p><img data-src="http://cdn.miaoerduo.com/images/illustration/2019-06-20.png" /></p>
]]></content>
      <categories>
        <category>插画</category>
      </categories>
      <tags>
        <tag>hide</tag>
      </tags>
  </entry>
  <entry>
    <title>端午啦</title>
    <url>/2019/06/08/2019-06-08/</url>
    <content><![CDATA[<p><img data-src="http://cdn.miaoerduo.com/images/illustration/2019-06-08.png" /></p>
<p>端午节，画了两天，才画了一张带颜色的图，感觉颜色啥的，好难画，阴影的过渡有待进步~</p>
]]></content>
      <categories>
        <category>插画</category>
      </categories>
      <tags>
        <tag>hide</tag>
      </tags>
  </entry>
  <entry>
    <title>随笔</title>
    <url>/2019/05/24/2019-05-24/</url>
    <content><![CDATA[<p>好久没有练习画画了。今天总算动起手来。这张画，我是先找了个淘宝的图片，然后大致画了下姿势，之后就完全不参考了，慢慢的一点点的画。还是使用之前的 Sakimichan大大的画法，只使用了19号画笔和套索工具。</p>
<p><img data-src="http://cdn.miaoerduo.com/images/illustration/2019-05-23.png" /></p>
<p>画完的时候在看最开始的草稿，完全无法想象这两个居然是同一次的产物。</p>
<p>肚子上的褶皱。也是参考Sakimichan大大的方式，使用高光和暗光的两种线条随意涂画，看起来效果还不错。比较遗憾的是，到现在还不会上色。还是先把黑白练好再说。</p>
]]></content>
      <categories>
        <category>插画</category>
      </categories>
      <tags>
        <tag>hide</tag>
      </tags>
  </entry>
  <entry>
    <title>2B</title>
    <url>/2019/04/13/2019-04-13-2b/</url>
    <content><![CDATA[<p>昨天晚上，终于把Nier的一周目打通了。最后2B小姐姐的眼罩居然掉了。真心好看。所以今天呢，就在没有任何参考的情况下，自己练习了一下头像，感觉效果似乎还可以。难道说，我的插画生涯终于要开始了吗，嘻嘻。</p>
<p><img data-src="http://cdn.miaoerduo.com/images/illustration/2019-04-13-2b.jpg" /></p>
]]></content>
      <categories>
        <category>插画</category>
      </categories>
  </entry>
  <entry>
    <title>随笔</title>
    <url>/2019/04/02/2019-04-02/</url>
    <content><![CDATA[<p>最近看了Sakimichan大神的插画过程，被真心的震撼到了！太强了！其中的PS的套索工具的使用真是让人叹为观止。在上色的时候，通过套索，避免色彩的溢出。对于出界的色彩，也用套索工具选中删除。另外，套索加上颜色调整，也是神器。</p>
<p>这是我采用类似的方法画的图，和目前为止的所以的作品都不同，完成度高了很多。</p>
<p><img data-src="http://cdn.miaoerduo.com/images/illustration/2019-04-02.png" /></p>
]]></content>
      <categories>
        <category>插画</category>
      </categories>
      <tags>
        <tag>hide</tag>
      </tags>
  </entry>
  <entry>
    <title>TICTOC: Header Only C++ Timer</title>
    <url>/2019/03/29/tictoc-header-only-c-timer/</url>
    <content><![CDATA[<p>感觉最近的更新频率略高啊～哈哈～</p>
<p>这次的带来的是一个十分简单便利的C++计时库。</p>
<p>项目地址：<span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL21pYW9lcmR1by90aWN0b2M=" title="https://github.com/miaoerduo/tictoc">https://github.com/miaoerduo/tictoc<i class="fa fa-external-link"></i></span></p>
<p>欢迎<strong>Start</strong>和提<strong>MR</strong>。</p>
<a id="more"></a>
<p>项目中有详细的说明和Demo，可以很直观的体验到这个库的易用性。</p>
<p>先看一下效果，如果我们正确使用的话，大致会出现类似下面的信息：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">demo.cpp @ main [    8,   13]   elapsed:      0.025 s      24.786 ms       24786 us</span><br><span class="line">demo.cpp @ main [    8,   18]   elapsed:      0.049 s      48.709 ms       48709 us</span><br><span class="line">demo.cpp @ main [    8,   23]   elapsed:      0.072 s      72.211 ms       72211 us</span><br><span class="line">demo.cpp @ main [    8,   24]   elapsed:      0.072 s      72.225 ms       72225 us</span><br><span class="line">demo.cpp @ main [   30,   36]   elapsed:      0.022 s      21.747 ms       21747 us</span><br><span class="line">demo.cpp @ main [   36,   41]   elapsed:      0.021 s      21.463 ms       21463 us</span><br></pre></td></tr></table></figure>
<p>可以显示，我们的每个区域的代码（包括行号）的消耗时间。精确到微秒。</p>
<p>起因是这样的，之前有很长时间的工作内容是优化一些特定的函数，保证新旧的SDK的速度的对齐。然后C++虽然有一些工具可以分析运行状态，但通常还是简单的打印时间来的方便 <span style="color: green">/* Print大法好 */</span>。</p>
<p>之后，和工程的小伙伴一起Debug的时候，就发现他写了一个头文件，然后用绝对路径的方式去include，而头文件里面就是各种常用的小工具，而最常用到的就是时间的打印。</p>
<p>之后，我专门要到了他的百宝箱，仔细分析了一下，发现计时器模块仍然存在一些问题：</p>
<ol type="1">
<li>在Debug的时候，如果加上工具代码，在Release的时候，还得一点点删掉，很麻烦。</li>
<li>修改时间精度的话，需要修改源码，略麻烦。</li>
<li>打印的时间戳的信息不完整，看不出来该段时间具体的代码的范围。</li>
<li>计时器如果在多个文件中都用到，会有各种奇怪的错误，重复定义变量啊，或者找不到变量啥的。</li>
<li>对更复杂的程序，比如各种库的编译，多个库的链接调用不支持。</li>
</ol>
<p>上面说的问题，说大不大，说小不小。如果能有个工具能解决上面5个问题，那也是一件十分惬意的事情。所以，也就有了本文和 <code>TICTOC</code> 这个库。接下来，我们会从上面的5个问题开始，一点一点介绍C++的小技巧。</p>
<h3 id="设计思路">〇、设计思路</h3>
<p>其实计时器的思路很简单，就是定义两个宏 <code>TIC</code> 和 <code>TOC</code>，如果插入 <code>TIC</code>，则记录为起始时间，当插入 <code>TOC</code> 的时候，则计算与上一次 <code>TIC</code> 之间的时间，并打印出来。</p>
<p>比较麻烦的是，如果我在使用 <code>TIC</code> 的时候，生成一个变量，那连续使用两次 <code>TIC</code> 的话，就会出现变量的重复定义。另一个方案就是在全局定义一个时间的变量，但这样会带来另一个问题，就是所有函数都共享这个变量，如果函数内部再运行一次 <code>TIC</code>，会覆盖掉这个时间戳，但是其他的 <code>TOC</code> 的结果不直观。</p>
<p>所以，这里就使用了一个字典，来存放 <code>TIC</code> 的时间戳。这个字典本身是使用单例模式去生成和维护的。每次 <code>TIC</code> 的时候都会初始化一次它，但是由于是单例，所以只有第一次会耗时。而字典的键是个字符串，由文件名+函数名联合构成。这样针对每个函数，都会有自己的一个计时器，就不用担心冲突了。之后运行 <code>TOC</code> 的时候，也会检查当前的文件名和函数名，从而与对应的 <code>TIC</code> 时间戳相减。是不是听起来很简单！</p>
<p>当然还会碰到很多奇怪的问题，其中最无语的是，当动态库使用这个库，而主程序也使用这个库的时候，所谓的单例模式就失效了，两段程序里面都会有这个字典，然后就冲突了，出现 <code>double free</code> 的情况。查了半天，才发现是动态库只在静态表导出这个单例，动态连接器默认查询动态表，没找到，从而主程序自己又重复构建了这个实例，导致了存在两个实例。最终用 <code>-rdynamic</code> 的方式编译就可以解决。但是用这种方式的话，又会显得很麻烦。我采用的解决方法是匿名命名空间，在每个文件中生成自己的单例。细节我们在后面会谈到。</p>
<h3 id="一debug-or-release">一、Debug or Release</h3>
<p>因为我们不希望在Deliver的时候，再修改代码，所以有没有办法，使用不同的宏来控制我们的程序呢？当然是可以的。<code>C/C++</code> 最常用到的预处理语句：<code>#define</code>, <code>#ifdef</code>, <code>#ifndef</code>，<code>#else</code>, <code>#endif</code>。采用下面的方式来进行就可以。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">ifndef</span> TICTOC_HPP</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> TICTOC_HPP</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">ifdef</span> WITH_TICTOC</span></span><br><span class="line"><span class="comment">// 一些计时器的逻辑单元</span></span><br><span class="line"><span class="comment">// 函数啥的</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">else</span></span></span><br><span class="line"><span class="comment">// 一些假的信息</span></span><br><span class="line"><span class="comment">// 比如宏函数，内容空的，免得编译不过</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">endif</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">endif</span></span></span><br></pre></td></tr></table></figure>
<p>首先，这个 <code>TICTOC_HPP</code> 的宏定义，是为了防止头文件的多次包含。不然在多处include这个头文件的时候，会出现函数重复定义的问题。是一个良好的编程习惯。</p>
<p><code>WITH_TICTOC</code> 这个宏才是用来控制我们的Debug/Release的关键。在Debug的时候，编译加入一个宏定义，用g++直接编译的话，就是编译的时候加上 <code>-DWITH_TICTOC</code>。用CMakeLists的话，就是另一套了，自己查一下吧。在Release的时候，去掉这个宏定义就行，这样编译走的就是 <code>#else</code> 的分之，里面可以不写代码（我这里还是写了几行，定义了一些宏，但是宏的操作是空的）。</p>
<p>总之，灵活的使用宏定义，就可以让我们的编译器按照我们的想法去工作！</p>
<h3 id="二多种精度">二、多种精度</h3>
<p>问题二就比较简单了，既然每设置一种精度，都要修改一下代码，不如一次性的将所有的精度都打印出来了！这部分似乎没有什么好说的，就简单的说一下，我这里用到的计时的函数吧。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span><span class="meta-string">&lt;sys/time.h&gt;</span></span></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">struct timeval &#123;</span></span><br><span class="line"><span class="comment">    time_t       tv_sec;     // seconds</span></span><br><span class="line"><span class="comment">    suseconds_t  tv_usec;    // microseconds</span></span><br><span class="line"><span class="comment">&#125;;</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="function">struct timeval <span class="title">get_tick</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">timeval</span> <span class="title">time</span>;</span></span><br><span class="line">    gettimeofday(&amp;time, <span class="literal">NULL</span>);</span><br><span class="line">    <span class="keyword">return</span> time;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><code>timeval</code> 是一个表示时间的结构体，可以精确到微秒级别，完全够我们使用了。</p>
<h3 id="三打印完整的信息">三、打印完整的信息</h3>
<p>首先，对于一个计时器，为了方便调试，我们希望知道什么信息呢？这里列出来我比较关心的：</p>
<ol type="1">
<li>这个时间戳所在的位置，包括：文件名，函数名</li>
<li>时间戳是哪一段代码产生的，即：起始和结束的代码行号</li>
<li>具体的时间（按不同精度显示）</li>
</ol>
<p>对于3，上文已经介绍了。那么如何获取文件名、函数名以及行号呢？</p>
<p>其实C++中（C语言中也有的）早就给我们定义好了一些宏。这里就简单的列一下常用的几个，大家感兴趣也可以自己去查询：</p>
<ol type="1">
<li><code>__FILE__</code> : 宏所在的文件名</li>
<li><code>__FUNCTION__</code> : 宏所在的函数名</li>
<li><code>__LINE__</code> : 当前行号</li>
<li><code>__DATE__</code>, <code>__TIME__</code> : 最后一次编译的时间</li>
<li><code>__TIMESTAMP__</code> : 文件最后的修改时间</li>
</ol>
<p>所以，我们这里主要用到三个：<code>__FILE__</code>, <code>__FUNCTION__</code>, <code>__LINE__</code> 。</p>
<h3 id="四working-everywhere">四、Working Everywhere</h3>
<p>上面的问题4和5，放在一起介绍。</p>
<p>针对问题4，是我们在多个文件同时使用了计时器，如果通过全局变量的方式去存储时间戳，那么每个文件都会有自己的时间戳，从而导致冲突（当然，把时间戳改成 <code>static</code> 的可能可以解决）。而且，同一个文件中，如果出现函数调用，也有修改这个全局的时间戳，导致打印时间很不友好。</p>
<p>这里使用字典来存放时间戳，给每个文件都创建自己的时间戳，从而解决了这个问题。在〇章中，也有介绍。</p>
<p>那么问题5就很复杂了，多个动态库同时使用时，会崩溃。首先，为了让字典在程序中，只存在一份，我这里使用了单例模式。如果把所有的文件都编译在一起，是完全OK的。问题就出在，如果动态库使用了这个工具，而主程序也使用该工具，且又链接了动态库，那么程序中就会出现多个字典，在程序退出析构的时候，就会出现多次 <code>free</code> 的情况（很奇怪吧，明明是两个实例，居然两次析构函数都调用同一个实例）。之前也说了，用 <code>-rdynamic</code> 的方式编译会很麻烦，而且我们不可能给整个大项目的每个部分都加这个编译选项吧。我们的工具库要足够的独立！</p>
<p>按照之前的分析，我们其实只需要给每个函数都分配自己的一个键就可以了，其实完全没必要只有一个Global的字典，只需要给每个文件都生成自己的字典不就OK了吗。但是，怎么去实现呢？</p>
<p>常见的方法有两个：</p>
<ol type="1">
<li><code>static 变量</code>，<code>static</code> 关键字有一个功能，是保证这个变量只在该文件中使用。不会导出。</li>
<li>匿名命名空间，也叫匿名名字空间，这里采用的就是这个方案。</li>
</ol>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">namespace</span> &#123;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">print</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">"hello world"</span> &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>上面就是最简单的匿名命名空间，如果我们在代码中这么定义，其等价于：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">namespace</span> thisisaspecificnamespace &#123;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">print</span><span class="params">()</span> </span>&#123;</span><br><span class="line">        <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">"hello world"</span> &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> thisisaspecificnamespace;</span><br></pre></td></tr></table></figure>
<p>里面的这个大长串是啥意思？</p>
<p>其实thisisaspecificnamespace这个名字是我瞎写的，对于编译器，他会给这个匿名命名空间生成一个独一无二的名字，保证一定不重复，然后在改文件中，using它。所以自然就只有这个文件本身能够调用里面的函数了。</p>
<p>我们的工具是一个纯头文件，所有的库想依赖该文件，都会直接include它，而include操作其实就是简单的copy文件的内容，所以这段代码就会进入每个文件自身中，成为其源码的一部分。如此，只要我们把单例维护的代码放在匿名命名空间中，就可以保证其在每个文件中有且只有一个。就不用担心不同的库之间的冲突了。</p>
<h3 id="五补充">五、补充</h3>
<p>最后，我编写的这个库，并没有花费太多的时间，不过编程的过程中，确实还是感受到一点快乐的。不知不觉，现在写代码的时候，更喜欢以一种工具或是框架的角度去审核自己的作品。相比于追求编程的速度，慢慢蜕变成追求更优雅的设计，更简洁和实用的功能以及尽可能好的兼容性。</p>
<p>这里，小喵与你共同进步！</p>
]]></content>
      <categories>
        <category>C++</category>
        <category>Miscellanea</category>
      </categories>
  </entry>
  <entry>
    <title>Easy Pipeline，一种轻量级的Python Pipeline库</title>
    <url>/2019/03/23/easy-pipeline/</url>
    <content><![CDATA[<blockquote>
<p>嗯，很久没有写博客了，最近的工作都是偏开发性质的，以至于没有时间对自己感兴趣的领域进行探索，感觉个人的成长停滞了一些。如何在枯燥的工作中，提取出有助于自己成长的养分，对于每个人来说都是不小的考验。</p>
</blockquote>
<p>这次，带来的是之前编写的一下挺简单的库，用来简化流水线作业的小框架。</p>
<p>Github: <span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL21pYW9lcmR1by9lYXN5LXBpcGVsaW5l" title="https://github.com/miaoerduo/easy-pipeline">https://github.com/miaoerduo/easy-pipeline<i class="fa fa-external-link"></i></span> 欢迎Star和提交MR。</p>
<a id="more"></a>
<p>起因是这样的，组内有一个需求，需要挖掘视频中的检测难样本，这样可以极大地减少标注的量，从而降低成本。难样本挖掘的策略，简单来说就是如果视频的前几帧和后几帧都能检测到目标，而就只有当前帧没有检测到，就说明当前帧很可能存在漏检（没有检测本到该检测到的目标）；反之，如果前后都没有检测到目标，而当前帧检测到了，那就很可能是误检（检测到不是目标的东西）。</p>
<p><img data-src="http://cdn.miaoerduo.com/images/simple-pipeline.png" alt="simple pipeline" /></p>
<p>初步的方案是这样的，我们先把视频抽帧，直接用FFMpeg就可以方便的完成。然后调用现在的检测器，进行逐帧的检测，把检测结果存下来。最后写个脚本，分析检测的结果，然后输出可能有问题的帧，然后这些帧就会进行送标（发给标注员进行标注）。最终我们就只需要标注一些比较hard的样本就行了。</p>
<p>但是这样会带来很多的问题，最显著的两个：1. 需要保存大量的中间结果（图片帧）；2. 必须依次完成每一步之后，才能得到最终的结果。</p>
<p>这时候，相比大家都知道了该如何去解决。对的，我们应该用流水线作业的方式去进行。</p>
<p><img data-src="http://cdn.miaoerduo.com/images/simple-pipeline2.png" alt="simple pipeline2" /></p>
<p>首先我们可以将每部分任务并行的去处理。抽帧之后的结果送入队列；之后检测模块从队列取帧，检测之后将结果送入下一个队列；最后一个队列得到检测结果，再做最终的分析。相比于之前的方式，这样可以尽量的减少中间的结果。</p>
<p>实现该方案，只需要使用最简单的生产者消费者队列即可以完成。所以说，相信大家都十分了解了。对于上面的逻辑，我们需要的队列的数目和我们的模块数是正相关的。如果单纯的进行实现的话，实在的太麻烦了，给队列命名都要我们绞尽脑汁了。所以，为了更优雅的编写代码，这里就推出本文标题中的Easy Pipeline框架。</p>
<p>首先，我们举个最简单的例子来说明该框架的工作模式。输入一个数字的序列，按要求对他们进行加减乘除的操作（这里的每个操作，其实可以等价于前面的抽帧或是检测的更复杂的操作 ），并且支持每个操作的进程数。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> easy_pipeline <span class="keyword">import</span> SimplePipeline, PipelineItem, Task, StopTask, EmptyTask</span><br><span class="line"><span class="keyword">import</span> multiprocessing <span class="keyword">as</span> mp</span><br><span class="line"></span><br><span class="line"><span class="comment"># define our Task</span></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">NumTask</span><span class="params">(Task)</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self, x)</span>:</span></span><br><span class="line">        super(NumTask, self).__init__()</span><br><span class="line">        self.val = x</span><br><span class="line"></span><br><span class="line"><span class="comment"># init function, here we use closure to get different function</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">get_init_fn</span><span class="params">(x)</span>:</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">init</span><span class="params">()</span>:</span></span><br><span class="line">        <span class="keyword">return</span> x</span><br><span class="line">    <span class="keyword">return</span> init</span><br><span class="line"></span><br><span class="line"><span class="comment"># operations</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">plus</span><span class="params">(res, task)</span>:</span></span><br><span class="line">    <span class="keyword">return</span> NumTask(task.val + res)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">mul</span><span class="params">(res, task)</span>:</span></span><br><span class="line">    <span class="keyword">return</span> NumTask(task.val * res)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">minus</span><span class="params">(res, task)</span>:</span></span><br><span class="line">    <span class="keyword">return</span> NumTask(task.val - res)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">div</span><span class="params">(res, task)</span>:</span></span><br><span class="line">    <span class="keyword">return</span> NumTask(task.val / res)</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">'__main__'</span>:</span><br><span class="line"></span><br><span class="line">    <span class="comment"># job queue</span></span><br><span class="line">    manager = mp.Manager()</span><br><span class="line">    job_queue = manager.Queue(<span class="number">1000</span>)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># define pipeline and start</span></span><br><span class="line"></span><br><span class="line">    <span class="comment"># x = （(x + 1) * 2 - 3）/ 5</span></span><br><span class="line">    pipeline_items = [</span><br><span class="line">        PipelineItem(plus, get_init_fn(<span class="number">1</span>), <span class="number">1</span>, <span class="number">10</span>),      <span class="comment"># plus 1</span></span><br><span class="line">        PipelineItem(mul, get_init_fn(<span class="number">2</span>), <span class="number">2</span>, <span class="number">10</span>),       <span class="comment"># mul 2</span></span><br><span class="line">        PipelineItem(minus, get_init_fn(<span class="number">3</span>), <span class="number">3</span>, <span class="number">10</span>),     <span class="comment"># minus 3</span></span><br><span class="line">        PipelineItem(div, get_init_fn(<span class="number">5.</span>), <span class="number">4</span>, <span class="number">10</span>),      <span class="comment"># div 5</span></span><br><span class="line">    ]</span><br><span class="line"></span><br><span class="line">    pipeline = SimplePipeline(pipeline_items, job_queue)</span><br><span class="line">    pipeline.start()</span><br><span class="line">    result_queue = pipeline.get_result_queue()</span><br><span class="line"></span><br><span class="line">    <span class="comment"># Feed jobs anytime (before StopTask)</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">10</span>):</span><br><span class="line">        job_queue.put(NumTask(i))</span><br><span class="line"></span><br><span class="line">    <span class="comment"># get partial output</span></span><br><span class="line">    print(<span class="string">'Get Output Start'</span>)</span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">5</span>):</span><br><span class="line">        result = result_queue.get()</span><br><span class="line">        <span class="keyword">if</span> isinstance(result, StopTask):</span><br><span class="line">            print(<span class="string">"get stop task"</span>)</span><br><span class="line">            <span class="keyword">break</span></span><br><span class="line">        <span class="keyword">if</span> isinstance(result, EmptyTask):</span><br><span class="line">            <span class="keyword">continue</span></span><br><span class="line">        print(result.val)</span><br><span class="line">    print(<span class="string">'Get Output End'</span>)</span><br><span class="line"></span><br><span class="line">    <span class="comment"># Feed jobs anytime (before StopTask)</span></span><br><span class="line">    <span class="keyword">for</span> i <span class="keyword">in</span> range(<span class="number">10</span>, <span class="number">20</span>):</span><br><span class="line">        job_queue.put(NumTask(i))</span><br><span class="line"></span><br><span class="line">    <span class="comment"># Stop pipeline, means no more job will be added then.</span></span><br><span class="line">    <span class="comment"># Every process will exit when it has done all current jobs in job_queue</span></span><br><span class="line">    pipeline.stop()</span><br><span class="line"></span><br><span class="line">    <span class="comment"># get all output</span></span><br><span class="line">    print(<span class="string">'Get Output Start'</span>)</span><br><span class="line">    <span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">        result = result_queue.get()</span><br><span class="line">        <span class="keyword">if</span> isinstance(result, StopTask):</span><br><span class="line">            print(<span class="string">"Output Queue Empty"</span>)</span><br><span class="line">            <span class="keyword">break</span></span><br><span class="line">        <span class="keyword">if</span> isinstance(result, EmptyTask):</span><br><span class="line">            <span class="keyword">continue</span></span><br><span class="line">        print(result.val)</span><br><span class="line">    print(<span class="string">'Get Output End'</span>)</span><br></pre></td></tr></table></figure>
<p>下面，我们来简单的说明一下工作逻辑。</p>
<ol type="1">
<li><strong>首先，我们需要定义自己的任务Task。</strong> 只需要继承Task这个类即可，内部可以存放自己喜欢的任何数据。这里只是为了计算，所以就只存放了一个数字。</li>
<li><strong>定义我们的初始化函数和工作函数。</strong> 初始化函数的作用是给每个进程初始化一些资源，如果不需要也可以不要。这里的初始化函数就是返回了一个值，表示操作数。工作函数是最重要的函数，他会处理接收到的Task，处理并返回新的Task（新的Task可以理解为处理的结果）。工作函数有两个输入，一个是资源，即初始化函数的返回值，另一个就是Task本身。</li>
<li><strong>构建Pipeline。</strong> 每个工作模块都只需要用PipelineItem这个对象进行封装即可。器参数分别是：工作函数、初始化函数、进程数、结果队列的长度（-1表示不限长度）。结果队列的长度，通常设置为较大的值即可。因为不能的模块的处理速度可能不同，因此很容易出现结果堆积的现象，如果不支持队列长度，会导致内存的大量的占用。最后将PipelineItem的数组和输入的对垒传给SimplePipeline对象即可构建完我们的整套Pipeline程序了！</li>
<li><strong>启动Pipeline程序，并输入数据。</strong></li>
<li><strong>得到结果！</strong> 完事了，优秀。</li>
</ol>
<p>上面这是一个最简单的例子，可以比较直观的感受到这个框架的便捷之处。完全屏蔽掉对队列，并发等的操作。</p>
<p>在我推荐给同事之后，确实一定程度地减小他的工作量，但同时，他也向我反馈了一些问题：这个框架在某些地方有些比较灵活的设计，应该给出足够多的实例，才能方便实用。关于该框架的设计思路和实例，将会在下一篇博客中进行详细介绍。</p>
<p>最后，欢迎大家Star和提交MR。愿与你们一同进步。</p>
]]></content>
      <categories>
        <category>Framework</category>
        <category>Python</category>
      </categories>
  </entry>
  <entry>
    <title>随笔</title>
    <url>/2019/02/11/2019-02-11/</url>
    <content><![CDATA[<p><img data-src="http://cdn.miaoerduo.com/images/illustration/2019-02-11.jpg" /></p>
<p>边听着盗墓笔记边画画，感觉很不错~</p>
]]></content>
      <categories>
        <category>插画</category>
      </categories>
      <tags>
        <tag>hide</tag>
      </tags>
  </entry>
  <entry>
    <title>随笔</title>
    <url>/2019/02/02/2019-02-02/</url>
    <content><![CDATA[<p><img data-src="http://cdn.miaoerduo.com/images/illustration/2019-02-02-1.jpeg" /></p>
<p><img data-src="http://cdn.miaoerduo.com/images/illustration/2019-02-02-2.jpeg" /></p>
<p>今天淘宝买到的本子终于到了！四本超厚超酷的本子。质量超好，还可以180°打开。用来画画的时候，感觉自己都下意识的更小心的一点，不要用自己拙劣的画技糟蹋了这些本子。手心都是汗 = =，看来需要买一个专用的手套了。</p>
<p><img data-src="http://cdn.miaoerduo.com/images/illustration/2019-02-02-3.jpeg" /></p>
]]></content>
      <categories>
        <category>插画</category>
      </categories>
      <tags>
        <tag>hide</tag>
      </tags>
  </entry>
  <entry>
    <title>随笔</title>
    <url>/2019/01/30/2019-01-30/</url>
    <content><![CDATA[<p><img data-src="http://cdn.miaoerduo.com/images/illustration/2019-01-29.jpg" /></p>
<p>基于上次的画，修改了眼睛和肤色。</p>
]]></content>
      <categories>
        <category>插画</category>
      </categories>
      <tags>
        <tag>hide</tag>
      </tags>
  </entry>
  <entry>
    <title>随笔</title>
    <url>/2019/01/27/2019-01-27/</url>
    <content><![CDATA[<p><img data-src="http://cdn.miaoerduo.com/images/illustration/2019-01-27.jpg" /></p>
<p>周末和女票看了白蛇缘起，十分好看，感慨国产的动漫也已经达到了如此的地步了。看到白蛇的人物的表情的刻画，十分的精细，然后似乎对绘画就有了新的理解。这张是第二天画的，本来想画整个身体，要花好久 = =。刚画完一个脸，觉得，嗯，比之前的都好看多了。所以提前拿出来展示一下。</p>
]]></content>
      <categories>
        <category>插画</category>
      </categories>
      <tags>
        <tag>hide</tag>
      </tags>
  </entry>
  <entry>
    <title>随笔</title>
    <url>/2019/01/13/2019-01-13/</url>
    <content><![CDATA[<p><img data-src="http://cdn.miaoerduo.com/images/illustration/2019-01-13.jpg" /></p>
<p>最近诸事不顺啊。最近被diss完，总算是告一段落了。</p>
<p>今天拿起笔来，试着画了一画，感觉还不错。上色的感觉比之前好多了，一定是因为在淘宝上买了个教程看了两集的缘故。只有画画的时候，才能放空自己。</p>
]]></content>
      <categories>
        <category>插画</category>
      </categories>
      <tags>
        <tag>hide</tag>
      </tags>
  </entry>
  <entry>
    <title>随笔</title>
    <url>/2018/12/27/2018-12-27/</url>
    <content><![CDATA[<p><img data-src="http://cdn.miaoerduo.com/images/illustration/2018-12-27.jpg" /></p>
<p>最近没咋打卡，但其实还是练习了挺久了，主要在学习画侧脸。</p>
<p>今天在B站上看到大佬画厚涂，然后照着试了试。一共就用了三个工具：喷枪，模糊和橡皮。感觉还挺有意思。</p>
]]></content>
      <categories>
        <category>插画</category>
      </categories>
      <tags>
        <tag>hide</tag>
      </tags>
  </entry>
  <entry>
    <title>随笔</title>
    <url>/2018/12/13/2018-12-13/</url>
    <content><![CDATA[<p><img data-src="http://cdn.miaoerduo.com/images/illustration/2018-12-13.bmp" /></p>
<p>今天花了一个侧脸，期间反复修改了不下5次，然后才画成现在这个样子。虽然看起来不是特别的好看，但是在这个过程中，我是真的切实的感受到了自己的进步！</p>
]]></content>
      <categories>
        <category>插画</category>
      </categories>
  </entry>
  <entry>
    <title>随笔</title>
    <url>/2018/12/09/2018-12-09/</url>
    <content><![CDATA[<p><img data-src="http://cdn.miaoerduo.com/images/illustration/2018-12-09-fiary.jpg" /></p>
<p>这张是今天的最后一张，感觉画的最好看，所以放在了最前面了~</p>
<a id="more"></a>
<p>客观上说，这里的线条很乱，脸上的线条有点粗，因为修正了好几次，但不太方便擦，所以就这样保留下来了。</p>
<p>下面的是板绘的练习：</p>
<p><img data-src="http://cdn.miaoerduo.com/images/illustration/2018-12-09-1.png" /></p>
<p><img data-src="http://cdn.miaoerduo.com/images/illustration/2018-12-09-2.png" /></p>
<p>画了两个简单的线稿（应该叫草稿更合适吧）。</p>
<p>看教程说，我们得把草稿变成精草，然后再变成线稿。这些是之后的事情啦。</p>
]]></content>
      <categories>
        <category>插画</category>
      </categories>
      <tags>
        <tag>hide</tag>
      </tags>
  </entry>
  <entry>
    <title>随笔</title>
    <url>/2018/12/08/2018-12-08/</url>
    <content><![CDATA[<p>最近决定从原理入手，所以，开始看头骨的结构，然后就花了一周的头骨（的侧面）。在这里记录一下成果。</p>
<a id="more"></a>
<p><img data-src="http://cdn.miaoerduo.com/images/illustration/2018-12-08-headbone1.jpg" /></p>
<p>首先是头骨的练习，对着《理解人体形态》这本书的头骨部分临摹。看起来有点像样了。</p>
<p><img data-src="http://cdn.miaoerduo.com/images/illustration/2018-12-08-headbone2.jpg" /></p>
<p>这张是不看书，直接自己手绘的，感觉已经很神似了。左边是我画的，右边是书上面的原图。通过测量了各分部的比例，我还试着做了简单的总结。</p>
<p><img data-src="http://cdn.miaoerduo.com/images/illustration/2018-12-08-headbone3.jpg" /></p>
<p>持之以恒！</p>
]]></content>
      <categories>
        <category>插画</category>
      </categories>
      <tags>
        <tag>hide</tag>
      </tags>
  </entry>
  <entry>
    <title>随笔</title>
    <url>/2018/11/16/2018-11-16/</url>
    <content><![CDATA[<p><img data-src="http://cdn.miaoerduo.com/images/illustration/2018-11-16.png" /></p>
<p>今天醒太早了。想着双十一买的东西居然还没有到，就没有了困意。只画了一小部分。慢慢迭代。</p>
]]></content>
      <categories>
        <category>插画</category>
      </categories>
      <tags>
        <tag>hide</tag>
      </tags>
  </entry>
  <entry>
    <title>vim配置</title>
    <url>/2018/11/13/vim/</url>
    <content><![CDATA[<p>下面是我的VIM的主要配置。应该就是网上找到的一个，加了点功能。权当在这里备份一下了。 将下面的内容写入自己的<code>~/.vimrc</code>文件即可。</p>
<a id="more"></a>
<p>针对自动跳转到上次关闭位置的这个功能，我在Ubuntu 14这个系统上失败了一次。是由于viminfo文件的权限不对。使用<code>ls -l ~/.viminfo</code>。如果发现其创建用户不是当前用户的话，那么可以将其删除。下次打开vim的时候会自动新建。这样就搞定了！</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&quot;自动跳到上次的行</span><br><span class="line">if has(&quot;autocmd&quot;)</span><br><span class="line">    au BufReadPost * if line(&quot;&#39;\&quot;&quot;) &gt; 1 &amp;&amp; line(&quot;&#39;\&quot;&quot;) &lt;&#x3D; line(&quot;$&quot;) | exe &quot;normal! g&#39;\&quot;&quot; | endif</span><br><span class="line">endif</span><br><span class="line"></span><br><span class="line">set backspace&#x3D;indent,eol,start</span><br><span class="line"></span><br><span class="line">&quot;去掉vi一致性</span><br><span class="line">filetype plugin indent on</span><br><span class="line"></span><br><span class="line">&quot; 定义F9为粘贴模式</span><br><span class="line">set pastetoggle&#x3D;&lt;F9&gt;</span><br><span class="line"></span><br><span class="line">&quot;设置编码</span><br><span class="line">set encoding&#x3D;utf-8</span><br><span class="line">set fencs&#x3D;utf-8,ucs-bom,shift-jis,gb18030,gbk,gb2312,cp936</span><br><span class="line">set fileencodings&#x3D;utf-8,ucs-bom,chinese</span><br><span class="line"></span><br><span class="line">&quot;语言设置</span><br><span class="line">set langmenu&#x3D;zh_CN.UTF-8</span><br><span class="line"></span><br><span class="line">&quot;设置语法高亮</span><br><span class="line">syntax enable</span><br><span class="line">syntax on</span><br><span class="line"></span><br><span class="line">&quot;设置配色方案</span><br><span class="line">&quot;colorscheme evening</span><br><span class="line"></span><br><span class="line">&quot;高亮显示匹配的括号</span><br><span class="line">set showmatch</span><br><span class="line"></span><br><span class="line">&quot;设置缩进</span><br><span class="line">set tabstop&#x3D;4</span><br><span class="line">set expandtab</span><br><span class="line">retab!</span><br><span class="line">set softtabstop&#x3D;4</span><br><span class="line">set shiftwidth&#x3D;4</span><br><span class="line">set autoindent</span><br><span class="line">set cindent</span><br><span class="line">if &amp;term&#x3D;&#x3D;&quot;xterm&quot;</span><br><span class="line">    set t_Co&#x3D;8</span><br><span class="line">    set t_Sb&#x3D;^[[4%dm</span><br><span class="line">    set t_Sf&#x3D;^[[3%dm</span><br><span class="line">endif</span><br><span class="line"></span><br><span class="line">&quot;打开文件类型自动检测功能</span><br><span class="line">filetype on</span><br><span class="line"></span><br><span class="line"></span><br><span class="line">&quot;设置taglist</span><br><span class="line">let Tlist_Show_One_File&#x3D;0   &quot;显示多个文件的tags</span><br><span class="line">let Tlist_File_Fold_Auto_Close&#x3D;1 &quot;非当前文件，函数列表折叠隐藏</span><br><span class="line">let Tlist_Exit_OnlyWindow&#x3D;1 &quot;在taglist是最后一个窗口时退出vim</span><br><span class="line">let Tlist_Use_SingleClick&#x3D;1 &quot;单击时跳转</span><br><span class="line">let Tlist_GainFocus_On_ToggleOpen&#x3D;1 &quot;打开taglist时获得输入焦点</span><br><span class="line">let Tlist_Process_File_Always&#x3D;1 &quot;不管taglist窗口是否打开，始终解析文件中的tag</span><br><span class="line"></span><br><span class="line">&quot;设置WinManager插件</span><br><span class="line">let g:winManagerWindowLayout&#x3D;&#39;FileExplorer|TagList&#39;</span><br><span class="line">nmap wm :WMToggle&lt;cr&gt;</span><br><span class="line">map &lt;silent&gt; &lt;F9&gt; :WMToggle&lt;cr&gt; &quot;将F9绑定至WinManager,即打开WimManager</span><br><span class="line"></span><br><span class="line">&quot;设置CSCOPE</span><br><span class="line">set cscopequickfix&#x3D;s-,c-,d-,i-,t-,e- &quot;设定是否使用quickfix窗口显示cscope结果</span><br><span class="line"></span><br><span class="line">&quot;设置一键编译</span><br><span class="line">map &lt;F6&gt; :make&lt;CR&gt;</span><br><span class="line"></span><br><span class="line">&quot;设置自动补全</span><br><span class="line">filetype plugin indent on   &quot;打开文件类型检测</span><br><span class="line">set completeopt&#x3D;longest,menu &quot;关掉智能补全时的预览窗口</span><br><span class="line"></span><br><span class="line">&quot;启动vim时如果存在tags则自动加载</span><br><span class="line">if exists(&quot;tags&quot;)</span><br><span class="line">    set tags&#x3D;.&#x2F;tags</span><br><span class="line">endif</span><br><span class="line"></span><br><span class="line">&quot;设置按F12就更新tags的方法</span><br><span class="line">map &lt;F12&gt; :call Do_CsTag()&lt;CR&gt;</span><br><span class="line">nmap &lt;C-@&gt;s :cs find s &lt;C-R&gt;&#x3D;expand(&quot;&lt;cword&gt;&quot;)&lt;CR&gt;&lt;CR&gt;:copen&lt;CR&gt;</span><br><span class="line">nmap &lt;C-@&gt;g :cs find g &lt;C-R&gt;&#x3D;expand(&quot;&lt;cword&gt;&quot;)&lt;CR&gt;&lt;CR&gt;</span><br><span class="line">nmap &lt;C-@&gt;c :cs find c &lt;C-R&gt;&#x3D;expand(&quot;&lt;cword&gt;&quot;)&lt;CR&gt;&lt;CR&gt;:copen&lt;CR&gt;</span><br><span class="line">nmap &lt;C-@&gt;t :cs find t &lt;C-R&gt;&#x3D;expand(&quot;&lt;cword&gt;&quot;)&lt;CR&gt;&lt;CR&gt;:copen&lt;CR&gt;</span><br><span class="line">nmap &lt;C-@&gt;e :cs find e &lt;C-R&gt;&#x3D;expand(&quot;&lt;cword&gt;&quot;)&lt;CR&gt;&lt;CR&gt;:copen&lt;CR&gt;</span><br><span class="line">nmap &lt;C-@&gt;f :cs find f &lt;C-R&gt;&#x3D;expand(&quot;&lt;cfile&gt;&quot;)&lt;CR&gt;&lt;CR&gt;:copen&lt;CR&gt;</span><br><span class="line">nmap &lt;C-@&gt;i :cs find i ^&lt;C-R&gt;&#x3D;expand(&quot;&lt;cfile&gt;&quot;)&lt;CR&gt;$&lt;CR&gt;:copen&lt;CR&gt;</span><br><span class="line">nmap &lt;C-@&gt;d :cs find d &lt;C-R&gt;&#x3D;expand(&quot;&lt;cword&gt;&quot;)&lt;CR&gt;&lt;CR&gt;:copen&lt;CR&gt;</span><br><span class="line">function Do_CsTag()</span><br><span class="line">        let dir &#x3D; getcwd()</span><br><span class="line">        if filereadable(&quot;tags&quot;)</span><br><span class="line">            if(g:iswindows&#x3D;&#x3D;1)</span><br><span class="line">                let tagsdeleted&#x3D;delete(dir.&quot;\\&quot;.&quot;tags&quot;)</span><br><span class="line">            else</span><br><span class="line">                let tagsdeleted&#x3D;delete(&quot;.&#x2F;&quot;.&quot;tags&quot;)</span><br><span class="line">            endif</span><br><span class="line">            if(tagsdeleted!&#x3D;0)</span><br><span class="line">                echohl WarningMsg | echo &quot;Fail to do tags! I cannot delete the tags&quot; | echohl None</span><br><span class="line">                return</span><br><span class="line">            endif</span><br><span class="line">        endif</span><br><span class="line">       if has(&quot;cscope&quot;)</span><br><span class="line">            silent! execute &quot;cs kill -1&quot;</span><br><span class="line">        endif</span><br><span class="line"></span><br><span class="line">        if filereadable(&quot;cscope.files&quot;)</span><br><span class="line">            if(g:iswindows&#x3D;&#x3D;1)</span><br><span class="line">                let csfilesdeleted&#x3D;delete(dir.&quot;\\&quot;.&quot;cscope.files&quot;)</span><br><span class="line">            else</span><br><span class="line">                let csfilesdeleted&#x3D;delete(&quot;.&#x2F;&quot;.&quot;cscope.files&quot;)</span><br><span class="line">            endif</span><br><span class="line">            if(csfilesdeleted!&#x3D;0)</span><br><span class="line">                echohl WarningMsg | echo &quot;Fail to do cscope! I cannot delete the cscope.files&quot; | echohl None</span><br><span class="line">                return</span><br><span class="line">            endif</span><br><span class="line">        endif</span><br><span class="line">        if filereadable(&quot;cscope.out&quot;)</span><br><span class="line">            if(g:iswindows&#x3D;&#x3D;1)</span><br><span class="line">                let csoutdeleted&#x3D;delete(dir.&quot;\\&quot;.&quot;cscope.out&quot;)</span><br><span class="line">            else</span><br><span class="line">                let csoutdeleted&#x3D;delete(&quot;.&#x2F;&quot;.&quot;cscope.out&quot;)</span><br><span class="line">            endif</span><br><span class="line">            if(csoutdeleted!&#x3D;0)</span><br><span class="line">                echohl WarningMsg | echo &quot;Fail to do cscope! I cannot delete the cscope.out&quot; | echohl None</span><br><span class="line">                return</span><br><span class="line">            endif</span><br><span class="line">        endif</span><br><span class="line">       if(executable(&#39;ctags&#39;))</span><br><span class="line">            &quot;silent! execute &quot;!ctags -R --c-types&#x3D;+p --fields&#x3D;+S *&quot;</span><br><span class="line">            silent! execute &quot;!ctags -R --c++-kinds&#x3D;+p --fields&#x3D;+iaS --extra&#x3D;+q .&quot;</span><br><span class="line">        endif</span><br><span class="line"></span><br><span class="line">        if(executable(&#39;cscope&#39;) &amp;&amp; has(&quot;cscope&quot;) )</span><br><span class="line">            if(g:iswindows!&#x3D;1)</span><br><span class="line">                silent! execute &quot;!find . -name &#39;*.h&#39; -o -name &#39;*.c&#39; -o -name &#39;*.cpp&#39; -o -name &#39;*.java&#39; -o -name &#39;*.cs&#39; &gt; cscope.files&quot;</span><br><span class="line">            else</span><br><span class="line">                silent! execute &quot;!dir &#x2F;s&#x2F;b *.c,*.cpp,*.h,*.java,*.cs &gt;&gt; cscope.files&quot;</span><br><span class="line">            endif</span><br><span class="line">            silent! execute &quot;!cscope -b&quot;</span><br><span class="line">            execute &quot;normal :&quot;</span><br><span class="line"></span><br><span class="line">            if filereadable(&quot;cscope.out&quot;)</span><br><span class="line">                execute &quot;cs add cscope.out&quot;</span><br><span class="line">            endif</span><br><span class="line">        endif</span><br><span class="line">endfunction</span><br><span class="line">&quot;设置默认shell</span><br><span class="line">set shell&#x3D;bash</span><br><span class="line"></span><br><span class="line">&quot;设置VIM记录的历史数</span><br><span class="line">set history&#x3D;1000</span><br><span class="line"></span><br><span class="line">&quot;设置当文件被外部改变的时侯自动读入文件</span><br><span class="line">if exists(&quot;&amp;autoread&quot;)</span><br><span class="line">    set autoread</span><br><span class="line">endif</span><br><span class="line"></span><br><span class="line">&quot;设置ambiwidth</span><br><span class="line">set ambiwidth&#x3D;double</span><br><span class="line"></span><br><span class="line">&quot;设置文件类型</span><br><span class="line">set ffs&#x3D;unix,dos,mac</span><br><span class="line"></span><br><span class="line">&quot;设置增量搜索模式</span><br><span class="line">set incsearch</span><br><span class="line"></span><br><span class="line">&quot;设置静音模式</span><br><span class="line">set noerrorbells</span><br><span class="line">set novisualbell</span><br><span class="line">set t_vb&#x3D;</span><br><span class="line"></span><br><span class="line">&quot;不要备份文件</span><br><span class="line">set nobackup</span><br><span class="line">set nowb</span><br><span class="line"></span><br><span class="line">&quot;显示行号</span><br><span class="line">set nu</span><br></pre></td></tr></table></figure>
<p>另外，附上Ubuntu下，VIM 8升级的命令：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo add-apt-repository ppa:jonathonf/vim</span><br><span class="line">sudo apt update</span><br><span class="line">sudo apt install vim</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>Miscellanea</category>
      </categories>
  </entry>
  <entry>
    <title>随笔</title>
    <url>/2018/11/08/2018-11-08/</url>
    <content><![CDATA[<p><img data-src="http://cdn.miaoerduo.com/images/illustration/2018-11-08.jpeg" /></p>
<p>昨天和今天都各画了一次，感觉今天画的稍微好了一点点！</p>
]]></content>
      <categories>
        <category>插画</category>
      </categories>
      <tags>
        <tag>hide</tag>
      </tags>
  </entry>
  <entry>
    <title>嗯，此刻一个非技术板块诞生了</title>
    <url>/2018/11/04/illustration-startup/</url>
    <content><![CDATA[<p>先贴上一张昨晚的作品，这样这篇博客会自动有个插图。：P</p>
<p><img data-src="http://cdn.miaoerduo.com/images/illustration/2018-11-03.png" /></p>
<p>左边的男生是想象着随便画的，右边的女生其实是参考的一个插画画的，画的和原图一点都不像就是了。</p>
<a id="more"></a>
<p>之前一直在博客里面记录一下自己学习的技术技能，最近写博客的频率已经降到了一个很可怕的数值。一方面是工作的内容过于枯燥，没有什么新技术的使用，不知道要写些什么有意思的内容，另一方面就是工作确实太忙了，写一篇博客需要至少半天的时间，平时肯定是没有的，周末又要陪女票大人出去逛街。这真是现实和生活之间的巨大的矛盾啊，果然有句话是对的：成人的世界里，没有容易二字。实在伐开心。</p>
<p>之前，就一直比较痴迷动漫，想着有朝一日自己也能成为P站上的大触。为次也买了很多的教程和工具。但事实上，并没有因为花了钱就学到了东西，所以最重要的还是自己的不断地尝试和积累。o(╯□╰)o</p>
<p>so，这个版块的诞生就是为了记录一下自己的学习插画的过程，等到一年后再回首看看之前的作品，希望能真的感受到进步。当然，另一个更重要的目的是鞭笞自己，既然都把话放到博客上了，怎么着也得坚持个一年半载吧。</p>
<p>这个版块，我设想的内容应该主要有3个部分吧。</p>
<p>第一个部分，是自己的作品，因为是初学者，画的肯定很烂，这些黑历史啥的也要放上来。不断鞭笞自己，等到真有进步了，对比起来也会很开心吧。最近有个同事经常提到一个名词：延迟满足感。大致意思就是不能想要什么就得到什么，而是稍后再得到，这样能带来更大的满足感。好像挺有道理。</p>
<p>第二个部分，是学习到的小技巧，比如软件的使用啊，某些具体的绘画技法呀这样的。所谓温故而知新，好的东西都要记录下来，再三的翻看。说到这里，突然想到，买了半年的CSP，到现在其中的功能甚至是快捷键，我其实都没有好好掌握。真心好菜啊。</p>
<p>最后一部分，应该就是一个大触的作品了。P站上有一大堆，所以呢，这部分估计会记录一些大触的ID啥的吧，换句话说，就是存上大触们的地址。以后想临摹的时候，直接就能找到了。</p>
<p>最后，说一下我的装备吧。</p>
<p>首先，最简单的就是纸笔了，公司有很多A4纸，用来推公式和绘画都很好。用来无聊的时候练习一下手绘。</p>
<p>之后呢，我在宿舍买了一个二手的电脑，还被骗了，哭。总之是一台能用的电脑，除了有时候开不了机之外都还凑合吧。数位板买的是Wacom的影拓CTL-690中号的板子，是之前一直想买，后来在中关村正好看到专卖店，然后就顺手买了的。还附带了一个蓝牙的发射器，能够无线连接。感觉蓝牙的话，有一点点的延时，平时还是用有线连的。说实话，我到现在都没有怎么用过这个板子，对板绘很不习惯，线条都画不好。</p>
<p>软件使用的是CSP，全称Clip Studio Paint，国内引进了，叫优动漫，价格还行。之所以没有用Sai是因为没有MAC版。之后就是PS，这个买不起正版，希望以后有钱了能支持正版吧。</p>
<p>最后说一下，我现在的水平吧。真心很菜，每天就是逛逛B站看看神仙画画，淘宝上买了一些视频教程（并没有怎么看），一堆乱七八糟的书（我都想呵呵我自己），每周画画能有2次？可能吧。从昨天开始，我才能稍微适应一点数位板。对着电脑，眼还容易花，不知道是不是天天看电脑太多了。</p>
<p>现在就只会画头像，但实际上也只是入门级别的（可能还不到），上色？不可能的。对于动态，看了一些教程，要么是火柴人，要么是块状的组合。一点一点慢慢画，可能还能看吧。总是就是很菜很菜。</p>
<p>哈哈，希望我这个菜鸡能够坚持下去，祝好~</p>
]]></content>
      <categories>
        <category>插画</category>
      </categories>
  </entry>
  <entry>
    <title>_rebuild_tensor_v2？pytorch版本间模型兼容性脱坑实践</title>
    <url>/2018/08/02/fix-rebuild-tensor-v2-error-in-pytorch/</url>
    <content><![CDATA[<blockquote>
<p>最近使用Pytorch 0.4.0 进行模型训练，之后使用一个转模型的工具时，报了一个错，就是标题里面的<code>_rebuild_tensor_v2</code>相关的错误。最后发现是本地使用的pytorch的版本是0.3.0，和0.4.0模型上不兼容。各论坛上的解决方案都是说pytorch版本不向后兼容，建议升级pytorch。无奈我这里不方便升级pytorch版本。那么问题就来了，有没有什么不需要修改pytorch源码，或是不升级pytorch，又能让老版本的pytorch读取新版本模型的方案呢？</p>
</blockquote>
<p>当然是有的，而且工作量很小。</p>
<a id="more"></a>
<h2 id="一pytorch模型存储和读取的流程">一、Pytorch模型存储和读取的流程</h2>
<p>首先，我们使用pytorch存储模型会使用 <code>torch.save</code> 这个函数，直接将模型的<code>state_dict()</code>保存下来。类似下面的代码：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">torch.save(&#123;</span><br><span class="line">    <span class="string">'state_dict'</span>: model.state_dict(),</span><br><span class="line">    <span class="string">'other'</span>: other_data</span><br><span class="line">  &#125;,</span><br><span class="line">  <span class="string">'model.pth'</span></span><br><span class="line">)</span><br></pre></td></tr></table></figure>
<p>读取参数的代码也十分简单：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">model = Model()</span><br><span class="line"></span><br><span class="line"><span class="keyword">with</span> open(<span class="string">'model.pth'</span>, <span class="string">'rb'</span>) <span class="keyword">as</span> fp:</span><br><span class="line">    param = torch.load(fp)</span><br><span class="line"></span><br><span class="line">model.load_state_dict(param[<span class="string">'state_dict'</span>])</span><br></pre></td></tr></table></figure>
<p>而低版本的pytorch就是在 <code>load_state_dict</code> 这里报了错。</p>
<h2 id="二state-dict">二、State Dict</h2>
<p>我们首先要知道，<code>model.state_dict()</code>的返回值究竟是什么。</p>
<p>这里我直接给出结论：</p>
<p><code>model.state_dict()</code> 的返回值是一个 <code>collections.OrderedDict</code> 对象，它的键是一个字符串，它的值是Tensor的对象。所以造成兼容性问题的其实是Tensor对象的不兼容。</p>
<p>那么是不是可以将Tensor转化成一个新的非Pytorch内置的数据类型呢？这样就可以避免兼容性问题。</p>
<p><code>numpy.ndarray</code> 就是我们需要的中间态。</p>
<h2 id="三模型转换">三、模型转换</h2>
<p>首先，我们需要将state_dict的参数转换成 <code>numpy.ndarray</code> 保存下来。这里使用高版本的pytorch。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> torch</span><br><span class="line"><span class="keyword">from</span> collections <span class="keyword">import</span> OrderedDict</span><br><span class="line"><span class="keyword">import</span> pickle</span><br><span class="line"></span><br><span class="line"><span class="keyword">with</span> open(<span class="string">'model.pth'</span>, <span class="string">'rb'</span>) <span class="keyword">as</span> fp:</span><br><span class="line">    param = torch.load(fp)</span><br><span class="line"></span><br><span class="line">state_dict = param[<span class="string">'state\_dict'</span>]</span><br><span class="line">numpy_state_dict = OrderedDict()</span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> key, tensor <span class="keyword">in</span> state_dict.items():</span><br><span class="line">    numpy_state_dict[key] = tensor.cpu().numpy()</span><br><span class="line"></span><br><span class="line"><span class="keyword">with</span> open(<span class="string">'state_dict.pic'</span>, <span class="string">'wb'</span>) <span class="keyword">as</span> fp:</span><br><span class="line">    pickle.dump(numpy_state_dict, fp)</span><br></pre></td></tr></table></figure>
<p>之后，用低版本的pytorch载入这个numpy的<code>state_dict</code>。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> pickle</span><br><span class="line"><span class="keyword">import</span> torch</span><br><span class="line"></span><br><span class="line"><span class="keyword">with</span> open(<span class="string">'numpy_state_dict.pic'</span>, <span class="string">'rb'</span>) <span class="keyword">as</span> fp:</span><br><span class="line">    state_dict = pickle.load(fp)</span><br><span class="line"></span><br><span class="line"><span class="comment"># numpy.ndarray -&gt; tensor</span></span><br><span class="line"><span class="keyword">for</span> key, ndarr <span class="keyword">in</span> state_dict.items():</span><br><span class="line">    state_dict[key] = torch.Tensor(ndarr)</span><br><span class="line"></span><br><span class="line">model = Model()</span><br><span class="line">model.load_state_dict(state_dict)</span><br></pre></td></tr></table></figure>
<h2 id="四总结">四、总结</h2>
<p>对于这个问题，还有很多的解决方案，这里是比较简单的一种。</p>
<p>PS. 这是目前为止，写的最快的一篇博客了。。。</p>
<p><strong>转载请注明出处，谢谢！</strong></p>
]]></content>
      <categories>
        <category>Deep Learning</category>
      </categories>
  </entry>
  <entry>
    <title>C++ Boost JSON解析库的使用</title>
    <url>/2018/06/22/the-usage-of-cpp-boost-json/</url>
    <content><![CDATA[<blockquote>
<p>最近在写一个C++项目的时候，有大量的配置信息，于是将这些配置信息整合进一个文本文件中，选择了JSON这种数据格式。C++在处理JSON数据的库有很多，比如Jsoncpp，Boost等，这个项目中由于本身就已经用到了Boost这个库，因此，也就选用Boost来进行JSON的解析了。</p>
</blockquote>
<p>Boost的JSON解析，使用的是property_tree这个数据类型，它可以方便的解析XML和JSON。</p>
<a id="more"></a>
<h2 id="一boost-json解析库的几个注意事项">一、Boost JSON解析库的几个注意事项</h2>
<p>在具体介绍之前，必须要强调一下，这个库默认不是线程安全的！不是线程安全的！不是线程安全的！不做任何处理的情况下，如果直接在多线程的程序中使用Boost解析JSON，可能会在奇怪的时候报段错误。这是由于Boost的JSON解析是基于SPIRIT语法解析的，而SPIRIT本身就不是线程安全的，我们如果需要它支持线程安全，就必须加入一个宏<code>#define BOOST_SPIRIT_THREADSAFE</code>，把它放在引用boost的头文件的最开始就行。理论上，在编译的时候加入宏也是可以的。另一个需要注意的是，一般网上找的教程中，property_tree都是不支持unicode编码的，如果想要支持unicode，需要一些额外的操作。这个从网上可以查到，我尝试了一下，最终还是放弃了。取而代之的一个方案就是把中文的各种路径啥的，用软链接替换成英文和数字。之后世界就美好了。</p>
<h2 id="二boostproperty_treeptree-类型">二、boost::property_tree::ptree 类型</h2>
<p>对于JSON或者XML，boost将他们解析之后都会生成一个ptree的数据结构。类似于下面的结构。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">ptree</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    data_type data;                         <span class="comment">// data associated with the node</span></span><br><span class="line">    <span class="built_in">list</span>&lt; pair&lt;key_type, ptree&gt; &gt; children; <span class="comment">// ordered list of named children</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>可以看出，这是一个很标准的树的结构。对于树中的每一个节点，都有自己的数值和子节点，每个子节点都有一个唯一的名字。<code>data_type</code>和<code>key_type</code>通常是<code>std::string</code>或<code>std::wstring</code>。如果希望处理unicode的字符串的话，就需要用到<code>std::wstring</code>了。下面的例子中，使用的全部都是<code>std::string</code>。</p>
<h2 id="三json文件的解析">三、JSON文件的解析</h2>
<p>首先，我们用一个小栗子，来介绍一下Boost是如何读取JSON数据的。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;boost/property_tree/ptree.hpp&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;boost/property_tree/json_parser.hpp&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sstream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">string</span> s = <span class="string">"&#123; \"a\": 100, \"b\": [1, 2, 3, 4] &#125;"</span>;</span><br><span class="line">    <span class="built_in">std</span>::<span class="function"><span class="built_in">stringstream</span> <span class="title">ss</span><span class="params">(s)</span></span>;</span><br><span class="line">    boost::property_tree::ptree ptree;</span><br><span class="line">    <span class="comment">// 读取JSON数据</span></span><br><span class="line">    boost::property_tree::read_json(ss, ptree);</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; ptree.get_child(<span class="string">"a"</span>).data() &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">    <span class="comment">// 写回JSON数据</span></span><br><span class="line">    boost::property_tree::write_json(<span class="string">"./tmp.json"</span>, ptree);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这里首先我们需要定义一个<code>boost::property_tree::ptree</code>类型的对象，之后通过<code>boost::property_tree::read_json</code>函数进行数据的读取，之后就可以使用各种ptree的接口进行数据的操作了。在<code>boost/property_tree/json_parser.hpp</code>文件中我们可以看到读写JSON的一些接口。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">namespace</span> boost &#123;</span><br><span class="line">    <span class="keyword">namespace</span> property_tree &#123;</span><br><span class="line">        <span class="keyword">namespace</span> json_parser &#123;</span><br><span class="line">            <span class="keyword">template</span>&lt;<span class="keyword">typename</span> Ptree&gt; </span><br><span class="line">            <span class="function"><span class="keyword">void</span> <span class="title">read_json</span><span class="params">(<span class="built_in">std</span>::basic_istream&lt; <span class="keyword">typename</span> Ptree::key_type::value_type &gt; &amp;, </span></span></span><br><span class="line"><span class="function"><span class="params">                           Ptree &amp;)</span></span>;</span><br><span class="line">            <span class="keyword">template</span>&lt;<span class="keyword">typename</span> Ptree&gt; </span><br><span class="line">            <span class="function"><span class="keyword">void</span> <span class="title">read_json</span><span class="params">(<span class="keyword">const</span> <span class="built_in">std</span>::<span class="built_in">string</span> &amp;, Ptree &amp;, </span></span></span><br><span class="line"><span class="function"><span class="params">                            <span class="keyword">const</span> <span class="built_in">std</span>::locale &amp; = <span class="built_in">std</span>::locale())</span></span>;</span><br><span class="line">            <span class="keyword">template</span>&lt;<span class="keyword">typename</span> Ptree&gt; </span><br><span class="line">            <span class="function"><span class="keyword">void</span> <span class="title">write_json</span><span class="params">(<span class="built_in">std</span>::basic_ostream&lt; <span class="keyword">typename</span> Ptree::key_type::value_type &gt; &amp;, </span></span></span><br><span class="line"><span class="function"><span class="params">                            <span class="keyword">const</span> Ptree &amp;, <span class="keyword">bool</span> = <span class="literal">true</span>)</span></span>;</span><br><span class="line">            <span class="keyword">template</span>&lt;<span class="keyword">typename</span> Ptree&gt; </span><br><span class="line">            <span class="function"><span class="keyword">void</span> <span class="title">write_json</span><span class="params">(<span class="keyword">const</span> <span class="built_in">std</span>::<span class="built_in">string</span> &amp;, <span class="keyword">const</span> Ptree &amp;, </span></span></span><br><span class="line"><span class="function"><span class="params">                            <span class="keyword">const</span> <span class="built_in">std</span>::locale &amp; = <span class="built_in">std</span>::locale(), <span class="keyword">bool</span> = <span class="literal">true</span>)</span></span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>它支持读写JSON，对于读取操作，它支持直接根据文件名称来加载JSON或者通过输入流来加载。输出也是相同。所以我们上面的Demo中，需要将字符串<code>s</code>转换成字符串流对象<code>ss</code>，之后才能进行加载。写文件支持写入到文件或者输出流中，最后一个bool值表示是否格式化输出json。</p>
<h2 id="四json对象的读取">四、JSON对象的读取</h2>
<p>我们知道JSON对象主要有两种格式：键值对和数组。JSON灵活就在于键值对的值还可以键值对或者数组，数组的每个元素也是。那么我们分别介绍键值对和数组的数据获取方式。</p>
<h3 id="键值对的解析">1）键值对的解析</h3>
<p>ptree支持一个操作叫做<code>get_child</code>，可以根据键的名字，来获取子节点。而且这个名字还可以是累加的。什么叫可以累加呢？我们看一下下面的代码：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;boost/property_tree/ptree.hpp&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;boost/property_tree/json_parser.hpp&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sstream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">string</span> s = <span class="string">"&#123; \"a\": &#123; \"b\":1, \"c\":2 &#125;, \"d\":3 &#125;"</span>;</span><br><span class="line">    <span class="built_in">std</span>::<span class="function"><span class="built_in">stringstream</span> <span class="title">ss</span><span class="params">(s)</span></span>;</span><br><span class="line">    boost::property_tree::ptree ptree;</span><br><span class="line">    boost::property_tree::read_json(ss, ptree);</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">"input text:"</span> &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">    boost::property_tree::write_json(<span class="built_in">std</span>::<span class="built_in">cout</span>, ptree);</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">"-------------------------"</span> &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">"parse result: "</span> &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">"a-&gt;b: "</span> &lt;&lt; ptree.get_child(<span class="string">"a"</span>).get_child(<span class="string">"b"</span>).get_value&lt;<span class="keyword">int</span>&gt;() &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">"a-&gt;b: "</span> &lt;&lt; ptree.get_child(<span class="string">"a.b"</span>).get_value&lt;<span class="keyword">int</span>&gt;() &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">"a-&gt;c: "</span> &lt;&lt; ptree.get_child(<span class="string">"a.c"</span>).get_value&lt;<span class="keyword">int</span>&gt;() &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">"d: "</span> &lt;&lt; ptree.get_child(<span class="string">"d"</span>).get_value&lt;<span class="keyword">int</span>&gt;() &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>输出的结果为：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">input text:</span><br><span class="line">&#123;</span><br><span class="line">    &quot;a&quot;: &#123;</span><br><span class="line">        &quot;b&quot;: &quot;1&quot;,</span><br><span class="line">        &quot;c&quot;: &quot;2&quot;</span><br><span class="line">    &#125;,</span><br><span class="line">    &quot;d&quot;: &quot;3&quot;</span><br><span class="line">&#125;</span><br><span class="line">-------------------------</span><br><span class="line">parse result:</span><br><span class="line">a-&gt;b: 1</span><br><span class="line">a-&gt;b: 1</span><br><span class="line">a-&gt;c: 2</span><br><span class="line">d: 3</span><br></pre></td></tr></table></figure>
<p><code>get_child</code>这个函数，可以根据节点的名字，获取到子节点的ptree对象。这个节点的名字可以使用<code>.</code>连接各个层级的名称。<code>get_value&lt;Type&gt;</code>方法，可以获取节点的值，并且转换成期望的数据类型。如果我们就是想获取节点的值。不期望有任何转换，可以使用<code>data</code>这个函数。 <code>get_child</code>要求输入的名称路径必须是存在的，否则会抛异常。如果我们不知道某个名称路径是否存在的话，可以使用<code>get_child_optional</code>这个函数，如果路径不存在，该函数会返回<code>boost::null</code>。<code>get_child_optional</code>返回的类似于指针的结构，如果需要获取值，可以用这样的写法：<code>pt.get_child_optional("some_key")-&gt;get_value&lt;int&gt;()</code>。 我们可以向现在这样通过各种树的操作，选择到我们的需要的节点，再通过<code>get_value&lt;Type&gt;</code>函数获取到数据值。但这样的操作有时候会有点繁琐。boost支持更简化的一些操作。下面是同样功能的一个例子：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;boost/property_tree/ptree.hpp&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;boost/property_tree/json_parser.hpp&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sstream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">string</span> s = <span class="string">"&#123; \"a\": &#123; \"b\":1, \"c\":2 &#125;, \"d\":3 &#125;"</span>;</span><br><span class="line">    <span class="built_in">std</span>::<span class="function"><span class="built_in">stringstream</span> <span class="title">ss</span><span class="params">(s)</span></span>;</span><br><span class="line">    boost::property_tree::ptree ptree;</span><br><span class="line">    boost::property_tree::read_json(ss, ptree);</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">"input text:"</span> &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">    boost::property_tree::write_json(<span class="built_in">std</span>::<span class="built_in">cout</span>, ptree);</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">"-------------------------"</span> &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">"parse result: "</span> &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">"a-&gt;b: "</span> &lt;&lt; ptree.get&lt;<span class="keyword">int</span>&gt;(<span class="string">"a.b"</span>) &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">"a-&gt;c: "</span> &lt;&lt; ptree.get&lt;<span class="keyword">int</span>&gt;(<span class="string">"a.c"</span>) &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">"d: "</span> &lt;&lt; ptree.get&lt;<span class="keyword">int</span>&gt;(<span class="string">"d"</span>) &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><code>get</code>这个函数相当于先<code>get_child</code>得到要找的节点，之后再调用<code>get_value&lt;Type&gt;</code>这个函数。<code>get_value&lt;Type&gt;</code>这个函数可以获取节点的值，同时把它转换成<code>Type</code>格式。即<code>ptree.get&lt;int&gt;("a.b")</code>等价于<code>ptree.get_child("a.b").get_value&lt;int&gt;()</code>。 通过<code>get</code>函数，我们可以很方便的获取某个节点的数据，而且还能顺便完成类型的转换，真的不能更方便了！</p>
<h3 id="数组的解析">2）数组的解析</h3>
<p>为什么数组的解析要单独来说呢？因为，数组格式中，没有键，所以我们不能根据名字来获取节点了，所以读取的方式有了些许的不同。 Boost针对数组，给我们提供了遍历子节点的迭代器接口。可以十分方便的遍历某节点的所有的子节点（当然在键值对的解析中也可以使用）。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;boost/property_tree/ptree.hpp&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;boost/property_tree/json_parser.hpp&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sstream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">string</span> s = <span class="string">"[1, 2, 3, 4]"</span>;</span><br><span class="line">    <span class="built_in">std</span>::<span class="function"><span class="built_in">stringstream</span> <span class="title">ss</span><span class="params">(s)</span></span>;</span><br><span class="line">    boost::property_tree::ptree ptree;</span><br><span class="line">    boost::property_tree::read_json(ss, ptree);</span><br><span class="line">    boost::property_tree::write_json(<span class="built_in">std</span>::<span class="built_in">cout</span>, ptree);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// visit array data</span></span><br><span class="line">    <span class="keyword">for</span> (boost::property_tree::ptree::iterator it = ptree.begin(); it != ptree.end(); ++ it) &#123;</span><br><span class="line">        <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; it-&gt;second.get_value&lt;<span class="keyword">int</span>&gt;() &lt;&lt; <span class="string">" "</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// simpler in c++11</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">auto</span> it: ptree) &#123;</span><br><span class="line">        <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; it.second.get_value&lt;<span class="keyword">int</span>&gt;() &lt;&lt; <span class="string">" "</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>打印的结果：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">    &quot;&quot;: &quot;1&quot;,</span><br><span class="line">    &quot;&quot;: &quot;2&quot;,</span><br><span class="line">    &quot;&quot;: &quot;3&quot;,</span><br><span class="line">    &quot;&quot;: &quot;4&quot;</span><br><span class="line">&#125;</span><br><span class="line">1 2 3 4</span><br><span class="line">1 2 3 4</span><br></pre></td></tr></table></figure>
<p>可以看出，Boost中将JSON数组也是按照键值对的方式去存储，只是键的内容是一个空的字符串。迭代器的<code>first</code>是键的结果，数组中就是空字符串。<code>second</code>就是我们的值。</p>
<h3 id="其他的实用接口">3）其他的实用接口</h3>
<ul>
<li><code>bool empty()</code>: 返回该节点是否含有子节点。比如当一个节点已经是叶子节点的时候，可以用这个函数来判断。</li>
<li><code>assoc_iterator find(const key_type &amp;key)</code>: 给定一个名字路径，返回指向该节点的迭代器或者<code>boost::property_tree::ptree::not_found</code>。</li>
<li><code>size_type count(const key_type &amp;key)</code>: 返回指定名称路径的节点的子节点的数目。</li>
</ul>
<h2 id="五json对象的编辑">五、JSON对象的编辑</h2>
<p>Boost支持很多的对JSON对象的写的操作，但是我在项目中没有用到，所以在这里暂时就没有动力整理下去了~~ 这里附上Boost ptree的文档，方便大家查阅：<span class="exturl" data-url="aHR0cHM6Ly93d3cuYm9vc3Qub3JnL2RvYy9saWJzLzFfNjVfMS9ib29zdC9wcm9wZXJ0eV90cmVlL3B0cmVlLmhwcA==" title="https://www.boost.org/doc/libs/1_65_1/boost/property_tree/ptree.hpp">https://www.boost.org/doc/libs/1_65_1/boost/property_tree/ptree.hpp<i class="fa fa-external-link"></i></span></p>
<h2 id="六疑难杂症">六、疑难杂症</h2>
<h3 id="怎么判断某个键是否存在">1.怎么判断某个键是否存在？</h3>
<p>使用<code>get_child_optional</code>，再判断返回是否为<code>boost::null</code>，这个对象直接相当于<code>false</code>。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">auto</span> node = ptree.get_child_optional(<span class="string">"somekey"</span>);</span><br><span class="line"><span class="keyword">if</span> (!node) &#123;</span><br><span class="line">    <span class="comment">// node not exists</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="怎么方便的遍历数组">2.怎么方便的遍历数组？</h3>
<p>这个功能，我还专门查过。其实懂了之前的迭代器的使用，就能方便的遍历了。下面是我用的一个代码。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="built_in">std</span>::<span class="built_in">vector</span>&lt;T&gt; as_vector(boost::property_tree::ptree <span class="keyword">const</span> &amp;pt, boost::property_tree::ptree::key_type <span class="keyword">const</span>&amp; key) &#123;</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">vector</span>&lt;T&gt; r;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">auto</span> &amp;item: pt.get_child(key)) &#123;</span><br><span class="line">        r.push_back(item.second.get_value&lt;T&gt;());</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> r;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>使用的话就这样：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;boost/property_tree/ptree.hpp&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;boost/property_tree/json_parser.hpp&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;sstream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;vector&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">typename</span> T&gt;</span><br><span class="line"><span class="built_in">std</span>::<span class="built_in">vector</span>&lt;T&gt; as_vector(boost::property_tree::ptree <span class="keyword">const</span> &amp;pt, boost::property_tree::ptree::key_type <span class="keyword">const</span>&amp; key) &#123;</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">vector</span>&lt;T&gt; r;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">auto</span> &amp;item: pt.get_child(key)) &#123;</span><br><span class="line">        r.push_back(item.second.get_value&lt;T&gt;());</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> r;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">string</span> s = <span class="string">"&#123;\"arr\": [1, 2, 3, 4]&#125;"</span>;</span><br><span class="line">    <span class="built_in">std</span>::<span class="function"><span class="built_in">stringstream</span> <span class="title">ss</span><span class="params">(s)</span></span>;</span><br><span class="line">    boost::property_tree::ptree ptree;</span><br><span class="line">    boost::property_tree::read_json(ss, ptree);</span><br><span class="line">    boost::property_tree::write_json(<span class="built_in">std</span>::<span class="built_in">cout</span>, ptree);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">auto</span> result = as_vector&lt;<span class="keyword">int</span>&gt;(ptree, <span class="string">"arr"</span>);</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">auto</span> &amp;&amp;d: result) &#123;</span><br><span class="line">        <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; d &lt;&lt; <span class="string">" "</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>不过这个解决方案有个问题，就是如果根节点就是数组的话，似乎就不能很好的work了。</p>
<h3 id="怎么解析中文">3.怎么解析中文</h3>
<p>/(ㄒoㄒ)/~~</p>
<p><strong>转载请注明出处，谢谢！</strong></p>
]]></content>
      <categories>
        <category>C++</category>
      </categories>
  </entry>
  <entry>
    <title>C++ 并发队列的原理简介与开源库concurrentqueue安利</title>
    <url>/2018/04/30/cpp-concurrent/</url>
    <content><![CDATA[<blockquote>
<p>由于最近在做一个项目，但是框架本身有个不合理的设计。其中的代码是单线程的，数据的读取和计算都在一个线程里面完成。也就是说，我们的程序有很大的一部分时间在读取文件数据，导致最终的运行速度很慢。这里就可以使用多线程来优化。</p>
<p>这里需要使用最基本的<strong>生产者消费者模式</strong>。使用若干个线程作为生产者，负责数据的读取和预处理，这部分任务是IO密集型的，也就是不太占CPU，但是比较占带宽，而且有延时。在处理完数据之后，将数据放到一个队列中。</p>
<p>同时，使用若干个线程充当消费者，从这个队列里面获取数据，然后进行计算。计算的部分是CPU密集型的（其实我这里计算是GPU做的，就只有一个消费者），计算完成之后输出结果。</p>
<p>那么贯穿这一整套方案的，就是我们的队列。</p>
<p>在并发任务中，通常都需要一个队列机制，将并行的任务转化成串行的任务，或者将串行的任务提供给并行工作的线程。这个队列会同时被多个线程读写，因此也必须是线程安全的。</p>
</blockquote>
<a id="more"></a>
<h2 id="一线程安全的实现策略">一、线程安全的实现策略</h2>
<p>对于线程安全的队列的实现，似乎经常成为企业的面试题，常见的实现方法就是互斥量和条件变量，本质上就是锁的机制。同一时间只有一个线程具有读写的权限。锁的机制在并发量不大情况下，十分的清晰有效。在并发量较大的时候，会因为对锁的竞争而越发不高效。同时，锁本身也需要维护一定的资源，也需要消耗性能。</p>
<p>这时候，大家肯定会想问，不使用锁机制，还可以处理这种并发的情况吗？</p>
<p>答案是肯定的，首先我们知道锁主要有两种，悲观锁和乐观锁。</p>
<p>对于悲观锁，它永远会假定最糟糕的情况，就像我们上面说到的互斥机制，每次我们都假定会有其他的线程和我们竞争资源，因此必须要先拿到锁，之后才放心的进行我们的操作，这就使得争夺锁成为了我们每次操作的第一步。乐观锁则不同，乐观锁假定在很多情况下，资源都不需要竞争，因此可以直接进行读写，但是如果碰巧出现了多线程同时操控数据的情况，那么就多试几次，直到成功（也可以设置重试的次数）。</p>
<p>我们生活的时候，总会碰到很多的不顺心的事情，比如模型训练崩了，被某些库搞得头大，或者女票又生气了什么的，不妨学习一下乐观锁的精神，再训一次？再编译一次？大不了再哄一次。一次不行就两次。</p>
<p>回到乐观锁上，乐观锁中，每次读写都不考虑锁的存在，那么他是如何知道自己这次操作和其他线程是冲突的呢？这就是Lock-free队列的关键——原子操作。原子操作可以保证一次操作在执行的过程中不会被其他线程打断，因此在多线程程序中也不需要同步操作。在C++的STL中其实也提供了atomic这个库，可以保证多线程在操控同一个变量的时候，即使不加锁也能保证起最终结果的正确性。而我们乐观锁需要的一个原子操作就是CAS（Compare And Swap），绝大多数的CPU都支持这个操作。</p>
<p>CAS操作的定义如下(STL中的一个)：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">atomic_compare_exchange_weak</span> <span class="params">(atomic* obj, T* expected, T val)</span></span>;</span><br></pre></td></tr></table></figure>
<p>首先函数会将 <code>obj</code> 与 <code>expected</code> 的内容作比较：</p>
<ol type="1">
<li>如果相等，那么将交换 <code>obj</code> 和 <code>val</code> 的值，并返回 <code>true</code>。</li>
<li>如果不相等，则什么也不做，之后返回 <code>false</code>。</li>
</ol>
<p>那么使用这个奇怪的操作，为什么就可以实现乐观锁了呢？这里我们看一个例子。这也是我学习的时候看的例子。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">list</span> &#123;</span></span><br><span class="line">    <span class="built_in">std</span>::atomic&lt;node*&gt; head;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">append</span><span class="params">(<span class="built_in">list</span>* s, node* n)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    node* head;</span><br><span class="line">    <span class="keyword">do</span> &#123;</span><br><span class="line">        head = s-&gt;head;</span><br><span class="line">        n-&gt;next = head;</span><br><span class="line">    &#125; <span class="keyword">while</span> (!<span class="built_in">std</span>::atomic_compare_exchange_weak(&amp;(s-&gt;head), &amp;head, n));</span><br><span class="line">    <span class="comment">// or while (!s-&gt;head.compare_exchange_weak(head, n));</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>在我们向list中插入元素的时候，首先获取到当前的头指针的值head，然后我们在写数据的时候，首先和此刻的头指针值作对比，如果相同，那么就把新的节点插入。如果不相同，说明有线程先我们一步成功了，那么我们就多尝试一次，直到写入成功。</p>
<p>以上就是使用CAS操作实现的乐观锁。上面的这个append就是最简单的Lock-free且线程安全的操作。</p>
<h2 id="二concurrentqueue">二、concurrentqueue</h2>
<p>最近在做这个项目的时候，就被安利了一个header only的C++并发队列库 <span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL2NhbWVyb24zMTQvY29uY3VycmVudHF1ZXVl" title="https://github.com/cameron314/concurrentqueue">concurrentqueue<i class="fa fa-external-link"></i></span>。本着不重复造轮子的原则，我在项目中用了这个库，由于它只是两个头文件，特别方便的就加入到了项目中。关于这个库的特点，项目的github上写了很多。这里直接照搬下来，不做解释。</p>
<blockquote>
<ul>
<li>Knock-your-socks-off <span class="exturl" data-url="aHR0cDovL21vb2R5Y2FtZWwuY29tL2Jsb2cvMjAxNC9hLWZhc3QtZ2VuZXJhbC1wdXJwb3NlLWxvY2stZnJlZS1xdWV1ZS1mb3ItYysrI2JlbmNobWFya3M=" title="http://moodycamel.com/blog/2014/a-fast-general-purpose-lock-free-queue-for-c++#benchmarks">blazing fast performance<i class="fa fa-external-link"></i></span>.</li>
<li>Single-header implementation. Just drop it in your project.</li>
<li>Fully thread-safe lock-free queue. Use concurrently from any number of threads.</li>
<li>C++11 implementation -- elements are moved (instead of copied) where possible.</li>
<li>Templated, obviating the need to deal exclusively with pointers -- memory is managed for you.</li>
<li>No artificial limitations on element types or maximum count.</li>
<li>Memory can be allocated once up-front, or dynamically as needed.</li>
<li>Fully portable (no assembly; all is done through standard C++11 primitives).</li>
<li>Supports super-fast bulk operations.</li>
<li>Includes a low-overhead blocking version (BlockingConcurrentQueue).</li>
<li>Exception safe.</li>
</ul>
</blockquote>
<p>我体验了一下，感觉最舒服的有以下几点：</p>
<ol type="1">
<li>这个库确实可以很好的实现线程安全队列，而且速度很快。接口也比较简单。很容易上手。</li>
<li>整个库就是两个头文件，而且没有其他的依赖，使用C++11实现，兼容各大平台，很容易融入项目。</li>
<li>这个并发队列支持阻塞和非阻塞两种。(只在获取元素的时候可以阻塞)</li>
</ol>
<p>因为这个队列的用法十分简单，这里就直接贴上官网的介绍，然后针对一些细节，补充说明一下，下面是<strong>非阻塞队列</strong>的常用接口：</p>
<blockquote>
<ul>
<li><code>ConcurrentQueue(size_t initialSizeEstimate)</code> Constructor which optionally accepts an estimate of the number of elements the queue will hold</li>
<li><code>enqueue(T&amp;&amp; item)</code> Enqueues one item, allocating extra space if necessary</li>
<li><code>try_enqueue(T&amp;&amp; item)</code> Enqueues one item, but only if enough memory is already allocated</li>
<li><code>try_dequeue(T&amp; item)</code> Dequeues one item, returning true if an item was found or false if the queue appeared empty</li>
</ul>
</blockquote>
<ul>
<li><code>ConcurrentQueue(size_t initialSizeEstimate)</code> 这个没什么好说的，一个构造函数，可以指定队列的容量。</li>
<li><code>enqueue(T&amp;&amp; item)</code> 入队操作。比较有意思的是，如果我们的队列已经满了的话，那么这个还是会把数据放到队列里，使得队列的容量变大。所以，如果希望队列的长度不变的话，尽量还是不要使用这个函数。</li>
<li><code>try_enqueue(T&amp;&amp; item)</code> 这个也是入队操作，与上一个不同，这个函数当队列已经满了的时候，并不会进行入队操作，而是返回一个bool类型的值，表示是否入队成功。我在使用的时候，会判断这个bool值，如果是false，就让线程等待10ms之后重试。</li>
<li><code>try_dequeue(T&amp; item)</code> 这个是出队操作，如果队列有值的话，则得到数据（放到参数item里面）。他也会返回一个bool类型的值，表示时候出队成功。</li>
</ul>
<p>在<strong>阻塞版本的队列</strong>中，主要多了如下两个函数： <code>wait_dequeue(T&amp;&amp; item)</code>和 <code>wait_dequeue_timed(T&amp;&amp; item, std::int64_t timeout_usecs)</code>。这两个函数的功能类似，都是进行出队操作，如果队列为空，则等待。唯一的区别是，前者永久等待，而后者可以指定等待的时间，如果超时，则会停止等待并返回false。</p>
<p>最后是块操作，两种模式的队列都支持批量插入的操作。这里，我没有用过这些接口，所以大家自行看文档就好。 完整的API：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"># Allocates more memory <span class="keyword">if</span> necessary</span><br><span class="line">enqueue(item) : <span class="keyword">bool</span></span><br><span class="line">enqueue(prod_token, item) : <span class="keyword">bool</span></span><br><span class="line">enqueue_bulk(item_first, count) : <span class="keyword">bool</span></span><br><span class="line">enqueue_bulk(prod_token, item_first, count) : <span class="keyword">bool</span></span><br><span class="line"></span><br><span class="line"># Fails <span class="keyword">if</span> <span class="keyword">not</span> enough memory to enqueue</span><br><span class="line">try_enqueue(item) : <span class="keyword">bool</span></span><br><span class="line">try_enqueue(prod_token, item) : <span class="keyword">bool</span></span><br><span class="line">try_enqueue_bulk(item_first, count) : <span class="keyword">bool</span></span><br><span class="line">try_enqueue_bulk(prod_token, item_first, count) : <span class="keyword">bool</span></span><br><span class="line"></span><br><span class="line"># <span class="function">Attempts to dequeue from the <span class="title">queue</span> <span class="params">(never allocates)</span></span></span><br><span class="line">try_dequeue(item&amp;) : bool</span><br><span class="line">try_dequeue(cons_token, item&amp;) : <span class="keyword">bool</span></span><br><span class="line">try_dequeue_bulk(item_first, max) : <span class="keyword">size_t</span></span><br><span class="line">try_dequeue_bulk(cons_token, item_first, max) : <span class="keyword">size_t</span></span><br><span class="line"></span><br><span class="line"># If you happen to know which producer you want to dequeue from</span><br><span class="line">try_dequeue_from_producer(prod_token, item&amp;) : <span class="keyword">bool</span></span><br><span class="line">try_dequeue_bulk_from_producer(prod_token, item_first, max) : <span class="keyword">size_t</span></span><br><span class="line"></span><br><span class="line"># A <span class="keyword">not</span>-necessarily-accurate count of the total number of elements</span><br><span class="line">size_approx() : <span class="keyword">size_t</span></span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>C++</category>
      </categories>
  </entry>
  <entry>
    <title>CNN的目标检测概述（三）</title>
    <url>/2018/04/03/cnn-object-detection3/</url>
    <content><![CDATA[<p>本次介绍的是Fast R-CNN，与之前的RCNN和SPPNet不同，Fast R-CNN是一个清晰和快速的目标检测的框架。在训练和测试的速度上都远超过上述两种方法。同时，Fast R-CNN的训练是一次性的端到端的训练，同时训练的分类和回归两个任务。极大的简化的训练的流程。</p>
<p>项目代码：<span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL3JiZ2lyc2hpY2svZmFzdC1yY25u" title="https://github.com/rbgirshick/fast-rcnn">https://github.com/rbgirshick/fast-rcnn<i class="fa fa-external-link"></i></span></p>
<a id="more"></a>
<h2 id="三fast-r-cnn">三、Fast R-CNN</h2>
<h3 id="r-cnn与sppnet的不足">1）R-CNN与SPPNet的不足</h3>
<p>R-CNN在目标检测中有很好的准确率，但是这个方法本身仍有很多的问题。</p>
<ol type="1">
<li><strong>训练过程是多级的。</strong> R-CNN的训练分成三个部分，首先是 <code>finetune</code> 一个网络（目标检测的类别和ImageNet不一样）。之后是使用SVM进行目标的分类的训练。最后是使用 <code>feature map</code> 来进行目标的 <code>bounding-box</code> 的回归训练。</li>
<li><strong>训练过程费时费空间。</strong> SVM和回归两个任务，需要存储目标的特征，需要很多空间。网络的训练过程很慢。</li>
<li><strong>测试速度太慢。</strong> 需要对每个 <code>proposal</code> 进行前馈，耗时太长。速度只有47s / image。</li>
</ol>
<p><code>SPPNet</code> 可以加速 <code>R-CNN</code>，在 <code>SPPNet</code> 中，在卷积的部分直接前馈整张图片，之后对于每个 <code>proposal</code>，计算出它在 <code>feature map</code> 上的位置，然后使用 <code>SPP Pooling</code> 的方式，得到定长的特征向量。使得测试时间大大缩短。但 <code>SPPNet</code> 也有很多的问题。首先，<code>SPPNet</code> 在训练的过程和 <code>R-CNN</code> 几乎相同，因此有上面所有的问题，其次，<code>SPPNet</code> 只训练的网络最后的FC层，这样，前面的卷积的部分就没有参与训练。</p>
<h3 id="fast-r-cnn的主要贡献">2）Fast R-CNN的主要贡献</h3>
<ol type="1">
<li>更好的检测效果（mAP）</li>
<li>训练是单步的，使用多任务loss</li>
<li>所有的网络的参数都可以训练和更新</li>
<li>不需要存储特征</li>
</ol>
<h3 id="fast-r-cnn的训练">3）Fast R-CNN的训练</h3>
<p><code>Fast R-CNN</code> 的结构如下：</p>
<p><img data-src="http://cdn.miaoerduo.com/images/fastrcnn.jpg" /></p>
<p>首先按照惯例，使用一个在ImageNet预训练好的模型。保留网络的卷积的部分，在卷积的后面接上 <code>RoI Pooling</code> 层，<code>RoI Pooing</code> 层可以将 <code>feature map</code> 上任意大小的特征区域 <code>Pooling</code> 到指定的大小。直接接入若干个全连接，最终再接上两组输出的分支。一个用于输出分类的结果，假设有K个类别的目标，则输出K+1个类别，其中多出的一个类别是背景类。另一个分支用来输出回归的结果，每个目标都有自己的回归的结果，因此总的输出数为4K（K个类别的 <code>x,y,w,h</code>）。</p>
<p>这里就有一个问题：我们为什么不直接输出4个数值直接表示目标的位置，而是这么费劲的得到4K个输出呢？其实回归出4个值，讲道理也是可行的。通常给每个类都进行回归，可以理解为每个类别都是用自己这个类别自己的信息进行位置的预测，而不是使用统一的信息进行预测。比如一张图是人骑自行车，那么我们应该回归出人还是自行车呢？而这里的策略就是两个都分别回归出两个目标的位置。</p>
<h4 id="roi-pooling层">1 RoI Pooling层</h4>
<p><code>RoI Pooling</code> 层是一种简化版本的 <code>SPP Pooling</code> 层。他的计算方式如下：</p>
<p>对于 <code>feature map</code> 上的一个感兴趣的区域（<code>Region of Interest</code>），即 <code>RoI</code>，其大小为 <code>W × H</code>。我们需要把它 <code>pooling</code> 到 <code>W x H</code> 的大小。那么我们就使用 <span class="math inline">\(\frac{W}{w}*\frac{H}{h}\)</span> 的 <code>pooling</code> 核，<code>stride_w</code> 设成 <span class="math inline">\(\frac{W}{w}\)</span> ，<code>stride_h</code> 设成 <span class="math inline">\(\frac{H}{h}\)</span> 。这样经过 <code>pooling</code> 就可以到的我们需要的 <code>W x H</code> 的输出了，且通道数不变。当然这里要考虑取整的问题，细节上可以看一下 <code>FastR-CNN</code> 的源码。</p>
<h4 id="使用预训练的网络">2 使用预训练的网络</h4>
<p>本文中，使用了3个在ImageNet预训练好的模型。他们都有5个 <code>max pooling</code> 层，卷积层数在5-13之间。对于这些用于初始化的模型，需要做三点改变：</p>
<ol type="1">
<li>将最后一个 <code>max pooling</code> 层替换成 <code>RoI Pooling</code> 层。输出固定的大小，并且这个大小和网络的全连接的输入要相匹配。（看样子，这里还想复用之前的FC层的参数）</li>
<li>网络的最后的FC层，替换成两并列的FC。一个用于输出新的类别（K+1），另一个用来输出回归的结果（4K）。</li>
<li>网络的输入变成两个，一个是图像数据（N个），另一个是 <code>RoI</code>（R个）。</li>
</ol>
<h4 id="fine-tuning-for-detection">3 Fine-tuning for detection</h4>
<p>在 <code>Fast R-CNN</code> 中，训练的样本是整张图像，网络的卷积部分可以处理任意尺度的输入，因此可以一次性得到整张图像的 <code>feature map</code>。之后，根据输入的 <code>RoI</code> 的位置（也就是之前的 <code>Proposal</code>），对每个区域在 <code>feature map</code> 上进行 <code>RoI Pooling</code>，得到固定大小的输出。最终经过若干个全连接，得到分类和回归的输出，监督训练。从而使得检测任务可以做到 <code>one stage</code>。整个过程包括：<code>Loss</code>，<code>mini-batch</code> 采样策略，<code>RoI Pooling</code> 层的后馈以及 <code>SGD</code> 的超参数。下面专门依次介绍。</p>
<h3 id="fine-tuning-for-detection-1">4）Fine-tuning for detection</h3>
<h4 id="multi-task-loss">1 Multi-Task loss</h4>
<p>网络共有两个输出，一个是每个RoI的分类的概率，使用 <code>Softmax</code> 之后得到概率分布 <span class="math inline">\(p=(p_{0},p_{1},p_{2},...,p_{K})\)</span>。</p>
<p>另一个输出是回归的结果，每个类别的结果用 <span class="math inline">\(t^{k}=(t^{k}_{x},t^{k}_{y},t^{k}_{w},t^{k}_{h}), k \in [0,K)\)</span> 表示。</p>
<p>这里使用多任务Loss的监督学习：</p>
<p><span class="math display">\[L(p,k^{*},t,t^{*})=L_{cls}(p,k^{*})+\lambda[k^{*}\ge1]L_{loc}(t,t^{*})\]</span></p>
<p>其中，<span class="math inline">\(k^{*}\)</span> 表示正确类别的 <code>label</code>，<span class="math inline">\(L_{cls}(p,k^{*})=-log(p_{k^{*}})\)</span> 是标准的交叉熵Loss。</p>
<p>对于回归的部分，对于一个类别 <span class="math inline">\(k^{*}\)</span> ，正确的回归值为 <span class="math inline">\(t^{*} = (t^{*}_{x},t^{*}_{y},t^{*}_{w},t^{*}_{h})\)</span> ，预测的回归值 <span class="math inline">\(t = (t_{x},t_{y},t_{w},t_{h})\)</span> ，对于 <span class="math inline">\([k^{*}\ge1]\)</span> 的值，当 <span class="math inline">\(k^{*}\ge1\)</span> 时为1，否则为0（0的时候表示背景类，不需要回归）。</p>
<p>对于回归，使用如下的Loss：</p>
<p><span class="math display">\[L_{loc}(t, t^{*}) = \sum_{i \in {x,y,w,h}} smooth_{L1}(t_{i}, t^{*}_{i})\]</span></p>
<p>其中：</p>
<p><span class="math display">\[smooth_{L1}(x) = \left\{\begin{matrix} 0.5x^{2} &amp; if |x| &lt; 1 \\ |x|-0.5 &amp; otherwise \end{matrix}\right.\]</span></p>
<p>使用 <code>smoothL1 Loss</code> 的好处是，在 <code>x</code>比较大的时候，他的梯度为固定的1，而使用 <code>L2 Loss</code> 的时候，其梯度为 <code>2x</code>，会变得很大，不利于训练。</p>
<p>等式中的 <code>lambda</code> 用来平衡两种 <code>Loss</code> 之间的关系，这里的对于 <code>groundtruth</code> 进行了归一化，使得他的均值为0，方差为单位方差。在这种情况下，<code>lambda</code> 取1，在各种实验中均取得了不错的结果。</p>
<p>这里的归一化的操作，在之前的 <code>RCNN</code> 和之后的 <code>Faster R-CNN</code> 等都没有再出现。让我仔细看了一下源码才大致知道他是怎么计算的。至于效果，论文中并没有提到。这里感兴趣的同学可以看看源码。</p>
<p>对于回归值的计算。使用如下的公式：</p>
<p><span class="math display">\[G=\{G_{x},G_{y},G_{w},G_{h}\}\]</span></p>
<p>表示 <code>groundtruth</code> 的位置，<span class="math inline">\(P=\{P_{x},P_{y},P_{w},P_{h}\}\)</span> 表示 <code>proposal</code> 的位置。<code>x</code> 和 <code>y</code> 表示目标中心的坐标，<code>w</code> 和 <code>h</code> 表示宽和高。对于每个 <code>proposal</code>，其回归的目标为：</p>
<p><span class="math display">\[\begin{matrix} t_{x} = \frac{G_{x} - P_{x}}{P_{w}} \\ t_{y} = \frac{G_{y} - P_{y}}{P_{h}} \\ t_{w} = \log{\frac{G_{w}}{P_{w}}} \\ t_{h} = \log{\frac{G_{h}}{P_{h}}} \end{matrix}\]</span></p>
<p>这里的 <code>tw</code> 和 <code>th</code> 用了一个 <code>log</code> 函数，为什么不直接使用类似于 <code>Gw/Pw - 1</code> 这样的形式呢？我想了很久，才恍然大悟。如果 <code>Gw/Pw</code> 的值分别为0.5和2的时候，二者都是两倍的关系，取log之后正好互为相反数。</p>
<h4 id="mini-batch的样本选择">2 Mini-batch的样本选择</h4>
<p>对于每一个 <code>mini-batch</code>，选择2张图片，每个图片选择64个 <code>RoI</code>，得到总计128个 <code>RoI</code>。其中25%的 <code>RoI</code> 与目标的 <code>IOU</code> 大于0.5，即25%的正样本，75%的负样本。在训练中，所有的样本按照50%的几率翻转。没有其他的额外的数据增强的策略。</p>
<h4 id="roi-pooling层的后馈">3 RoI Pooling层的后馈</h4>
<p>这部分没什么好说的，和 <code>Max Pooling</code> 一样，<code>RoI Pooling</code> 前馈的时候保存的最大值的索引信息，直接后馈就行。</p>
<h4 id="sgd-超参数">4 SGD 超参数</h4>
<blockquote>
<p>The fully-connected layers used for softmax classification and bounding-box regression are initialized randomly from a zero-mean Gaussian distribution with standard deviations 0.01 and 0.001, respectively. All layers use a per-layer learning rate of 1 for weights and 2 for biases (following standard practice) and a global learning rate of 0.001. When training on VOC07 or VOC12 trainval we run SGD for 30k mini-batch iterations, and then lower the learning rate to 0.0001 and train for another 10k iterations. When we train on larger datasets, we run SGD for more iterations, as described later. A momentum term with weight 0.9 and weight decay factor of 0.0005 are used in all experiments.</p>
</blockquote>
<h3 id="尺度变化">5）尺度变化</h3>
<p>由于目标的尺度跨度很大，这里有两种处理的办法：</p>
<h4 id="暴力学习">1 暴力学习</h4>
<p>使用整张图片训练，一次性处理所有尺度的目标。这样依赖网络学习出不同尺度的目标。</p>
<h4 id="图像金字塔">2 图像金字塔</h4>
<p>将图像进行不同尺度的缩放，每种尺度的图像，网络都只学习最适合学习的尺度的目标。在测试的时候，也是，输入不同尺度的图像，得到不同尺度的目标。在实验中，这种方法的mAP更高，但是更加的耗时。</p>
<h3 id="svd分解压缩检测">6）SVD分解，压缩检测</h3>
<p>在一般的分类的网络中，计算量主要在前面的卷积的部分。但是对于检测来说，由于有大量的RoI，对于每个RoI都需要经过一次全连接，最终卷积和全连接的计算量差不多。</p>
<p>对于一个全连接，它的权重是一个 <code>u × v</code> 的矩阵 <code>W</code>。可以使用 <code>SVD</code> 近似的分解成：</p>
<p><span class="math display">\[W \approx U\Sigma _{t}V^{T}\]</span></p>
<p>这样分解之后，<code>U</code> 是个 <code>u × t</code> 的矩阵，<span class="math inline">\(\Sigma_{t}\)</span> 是个 <code>t × t</code> 的对角阵，<code>V</code> 是个 <code>v × t</code> 的矩阵。当 <code>t</code> 远小于 <code>min(u,v)</code> 的时候，参数量就可以从 <code>uv</code> 缩减到 <code>t(u+v)</code>。同时计算量也大大减少了。</p>
<h3 id="个人总结">7）个人总结</h3>
<p><code>Fast R-CNN</code> 中，将目标检测的任务集成为一个清晰的框架，极大的简化了目标检测的训练过程，同时在速度和精度上均得到的很好的保证。其思想影响了之后的各种改进版本的目标检测算法。真的十分值得仔细的阅读。</p>
]]></content>
      <categories>
        <category>Deep Learning</category>
      </categories>
  </entry>
  <entry>
    <title>CNN的目标检测概述（二）</title>
    <url>/2018/03/01/cnn-object-detection2/</url>
    <content><![CDATA[<blockquote>
<p>这次介绍的是2015年的Kaiming He的一篇论文：Spatial Pyramid Pooling in Deep Convolutional Networks for Visual Recognition，以下简称SPP-net。 SPP-net的主要贡献是提出了一种新的pooling的方式，spatial pyramid pooling，简称为SPP。使用这种pooling的方式，可以将任意大小的输入feature map给pooling到固定的大小。使用这种pooling的方式，最终在分类和检测任务上均有一定的效果。</p>
</blockquote>
<a id="more"></a>
<h3 id="二spp-net">二、SPP-net</h3>
<h4 id="问题描述">1）问题描述</h4>
<p>在介绍SPP这个pooling方式之前，我们先说一下，为什么需要这种特殊的pooling。</p>
<p>一般来看，CNN结构通常都由两个部分组成：卷积层和全连接层。比如7层的AlexNet，就是由5层的卷积层和2层的全连接层组成。对于卷积层，它可以处理任意尺度的输入（= = 请忽略极端情况）。而全连接层需要固定大小的输入。因此最终，我们的CNN结构的输入大小是由全连接层所固定。</p>
<p>那么固定大小的输入会造成什么问题呢？一般图像的大小并不是固定的，但是CNN要求输入固定，这样我们通常会采取两种方式得到固定大小的图像：裁剪和变形（仿射变换，缩放等）。</p>
<p><img data-src="http://cdn.miaoerduo.com/images/cnn-object-detection-crop-and-warp.jpg" /></p>
<p>裁剪操作很难正好的包含需要的目标，而变形的方式会导致目标发生形变。两种方式都不能很好的处理图片的尺度问题。</p>
<p>而SPP这种pooling方式的引入，就可以突破CNN固定输入的约束。SPP可以将任意大小的输入feature map给pooling到固定的大小。将SPP层加在最后一个卷积层的后面，这样就可以pooling出固定的大小，之后再接上全连接层。这样得到的CNN结构，就可以以任意尺度的图像作为输出了，而使用了SPP层的网络，就成为SPP-net。</p>
<p><img data-src="http://cdn.miaoerduo.com/images/cnn-and-spp-net.jpg" /></p>
<p>这张图就是一般的CNN结构（上）和SPP-net（下）的结构示意图。</p>
<h4 id="spp层工作流程">2）SPP层工作流程</h4>
<p>使用SPP层的CNN结构如下：</p>
<p><img data-src="http://cdn.miaoerduo.com/images/cnn-object-detection-spp-framework.jpg" /></p>
<p>这里重点介绍一下SPP层的工作方式，之前查阅其他的博客，发现都没有得到很好地解释，为此专门阅读了Caffe的SPPLayer实现代码，发现实现的方式很简单。</p>
<p>上图中，最下方是CNN的卷积部分，黑色的部分是最后一个卷积层的输出，在这个图里面，卷积的最终输出的通道数为256。</p>
<p>对于一个feature map，我们按照固定的方式对他进行划分。比如现在有一个feature map的宽和高分别是W和H，通道数不妨就取256。我们使用MAX-Pooling的方式做处理，金字塔的层数设置为3。</p>
<p>首先，我们将这个feature map复制为3份。每一份都看成金字塔的一层。</p>
<ol type="1">
<li>对于第一层，也就是顶层，即图中最右边的示意图。我们将整个feature map看做一个整体。这样使用一个 <code>(W, H)</code> 的kernel来进行pooling，这样就得到了 <code>1*1*256</code> 的输出。</li>
<li>对于第二层，即图中的中间的示意图。我们将feature map看成 <code>2*2</code> 个独立的区域，每个区域单独的pooling。实现上即使用了kernel大小为 <span class="math inline">\((\lceil \frac{W}{2}\rceil ,\lceil \frac{H}{2}\rceil )\)</span>，stride大小为 <span class="math inline">\((\lceil \frac{W}{2}\rceil ,\lceil \frac{H}{2}\rceil )\)</span> 的一个pooling层进行pooling。最终得到了 <code>2*2*256</code> 的输出。</li>
<li>同理，第三层，即图中的左边的示意图。将feature map看成 <code>4*4</code> 个独立的区域，单独pooling。使用kernel大小为 <span class="math inline">\((\lceil \frac{W}{4}\rceil ,\lceil \frac{H}{4}\rceil )\)</span>，stride大小为 <span class="math inline">\((\lceil \frac{W}{4}\rceil ,\lceil \frac{H}{4}\rceil )\)</span> 的pooling层进行pooling。最终得到 <code>4*4*256</code> 的输出。</li>
<li>总结下来，就是对于第N层，我们将整个feature map划分成 <span class="math inline">\((2^{N-1}*2^{N-1})\)</span> 的区域，分别pooling。实现上使用kernel为 <span class="math inline">\((\lceil \frac{W}{2^{N-1}}\rceil ,\lceil \frac{H}{2^{N-1}}\rceil )\)</span>，stride为 <span class="math inline">\((\lceil \frac{W}{2^{N-1}}\rceil ,\lceil \frac{H}{2^{N-1}}\rceil )\)</span> 的pooling层进行pooling即可，最终得到 <span class="math inline">\((2^{N-1}*2^{N-1}*dim)\)</span> 的输出。</li>
</ol>
<p>最终将所有的pooling的结果进行flatten，最后concat在一起就完成。这里由于输入的feature map的大小可能不能被 <span class="math inline">\(2^N\)</span>整除，采取了向上取整的做法，因此有时候需要额外的pad来填充feature map。</p>
<p>还有，这里使用的 <span class="math inline">\(2^N\)</span> 这种区域划分的策略，那么我们能不能使用其他的策略呢？比如划分成 <code>1*1</code>，<code>3*3</code>，<code>5*5</code>，<code>7*7</code>...这个样子呢？这里，其实是可以的。SPP提供的是一个pooling的策略，只要能理解他的原理，我们完全可以设计一个自己的pooling方式。事实上，作者也使用了<code>1*1</code>，<code>2\*2</code>，<code>3*3</code>这样的三级划分。</p>
<p>以上就是SPP层的实现原理，总结下来就是，对输入feature map使用多个不同大小的pooling层进行无重叠的pooling操作，最终再合并。</p>
<p>如果这里解释的还不够清楚地话，可以参看一下Caffe的源码，相信聪慧的你一定能轻松的理解。</p>
<h4 id="spp层的特性">3）SPP层的特性</h4>
<p>这里直接照搬论文的说法：</p>
<blockquote>
<p>We note that SPP has several remarkable properties for deep CNNs: 1) SPP is able to generate a fixed-length output regardless of the input size, while the sliding window pooling used in the previous deep networks cannot; 2) SPP uses multi-level spatial bins, while the sliding window pooling uses only a single window size. Multi-level pooling has been shown to be robust to object deformations; 3) SPP can pool features extracted at variable scales thanks to the flexibility of input scales. Through experiments we show that all these factors elevate the recognition accuracy of deep networks.</p>
</blockquote>
<p>SPP有很多特性：</p>
<ol type="1">
<li>SPP能够对于任意大小的输入，得到固定大小的输出。（强调输出大小固定）</li>
<li>SPP使用了多级的pooling维度（从上面的SPP工作流程就能看出来，他有各式各样的pooling尺寸），而传统的pooling策略是固定pooling大小的，多级pooling对目标的变形等有很好的鲁棒性。</li>
<li>SPP能够处理任意大小的输入。（强调输入大小可变）</li>
</ol>
<h4 id="分类任务中的spp">4）分类任务中的SPP</h4>
<p>分类不是这个系列要研究的重点，这里就简单的介绍一下，作者如何训练SPP-net的。</p>
<h5 id="单尺度训练">1 单尺度训练</h5>
<p>单尺度训练和一般的CNN的训练没有什么不同，图像经过缩放，然后截取224*224的区域用来训练。</p>
<h5 id="多尺度训练">2 多尺度训练</h5>
<p>由于SPP可以处理任意尺度的输入，这就使得多尺度的输入图像的训练成为可能。但是各种CNN框架的卷积的实现都是使用矩阵的操作来完成的，一个mini-batch中的图像的大小必须是相同的。这里，作者先使用一个尺度的图像进行训练，1个epoch之后，再换成另一种尺度进行训练，这样不断的改变尺度。</p>
<p>最终，训练的收敛情况和一般的单尺度训练是差不多的，但是测试的效果要好了一些。</p>
<p>上面是训练上的技巧，细节可以看原论文。在测试的时候，也有相应的不同策略。以下简单说明一下作者的实验和结论：</p>
<h5 id="多级pooling提高准确率">1 多级Pooling提高准确率</h5>
<p>将一般的网络，ZFNet，OverFeat等的最后一个卷积替换成SPP层，发现最终的效果有提升。</p>
<h5 id="多尺度训练可以提高准确率">2 多尺度训练可以提高准确率</h5>
<p><img data-src="http://cdn.miaoerduo.com/images/cnn-object-detection-multi-scale-test.jpg" /></p>
<p>多尺度训练即上面训练中的策略。多尺度训练的结果要比单尺度好。</p>
<h5 id="使用整张图片会提高准确率">3 使用整张图片会提高准确率</h5>
<p><img data-src="http://cdn.miaoerduo.com/images/cnn-object-detection-full-image-train.jpg" /></p>
<p>这里作者做了两组实验。一是中心crop的224*224的图像作为输入，另一个是将图像的短边缩放到256像素并保证长宽比，然后输入网络做前馈。</p>
<h5 id="multi-view的测试方式">4 Multi-View的测试方式</h5>
<p>这部分和之后的目标检测的方式类似。</p>
<h4 id="目标检测中的spp">5）目标检测中的SPP</h4>
<p>在R-CNN中，需要先是用selective search等方法，生成候选框，然后使用候选框的图像进行分类和回归的训练。在SPP-net，可以很大的简化这一流程，加速训练和测试。</p>
<h5 id="目标检测中spp的训练">1 目标检测中SPP的训练</h5>
<p>首先使用selective search得到2K的候选框。之后将图像缩放到短边固定大小（可以是多种尺度，这样可以一定程度提升性能），然后使用一个预训练好的网络的卷积的部分（比如ZFNet的前5层）进行前馈，得到整张图的feature map。根据候选框的位置，可以推出候选框在feature map上相应的位置，对这些位置上的feature map进行SPP操作，从而可以得到候选框的特征向量。</p>
<p>作者做了两个方式的训练，一个是使用SVM做的类别分类器，另一种是fine-tune预训练的模型。 对于SVM的训练，目的是给每一种类别都训练一个二分类的分类器。因此总共需要训练20个SVM分类器。根据和groundtruth的iou来选择正负样本，具体的选择策略和R-CNN相同。最终得到了20个分类器，给每个候选框打分，最终的结果使用NMS来处理。</p>
<p>预训练模型的fine-tune。这里只fune-tune了fc层。保留了前面的卷积部分。这样的话，就也只需要使用前面提取的候选框的特征向量作为输入了。训练十分的迅速。回归的训练，和R-CNN相同，使用了最后的pool5的特征来训练线性回归模型。</p>
<h5 id="目标检测的测试流程">2 目标检测的测试流程</h5>
<p>和训练部分类似，目标检测的首先使用selective search得到候选框，之后将图像缩放成固定尺度（可以是多个尺度，这样会有更好的效果），使用卷积部分前馈得到整张图的feature map。使用SPP得到每个候选框的特征向量。之后将特征向量输入到SVM或者fine-tune之后的网络的fc层，从而得到最终的框的得分和位置。最终使用NMS处理即可。</p>
<h5 id="多模型融合">3 多模型融合</h5>
<p>多模型融合可以很好的提高最终的准确率。论文中，作者使用了两种不同的网络，分别按照上面的方式得到候选框的得分和位置。之后将两个网络的候选框放在一起，使用NMS处理之后，作为最终的结果。检测的效果的确有了一些提升。</p>
<h4 id="个人总结">6）个人总结</h4>
<p>总体来说，本文最重要的部分还是在于SPP层的提出。这个层可以有效的将各种尺度的输入pooling到固定大小的输出。在分类任务上，多级pooling的策略可以从各种维度上保留特征的信息，在检测任务上，又可以通过一次性计算全图的feature map，再pooling出每个候选区域的特征，极大地减小特征提取部分的计算量。</p>
<p>论文中有大量的实验和训练中的细节，这里并没有介绍到。感兴趣的同学最好还是看一下原文，相信应该可以收获更多。</p>
<p>在检测部分，该方法还是有一些缺点的。首先是这个的训练过程和R-CNN还是差不多的，相比于之后的目标检测的方法，可以看出这里还是需要大量的人工的参与。其次，这里的特征是固定的，在SVM或者模型的fine-tune过程中，都是直接使用网络的卷积层的输出，并没有对卷及部分进行训练。那么图像的特征在检测任务中是否还具有优化的潜力呢？我相信还是有的。</p>
<p>技术总是在不断的进步的。这里的SPP层的提出。可以说，一定程度上造就了另一个经典的目标检测方法Fast R-CNN的提出，Fast R-CNN就是下一篇博客的主角！</p>
]]></content>
      <categories>
        <category>Deep Learning</category>
      </categories>
  </entry>
  <entry>
    <title>CNN的目标检测概述（一）</title>
    <url>/2018/02/26/cnn-object-detection1/</url>
    <content><![CDATA[<p>在2012年的ImageNet中，AlexNet拔得头筹。之后，CNN成为了图像识别中的一大利器。</p>
<p>在目标检测中引入CNN，开山之作就是2013年的 Rich feature hierarchies for accurate object detection and semantic segmentation，之后简称R-CNN。</p>
<a id="more"></a>
<h2 id="一r-cnn">一、R-CNN</h2>
<h3 id="算法原理">1）算法原理</h3>
<p>R-CNN中，将目标检测分成两步来实现：</p>
<ol type="1">
<li>首先是生成Region proposals，也就是候选框。有许多的基于图像的低维特征生成候选框的算法，例如selective search等。</li>
<li>训练一个分类和回归的网络。这个网络可以根据候选框的区域的图片，判断这个图片的类别，以及它应该回归到的位置。</li>
</ol>
<h3 id="检测过程">2）检测过程</h3>
<p>这样，在具体的一张图片的目标检测的时候，我们可以通过下面的过程来得到结果：</p>
<p><img data-src="http://cdn.miaoerduo.com/images/RCNN-Framework.jpg" alt="RCNN-Framework" /></p>
<ol type="1">
<li>对于给定一张图片，通过算法得到大量的候选框(2k个左右)。</li>
<li>将候选框的图片裁剪出，然后输入到CNN网络中。</li>
<li>网络的输出为分类的结果和回归的结果。然后我们就知道这个区域是不是目标，如果是，计算出它的回归的位置。</li>
<li>得到大量的有类别的框，通过NMS算法，得到最终的目标的框。</li>
</ol>
<h3 id="网络训练">3）网络训练</h3>
<p>对于目标检测这个任务，我们现有的标注数据其实并不多。所以通常都是使用ImageNet等大的公开的数据进行预训练，或者直接使用预训练好的模型在finetune。</p>
<h4 id="预训练supervised-pre-training">1. 预训练（Supervised pre-training）</h4>
<p>这里使用ILSVRC 2012（也就是ImageNet的训练数据集）进行分类任务的CNN网络的预训练。</p>
<p>在我们自己复现论文的时候，可以选择现有的开源模型。在各种框架的Model Zoo中可以方便的下载各种网络的模型。常见的有AlexNet，VGGNet，GoogleNet，ResNet等的模型。据师弟的实验，VGGNet在目标检测中使用的频率最高，效果也最好。</p>
<h4 id="微调domain-specific-fine-tuning">2. 微调（Domain-specific fine-tuning）</h4>
<p>之前的模型是用于分类的模型，因此对于VOC中的目标检测并不完全的切合。这里需要再使用VOC的数据进行fine-tuning。</p>
<p>VOC中需要检测的目标共有20种，因此我们的分类任务需要的类别数为21类，其中包含一个背景类。</p>
<p>训练数据使用VOC的数据，首先用Selective search等方法得到候选框，截取候选框的图片。根据候选框和Groundtruth直接的IOU（交比并，通常认为大于0.5就是目标）判断这个候选框是不是目标，如果是目标，再计算出应该的回归值。这里选择的IOU的阈值为0.3。</p>
<h4 id="目标分类器object-category-classifiers">3. 目标分类器（Object category classifiers）</h4>
<p>对每个类别的特征训练SVM。由于训练数据过大，这里使用了难负样本最小化策略。最终的分类按照SVM的输出打分而不是上面的网络的21分类的输出。</p>
<p>通常，我们可以使用CNN的输出表示类别的得分，但有时候为了得到更好地结果，会将最后一个隐含层的输出作为特征，然后给每个类别再训练一个SVM模型。</p>
<h4 id="bounding-box回归">4. Bounding Box回归</h4>
<p>这里使用了每个proposal的最后的卷积的输出（pool5）作为特征，训练了线性回归模型，用来预测新的窗口的位置。</p>
<h3 id="个人理解">4）个人理解</h3>
<p>R-CNN作为目标检测的开山之作，提出了一套目标检测的框架，是两段式的目标检测的代表。相比于传统的目标检测算法，他并不依赖手工设计的特征算子，而是通过CNN这一工具完成特征提取和分类回归的任务。</p>
<p>当然，R-CNN的缺点也很明显。首先是，速度慢。由于一张图片可以提取大量的候选框（~2K），每个候选框都要通过网络进行前馈，这样造成大量的计算量。其次是训练的过程需要人工的处理数据，相比于之后的目标检测的方法，并不是十分的方便。</p>
]]></content>
      <categories>
        <category>Deep Learning</category>
      </categories>
  </entry>
  <entry>
    <title>本站的WordPress插件一览</title>
    <url>/2018/02/12/my-wordpress-plugins/</url>
    <content><![CDATA[<p>其实Jetpack这个插件里面已经提供了大量的工具。如果安装了这个插件，那么下面的很多插件都是可以不用安装的。不过在国内使用Jetpack这个插件有点麻烦。我这里就一直没有用，等换了国外的服务器之后，之前的博客的很多内容都得重新编辑，太麻烦了，所以干脆不用了。</p>
<a id="more"></a>
<h3 id="akismet-anti-spam">1. Akismet Anti-Spam</h3>
<p>网址：<span class="exturl" data-url="aHR0cHM6Ly93b3JkcHJlc3Mub3JnL3BsdWdpbnMvYWtpc21ldC8=" title="https://wordpress.org/plugins/akismet/">https://wordpress.org/plugins/akismet/<i class="fa fa-external-link"></i></span></p>
<blockquote>
<p>由千百万人使用，Akismet可能是保护您的站点免受垃圾评论的世界上最好的方式。 您的站点已完全配置并受到不断的保护。</p>
</blockquote>
<p>这个插件可以很好的过滤掉垃圾的评论。</p>
<h3 id="captcha">2. Captcha</h3>
<p>网址：<span class="exturl" data-url="aHR0cHM6Ly93b3JkcHJlc3Mub3JnL3BsdWdpbnMvY2FwdGNoYS8=" title="https://wordpress.org/plugins/captcha/">https://wordpress.org/plugins/captcha/<i class="fa fa-external-link"></i></span></p>
<blockquote>
<p>This plugin allows you to implement super security captcha form into web forms.</p>
</blockquote>
<p>顾名思义，这个插件可以为用户登录，访客评论添加验证码的服务。用上验证码插件之后，基本上就没有什么垃圾评论了。最重要的是，这个验证码插件很有逼格！</p>
<h3 id="wp-super-cache">3. WP Super Cache</h3>
<p>网址：<span class="exturl" data-url="aHR0cHM6Ly93b3JkcHJlc3Mub3JnL3BsdWdpbnMvd3Atc3VwZXItY2FjaGUv" title="https://wordpress.org/plugins/wp-super-cache/">https://wordpress.org/plugins/wp-super-cache/<i class="fa fa-external-link"></i></span></p>
<blockquote>
<p>WordPress的快速缓存插件。</p>
</blockquote>
<p>这个插件支持缓存我们网站上的很多资源，这样可以避免每次PHP都重新生成，所以能加速网站的响应。同时它支持CDN服务，比较麻烦的是它不支持直接把静态资源上传到CDN服务器，因此得我们自己上传。 这里，我写了一个小工具，可以使用FTP，同步本地的静态资源到远程服务器。项目地址：<span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL21pYW9lcmR1by9GVFBTeW5j" title="https://github.com/miaoerduo/FTPSync">https://github.com/miaoerduo/FTPSync<i class="fa fa-external-link"></i></span> 注意，不要把php等文件上传了，这样会泄露你的密码。</p>
<h3 id="updraftplus-wordpress-backup-plugin">4. UpdraftPlus WordPress Backup Plugin</h3>
<p>网址：<span class="exturl" data-url="aHR0cHM6Ly93b3JkcHJlc3Mub3JnL3BsdWdpbnMvdXBkcmFmdHBsdXMv" title="https://wordpress.org/plugins/updraftplus/">https://wordpress.org/plugins/updraftplus/<i class="fa fa-external-link"></i></span></p>
<blockquote>
<p>Backup and restore: take backups locally, or backup to Amazon S3, Dropbox, Google Drive, Rackspace, (S)FTP, WebDAV &amp; email, on automatic schedules.</p>
</blockquote>
<p>用来备份网站的插件，数据库，服务器等都是不靠谱的，要想我们的网站长存，就得定期备份。BTW，我的网站更新频率很低，所以是一周一备份的。</p>
<h3 id="tinymce-advanced">5. TinyMCE Advanced</h3>
<p>网址：<span class="exturl" data-url="aHR0cHM6Ly93b3JkcHJlc3Mub3JnL3BsdWdpbnMvdGlueW1jZS1hZHZhbmNlZC8=" title="https://wordpress.org/plugins/tinymce-advanced/">https://wordpress.org/plugins/tinymce-advanced/<i class="fa fa-external-link"></i></span></p>
<blockquote>
<p>Enables advanced features and plugins in TinyMCE, the visual editor in WordPress.</p>
</blockquote>
<p>强大的在线编辑器插件。以至于装上这个插件之后，我基本上都是在网页端写博客。</p>
<h3 id="crayon-syntax-highlighter">6. Crayon Syntax Highlighter</h3>
<p>网址：<span class="exturl" data-url="aHR0cHM6Ly93b3JkcHJlc3Mub3JnL3BsdWdpbnMvY3JheW9uLXN5bnRheC1oaWdobGlnaHRlci8=" title="https://wordpress.org/plugins/crayon-syntax-highlighter/">https://wordpress.org/plugins/crayon-syntax-highlighter/<i class="fa fa-external-link"></i></span></p>
<blockquote>
<p>支持多语言、不同主题，高亮来自URL、或本地文件或文章文本。</p>
</blockquote>
<p>代码高亮插件。我一直再用。截止到目前，已经有2年没有更新了。。。</p>
<h3 id="tablepress">7. TablePress</h3>
<p>网址：<span class="exturl" data-url="aHR0cHM6Ly93b3JkcHJlc3Mub3JnL3BsdWdpbnMvdGFibGVwcmVzcy8=" title="https://wordpress.org/plugins/tablepress/">https://wordpress.org/plugins/tablepress/<i class="fa fa-external-link"></i></span></p>
<blockquote>
<p>为您的文章和页面嵌入美观且功能丰富的表格而不需要编写任何代码。</p>
</blockquote>
<p>WordPress内置的表格不好看，我都是用这个的。</p>
<h3 id="wp-quicklatex">8. WP QuickLaTeX</h3>
<p>网址：<span class="exturl" data-url="aHR0cHM6Ly93b3JkcHJlc3Mub3JnL3BsdWdpbnMvd3AtcXVpY2tsYXRleC8=" title="https://wordpress.org/plugins/wp-quicklatex/">https://wordpress.org/plugins/wp-quicklatex/<i class="fa fa-external-link"></i></span></p>
<blockquote>
<p>Access to complete LaTeX distribution. Publish formulae &amp; graphics using native LaTeX syntax directly in the text. Inline formulas, displayed equations auto-numbering, labeling and referencing, AMS-LaTeX, TikZ, custom LaTeX preamble. No LaTeX installation required. Easily customizable using UI dialog. Actively developed and maintained. Visit QuickLaTeX homepage for more info.</p>
</blockquote>
<p>让WordPress支持Latex，这样就可以插入复杂的Latex公式了。</p>
<h3 id="wp-statistics">9. WP Statistics</h3>
<p>网址：<span class="exturl" data-url="aHR0cHM6Ly93b3JkcHJlc3Mub3JnL3BsdWdpbnMvd3Atc3RhdGlzdGljcy8=" title="https://wordpress.org/plugins/wp-statistics/">https://wordpress.org/plugins/wp-statistics/<i class="fa fa-external-link"></i></span></p>
<blockquote>
<p>Complete WordPress Analytics and Statistics for your site!</p>
</blockquote>
<p>统计功能。每天看看自己博客的访问量，也会很开心。</p>
<h3 id="easy-custom-auto-excerpt">10. Easy Custom Auto Excerpt</h3>
<p>网址：<span class="exturl" data-url="aHR0cHM6Ly93b3JkcHJlc3Mub3JnL3BsdWdpbnMvZWFzeS1jdXN0b20tYXV0by1leGNlcnB0Lw==" title="https://wordpress.org/plugins/easy-custom-auto-excerpt/">https://wordpress.org/plugins/easy-custom-auto-excerpt/<i class="fa fa-external-link"></i></span></p>
<blockquote>
<p>Auto Excerpt for your post on home, front_page, search and archive.</p>
</blockquote>
<p>我们一般的WordPress文章，文章的所有内容都会全部显示，这样很不友好。这个插件可以帮我们自动生成摘要。</p>
<h3 id="scalable-vector-graphics-svg">11. Scalable Vector Graphics (SVG)</h3>
<p>网址：<span class="exturl" data-url="aHR0cHM6Ly93b3JkcHJlc3Mub3JnL3BsdWdpbnMvc2NhbGFibGUtdmVjdG9yLWdyYXBoaWNzLXN2Zy8=" title="https://wordpress.org/plugins/scalable-vector-graphics-svg/">https://wordpress.org/plugins/scalable-vector-graphics-svg/<i class="fa fa-external-link"></i></span></p>
<blockquote>
<p>Scalable Vector Graphics are two-dimensional vector graphics, that can be both static and dynamic. This plugin allows your to easily use them on your site.</p>
</blockquote>
<p>让网站支持SVG图片。</p>
<h3 id="post-smtp">12. Post SMTP</h3>
<p>网址：<span class="exturl" data-url="aHR0cHM6Ly93b3JkcHJlc3Mub3JnL3BsdWdpbnMvc2NhbGFibGUtdmVjdG9yLWdyYXBoaWNzLXN2Zy8=" title="https://wordpress.org/plugins/scalable-vector-graphics-svg/">https://wordpress.org/plugins/scalable-vector-graphics-svg/<i class="fa fa-external-link"></i></span></p>
<blockquote>
<p>Email not reliable? Post SMTP is the first and only WordPress SMTP plugin to implement OAuth 2.0 for Gmail, Hotmail and Yahoo Mail. Setup is a breeze with the Configuration Wizard and integrated Port Tester. Enjoy worry-free delivery even if your password changes!</p>
</blockquote>
<p>让网站支持邮件服务。</p>
<h3 id="better-notifications-for-wordpress">13. Better Notifications for WordPress</h3>
<p>网址：<span class="exturl" data-url="aHR0cHM6Ly93b3JkcHJlc3Mub3JnL3BsdWdpbnMvYm5mdy8=" title="https://wordpress.org/plugins/bnfw/">https://wordpress.org/plugins/bnfw/<i class="fa fa-external-link"></i></span></p>
<blockquote>
<p>Supercharge your WordPress notifications using a WYSIWYG editor and shortcodes. Default and new notifications available. Add more power with Add-ons.</p>
</blockquote>
<p>这个插件可以让我们在文章被评论的时候，通过邮件来提醒我们。他依赖<strong>Post SMTP</strong>这个插件。</p>
<h3 id="all-in-one-wp-security">14. All In One WP Security</h3>
<p>网址：<span class="exturl" data-url="aHR0cHM6Ly93b3JkcHJlc3Mub3JnL3BsdWdpbnMvYWxsLWluLW9uZS13cC1zZWN1cml0eS1hbmQtZmlyZXdhbGwv" title="https://wordpress.org/plugins/all-in-one-wp-security-and-firewall/">https://wordpress.org/plugins/all-in-one-wp-security-and-firewall/<i class="fa fa-external-link"></i></span></p>
<blockquote>
<p>All round best WordPress security plugin!</p>
</blockquote>
<p>强大的安全检查和防护的插件。</p>
<h3 id="to-top">15. To Top</h3>
<p>网址：<span class="exturl" data-url="aHR0cHM6Ly93b3JkcHJlc3Mub3JnL3BsdWdpbnMvdG8tdG9wLw==" title="https://wordpress.org/plugins/to-top/">https://wordpress.org/plugins/to-top/<i class="fa fa-external-link"></i></span></p>
<blockquote>
<p>To Top plugin allows the visitor as well as admin to easily scroll back to the top of the page, with fully customizable options and ability to use image.</p>
</blockquote>
<p>装这个插件是因为有人留言说，有的博客太长，想一键回到顶端。这个插件的作用就是给页面生成一个按钮，可以一键返回顶端。</p>
<h3 id="yoast-seo">16. Yoast SEO</h3>
<p>网址：<span class="exturl" data-url="aHR0cHM6Ly93b3JkcHJlc3Mub3JnL3BsdWdpbnMvd29yZHByZXNzLXNlby8=" title="https://wordpress.org/plugins/wordpress-seo/">https://wordpress.org/plugins/wordpress-seo/<i class="fa fa-external-link"></i></span></p>
<blockquote>
<p>The first true all-in-one SEO solution for WordPress, including on-page content analysis, XML sitemaps and much more.</p>
</blockquote>
<p>对网站进行SEO优化的插件，我主要用到他的sitemap的功能。这个插件在添加中文关键字的时候，总是提示我有错误，我也不知道为啥。</p>
<h3 id="relevanssi-a-better-search">17. Relevanssi – A Better Search</h3>
<p>网址：<span class="exturl" data-url="aHR0cHM6Ly93b3JkcHJlc3Mub3JnL3BsdWdpbnMvcmVsZXZhbnNzaS8=" title="https://wordpress.org/plugins/relevanssi/">https://wordpress.org/plugins/relevanssi/<i class="fa fa-external-link"></i></span></p>
<blockquote>
<p>This plugin replaces WordPress search with a relevance-sorting search. 增强WordPress的搜索功能。然而统计显示，我的博客基本上没有用搜索功能，囧。</p>
</blockquote>
<h3 id="really-simple-ssl">18. Really Simple SSL</h3>
<p>网址：<span class="exturl" data-url="aHR0cHM6Ly93b3JkcHJlc3Mub3JnL3BsdWdpbnMvcmVhbGx5LXNpbXBsZS1zc2wv" title="https://wordpress.org/plugins/really-simple-ssl/">https://wordpress.org/plugins/really-simple-ssl/<i class="fa fa-external-link"></i></span></p>
<blockquote>
<p>Lightweight plugin without any setup to make your site SSL proof</p>
</blockquote>
<p>让网站支持SSL。这个首先你的网站本身要有证书。这个插件只是为了解决HTTP和HTTPS之间的转换的问题。</p>
<h3 id="ultimate-category-excluder">19. Ultimate Category Excluder</h3>
<p>网址：<span class="exturl" data-url="aHR0cHM6Ly93b3JkcHJlc3Mub3JnL3BsdWdpbnMvdWx0aW1hdGUtY2F0ZWdvcnktZXhjbHVkZXIv" title="https://wordpress.org/plugins/ultimate-category-excluder/">https://wordpress.org/plugins/ultimate-category-excluder/<i class="fa fa-external-link"></i></span></p>
<blockquote>
<p>Easily exclude categories from your front page, feeds, archives, and search results.</p>
</blockquote>
<p>这个插件可以选择一个文章分类，让这个分类的文章不显示在首页。比如个人笔记啥的，不想放首页，就可以用这个插件。我用这个插件就是想把一些学习笔记啥的屏蔽一下，但后来又想到，我这个博客也没有多少人看，最终这个插件就是装上，但是没有实际用上。</p>
<h3 id="disable-google-fonts">20. Disable Google Fonts</h3>
<p>网址：<span class="exturl" data-url="aHR0cHM6Ly93b3JkcHJlc3Mub3JnL3BsdWdpbnMvZGlzYWJsZS1nb29nbGUtZm9udHMv" title="https://wordpress.org/plugins/disable-google-fonts/">https://wordpress.org/plugins/disable-google-fonts/<i class="fa fa-external-link"></i></span></p>
<blockquote>
<p>Disable enqueuing of Open Sans and other fonts used by WordPress from Google.</p>
</blockquote>
<p>国内很难或者不能加载Google字体，这个插件可以屏蔽掉网站对Google字体的依赖，从而进行加速。</p>
<h3 id="wp-user-rewards">21. WP User Rewards</h3>
<p>网址：<span class="exturl" data-url="aHR0cHM6Ly93b3JkcHJlc3Mub3JnL3BsdWdpbnMvd3AtdXNlci1yZXdhcmRzLw==" title="https://wordpress.org/plugins/wp-user-rewards/">https://wordpress.org/plugins/wp-user-rewards/<i class="fa fa-external-link"></i></span></p>
<blockquote>
<p>Adds user reward/donate fields to user profiles, including Wechat pay QRCode, Alipay QRCode, and PayPal link.</p>
</blockquote>
<p>给网站增加打赏功能。不得不承认，这个打赏的页面真心很丑。。。我还是改了一下才放到网站上的。当然有没有人打赏，这个就随缘了，我就是图个开心。</p>
<h3 id="wp-user-avatar">22. WP User Avatar</h3>
<p>网址：<span class="exturl" data-url="aHR0cHM6Ly93b3JkcHJlc3Mub3JnL3BsdWdpbnMvd3AtdXNlci1hdmF0YXIv" title="https://wordpress.org/plugins/wp-user-avatar/">https://wordpress.org/plugins/wp-user-avatar/<i class="fa fa-external-link"></i></span></p>
<blockquote>
<p>Use any image from your WordPress Media Library as a custom user avatar. Add your own Default Avatar.</p>
</blockquote>
<p>一个可以自定义头像的插件。</p>
]]></content>
      <categories>
        <category>Miscellanea</category>
      </categories>
  </entry>
  <entry>
    <title>博客服务器维护中的小技巧</title>
    <url>/2018/02/02/tricks-for-maintaining-blog-server/</url>
    <content><![CDATA[<p>这里记录了小喵的博客的服务器的一些小的技巧，包括安装和各种参数的配置，便于以后查看。不定期更新。</p>
<a id="more"></a>
<h3 id="lnmp环境的配置oneinstack">1. LNMP环境的配置——oneinstack</h3>
<p><code>LNMP</code> 环境就是 <code>Linux</code>、<code>Nginx</code>、<code>Mysql</code> 和 <code>Php</code> 这四个的组合，由于这四个都是免费的，因此在网站搭建的时候最常使用到，另一个名词是 <code>LAMP</code> ，其中 <code>A</code> 表示的是 <code>Apache</code>。但 <code>Apache</code> 比较占资源，所以我用的还是 <code>Nginx</code>。</p>
<p>这里，网上应该有超多的教程，我当时配置的时候也是参考了各种教程，但最终还是放弃了，自己去配置这些东西真的好难。于是，我就把目标转向了各种集成好的环境了。</p>
<p>如果使用的是阿里云或者腾讯云的服务器，那么直接在应用市场上中就有配置好环境的 <code>LNMP</code>。如果是自己的服务器，就可以使用 <span class="exturl" data-url="aHR0cHM6Ly9vbmVpbnN0YWNrLmNvbS8=" title="https://oneinstack.com/">https://oneinstack.com/<i class="fa fa-external-link"></i></span>。这里提供的一键安装的脚本。同时 <strong>oneinstack</strong> 中，还提供了新建虚拟主机，新建FTP服务器，一键升级软件等各种超实用的功能，十分建议使用。</p>
<h3 id="优化php-fpm的内存占用问题">2. 优化php-fpm的内存占用问题</h3>
<p>这里，我们如果使用htop命令查看各种资源占用，会发现，<code>php-fpm</code> 这个程序居然占用了10%的内存！明明网站是空闲的，但是内存还是被占用着。</p>
<p>参考 <span class="exturl" data-url="aHR0cHM6Ly9zZWdtZW50ZmF1bHQuY29tL2EvMTE5MDAwMDAxMDQxMzQ2Mw==" title="https://segmentfault.com/a/1190000010413463">https://segmentfault.com/a/1190000010413463<i class="fa fa-external-link"></i></span> 中的解决方案。</p>
<p>打开 <code>/usr/local/php/etc/php-fpm.conf</code> 这个文件，该文件是 <code>php-fpm</code> 的配置文件。不同的版本可能安装的地方不太一样。打开之后，有这么一段配置：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">pm &#x3D; dynamic</span><br><span class="line">pm.max_children &#x3D; 16</span><br><span class="line">pm.start_servers &#x3D; 10</span><br><span class="line">pm.min_spare_servers &#x3D; 8</span><br><span class="line">pm.max_spare_servers &#x3D; 16</span><br><span class="line">pm.max_requests &#x3D; 2048</span><br><span class="line">pm.process_idle_timeout &#x3D; 10s</span><br></pre></td></tr></table></figure>
<p>其中 <code>pm = dynamic</code> 会使得内存中永远都有 <code>php-fpm</code> 的进程存在，这样会很大长度的浪费内存，所以把这句改成 <code>pm = ondemand</code>。变成：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">pm &#x3D; ondemand</span><br><span class="line">pm.max_children &#x3D; 16</span><br><span class="line">pm.start_servers &#x3D; 10</span><br><span class="line">pm.min_spare_servers &#x3D; 8</span><br><span class="line">pm.max_spare_servers &#x3D; 16</span><br><span class="line">pm.max_requests &#x3D; 2048</span><br><span class="line">pm.process_idle_timeout &#x3D; 10s</span><br></pre></td></tr></table></figure>
<p>这样，只有在系统需要的时候，才回产生 <code>php-fpm</code> 的子进程。子进程会在内存中保留 <code>pm.process_idle_timeout</code> 这么久的时间之后退出，这里就是10s的时间。</p>
<p>之后重启我们的 <code>php-fpm</code>，就会发现内存占用变少了很多。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 重启php-fpm</span></span><br><span class="line">service php-fpm restart</span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>Miscellanea</category>
      </categories>
  </entry>
  <entry>
    <title>shadowsocks安装和配置</title>
    <url>/2018/01/25/shadowsocks-toturial/</url>
    <content><![CDATA[<h2 id="shadowsocks服务端安装和配置">Shadowsocks服务端安装和配置</h2>
<p>shadowsocks是一个十分方便安装和配置的代理工具。官网是：<span class="exturl" data-url="aHR0cDovL3NoYWRvd3NvY2tzLm9yZw==" title="http://shadowsocks.org">http://shadowsocks.org<i class="fa fa-external-link"></i></span> 这里记录一下，shadowsocks的安装和配置的过程，方便以后的查阅。这里使用的是python版本的shadowsocks。</p>
<a id="more"></a>
<p>首先安装shadowsocks，使用pip可以方便的安装：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">pip install shadowsocks</span><br></pre></td></tr></table></figure>
<p>安装完成之后，这时候就会发现我们的系统里多了一些命令，主要需要用到的是 ssserver 这个工具。 使用 <code>ssserver -h</code> 可以查看其具体的用法。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">ssserver -s 127.0.0.1 -p 8388 -k password -m aes-256-cfb --workers 10 -t 300 -q -d start</span><br></pre></td></tr></table></figure>
<p>上述命令就可以以后台程序的方式启动shadowsocks服务，其中监听的端口是8388，密码是password，加密方式是aes-256-cfb，其他的参数使用<code>ssserver -h</code>就能看到。</p>
<p>这里介绍另一种配置文件的方式，新建一个文件，比如叫<code>config.json</code>，内容为:</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">    <span class="attr">"server"</span>:<span class="string">"::"</span>,</span><br><span class="line">    <span class="attr">"server_port"</span>:<span class="number">8388</span>,</span><br><span class="line">    <span class="attr">"local_address"</span>: <span class="string">"127.0.0.1"</span>,</span><br><span class="line">    <span class="attr">"local_port"</span>:<span class="number">1080</span>,</span><br><span class="line">    <span class="attr">"password"</span>:<span class="string">"password"</span>,</span><br><span class="line">    <span class="attr">"timeout"</span>:<span class="number">300</span>,</span><br><span class="line">    <span class="attr">"method"</span>:<span class="string">"aes-256-cfb"</span>,</span><br><span class="line">    <span class="attr">"workers"</span>: <span class="number">2</span>,</span><br><span class="line">    <span class="attr">"fast_open"</span>: <span class="literal">false</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>如果希望支持多个端口的话，可以参考一下的配置：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">    <span class="attr">"server"</span>:<span class="string">"::"</span>,</span><br><span class="line">    <span class="attr">"local_address"</span>: <span class="string">"127.0.0.1"</span>,</span><br><span class="line">    <span class="attr">"local_port"</span>:<span class="number">1080</span>,</span><br><span class="line">    <span class="attr">"port_password"</span>: &#123;</span><br><span class="line">        <span class="attr">"10000"</span>: <span class="string">"password0"</span>,</span><br><span class="line">        <span class="attr">"10001"</span>: <span class="string">"password1"</span>,</span><br><span class="line">        <span class="attr">"10002"</span>: <span class="string">"password2"</span></span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="attr">"timeout"</span>:<span class="number">300</span>,</span><br><span class="line">    <span class="attr">"method"</span>:<span class="string">"aes-256-cfb"</span>,</span><br><span class="line">    <span class="attr">"workers"</span>: <span class="number">2</span>,</span><br><span class="line">    <span class="attr">"fast_open"</span>: <span class="literal">false</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这里需要注意的是，<code>"server"</code>字段填写的是<code>"::"</code>，这样可以同时监听ipv4和ipv6的地址，如果你的服务器同时支持ipv4和ipv6的话，那么你就可以通过代理顺便完成ipv4和ipv6的相互转换了。其他的参数可以按照自己的喜好去填写，加密方式也可以改成其他的更有效的方式。 启动方式现在变成了：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ssserver -c /path/to/config.json -d start</span><br></pre></td></tr></table></figure>
<h2 id="shadowsocks客户端">Shadowsocks客户端</h2>
<p>客户端下载： <span class="exturl" data-url="aHR0cDovL3NoYWRvd3NvY2tzLm9yZy9lbi9kb3dubG9hZC9jbGllbnRzLmh0bWw=" title="http://shadowsocks.org/en/download/clients.html">http://shadowsocks.org/en/download/clients.html<i class="fa fa-external-link"></i></span></p>
<p>上述网址安卓版需要用Google Play下载，国内可以使用github的release程序：</p>
<p><span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL3NoYWRvd3NvY2tzL3NoYWRvd3NvY2tzLWFuZHJvaWQvcmVsZWFzZXM=" title="https://github.com/shadowsocks/shadowsocks-android/releases">https://github.com/shadowsocks/shadowsocks-android/releases<i class="fa fa-external-link"></i></span></p>
<h2 id="bbr安装">BBR安装</h2>
<p>据说安装可以对服务器进行网络加速，我也试了一下，可能服务器本来就很快吧，没什么特别的感觉。</p>
<p>可以参考教程：<span class="exturl" data-url="aHR0cHM6Ly9xLmFrYS5tb2UvMjAxNy8wMS8yOC9nb29nbGUtYmJyLw==" title="https://q.aka.moe/2017/01/28/google-bbr/">https://q.aka.moe/2017/01/28/google-bbr/<i class="fa fa-external-link"></i></span></p>
<p>这里简单复制一下我使用的操作： 使用root用户登录，运行以下命令：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">wget --no-check-certificate https://github.com/teddysun/across/raw/master/bbr.sh</span><br><span class="line">chmod +x bbr.sh</span><br><span class="line">./bbr.sh</span><br></pre></td></tr></table></figure>
<p>安装完成后，脚本会提示需要重启VPS，输入<code>y</code>并回车后重启。重启完成后，进入 VPS，验证一下是否成功安装最新内核并开启 TCP BBR，输入以下命令：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">uname -r</span><br></pre></td></tr></table></figure>
<p>查看内核版本，含有 4.9 就表示 OK 了。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sysctl net.ipv4.tcp_available_congestion_control</span><br></pre></td></tr></table></figure>
<p>返回值一般为： <code>net.ipv4.tcp_available_congestion_control = bbr cubic reno</code></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sysctl net.ipv4.tcp\_congestion\_control</span><br></pre></td></tr></table></figure>
<p>返回值一般为： <code>net.ipv4.tcp_congestion_control = bbr</code></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sysctl net.core.default_qdisc</span><br></pre></td></tr></table></figure>
<p>返回值一般为： <code>net.core.default_qdisc = fq</code></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">lsmod | grep bbr</span><br></pre></td></tr></table></figure>
<p>返回值有<code>tcp_bbr</code>模块即说明bbr已启动。</p>
<h2 id="开机启动">开机启动</h2>
<p>这里使用的是CentOS7，修改<code>rc.local</code>文件实现。原理这里不解释，我也是查的，不适用于其他的操作系统。</p>
<p>首先赋予<code>rc.local</code>可执行权限：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">chmod +x /etc/rc.local</span><br></pre></td></tr></table></figure>
<p>然后修改<code>/etc/rc.local</code>文件，在下面增加我们需要执行的内容，我这里就比较简单的添加了启动ss服务的脚本：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">/usr/bin/ssserver -c /path/to/shadowsocks/config.json -d start</span><br></pre></td></tr></table></figure>
<p>建议全部写上绝对路径，免得系统找不到我们的程序和配置文件。</p>
<h2 id="utorrent配置代理">utorrent配置代理</h2>
<p>现在以utorrent为例，需要在“网络”中设置代理。shadowsocks默认的socks5代理地址是<code>127.0.0.1:1080</code>。最终的配置如下图：</p>
<p><img data-src="http://cdn.miaoerduo.com/images/utorrent.jpg" alt="utorrent config" /></p>
]]></content>
      <categories>
        <category>Miscellanea</category>
      </categories>
  </entry>
  <entry>
    <title>Python Trick</title>
    <url>/2018/01/25/python-trick/</url>
    <content><![CDATA[<p>这里主要记录一些Python使用上的小技巧，以后使用的时候可以方便查阅。不定期更新。</p>
<a id="more"></a>
<h3 id="豆瓣源">1. 豆瓣源</h3>
<p>使用pip为python安装一个第三方库的时候，由于国内可能不方便下载国外的库，这里可以使用豆瓣源来加速。只需要加上参数<code>-i https://pypi.douban.com/simple/</code>即可。比如安装scrapy就可以使用如下的指令:</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">pip install -i https://pypi.douban.com/simple/ scrapy</span><br></pre></td></tr></table></figure>
<h3 id="python环境管理">2. Python环境管理</h3>
<p>很多时候，我们可能需要用到不同版本的 <code>python</code>，比如2.7和3.4两个版本。这时候可以使用<code>virtualenv</code>，<code>virtualenvwrapper</code> 等工具。但实际上 <code>anaconda</code> 本身就已经支持 <code>python</code> 的版本控制了（我一直用的 <code>anaconda</code>）。比如我们要新建一个2.7版本的 <code>python</code> 环境，就只需要输入：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">conda create -n py27 python=2.7</span><br></pre></td></tr></table></figure>
<p>这样就创建了一个2.7版本的 <code>python</code>，且这个 <code>python</code> 环境的名字叫做py27。 我们如果需要使用这个环境只需要输入指令：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">source</span> activate py27</span><br></pre></td></tr></table></figure>
<p>之后就进入我们的全新的 <code>python</code> 环境了，想退出环境则输入：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">source</span> deactivate py27</span><br></pre></td></tr></table></figure>
<p>如果需要给我们新建的环境也安装 <code>anaconda</code> 的全套环境，则需要输入：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">conda install -n py27 anaconda</span><br></pre></td></tr></table></figure>
<p>安装过程是很慢的。我们如果需要给这个环境安装第三方库的话，可以激活这个环境然后使用pip安装，或者使用：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">conda install -n py27 package_name</span><br></pre></td></tr></table></figure>
<p>来安装，<code>package_name</code> 就是我们要安装的库。 如果我们需要删除现有的环境，使用指令：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">conda remove -n py27 --all</span><br></pre></td></tr></table></figure>
<p>如果不使用 <code>--all</code> ,而是指定一个库，则会删除该库，比如：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">conda remove -n py27 package_name</span><br></pre></td></tr></table></figure>
<p>就会删除 <code>package_name</code> 对应的库。 最后一个指令是查看现有的 <code>python</code> 环境：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">conda info -e</span><br></pre></td></tr></table></figure>
<h3 id="简单http服务">3. 简单HTTP服务</h3>
<p>使用 <code>Python</code> 可以通过命令创建HTTP服务器，这在局域网中传输文件，或者测试服务器连通等情况下十分有效。</p>
<p>在Python2中，使用如下命令：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">python -m SimpleHTTPServer 8000</span><br></pre></td></tr></table></figure>
<p>这样就可以创建一个监听本地8000端口的HTTP静态资源服务。 在Python3中，命令改成：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">python -m http.server 8000</span><br></pre></td></tr></table></figure>
<p>效果同上。</p>
<h3 id="给python的类或对象动态添加成员函数">4. 给Python的类或对象动态添加成员函数</h3>
<p><span class="exturl" data-url="aHR0cHM6Ly93d3cuaWFubGV3aXMub3JnL2VuL2R5bmFtaWNhbGx5LWFkZGluZy1tZXRob2QtY2xhc3Nlcy1vci1jbGFzcy1pbnN0YW5j" title="https://www.ianlewis.org/en/dynamically-adding-method-classes-or-class-instanc">https://www.ianlewis.org/en/dynamically-adding-method-classes-or-class-instanc<i class="fa fa-external-link"></i></span></p>
]]></content>
      <categories>
        <category>Miscellanea</category>
        <category>Python</category>
      </categories>
  </entry>
  <entry>
    <title>dlib人脸关键点检测的模型分析与压缩</title>
    <url>/2018/01/08/dlib-landmark-model-compression/</url>
    <content><![CDATA[<p>人脸关键点检测的技术在很多领域上都有应用，首先是人脸识别，常见的人脸算法其实都会有一步，就是把人脸的图像进行对齐，而这个对齐就是通过关键点实现的，因此关于人脸关键点检测的论文也常叫face alignment，也就是人脸对齐。另一方面，对于美颜，2D/3D建模等等也需要一来人脸的关键点技术，而且通常也要求有尽可能多的人脸关键点。</p>
<blockquote>
<p>Dlib is a modern C++ toolkit containing machine learning algorithms and tools for creating complex software in C++ to solve real world problems. It is used in both industry and academia in a wide range of domains including robotics, embedded devices, mobile phones, and large high performance computing environments. Dlib's <span class="exturl" data-url="aHR0cDovL2RsaWIubmV0L2xpY2Vuc2UuaHRtbA==" title="http://dlib.net/license.html">open source licensing<i class="fa fa-external-link"></i></span> allows you to use it in any application, free of charge.</p>
</blockquote>
<p>Dlib是一个包含了大量的机器学习和复杂软件开发工具的现代C++工具箱，被广泛的用于软件开发等领域。</p>
<p>本篇博客主要研究的就是Dlib中的人脸关键点检测的工具。该工具的方法依据是 One Millisecond Face Alignment with an Ensemble of Regression Trees by Vahid Kazemi and Josephine Sullivan, CVPR 2014 这篇论文，在速度和精度上均达到了极好的效果。 本文的侧重点在于人脸关键点模型的存储结构的分析和模型的压缩策略分析，最终在性能几乎不变的情况下，得到模型的至少10倍的压缩比。项目最终的github地址为：<span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL21pYW9lcmR1by9kbGliLWZhY2UtbGFuZG1hcmstY29tcHJlc3Npb24=" title="https://github.com/miaoerduo/dlib-face-landmark-compression">https://github.com/miaoerduo/dlib-face-landmark-compression<i class="fa fa-external-link"></i></span> 欢迎fork、star和pr。</p>
<a id="more"></a>
<p>注意：</p>
<ol type="1">
<li>本文假定了读者对该论文有一定的了解，可以使用Dlib完成人脸关键点的训练和部署，因此不做论文的相关方法的解释。</li>
<li>本文中分析的数据都是Dlib的shape_predictor类的私有成员，这里不得不把他们的修饰符从private改成了public，但文中并没有专门指出。</li>
<li>本文中所有的代码均在本地的64位操作系统上运行，在变量数据存储的大小描述的时候也均以64位来说明，即使是不同的编译器也会对数据大小造成影响，但这不是本文的重点。</li>
<li>本文中的数据类型如果不在C++中见到的数据类型，则为下面的typedef的数据类型：</li>
</ol>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="keyword">char</span>        int8;</span><br><span class="line"><span class="keyword">typedef</span> short       int16;</span><br><span class="line"><span class="keyword">typedef</span> <span class="keyword">int</span>         int32;</span><br><span class="line"><span class="keyword">typedef</span> <span class="keyword">long</span> <span class="keyword">long</span>   int64;</span><br><span class="line"><span class="keyword">typedef</span> <span class="keyword">float</span>       float32;</span><br><span class="line"><span class="keyword">typedef</span> <span class="keyword">double</span>      float64;</span><br><span class="line"><span class="keyword">typedef</span> <span class="keyword">unsigned</span> <span class="keyword">char</span>       uint8;</span><br><span class="line"><span class="keyword">typedef</span> <span class="keyword">unsigned</span> short      uint16;</span><br><span class="line"><span class="keyword">typedef</span> <span class="keyword">unsigned</span> <span class="keyword">int</span>        uint32;</span><br><span class="line"><span class="keyword">typedef</span> <span class="keyword">unsigned</span> <span class="keyword">long</span> <span class="keyword">long</span>  uint64;</span><br></pre></td></tr></table></figure>
<p>Dlib中人脸关键点实现的类是<code>dlib::shape_predictor</code>，源码为：<span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL2Rhdmlza2luZy9kbGliL2Jsb2IvbWFzdGVyL2RsaWIvaW1hZ2VfcHJvY2Vzc2luZy9zaGFwZV9wcmVkaWN0b3IuaA==" title="https://github.com/davisking/dlib/blob/master/dlib/image_processing/shape_predictor.h">https://github.com/davisking/dlib/blob/master/dlib/image_processing/shape_predictor.h<i class="fa fa-external-link"></i></span></p>
<p>这里简单的抽取了数据相关的接口定义：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">namespace</span> dlib</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">namespace</span> impl</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="class"><span class="keyword">struct</span> <span class="title">split_feature</span></span></span><br><span class="line"><span class="class">        &#123;</span></span><br><span class="line">            <span class="keyword">unsigned</span> <span class="keyword">long</span> idx1;</span><br><span class="line">            <span class="keyword">unsigned</span> <span class="keyword">long</span> idx2;</span><br><span class="line">            <span class="keyword">float</span> thresh;</span><br><span class="line">        &#125;;</span><br><span class="line"></span><br><span class="line">        <span class="class"><span class="keyword">struct</span> <span class="title">regression_tree</span></span></span><br><span class="line"><span class="class">        &#123;</span></span><br><span class="line">            <span class="built_in">std</span>::<span class="built_in">vector</span>&lt;split_feature&gt; splits;</span><br><span class="line">            <span class="built_in">std</span>::<span class="built_in">vector</span>&lt;matrix&lt;<span class="keyword">float</span>,0,1&gt; &gt; leaf_values;</span><br><span class="line">        &#125;;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="class"><span class="keyword">class</span> <span class="title">shape_predictor</span></span></span><br><span class="line"><span class="class">    &#123;</span></span><br><span class="line">    <span class="keyword">private</span>:</span><br><span class="line">        matrix&lt;<span class="keyword">float</span>,<span class="number">0</span>,<span class="number">1</span>&gt; initial_shape;</span><br><span class="line">        <span class="built_in">std</span>::<span class="built_in">vector</span>&lt;<span class="built_in">std</span>::<span class="built_in">vector</span>&lt;impl::regression_tree&gt; &gt; forests;</span><br><span class="line">        <span class="built_in">std</span>::<span class="built_in">vector</span>&lt;<span class="built_in">std</span>::<span class="built_in">vector</span>&lt;<span class="keyword">unsigned</span> <span class="keyword">long</span>&gt; &gt; anchor_idx;</span><br><span class="line">        <span class="built_in">std</span>::<span class="built_in">vector</span>&lt;<span class="built_in">std</span>::<span class="built_in">vector</span>&lt;dlib::<span class="built_in">vector</span>&lt;<span class="keyword">float</span>,2&gt; &gt; &gt; deltas;</span><br><span class="line">    &#125;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>下面，我们逐一对每个部分的参数进行分析。<code>Dlib</code>内置了很多的数据类型，像<code>vector</code>、<code>metrix</code>等等，每种数据类型又可以单独序列化成二进制的数据。对于<code>shape_predictor</code>的序列化，本质上就是不断的调用成员变量数据的序列化方法，由此极大地简化代码，提高了代码的复用率。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">inline</span> <span class="keyword">void</span> <span class="title">serialize</span> <span class="params">(<span class="keyword">const</span> shape_predictor&amp; item, <span class="built_in">std</span>::ostream&amp; out)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> version = <span class="number">1</span>;</span><br><span class="line">    dlib::serialize(version, out);</span><br><span class="line">    dlib::serialize(item.initial_shape, out);</span><br><span class="line">    dlib::serialize(item.forests, out);</span><br><span class="line">    dlib::serialize(item.anchor_idx, out);</span><br><span class="line">    dlib::serialize(item.deltas, out);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>但，对于移动端等应用场景，需要模型占用尽可能少的存储空间，这样一来，这些标准的存储方式就会造成数据的很大程度的冗余。我们的任务就是一点点的减少这些冗余，只存有用的数据。</p>
<h2 id="一常量部分">一、常量部分</h2>
<p>首先，我们需要知道一些常量的数据。这些数据完成了对模型的描述。 [table id=6 /]</p>
<table>
<thead>
<tr class="header">
<th>变量名</th>
<th>数据类型</th>
<th>作用</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>version</td>
<td>uint64</td>
<td>记录模型版本号</td>
</tr>
<tr class="even">
<td>cascade_depth</td>
<td>uint64</td>
<td>回归树的级数</td>
</tr>
<tr class="odd">
<td>num_trees_per_cascade_level</td>
<td>uint64</td>
<td>每一级中的树的个数</td>
</tr>
<tr class="even">
<td>tree_depth</td>
<td>uint64</td>
<td>树的深度</td>
</tr>
<tr class="odd">
<td>feature_pool_size</td>
<td>uint64</td>
<td>特征池的大小</td>
</tr>
<tr class="even">
<td>landmark_num</td>
<td>uint64</td>
<td>特征点的数目</td>
</tr>
<tr class="odd">
<td>quantization_num</td>
<td>uint64</td>
<td>量化的级数</td>
</tr>
<tr class="even">
<td>prune_thresh</td>
<td>float32</td>
<td>剪枝的阈值</td>
</tr>
</tbody>
</table>
<h2 id="二初始形状-initial_shape">二、初始形状 initial_shape</h2>
<p><code>matrix&lt;float,0,1&gt; initial_shape;</code> 表示的是初始化人脸关键点的坐标，存储类型是<code>float</code>型，个数为<code>landmark_num * 2</code>（不要忘了一个点是两个数组成 :P）。</p>
<h2 id="三锚点-anchor_idx">三、锚点 anchor_idx</h2>
<p><code>std::vector&lt;std::vector&lt;unsigned long&gt; &gt; anchor_idx;</code> 是一个二维的数组，存放的是landmark点的下标。在常见的68点和192点的任务中，使用一个<code>uint8</code>就可以存放下标，而这里使用的是<code>unsigned long</code>，显然过于<strong>冗余</strong>，这里可以简化成uint8存储。这个二维数组的大小为 <code>cascade_depth * feature_pool_size</code>。每一级回归树使用一套锚点。</p>
<h2 id="四deltas">四、deltas</h2>
<p><code>std::vector&lt;std::vector&lt;dlib::vector&lt;float,2&gt; &gt; &gt; deltas;</code>和anchor_idx类似，是一个二维数组，不同的是，数组的每个值都是<code>dlib::vector&lt;float,2&gt;</code>的结构。这个数组的大小为<code>cascade_depth * feature_pool_size * 2</code>，存放的内容是<code>float</code>数值。考虑到这里的参数量很少，没有压缩的必要，这里我们直接存储原数据。</p>
<h2 id="五森林-forests">五、森林 forests</h2>
<p>这部分是模型参数量最大的部分，一个模型大概2/3的存储都耗在了这个地方。这里才是我们压缩的重点！ <code>std::vector&lt;std::vector&lt;impl::regression_tree&gt; &gt; forests;</code>一个<code>shape_predictor</code>中，有<code>cascade_depth</code>级，每一级有<code>num_trees_per_cascade_level</code>棵树。对于每棵树，它主要存放了两个部分的数据：分割的阈值<code>splits</code>和叶子的值<code>leaf_values</code>。为了便于阅读，再把数据结构的定义附上。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">namespace</span> dlib</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">namespace</span> impl</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="class"><span class="keyword">struct</span> <span class="title">split_feature</span></span></span><br><span class="line"><span class="class">        &#123;</span></span><br><span class="line">            <span class="keyword">unsigned</span> <span class="keyword">long</span> idx1;</span><br><span class="line">            <span class="keyword">unsigned</span> <span class="keyword">long</span> idx2;</span><br><span class="line">            <span class="keyword">float</span> thresh;</span><br><span class="line">        &#125;;</span><br><span class="line"></span><br><span class="line">        <span class="class"><span class="keyword">struct</span> <span class="title">regression_tree</span></span></span><br><span class="line"><span class="class">        &#123;</span></span><br><span class="line">            <span class="built_in">std</span>::<span class="built_in">vector</span>&lt;split_feature&gt; splits;</span><br><span class="line">            <span class="built_in">std</span>::<span class="built_in">vector</span>&lt;matrix&lt;<span class="keyword">float</span>,0,1&gt; &gt; leaf_values;</span><br><span class="line">        &#125;;</span><br><span class="line">    &#125; <span class="comment">// end namespace impl</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h3 id="splits">5.1 splits</h3>
<p>splits存放的数据是阈值和特征像素值的下标，这个下标的范围是<code>[0, feature_pool_size)</code>，在通常情况下，<code>feature_pool_size</code>不会太大，论文中最大也就设到了2000。这里我们可以使用一个<code>uint16</code>来存储。<code>thresh</code>就直接存储。对于一棵树，树的深度为<code>tree_depth</code>，则有 <code>2^tree_depth - 1</code>个split_node。（这里认为只有根节点的树深度为0）。</p>
<h3 id="leaf_values">5.2 leaf_values</h3>
<p><code>std::vector&lt;matrix&lt;float,0,1&gt; &gt; leaf_values;</code>对于深度为<code>tree_depth</code>的树，有<code>2^tree_depth</code>个叶子节点。对于每个叶子节点，需要存储整个关键点的偏移量，也就是说每个节点存放了<code>landmark_num * 2</code>个<code>float</code>的数值。那么这部分的参数量到底有多大呢？</p>
<p>举个例子，在<code>cascade_num</code>为10，<code>num_trees_per_cascade_level</code>为500，<code>tree_depth</code>为5，<code>landmark_num</code>为68的时候。<code>leaf_values</code>的值有<code>cascade_num * num_trees_per_cascade_level * (2 ^ tree_depth) * landmark_num * 2 = 21760000 = 20.8M</code>的参数量，由于使用<code>float</code>存储，通常一个<code>float</code>是4个字节，因此总的存储量达到了逆天的<strong>80MB</strong>！远大于其他的参数的总和。 那么如何才能有效的降低这部分的存储量呢？ 这就要要用到传说中的模型压缩三件套：<strong>剪枝，量化</strong>和<strong>编码</strong>。</p>
<h4 id="参数分布分析">5.2.1 参数分布分析</h4>
<p>首先笔者统计了参数的分布，大致的情况是这样的，（具体的结果找不到了）。 叶子节点里的参数的范围在<code>[-0.11, 0.11]</code>之间，其中<code>[-0.0001, 0.0001]</code>的参数占了50%以上。说明模型中有大量的十分接近0的数字。</p>
<h4 id="剪枝">5.2.2 剪枝</h4>
<p>剪枝的策略十分粗暴，选择一个剪枝的阈值prune_thresh，将模小于阈值的数全部置0。</p>
<h4 id="量化">5.2.3 量化</h4>
<p>量化的过程，首先获取数据中的最小值和最大值，记为：<code>leaf_min_value</code>和<code>leaf_max_value</code>。之后根据量化的级数<code>quantization_num</code>，计算出每一级的步长：<code>quantization_precision = (leaf_max_value - leaf_min_value) / quantization_num</code>。之后对于任意数值<code>x</code>，那么它最终为<code>x / quantization_precision</code>进行四舍五入的结果。这样就可以把<code>float</code>的数字转换成整形来表示。量化级数越高，则量化之后的值损失就越小。</p>
<h4 id="编码">5.3.3 编码</h4>
<p>如果我们不做任何的编码操作，直接存储量化之后的结果，也是可以一定程度上进行模型的压缩的。比如使用256级量化，则量化的结果使用一个<code>uint8</code>就可以存储，从而把存储量降为原来的1/4。但是这样有两个问题：1，依赖量化的级数；2，存储量减少不大。 在信息论中有个信息熵的概念。为了验证存储上的可以再优化，这里选择了一个68点的模型，经过256级量化之后，计算出信息熵（信息熵的计算请查阅其他的资料），其数值为1.53313，也就是说，理想情况下，一个数值只需要不到<code>2 bits</code>就可以存储了。如果不编码则需要<code>8 bits</code>。压缩比为<code>1.53313 / 8 = 19.2%</code>，前者仅为后者的1/5不到！</p>
<p>这里，我采用的是经典的huffman编码，使用了github上的<span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL25pbmdrZS9odWZmbWFuLWNvZGVz" title="https://github.com/ningke/huffman-codes">https://github.com/ningke/huffman-codes<i class="fa fa-external-link"></i></span>项目中的代码，感谢作者的贡献！原项目中只能对char类型的数据进行编码，因此这里也做了相应的修改，以适应于int类型的编码，同时删除了一些用不到的函数。使用huffman对上述的256级的数值进行编码，最终的每个数字的平均长度为1.75313，已经很接近理想情况。使用huffman编码时，同时需要将码表进行储存，这部分细节较为繁琐，读者可以自行阅读源码。</p>
<p>至此，Dlib的模型的分析和压缩就全部介绍完了。对代码感兴趣的同学可以在：<span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL21pYW9lcmR1by9kbGliLWZhY2UtbGFuZG1hcmstY29tcHJlc3Npb24=" title="https://github.com/miaoerduo/dlib-face-landmark-compression">https://github.com/miaoerduo/dlib-face-landmark-compression<i class="fa fa-external-link"></i></span> ，也就是我的github上clone到最新的代码，代码我目前也在不断的测试，如果有问题，也会及时更新的。</p>
<p>在本地的实验中，原模型的大小为127M，压缩之后只有5.9M，且性能几乎不变（这里<code>prune_thresh</code>设为0.0001，<code>quantization_num</code>设为256，<code>quantization_num</code>设置越大，则精度越接近原模型，同时<code>prune_thresh</code>的大小很多时候是没有用的）。</p>
<p>马上就要毕业了，希望写博客的习惯能够一直保持下去。</p>
<p>最后，再一次，希望小喵能和大家一起学习和进步~~</p>
]]></content>
      <categories>
        <category>C++</category>
      </categories>
  </entry>
  <entry>
    <title>小喵的在线共享编辑器</title>
    <url>/2017/07/02/shared-online-editor/</url>
    <content><![CDATA[<blockquote>
<p>小喵的唠叨话：写这篇博客的初衷是因为看到了室友电脑面试的时候，面试官要求在线写代码。然后就想到，如果两个人能够在同一个页面进行编辑工作，不就能更方便的调试代码了吗？（PS.懂linux的screen或tmux的可以绕道了。）代码十分简单，在一个月前就写完了，只是一直没有时间写博客说明一下。</p>
</blockquote>
<p>github：<span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL21pYW9lcmR1by9zaGFyZWQtZWRpdG9y" title="https://github.com/miaoerduo/shared-editor">https://github.com/miaoerduo/shared-editor<i class="fa fa-external-link"></i></span> 欢迎 <strong>fork</strong> 和 <strong>star</strong>。</p>
<p>那么，实现一个这样的在线的共享编辑器需要哪些工作呢？我们下面一点一点的说明。</p>
<a id="more"></a>
<h2 id="一写在前面">一、写在前面</h2>
<p>熟悉Linux的同学都知道screen和tmux这两个工具。通常我们可以使用他们来执行一些长时间的任务，也可以使用他们的共享终端的功能。在结对编程中，这是很有效的一个工具。</p>
<p>本文要实现的，是和上述两个工具类似的共享编辑器。要说优点的话，可能就是更亲民一些，打开网页就能使用。很适合远程帮女票看个代码啥的（好像很多公司里面会截断websocket，这样就没法用了）。</p>
<p>实现一个这样的编辑器，主要有两个部分。编辑器和同步数据的服务端。下面我们依次介绍。</p>
<h2 id="二在线编辑器">二、在线编辑器</h2>
<p>首先，我们需要一个好看的编辑器。调研了一下，找到了ACE这个编辑器，网址是 <span class="exturl" data-url="aHR0cHM6Ly9hY2UuYzkuaW8=" title="https://ace.c9.io">https://ace.c9.io<i class="fa fa-external-link"></i></span> ，简单的了解了一下这个编辑器，发现居然连Github用的都是这个编辑器！看来我们选择这个编辑器是没错的啦。</p>
<p>使用起来也异常的简单，官方的Demo如下：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="meta">&lt;!DOCTYPE <span class="meta-keyword">html</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">html</span> <span class="attr">lang</span>=<span class="string">"en"</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">head</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">title</span>&gt;</span>ACE in Action<span class="tag">&lt;/<span class="name">title</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">style</span> <span class="attr">type</span>=<span class="string">"text/css"</span> <span class="attr">media</span>=<span class="string">"screen"</span>&gt;</span></span><br><span class="line"><span class="css">  <span class="selector-id">#editor</span> &#123;</span></span><br><span class="line">    position: absolute;</span><br><span class="line">    top: 0;</span><br><span class="line">    right: 0;</span><br><span class="line">    bottom: 0;</span><br><span class="line">    left: 0;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="tag">&lt;/<span class="name">style</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">head</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;<span class="name">body</span>&gt;</span></span><br><span class="line"></span><br><span class="line">  <span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">"editor"</span>&gt;</span>function foo(items) &#123;</span><br><span class="line">  var x = "All this is syntax highlighted";</span><br><span class="line">  return x;</span><br><span class="line">  &#125;<span class="tag">&lt;/<span class="name">div</span>&gt;</span></span><br><span class="line"></span><br><span class="line">  <span class="tag">&lt;<span class="name">script</span> <span class="attr">src</span>=<span class="string">"/ace-builds/src-noconflict/ace.js"</span> <span class="attr">type</span>=<span class="string">"text/javascript"</span> <span class="attr">charset</span>=<span class="string">"utf-8"</span>&gt;</span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">script</span>&gt;</span></span><br><span class="line"><span class="actionscript">  <span class="keyword">var</span> editor = ace.edit(<span class="string">"editor"</span>);</span></span><br><span class="line"><span class="actionscript">  editor.setTheme(<span class="string">"ace/theme/monokai"</span>);</span></span><br><span class="line"><span class="actionscript">  editor.getSession().setMode(<span class="string">"ace/mode/javascript"</span>);</span></span><br><span class="line">  <span class="tag">&lt;/<span class="name">script</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">body</span>&gt;</span></span><br><span class="line"><span class="tag">&lt;/<span class="name">html</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p>上面的script中的src可能需要换成可以访问的链接。之后就能预览到编辑器的效果。大致和前面小喵的效果类似。 具体的其他的用法可以在官网中查到，这里小喵就不着重介绍了。</p>
<h2 id="三消息同步机制">三、消息同步机制</h2>
<p>选择好合适的编辑器之后，我们需要做的就是消息通信的功能了。</p>
<p>这里主要有四种情况：</p>
<h3 id="文档同步">1. 文档同步</h3>
<p>当用户修改文档的时候，其修改的部分必须同步到所有的阅读改文档的用户。这里只同步修改的部分，因为每次都同步整个文档，那么会很消耗带宽（总不能输入一个 "hello world"都同步十几次文档吧）。</p>
<p>下图是一个示例（强势安利一下：<span class="exturl" data-url="aHR0cHM6Ly93d3cucHJvY2Vzc29uLmNvbQ==" title="https://www.processon.com">https://www.processon.com<i class="fa fa-external-link"></i></span> 这个画图的工具）。用户1编辑了文档，文档更新的内容发送给了服务器，服务器将更新的内容组播到所有打开相同文档的用户（注意，这里不是广播，广播是向所有的用户发送），同时更新自己的远程备份。</p>
<p><img data-src="http://cdn.miaoerduo.com/images/shared-online-editor-update.png" /></p>
<center>
图1 文档更新
</center>
<h3 id="文档副本">2. 文档副本</h3>
<p>当用户第一次访问已经存在的文档的时候。这个时候，该用户需要加载页面的所有的内容。因此我们的服务器端需要存放完整的文档的副本。</p>
<p>如下图，用户3打开了这个文档，这时候会请求服务器发送完整的文档信息。</p>
<p><img data-src="http://cdn.miaoerduo.com/images/shared-online-editor-new-user.png" /></p>
<center>
图2 新增用户
</center>
<h3 id="文档销毁">3. 文档销毁</h3>
<p>小喵这里的文档的内容是直接在内存中保存的。这样的好处是很方便，不需要额外的控制数据库啥的。但是弊端也很明显，虽然每个文档可能比较小，但如果文档创建的比较多，就会一直消耗内存。所以当没有用户使用文档的时候，需要删除文档，这样服务器端就需要保存一个引用计数。计数为0，就删除文档。</p>
<h3 id="冲突解决">4. 冲突解决</h3>
<p>考虑到网络可能会出现故障，用户在编辑文档之后，其他的用户可能并没有即使同步，这样就出现文档落后的情况。一个简单的策略就是，每次文档修改之后都返回一个时间戳，下一次修改文档的时候要将这个时间戳作为参数发送到服务器，如果时间戳不是最新的，那么就刷新整个文档。当然这个策略也有很多的不足之处，如果大家能有什么改进，烦请告诉小喵一下~</p>
<p>上面就是设计部分，具体实现的话，需要用到WebSocket技术，这是浏览器和服务器实时通信的一个很好的工具。WebSocket有很多语言的实现，小喵这里选择的是比较容易上手的 <span class="exturl" data-url="aHR0cHM6Ly9zb2NrZXQuaW8=" title="https://socket.io">socket.io<i class="fa fa-external-link"></i></span>。</p>
<p>socket.io 不仅支持用户和服务器的点对点通信，还支持组播、广播的操作。简单的学习一下，就可以完成上面的设计。</p>
<p>这里，小喵也不在代码层面上解释实现了。感兴趣的同学可以看看小喵的github: <span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL21pYW9lcmR1by9zaGFyZWQtZWRpdG9y" title="https://github.com/miaoerduo/shared-editor">https://github.com/miaoerduo/shared-editor<i class="fa fa-external-link"></i></span></p>
<p>能够给小喵提交一些PR的话就更好了。</p>
<h2 id="四写在后面">四、写在后面</h2>
<p>终于写完这篇博客了，拖了快两个月了。之前迟迟没有动手的一个原因是比较忙，更多的可能因为自己有点懒，一直不想画解释原理的示意图。现在终于写完了，还是挺开心的。</p>
<p>这次的博客相比之前的，更多的是介绍设计的思路。反正代码都在github上了，大家可以随意食用~ 希望能和大家一起进步！</p>
<p><strong>转载</strong> 请注明出处，谢谢~</p>
]]></content>
      <categories>
        <category>Miscellanea</category>
        <category>Nodejs</category>
      </categories>
  </entry>
  <entry>
    <title>简易漫画网站搭建-漫画喵Server版</title>
    <url>/2017/03/14/cartoon-cat-server/</url>
    <content><![CDATA[<blockquote>
<p>小喵的唠叨话：寒假的时候写了一个漫画爬虫，爬取了好几个漫画，不过一直没有找到合适的漫画阅读的工具。因此最近就试着自己写一个漫画的网站，放在公网上或者局域网里，这样就能随时随地用手机、Pad看漫画了。</p>
</blockquote>
<p>先放上项目的地址：<span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL21pYW9lcmR1by9jYXJ0b29uLWNhdC1zZXJ2ZXI=" title="https://github.com/miaoerduo/cartoon-cat-server">https://github.com/miaoerduo/cartoon-cat-server<i class="fa fa-external-link"></i></span>，欢迎大家随时star、fork和指教。</p>
<a id="more"></a>
<p>关于漫画爬虫的内容，请参看之前的博客。</p>
<h2 id="写在前面">写在前面</h2>
<p>这里，我们先试着思考几个问题：</p>
<h3 id="为什么要做一个漫画网站而不是app呢">1、为什么要做一个漫画网站，而不是APP呢</h3>
<p>小喵最开始其实是打算做一个QT的漫画阅读软件，这样能够在各种操作系统上用。开发了一小段时间之后，觉得用手机或者Pad看漫画的情况会更多。难道要给手机、平板甚至是PC都编写一套软件吗？这个小喵确实能力上达不到。其次是，如果是本地的APP的话，图片可能还是需要存到设备上，这样同样很麻烦。于是，漫画网站就成为最适合的选择，只要在联网的情况下（或者在同一个局域网中），只要有浏览器，就能够看漫画。这才是真正的跨平台，也是小喵要编写这个工具的原因。</p>
<h3 id="那么一个简单的漫画网站可以怎么去实现">2、那么，一个简单的漫画网站可以怎么去实现</h3>
<p>讲道理的话，其实单纯的静态的网页可能就足够了，给每个漫画图片编写一个页面，包含图片、下一页、上一页等部分就可以。页面的生成可以是一个脚本程序。不过这样的话，也有点麻烦，而且最终的页面的数目可能会很多。小喵不喜欢这种方式。这里，小喵采用的方式是前、后端分离的方式。</p>
<p>整个网站由三个部分组成：</p>
<ul>
<li><strong>数据</strong>：也就是漫画本身。</li>
<li><strong>后台</strong>：后台程序的功能是根据请求返回用户漫画的列表、章节和图片URL等信息。</li>
<li><strong>前端</strong>：调用后台的程序，得到漫画的信息并友好的进行显示。</li>
</ul>
<h3 id="最后我们使用什么技术来实现呢">3、最后，我们使用什么技术来实现呢</h3>
<p>前端的话，使用标准的 <code>Html</code>，<code>Css</code> 和 <code>Js</code> 就可以。后台是个 <code>API Server</code>，<code>Python + Flask</code> 或者 <code>Nodejs + Express</code> 都是不错的选择（使用起来很简单。。。），小喵这里就选用 <code>Nodejs + Express</code> 的方式编写后台（一直写Js就可以了 :P ）。另外，大型的漫画网站，漫画的信息应该会存到数据库中，这样会方便查找和管理。这里考虑到漫画数目比较少，就去掉了数据库这一步骤，直接通过文件操作来得到漫画信息，工作量也大大减少了。</p>
<p>先看一下最终的效果图（虽然界面很简单）：</p>
<p><img data-src="http://cdn.miaoerduo.com/images/cartooncat-server.png" alt="cartooncat-server" /></p>
<center>
图1 网站的最终效果图
</center>
<p>现在，喵粉们是不是已经迫不及待的想要动手写代码了呢？</p>
<h2 id="一目录结构">一、目录结构</h2>
<p>下面是小喵的目录结构：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ tree blog -N -L 2</span><br><span class="line">blog</span><br><span class="line">├── README.md</span><br><span class="line">├── main.js</span><br><span class="line">├── package.json</span><br><span class="line">└── public</span><br><span class="line">    ├── api.html</span><br><span class="line">    ├── cartoon.html</span><br><span class="line">    ├── chapter.html</span><br><span class="line">    ├── css</span><br><span class="line">    ├── img</span><br><span class="line">    ├── index.html</span><br><span class="line">    ├── js</span><br><span class="line">    └── store</span><br></pre></td></tr></table></figure>
<p><code>main.js</code> 这里就是后台API的程序。因为功能很简单，所以就放在一个文件中。<code>package.json</code> 是nodejs的包管理器，在这里可以定义依赖。我们这里只依赖Express。<code>public</code> 这个文件夹用来存放静态的资源，包括Html、Css、js、image以及漫画资源(store)。</p>
<h2 id="二后台程序">二、后台程序</h2>
<h3 id="依赖安装">1. 依赖安装</h3>
<p>原生的 <code>Nodejs</code> 就已经十分适合写API程序了，<code>Express</code> 只是让它更加的方便了而已（至少对于这个项目来说）。</p>
<p><code>Nodejs</code> 的安装十分简单，在官网上 <span class="exturl" data-url="aHR0cHM6Ly9ub2RlanMub3Jn" title="https://nodejs.org">https://nodejs.org<i class="fa fa-external-link"></i></span> 有下载链接，服务器上使用 <code>apt-get</code> 等工具也很容易安装。</p>
<p><code>Express</code> 的话，可以使用 <code>npm install express —save</code> 来安装。这里小喵使用的 <code>package</code> 的方式来安装。在项目根目录新建 <code>package.json</code> 文件，写入配置信息：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><span class="line">&#123;</span><br><span class="line">  <span class="attr">"name"</span>: <span class="string">"cartoon-cat-server"</span>,</span><br><span class="line">  <span class="attr">"version"</span>: <span class="string">"0.0.1"</span>,</span><br><span class="line">  <span class="attr">"dependencies"</span>: &#123;</span><br><span class="line">    <span class="attr">"express"</span>: <span class="string">"visionmedia/express"</span></span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>之后使用 <code>npm install</code> 命令就可以完成安装。安装完成之后会发现根目录多了一个 <code>node_modules</code> 文件夹，里面就是我们的依赖库了。喵粉们如果下载了我的这个项目的话，第一步也是要进入项目目录然后输入 <code>npm install</code> 。</p>
<h3 id="漫画的文件结构">2. 漫画的文件结构</h3>
<p>我们的漫画资源都是通过 <code>漫画喵</code> 这个爬虫工具下载下来的，因此漫画都是每个章节都是一个文件夹，每个章节的漫画图片都放在对应的文件夹中，而且按照页码来命名。</p>
<p>这样通过遍历文件夹似乎就能获取漫画的信息了！</p>
<p>漫画列表和章节中的图片列表都可以通过上述的方式来解决，但是章节的列表却不行。因为漫画的章节有时候并没有明确的顺序（比如突然出现一个番外篇啥的），这样遍历文件夹默认的顺序（按名称）就可能是错误的。 我们有两个解决办法：</p>
<ol type="1">
<li>按照文件夹的创建时间来显示文件名。这样有点不灵活。</li>
<li>在每个漫画的根目录建一个文本文件，用来存放章节的信息。</li>
</ol>
<p>小喵选择第二种策略，创建这个list的方法十分简单粗暴，在漫画目录下面使用:</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">ls -t -r &gt; index</span><br></pre></td></tr></table></figure>
<p><code>ls</code> 是linux上面的显示目录的工具，<code>-t</code> 表示按时间排序（最上面是最新的），<code>-r</code> 表示倒序，<code>&gt;</code> 是重定向，最终输出到 <code>index</code> 这个文件。然后编辑这个文件，删掉 <code>index</code> 这一行(系统貌似是先生成 <code>index</code> 这个文件，然后再执行 <code>ls</code>，最后把结果输入到文件中，因此文件里面多了一个 <code>index</code> 的文件名)，再做一些必要的调整。</p>
<p>Windows上可以使用:</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">dir /OD /B &gt; index</span><br></pre></td></tr></table></figure>
<p><code>dir</code> 是windows的查看目录的命令，<code>/OD</code> 表示按照时间排序，<code>/B</code> 表示只显示文件名，<code>&gt;</code> 重定向到 <code>index</code>。<code>windows</code> 上的这个列表文件中也会出现 <code>index</code> 这个文件名(看来各种操作系统都一样)。另外需要注意的是 <code>windows</code> 的换行和 <code>linux</code> 或 <code>mac</code> 不一样。</p>
<p>这样，我们就可以通过读这个 <code>index</code> 文件来获取章节的信息了。</p>
<p>最终的漫画的结构（为了显示的方便，删除了很多图片和章节）如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">$ tree store -N -L 3</span><br><span class="line">store</span><br><span class="line">├── 犬夜叉</span><br><span class="line">│   ├── index</span><br><span class="line">│   ├── 第1章</span><br><span class="line">│   │   ├── 00001.jpg</span><br><span class="line">│   │   └── 00002.jpg</span><br><span class="line">│   └── 第2章</span><br><span class="line">│       ├── 00001.jpg</span><br><span class="line">│       └── 00002.jpg</span><br><span class="line">└── 极黑的布伦希尔特</span><br><span class="line">    ├── index</span><br><span class="line">    ├── 第1章</span><br><span class="line">    │   ├── 00001.jpg</span><br><span class="line">    │   └── 00002.jpg</span><br><span class="line">    └── 第2章</span><br><span class="line">        ├── 00001.jpg</span><br><span class="line">        └── 00002.jpg</span><br></pre></td></tr></table></figure>
<h3 id="api-编写">3. API 编写</h3>
<p><code>Express</code> 十分的容易使用。这里小喵给一个官网的Hello World的教程让大家看一下：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> express = <span class="built_in">require</span>(<span class="string">'express'</span>)</span><br><span class="line"><span class="keyword">var</span> app = express()</span><br><span class="line"></span><br><span class="line">app.get(<span class="string">'/'</span>, <span class="function"><span class="keyword">function</span> (<span class="params">req, res</span>) </span>&#123;</span><br><span class="line">  res.send(<span class="string">'Hello World!'</span>)</span><br><span class="line">&#125;)</span><br><span class="line"></span><br><span class="line">app.listen(<span class="number">3000</span>, <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="built_in">console</span>.log(<span class="string">'Example app listening on port 3000!'</span>)</span><br><span class="line">&#125;)</span><br></pre></td></tr></table></figure>
<p>将上述代码保存成 <code>main.js</code>，使用 <code>node main.js</code> 就可以启动这个程序，然后用浏览器访问 <code>http://localhost:3000</code> ，就能看到一个显示着 <code>hello world</code> 的页面。</p>
<p><code>require</code> 语句用来引入依赖，<code>app</code> 是express的封装的对象。</p>
<p>通过 <code>app.get</code> 方法就可以给指定的url（官方说法叫route）绑定相应的处理方法（GET方法的请求）。处理函数有2个参数 <code>req</code> 表示request，也就是用户的请求，通过这个对象我们可以获取用户的输入的参数，<code>res</code> 表示response，是一个向用户返回数据的对象。</p>
<p><code>listen</code> 用来监听一个端口启动服务。</p>
<p>这里小喵先给出自己定义的一些辅助的函数，定义错误信息和参数校验，后面会使用到：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 引入依赖</span></span><br><span class="line"><span class="keyword">var</span> express = <span class="built_in">require</span>(<span class="string">'express'</span>);</span><br><span class="line"><span class="keyword">var</span> fs = <span class="built_in">require</span>(<span class="string">"fs"</span>); <span class="comment">// 即file system，用来进行文件操作</span></span><br><span class="line"><span class="keyword">var</span> app = express();</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 错误提示</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">var</span> ErrorHelper = &#123;</span><br><span class="line">    <span class="string">'internal_error'</span>: <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> &#123;</span><br><span class="line">            <span class="string">'msg'</span>: <span class="string">'something wrong with server'</span>,</span><br><span class="line">            <span class="string">'code'</span>: <span class="number">1</span></span><br><span class="line">        &#125;;</span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="string">'missing_param'</span>: <span class="function"><span class="keyword">function</span> (<span class="params">param</span>) </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> &#123;</span><br><span class="line">            <span class="string">'msg'</span>: <span class="string">'missing param: '</span> + param,</span><br><span class="line">            <span class="string">'code'</span>: <span class="number">2</span></span><br><span class="line">        &#125;;</span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="string">'error_param'</span>: <span class="function"><span class="keyword">function</span> (<span class="params">param, data</span>) </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> &#123;</span><br><span class="line">            <span class="string">'msg'</span>: <span class="string">'the param '</span> + param + <span class="string">'('</span> + data + <span class="string">') is illegal'</span>,</span><br><span class="line">            <span class="string">'code'</span>: <span class="number">3</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;,</span><br><span class="line">    <span class="string">'not_found'</span>: <span class="function"><span class="keyword">function</span> (<span class="params">param</span>) </span>&#123;</span><br><span class="line">        <span class="keyword">return</span> &#123;</span><br><span class="line">            <span class="string">'msg'</span>: <span class="string">'cannot find '</span> + param,</span><br><span class="line">            <span class="string">'code'</span>: <span class="number">4</span></span><br><span class="line">        &#125;;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 检查参数格式，只能输入字母，数字和汉字</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">checkParam</span>(<span class="params">param</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="regexp">/^[\u4e00-\u9fa5_a-zA-Z0-9]+$/</span>.test(param);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h4 id="get_cartoon_list">1) get_cartoon_list</h4>
<p>这个接口用来获取所有的漫画列表。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 获取漫画列表</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line">app.get(<span class="string">'/get_cartoon_list'</span>, <span class="function"><span class="keyword">function</span> (<span class="params">req, res</span>) </span>&#123;</span><br><span class="line">    fs.readdir(__dirname + <span class="string">'/public/store'</span>, <span class="function"><span class="keyword">function</span> (<span class="params">err , files</span>) </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (err) &#123;</span><br><span class="line">            res.jsonp(ErrorHelper.internal_error());</span><br><span class="line">        &#125;</span><br><span class="line">        res.jsonp(&#123;<span class="string">'cartoon'</span>: files, <span class="string">'code'</span>: <span class="number">0</span>&#125;);</span><br><span class="line">    &#125;);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>
<p>这个函数十分的简单，通过 <code>fs</code> 读取store中的文件名，然后用json的格式返回回去。这里小喵用的jsonp，为了解决跨域请求的问题，不过我们的页面和服务是一台机器的，所以这部分并不需要。</p>
<h4 id="get_chapter_list">2) get_chapter_list</h4>
<p>这个接口用来获取漫画的章节的信息，所以需要输入参数，这里定为cartoon。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * 获取章节信息</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line">app.get(<span class="string">'/get_chapter_list'</span>, <span class="function"><span class="keyword">function</span> (<span class="params">req, res</span>) </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">var</span> cartoon = req.query.cartoon;</span><br><span class="line">    <span class="keyword">if</span> (!cartoon) &#123;</span><br><span class="line">        res.jsonp(ErrorHelper.missing_param(<span class="string">'cartoon'</span>));</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (!checkParam(cartoon)) &#123;</span><br><span class="line">        res.jsonp(ErrorHelper.error_param(<span class="string">'cartoon'</span>, cartoon));</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">var</span> cartoon_dir = __dirname + <span class="string">'/public/store/'</span> + cartoon;</span><br><span class="line">    fs.exists(cartoon_dir + <span class="string">'/index'</span>, <span class="function"><span class="keyword">function</span> (<span class="params">exists</span>) </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (!exists) &#123;</span><br><span class="line">            res.jsonp(ErrorHelper.not_found(cartoon));</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        fs.readFile(cartoon_dir + <span class="string">'/index'</span>, <span class="function"><span class="keyword">function</span> (<span class="params">err, data</span>) </span>&#123;</span><br><span class="line">            <span class="keyword">if</span> (err) &#123;</span><br><span class="line">                res.jsonp(ErrorHelper.internal_error());</span><br><span class="line">                <span class="keyword">return</span>;</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="keyword">var</span> chapter_list = data.toString().split(<span class="string">'\n'</span>).filter(<span class="function"><span class="keyword">function</span> (<span class="params">d</span>) </span>&#123;</span><br><span class="line">                <span class="keyword">return</span> d.length &gt; <span class="number">0</span>;</span><br><span class="line">            &#125;);</span><br><span class="line"></span><br><span class="line">            res.jsonp(&#123;<span class="string">'chapter'</span>: chapter_list, <span class="string">'code'</span>: <span class="number">0</span>&#125;);</span><br><span class="line">        &#125;);</span><br><span class="line">    &#125;);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>
<p>首先判断输入的参数，之后判断对应漫画的文件夹中是否有index这个文件，如果有的话就读取然后返回给用户。</p>
<h4 id="get_img_list">3) get_img_list</h4>
<p>这个接口用来返回漫画的具体章节的图片的URL，用户需要输入漫画名(cartoon)和章节名(chapter)。注意要修改自己的HOST的地址。</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> HOST = <span class="string">"localhost"</span>; <span class="comment">// 如果不是在本机上使用，请改成实际的ip地址</span></span><br><span class="line">                        <span class="comment">// 后面的图片的URL会使用这个变量来构造</span></span><br><span class="line"><span class="keyword">var</span> PORT = <span class="number">3000</span>;</span><br><span class="line"></span><br><span class="line">app.get(<span class="string">'/get_img_list'</span>, <span class="function"><span class="keyword">function</span> (<span class="params">req, res</span>) </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">var</span> cartoon = req.query.cartoon;</span><br><span class="line">    <span class="keyword">if</span> (!cartoon) &#123;</span><br><span class="line">        res.jsonp(ErrorHelper.missing_param(<span class="string">'cartoon'</span>));</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (!checkParam(cartoon)) &#123;</span><br><span class="line">        res.jsonp(ErrorHelper.error_param(<span class="string">'cartoon'</span>, cartoon));</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">var</span> chapter = req.query.chapter;</span><br><span class="line">    <span class="keyword">if</span> (!chapter) &#123;</span><br><span class="line">        res.jsonp(ErrorHelper.missing_param(<span class="string">'chapter'</span>));</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (!checkParam(chapter)) &#123;</span><br><span class="line">        res.jsonp(ErrorHelper.error_param(<span class="string">'chapter'</span>, chapter));</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">var</span> cartoon_dir = __dirname + <span class="string">'/public/store/'</span> + cartoon;</span><br><span class="line">    fs.exists(cartoon_dir + <span class="string">'/index'</span>, <span class="function"><span class="keyword">function</span> (<span class="params">exists</span>) </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (!exists) &#123;</span><br><span class="line">            res.jsonp(ErrorHelper.not_found(cartoon));</span><br><span class="line">            <span class="keyword">return</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        fs.readdir(cartoon_dir + <span class="string">'/'</span> + chapter, <span class="function"><span class="keyword">function</span> (<span class="params">err, images</span>) </span>&#123;</span><br><span class="line">            <span class="keyword">if</span> (err) &#123;</span><br><span class="line">                res.jsonp(ErrorHelper.error_param(<span class="string">'chapter'</span>, chapter));</span><br><span class="line">                <span class="keyword">return</span>;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="comment">// 按名字排序</span></span><br><span class="line">            images.sort(<span class="function"><span class="keyword">function</span> (<span class="params">lhs, rhs</span>) </span>&#123;</span><br><span class="line">                <span class="keyword">return</span> <span class="built_in">parseInt</span>(lhs.split(<span class="string">'.'</span>)[<span class="number">0</span>]) - <span class="built_in">parseInt</span>(rhs.split(<span class="string">'.'</span>)[<span class="number">0</span>]);</span><br><span class="line">            &#125;);</span><br><span class="line"></span><br><span class="line">            <span class="keyword">var</span> urls = images.map(<span class="function"><span class="keyword">function</span> (<span class="params">image</span>) </span>&#123;</span><br><span class="line">                <span class="keyword">return</span> <span class="string">'http://'</span> + HOST + <span class="string">':'</span> + PORT + <span class="string">'/store/'</span> + cartoon + <span class="string">'/'</span> + chapter + <span class="string">'/'</span> + image;</span><br><span class="line">            &#125;);</span><br><span class="line">            res.jsonp(&#123;<span class="string">'img'</span>: urls, <span class="string">'code'</span>: <span class="number">0</span>&#125;);</span><br><span class="line">        &#125;);</span><br><span class="line">    &#125;);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>
<p>这是目前最复杂的函数了，先检查参数，然后判断漫画是否存在，再判断章节是否存在，列出章节文件夹里面的图片名，并按数字的顺序排序。最终构造成URL，返回给用户。</p>
<h4 id="静态资源">4) 静态资源</h4>
<p>public文件夹中的资源都是静态资源，用户可以通过URL访问。在这里Nodejs也是支持的：</p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line">app.use(<span class="string">'/'</span>, express.static(<span class="string">'public'</span>));</span><br></pre></td></tr></table></figure>
<p>不过Nodejs本身并不适这种静态资源的工作，所以如果是生产环境中，建议大家还是使用Nginx等工具，让Nodejs安心的处理业务逻辑吧。</p>
<h4 id="启动服务">5) 启动服务</h4>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> server = app.listen(PORT, <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="built_in">console</span>.log(<span class="string">"应用实例，访问地址为 http://%s:%s"</span>, HOST, PORT);</span><br><span class="line">&#125;);</span><br></pre></td></tr></table></figure>
<h2 id="三前端">三、前端</h2>
<h3 id="ajax">1. Ajax</h3>
<p>前端使用Ajax就可以很容易完成，相信即使是前端小白也能实现，而且还比小喵做的好看（无奈脸）。小喵使用了<span class="exturl" data-url="aHR0cDovL2pxdWVyeS5jb20=" title="http://jquery.com">JQuery<i class="fa fa-external-link"></i></span> 来处理Ajax的内容，界面库使用了 <span class="exturl" data-url="aHR0cHM6Ly9tZXRyb3VpLm9yZy51YQ==" title="https://metroui.org.ua">Metro<i class="fa fa-external-link"></i></span>，然而即使这样也没有提高网站的颜值。</p>
<p>源码可以从github上下载到，所以小喵就不重点介绍前端了。</p>
<h3 id="懒加载">2. 懒加载</h3>
<p>有一点需要注意，在漫画图片的页面中，通常会出现大量的图片，如果只是简单的使用 <code>img</code> 标签的话，可能会导致浏览器同时加载所有的图片，如果网速不好的话，我们的体验也会相当的差（局域网请无视）。所以我们使用一种 <strong>懒加载</strong> 的策略，只有可见的图片才会加载。然后小喵就从github上找相关的插件，然后发现了一个使用比较方便的代码，还有详细的原理介绍，感兴趣的话大家可以看一下。</p>
<p>图片懒加载插件实战：<span class="exturl" data-url="aHR0cDovL3d3dy5jbmJsb2dzLmNvbS9iZWlkYW4vcC81NjQ4MjQwLmh0bWw=" title="http://www.cnblogs.com/beidan/p/5648240.html">http://www.cnblogs.com/beidan/p/5648240.html<i class="fa fa-external-link"></i></span></p>
<p>插件的github：<span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL2JlaWRhbi9sYXplTG9hZEltZw==" title="https://github.com/beidan/lazeLoadImg">https://github.com/beidan/lazeLoadImg<i class="fa fa-external-link"></i></span></p>
<h2 id="四写在后面">四、写在后面</h2>
<p>至此，我们就搭建好了一个可以随时玩耍的个人漫画网站了。喵粉们感兴趣可以star、fork这个项目，如果喜欢开发的话，能帮忙一起提高网站的颜值就更好了 O(∩_∩)O哈！</p>
<p>项目地址：<span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL21pYW9lcmR1by9jYXJ0b29uLWNhdC1zZXJ2ZXI=" title="https://github.com/miaoerduo/cartoon-cat-server">https://github.com/miaoerduo/cartoon-cat-server<i class="fa fa-external-link"></i></span> ，欢迎大家随时star、fork和指教。</p>
<p>PS. 请搭配漫画喵爬虫版一起食用：<span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL21pYW9lcmR1by9jYXJ0b29uLWNhdA==" title="https://github.com/miaoerduo/cartoon-cat">https://github.com/miaoerduo/cartoon-cat<i class="fa fa-external-link"></i></span></p>
<p>使用的话，按如下的流程：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git <span class="built_in">clone</span> git@github.com:miaoerduo/cartoon-cat-server.git</span><br><span class="line"><span class="built_in">cd</span> cartoon-cat-server</span><br><span class="line">npm install</span><br><span class="line">node main.js</span><br></pre></td></tr></table></figure>
<p>另外，为了避免程序突然崩掉，建议大家使用 <code>forever</code> 这个工具。上面的流程的最后一句 <code>node main.js</code> 就可以改成下面的。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">npm install forever -g</span><br><span class="line">forever start main.js</span><br></pre></td></tr></table></figure>
<p>这样，我们的程序就更健壮了。 最后，小喵再说一句，这个项目有很多的缺陷，比如直接访问文件，Nodejs直接管理静态文件，需要提供奇怪的参数，没有验证用户等等。因此不适合真的生成中的使用。不过，自娱自乐应该是足够了。小喵自己是放在宿舍的树莓派里，睡前躺床上看会儿漫画。</p>
<p>希望小喵能和大家一起学习和进步~~</p>
<p><strong>转载请注明出处~</strong></p>
]]></content>
      <categories>
        <category>Miscellanea</category>
        <category>Nodejs</category>
      </categories>
      <tags>
        <tag>Interest</tag>
      </tags>
  </entry>
  <entry>
    <title>爬虫-漫画喵的100行逆袭</title>
    <url>/2017/02/19/cartoon-cat-client/</url>
    <content><![CDATA[<blockquote>
<p>小喵的唠叨话：这次的博客，讲的是使用python编写一个爬虫工具。为什么要写这个爬虫呢？原因是小喵在看完《极黑的布伦希尔特》这个动画之后，又想看看漫画，结果发现各大APP都没有资源，最终好不容易找到一个网站可以看，但是由于网速太渣，看起来额外的费劲。这时候如果能提前下载下来就好了。</p>
</blockquote>
<p>先上项目地址（github）：<span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL21pYW9lcmR1by9jYXJ0b29uLWNhdA==" title="https://github.com/miaoerduo/cartoon-cat">https://github.com/miaoerduo/cartoon-cat<i class="fa fa-external-link"></i></span> 。欢迎大家随时<strong>fork</strong>、<strong>star</strong>和指教。</p>
<a id="more"></a>
<p>原因就是这样，作为技术喵，任何问题都不能阻碍一颗爱漫画的心。所以问题就来了，挖掘机技修哪家强？ 在bing上搜索Python、爬虫框架。找到大家常用的框架。</p>
<p>Scrapy似乎是个很不错的选择。至于相对于其他框架的优点，小喵没有细查，至少这个框架是之前听过的。但是在实现的时候发现有一些问题，scrapy不能直接抓取动态的页面。小喵需要抓取的网站的漫画都是使用Ajax生成的。需要自己分析各种数据，这个有点麻烦。</p>
<p>那么有没有可以渲染页面的工具呢？像浏览器一样的？有。</p>
<p>这里介绍两个工具：</p>
<ul>
<li><span class="exturl" data-url="aHR0cDovL3BoYW50b21qcy5vcmc=" title="http://phantomjs.org">PhantomJs<i class="fa fa-external-link"></i></span>，可以理解是一个浏览器。不过它没有界面，我们可以通过js的代码模拟用户的行为。这就要求了解它的api并有js基础了。</li>
<li><span class="exturl" data-url="aHR0cHM6Ly9zZWxlbml1bS1weXRob24ucmVhZHRoZWRvY3MuaW8=" title="https://selenium-python.readthedocs.io">Selenium<i class="fa fa-external-link"></i></span>，这是个浏览器自动化测试框架。它依赖于浏览器（这个浏览器也可以是PhantomJs），通过Selenium可以模拟用户的行为。而且有Python接口，所以相对简单一些。</li>
</ul>
<p>我们这个爬虫使用 <code>selenium + phantomjs</code> 来实现。哟，这个爬虫软件应该有个响当当的名字。。。就叫 <strong>漫画喵</strong> 吧，英文名 <strong>Cartoon Cat</strong>。</p>
<p>下面我们一点点的介绍这个爬虫的实现过程吧。</p>
<h2 id="一初生-环境搭建">一、初生-环境搭建</h2>
<p>小喵这里选用 <code>Python</code> 作为开发语言，框架是 <code>selenium</code>。原因是 <code>python</code> 经常用来写爬虫，<code>selenium</code> 可以用来模拟用户行为，<code>PhantomJs</code> 是可选的，不过小喵最终会在一个服务器上运行，所以也是需要的。</p>
<p>为了不影响本机上的 <code>python</code>，我们还需要使用 <code>virtualenv</code> 来创建一个独立的 <code>python</code> 环境。具体步骤如下：</p>
<h3 id="安装virtualenv">1、安装virtualenv</h3>
<p><code>virtualenv</code> 是一个常用的用来创建 <code>python</code> 环境的工具。小喵用这个有两个原因，一是为了不污染本机的环境，二是在本机直接安装库的时候出了一个权限的问题。<code>virtualenv</code> 的安装十分简单，使用 <code>pip</code> 工具就可以安装。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">pip install virtualenv</span><br></pre></td></tr></table></figure>
<p>待程序执行结束，你就会开心的发现自己已经有了 <code>virtualenv</code> 这个工具了。</p>
<h3 id="创建python环境">2、创建python环境</h3>
<p><code>virtualenv</code> 的使用非常的方便。建立新的运行环境：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">virtualenv &lt;env-name&gt;</span><br></pre></td></tr></table></figure>
<p>进入相应的独立环境：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">source</span> &lt;env-path&gt;/bin/activate</span><br></pre></td></tr></table></figure>
<p>执行完第一个指令后，就会创建成功一个 <code>python</code> 环境，执行第二个指令后，就会发现命令行的起始位置有变化。这时候 <code>python</code>、<code>pip</code> 等工具就变成使用这个新环境的了，当然也可以使用 <code>which python</code> 来查看。</p>
<h3 id="安装selenium">3、安装selenium</h3>
<p>进入新环境后，pip安装的依赖库都会在新环境中安装，不会影响主机自身的 <code>python</code>。使用 <code>pip</code> 安装 <code>selenium</code>：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">pip install selenium</span><br></pre></td></tr></table></figure>
<p>至此，我们的基本环境就搭建完了。</p>
<h3 id="安装phantomjs">4、安装PhantomJs</h3>
<p>这个只在从官网上下载就可以：<span class="exturl" data-url="aHR0cDovL3BoYW50b21qcy5vcmcvZG93bmxvYWQuaHRtbA==" title="http://phantomjs.org/download.html">http://phantomjs.org/download.html<i class="fa fa-external-link"></i></span> 小喵的本地实验环境是Mac，所以下载了Mac版本。解压之后就可以使用。</p>
<h2 id="二寻觅-搜资源">二、寻觅-搜资源</h2>
<p>小喵想看的这个漫画貌似各大网站都没有资源，在费了九牛二虎之力后，终于找到了一个网站！<span class="exturl" data-url="aHR0cDovL3d3dy50YXpoZS5jb20vbWgvOTE3MC8=" title="http://www.tazhe.com/mh/9170/">http://www.tazhe.com/mh/9170/<i class="fa fa-external-link"></i></span>。每个网站的结构都不相同，因此都需要定制一套爬虫程序。本文的爬虫就只能针对这个漫画网站使用，喵粉们需要爬其他网站的话，需要自己做相应的修改。</p>
<h2 id="三分析-资源解析">三、分析-资源解析</h2>
<p>这里需要解析两个页面，一个是漫画的首页，比如前面的：<span class="exturl" data-url="aHR0cDovL3d3dy50YXpoZS5jb20vbWgvOTE3MC8=" title="http://www.tazhe.com/mh/9170/">http://www.tazhe.com/mh/9170/<i class="fa fa-external-link"></i></span> 另一个就是具体章节的页面。</p>
<h3 id="首页">1，首页</h3>
<p>为了减小图片的大小，小喵把窗口做了缩放。首页大致是这个样子。</p>
<img data-src="http://cdn.miaoerduo.com/images/tazhe-index.png" alt="首页" />
<center>
图1 漫画首页
</center>
<p>各类信息十分的明了。我们关注的就是下面的漫画列表。通过Chrome强大的<strong>审查元素</strong>的功能，我们立刻就能定位到章节的位置。（<strong>对着感兴趣的位置-&gt;右键-&gt;审查</strong> 就能找到）</p>
<p><img data-src="http://cdn.miaoerduo.com/images/tazhe-inspect-element.png" alt="审查" /></p>
<center>
图2 章节的节点
</center>
<p>可以看到，章节所在的区域的 <code>id</code> 是 <code>play_0</code>，学过前端的童鞋都应该知道，一个页面中 <code>id</code> 通常唯一标示一个节点。因此如果我们能够获取这个页面的话，查找 <code>id</code> 为 <code>play_0</code> 的节点就能一下子缩小搜索范围。 而每个章节的信息都是一个 <code>a</code> 标签，标签的 <code>href</code> 是对应章节的具体网址，标签的文本部分是章节名。这样相对关系就得出了：<code>div#play_0 &gt; ul &gt; li &gt; a</code>。首页的分析就到此结束。</p>
<h3 id="章节页面">2、章节页面</h3>
<p>我们随意打开一个具体章节的页面。比如：<span class="exturl" data-url="aHR0cDovL3d3dy50YXpoZS5jb20vbWgvOTE3MC8xMTg3MDg2Lmh0bWw=" title="http://www.tazhe.com/mh/9170/1187086.html">http://www.tazhe.com/mh/9170/1187086.html<i class="fa fa-external-link"></i></span></p>
<p>映入眼帘的是一个很干净的页面（简直是漫画界的清流，好多漫画网站上全部是广告）。我们把<strong>鼠标放在图片这个区域-&gt;右键-&gt;审查</strong>。咦，我们的右键怎么按不了？</p>
<p>其实呢，这个现象在小说网站上遇到的机会会更多。当我们看到比较优美的文字或是炫酷的图片，都会下意识的选中-&gt;右键-&gt;保存。而很多时候，这些资源都是有版权的。并不应该随意的传播（狠狠的打了自己的脸/(ㄒoㄒ)/~~）。因此限制鼠标右键会是一个很简单却有效的办法。那么我们如何绕过这个陷阱呢？</p>
<p>很简单，我们不用右键即可。打开浏览器的开发者工具选项，找到 <code>elements</code> 这个选项。可以看到一个复杂的结构（其实和上面审查元素之后的结果一样）。之后不断的选中标签，当标签被选中时，左侧页面中对应的位置会有蓝色。多试几次，最终就能找到对应的位置。</p>
<p><img data-src="http://cdn.miaoerduo.com/images/tazhe-find-element.png" alt="tazhe-find-element" /></p>
<center>
图3 漫画图片
</center>
<p>这是一个 <code>img</code> 标签，对应的 <code>id</code> 是 <code>qTcms_pic</code>。这样找到这个 <code>id</code>，就能找到这个 <code>img</code> 标签，根据 <code>src</code> 就能找到图片的具体URI地址。</p>
<p>接下来是找到下一张图片的地址。这时候需要查看下一页这个按钮的内容。用相同的方法，很容易定位成功。</p>
<p><img data-src="http://cdn.miaoerduo.com/images/tazhe-next-uri.png" alt="next" /></p>
<center>
图4 下一页
</center>
<p>小喵本来是用scrapy来做爬虫的，看到这里的时候就果断放弃了。我们分析一下，选中的<code>a</code>标签的代码如下：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><span class="line"><span class="tag">&lt;<span class="name">a</span> <span class="attr">class</span>=<span class="string">"next"</span> <span class="attr">href</span>=<span class="string">"javascript:a_f_qTcms_Pic_nextUrl_Href();"</span> <span class="attr">title</span>=<span class="string">"下一页"</span>&gt;</span><span class="tag">&lt;<span class="name">span</span>&gt;</span>下一页<span class="tag">&lt;/<span class="name">span</span>&gt;</span><span class="tag">&lt;/<span class="name">a</span>&gt;</span></span><br></pre></td></tr></table></figure>
<p>比较简单的网站，“下一页”可以用真的 <code>a</code> 标签和 <code>href</code> 属性来做。这样的好处是实现比较简单，坏处是一旦得到网页源码就能很容易的解析。而像 <code>scrapy</code> 这样的爬虫工具只能抓取静态的代码（动态的需要自己分析ajax，有点麻烦）。而显然这里的页面是动态的，使用了ajax来实现。所以光是得到网页源码并不能真的得到图片，而是必须让其中的js代码运行才可以。所以我们才需要使用浏览器或者PhantomJs这样的可以执行js代码的工具。</p>
<p>上面的 <code>a</code> 标签的代码告诉了我们很多信息。首先是告诉了我们，这个节点的位置，通过 <code>next</code> 这个类名可以方便的找到该节点（其实有两个类名为 <code>next</code> 的按钮，另一个在下面，但是功能都一样）。其次，当这个按钮被点击时会调用：<code>a_f_qTcms_Pic_nextUrl_Href()</code> 这个js函数。难道我们需要再研究这个函数？</p>
<p>不用。因为 <code>PhantomJs</code> 的角色就是一个浏览器。我们只需要向真正的用户一样点击一下这个 <code>next</code> 按钮，就会进入下一个页面。<span style="color: green">/* 感受到这个工具的强大了吗？*/</span></p>
<h3 id="判断章节的末尾">3、判断章节的末尾</h3>
<p>最后一个问题就是，如何判断这个章节结束了？</p>
<p>我们跳到章节的最后一页，然后再次点击“下一页”，这时候会出现一个弹窗。</p>
<p><img data-src="http://cdn.miaoerduo.com/images/tazhe-the-last-page.png" alt="tazhe-the-last-page" /></p>
<center>
图5 最后一页
</center>
<p>多次试验之后，我们会发现，只有在最后一页的时候才会弹出这个弹窗，这样的话，我们每抓取完一页，点击一次“下一页”，判断有无弹窗就知道是不是最后一页了。在右侧的开发者工具中我们能够看到，这个弹窗是一个 <code>id</code> 为 <code>msgDiv</code> 的 <code>div</code>（而且它的出现和消失是通过增减节点来实现的，另一种实现方法是将 <code>display</code> 设成 <code>none</code> 和 <code>block</code>，这种情况可以根据 <code>display</code> 的属性来判断）。所以我们判断这个节点存不存在就行了。</p>
<p>至此，两种页面的解析都完成了。下一步就开始我们的代码实现吧。</p>
<h2 id="四逆袭代码实现">四、逆袭——代码实现</h2>
<h3 id="selenium的简单用法">1，selenium的简单用法</h3>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> selenium <span class="keyword">import</span> webdriver</span><br><span class="line"></span><br><span class="line">browser = webdriver.Firefox()</span><br><span class="line"><span class="comment"># browser = webdriver.Safari()</span></span><br><span class="line"><span class="comment"># browser = webdriver.Chrome()</span></span><br><span class="line"><span class="comment"># browser = webdriver.Ie()</span></span><br><span class="line"><span class="comment"># browser = webdriver.PhantomJs()</span></span><br><span class="line"></span><br><span class="line">browser.get(<span class="string">'http://baidu.com'</span>)</span><br><span class="line"></span><br><span class="line"><span class="keyword">print</span> browser.title</span><br><span class="line"><span class="comment"># do anything you want</span></span><br></pre></td></tr></table></figure>
<p>上面是一个简单的例子，第一步import依赖的库。</p>
<p>第二步，获得一个浏览器实例。selenium支持多种浏览器。使用firefox之外的浏览器都需要下载驱动（selenium本身自带了firefox的驱动）。驱动下载地址：<span class="exturl" data-url="aHR0cHM6Ly9weXBpLnB5dGhvbi5vcmcvcHlwaS9zZWxlbml1bQ==" title="https://pypi.python.org/pypi/selenium">https://pypi.python.org/pypi/selenium<i class="fa fa-external-link"></i></span> 。驱动下载完之后将它的路径加入到 <code>PATH</code> 里，确保驱动程序能够被访问到。或者显式的把驱动程序的地址当参数传入。像下面一样调用：</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line">browser = webdriver.PhantomJs(<span class="string">'path/to/phantomjs'</span>)</span><br></pre></td></tr></table></figure>
<p>第三步，用 <code>get</code> 的方式打开网页。</p>
<p>最后，通过 <code>browser</code> 对象来解析和处理页面。</p>
<h3 id="获取章节的链接信息">2，获取章节的链接信息</h3>
<p>在上面的解析页面的时候，我们知道了章节信息的位置：<code>div#play_0 &gt; ul &gt; li &gt; a</code>。这样就可以解析出章节信息。browser支持一大堆的选择器。大大简化我们查找节点的工作。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">from selenium import webdriver</span><br><span class="line"></span><br><span class="line">if __name__ &#x3D;&#x3D; &quot;__main__&quot;:</span><br><span class="line">    driver &#x3D; &quot;path&#x2F;to&#x2F;driver&quot; # 驱动地址</span><br><span class="line">    browser &#x3D; webdriver.PhantomJS(driver) # 浏览器实例</span><br><span class="line"></span><br><span class="line">    main_page &#x3D; &quot;http:&#x2F;&#x2F;www.tazhe.com&#x2F;mh&#x2F;9170&#x2F;&quot;</span><br><span class="line">    browser.get(main_page) # 加载页面</span><br><span class="line"></span><br><span class="line">    # 解析出章节的元素节点</span><br><span class="line">    chapter_elem_list &#x3D; browser.find_elements_by_css_selector(&#39;#play_0 ul li a&#39;) # 通过css选择器找出章节节点</span><br><span class="line">    chapter_elem_list.reverse()  # 原本的章节是倒叙的</span><br><span class="line"></span><br><span class="line">    chapter_list &#x3D; []</span><br><span class="line">    for chapter_elem in chapter_elem_list:</span><br><span class="line">        # 元素的text和href属性分别就是章节的名称和地址</span><br><span class="line">        chapter_list.append((chapter_elem.text, chapter_elem.get_attribute(&#39;href&#39;)))</span><br><span class="line"></span><br><span class="line">    # chapter_list 就是章节的信息</span><br></pre></td></tr></table></figure>
<h3 id="给定一个章节的地址章节中的图片">3，给定一个章节的地址，章节中的图片</h3>
<p>这一步涉及到节点的获取、模拟鼠标的点击以及资源的下载。<code>selenium</code> 的点击实现特别的人性化。只需要获取节点然后调用 <code>click()</code> 方法就搞定。资源的下载网上有许多教程，主要有两个方法，通过模拟 <strong>右键另存为</strong>，和 <strong>获取url用其他工具下载</strong>。考虑到这里的右键不一定可用，而且操作有一点点复杂。小喵选用了第二种方案。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> selenium <span class="keyword">import</span> webdriver</span><br><span class="line"><span class="keyword">from</span> selenium.common.exceptions <span class="keyword">import</span> NoSuchElementException</span><br><span class="line"><span class="keyword">import</span> os</span><br><span class="line"><span class="keyword">from</span> os <span class="keyword">import</span> path <span class="keyword">as</span> osp</span><br><span class="line"><span class="keyword">import</span> urllib</span><br><span class="line"></span><br><span class="line"><span class="comment"># 一个简单的下载器</span></span><br><span class="line">download(url, save_path):</span><br><span class="line">    <span class="keyword">try</span>:</span><br><span class="line">        <span class="keyword">with</span> open(save_path, <span class="string">'wb'</span>) <span class="keyword">as</span> fp:</span><br><span class="line">            fp.write(urllib.urlopen(url).read())</span><br><span class="line">    <span class="keyword">except</span> Exception, et:</span><br><span class="line">        print(et)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> __name__ == <span class="string">"__main__"</span>:</span><br><span class="line">    </span><br><span class="line">    driver = <span class="string">"path/to/driver"</span> <span class="comment"># 驱动地址</span></span><br><span class="line">    browser = webdriver.PhantomJS(driver) <span class="comment"># 浏览器实例</span></span><br><span class="line"></span><br><span class="line">    chapter_url = <span class="string">"http://www.tazhe.com/mh/9170/1187061.html"</span></span><br><span class="line">    save_folder = <span class="string">"./download"</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> <span class="keyword">not</span> osp.exists(save_folder):</span><br><span class="line">        os.mkdir(save_folder)</span><br><span class="line"></span><br><span class="line">    image_idx = <span class="number">1</span></span><br><span class="line"></span><br><span class="line">    browser.get(chapter_url) <span class="comment"># 加载第一个页面</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">        <span class="comment"># 根据前文的分析，找到图片的URI地址</span></span><br><span class="line">        image_url = browser.find_element_by_css_selector(<span class="string">'#qTcms_pic'</span>).get_attribute(<span class="string">'src'</span>)</span><br><span class="line">        save_image_name = osp.join(save_folder, (<span class="string">'%05d'</span> % image_idx) + <span class="string">'.'</span> + osp.basename(image_url).split(<span class="string">'.'</span>)[<span class="number">-1</span>])</span><br><span class="line">        download(image_url, save_image_name) <span class="comment"># 下载图片</span></span><br><span class="line"></span><br><span class="line">        <span class="comment"># 通过模拟点击加载下一页，注意如果是最后一页，会出现弹窗提示</span></span><br><span class="line">        browser.find_element_by_css_selector(<span class="string">'a.next'</span>).click()</span><br><span class="line">        <span class="keyword">try</span>:</span><br><span class="line">            <span class="comment"># 找寻弹窗，如果弹窗存在，说明这个章节下载完毕，这个大循环也就结束了</span></span><br><span class="line">            browser.find_element_by_css_selector(<span class="string">'#bgDiv'</span>)</span><br><span class="line">            <span class="keyword">break</span></span><br><span class="line">        <span class="keyword">except</span> NoSuchElementException:</span><br><span class="line">            <span class="comment"># 没有结束弹窗，继续下载</span></span><br><span class="line">            image_idx += <span class="number">1</span></span><br></pre></td></tr></table></figure>
<h2 id="五终焉-写在后面">五、终焉-写在后面</h2>
<p>至此，漫画喵的设计思路和主要的代码实现都介绍完了。上面的代码只是用来示意，小喵自己下载漫画用的代码是另一套。</p>
<p>github的地址是：<span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL21pYW9lcmR1by9jYXJ0b29uLWNhdA==" title="https://github.com/miaoerduo/cartoon-cat">https://github.com/miaoerduo/cartoon-cat<i class="fa fa-external-link"></i></span> 。项目只有100多行。不过也用了小喵不少的一段时间。</p>
<p>博客写完了~ 小喵的漫画也下完了~</p>
<p><img data-src="http://cdn.miaoerduo.com/images/tazhe-downloaded-content.png" alt="tazhe-downloaded-content" /></p>
<center>
图6 下载好的漫画
</center>
]]></content>
      <categories>
        <category>Python</category>
      </categories>
  </entry>
  <entry>
    <title>C++ Lambda表达式</title>
    <url>/2017/02/03/c-lambda-tutorial/</url>
    <content><![CDATA[<blockquote>
<p>小喵的唠叨话: 寒假之后，小喵在家里无所事事，最近用C++写代码的时候，用到了std::sort这个函数，每次用这个函数，小喵似乎都得查一下lambda表达式的写法。正好最近很闲，不如总结一下。 在Bing上搜索_C++ lambda_，第一条记录就是MSDN上的C++ lambda的介绍。本文也是基于这篇文章来写的。 那么接下来，我们分几个部分来介绍。</p>
</blockquote>
<a id="more"></a>
<h2 id="一什么是lambda表达式">一、什么是Lambda表达式</h2>
<p>MSDN上对lambda表达式的解释：</p>
<blockquote>
<p>在 C++ 11 中，lambda 表达式（通常称为 "lambda"）是一种在被调用的位置或作为参数传递给函数的位置定义匿名函数对象的简便方法。 Lambda 通常用于封装传递给算法或异步方法的少量代码行。<span class="exturl" data-url="aHR0cHM6Ly9tc2RuLm1pY3Jvc29mdC5jb20vemgtY24vbGlicmFyeS9kZDI5MzYwOC5hc3B4" title="https://msdn.microsoft.com/zh-cn/library/dd293608.aspx">[1]<i class="fa fa-external-link"></i></span></p>
</blockquote>
<p>看了这个解释，相信大家已经理解lambda表达式是什么。简而言之，lambda表达式就是一种定义函数的简单的方法。</p>
<p>举一个简单的例子：求一个数的阶乘。这是一般的函数的写法：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 这里要求n&gt;=0，同时n的取值不能太大，会溢出</span></span><br><span class="line"><span class="comment">// 为了方便，这里并没有处理上面说到的问题</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">factorial</span><span class="params">(<span class="keyword">int</span> n)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">int</span> fact = <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt;= n; ++ i) fact *= i;</span><br><span class="line">    <span class="keyword">return</span> fact;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>Lambda表达式的写法：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">auto</span> factorial = [](<span class="keyword">int</span> n) &#123;</span><br><span class="line">    <span class="keyword">int</span> fact = <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt;= n; ++ i) fact *= i;</span><br><span class="line">    <span class="keyword">return</span> fact;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>乍一看，这两种定义方式十分的相似。但其实这是两种完全不同的方式，前一种是函数定义式，而后一种是一个表达式。<code>factorial</code>是变量名，等于号后面的是值，也就是一个<code>lambda表达式</code>，本质上是一个匿名的函数。最终<code>factorial</code>就是一个函数。</p>
<p>很多时候，我们只是直接书写<code>lambda表达式</code>，而不需要给他一个名字。比如排序的时候，sort可以接受一个自定义的比较函数，这时候直接书写lambda表达式即可。</p>
<h2 id="二lambda表达式的作用">二、Lambda表达式的作用</h2>
<p>由于lambda本身其实也就是一种函数的定义方式。因此它的主要作用还是和一般函数一样。但是lambda表达式相对于一般函数，又有一些功能之外的作用。参考了知乎上的一些回答<span class="exturl" data-url="aHR0cHM6Ly93d3cuemhpaHUuY29tL3F1ZXN0aW9uLzIwMTI1MjU2" title="https://www.zhihu.com/question/20125256">[2]<i class="fa fa-external-link"></i></span>，小喵也进行了总结。 1) 可以用表达式来定义函数，这样使得函数的定义和调用在一起，语意和逻辑上更为紧凑。同时，对于只是用一次的短小的函数，直接调用匿名的lambda表达式是最好的选择，这样就不需要给每个函数起名字了。<span style="color: green">/* 起名字一直是一个很令人头疼的问题 */</span> 2) 闭包(Closure)。这个小喵的写javascript的时候时常会用到。闭包本质上就是能够访问上下文环境中变量的代码块。</p>
<p>这里我们简单的举个例子，还是之前的求阶乘的问题，现在我们有些提高需求。 现在需要完成下面的三种阶乘的运算：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">n! &#x3D; n * (n - 1) * (n - 2) * ...</span><br><span class="line">n!! &#x3D; n * (n - 2) * (n - 4) * ...</span><br><span class="line">n!!! &#x3D; n * (n - 3) * (n - 6) * ...</span><br></pre></td></tr></table></figure>
<p>要求编写3个函数，分别完成上述3种计算。 使用一般的方式写很容易实现，我们这里直接使用lambda表达式来实现：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;functional&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="built_in">std</span>::function&lt;<span class="keyword">int</span>(<span class="keyword">int</span>)&gt; getFactorialFunc(<span class="keyword">int</span> n) &#123;</span><br><span class="line">    <span class="keyword">return</span> [n](<span class="keyword">int</span> x) &#123;</span><br><span class="line">        <span class="keyword">int</span> fact = <span class="number">1</span>;</span><br><span class="line">        <span class="keyword">for</span> (; x &gt;= <span class="number">1</span>; x -= n) fact *= x;</span><br><span class="line">        <span class="keyword">return</span> fact;</span><br><span class="line">    &#125;;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 构造要求的三个函数</span></span><br><span class="line">    <span class="keyword">auto</span> factorial1 = getFactorialFunc(<span class="number">1</span>);</span><br><span class="line">    <span class="keyword">auto</span> factorial2 = getFactorialFunc(<span class="number">2</span>);</span><br><span class="line">    <span class="keyword">auto</span> factorial3 = getFactorialFunc(<span class="number">3</span>);</span><br><span class="line">    <span class="comment">// 调用</span></span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; factorial1(<span class="number">10</span>) &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; factorial2(<span class="number">10</span>) &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; factorial3(<span class="number">10</span>) &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>编译的时候要注意，lambda表达式是C++11开始支持的，所以需要指定一下C++的版本。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">g++ factorial_lambda.cpp -o factorial_lambda.out --std=c++11</span><br></pre></td></tr></table></figure>
<p>运行之后的结果为：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">.&#x2F;factorial_lambda.out</span><br><span class="line">3628800</span><br><span class="line">3840</span><br><span class="line">280</span><br></pre></td></tr></table></figure>
<p>这里作为返回值的lambda表达式，可以访问先前传入的参数，这也就是闭包。具体的语法，我们后面会讲到。</p>
<ol start="3" type="1">
<li>柯里化(Currying)。这部分小喵也是第一次接触，维基百科有如下解释：</li>
</ol>
<blockquote>
<p>在计算机科学中，柯里化（英语：Currying），又译为卡瑞化或加里化，是把接受多个参数的函数变换成接受一个单一参数（最初函数的第一个参数）的函数，并且返回接受余下的参数而且返回结果的新函数的技术。<span class="exturl" data-url="aHR0cHM6Ly96aC53aWtpcGVkaWEub3JnL3dpa2kv5p+v6YeM5YyW" title="https://zh.wikipedia.org/wiki/柯里化">[3]<i class="fa fa-external-link"></i></span></p>
</blockquote>
<p>下面给出一个例子（也是实现之前的阶乘）：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;functional&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="comment">// 两个参数的阶乘</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">factorial</span><span class="params">(<span class="keyword">int</span> n, <span class="keyword">int</span> step)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">int</span> r = <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">for</span> (; n &gt;= <span class="number">1</span>; n -= step) &#123;</span><br><span class="line">        r *= n;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> r;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// curring化的阶乘</span></span><br><span class="line"><span class="built_in">std</span>::function&lt;<span class="keyword">int</span>(<span class="keyword">int</span>)&gt; currying_factorial(<span class="keyword">int</span> step) &#123;</span><br><span class="line">    <span class="keyword">return</span> [step](<span class="keyword">int</span> n) &#123;</span><br><span class="line">        <span class="keyword">return</span> factorial(n, step);</span><br><span class="line">    &#125;;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 调用普通函数</span></span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; factorial(<span class="number">10</span>, <span class="number">1</span>) &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; factorial(<span class="number">10</span>, <span class="number">2</span>) &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; factorial(<span class="number">10</span>, <span class="number">3</span>) &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 调用currying函数</span></span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; currying_factorial(<span class="number">1</span>)(<span class="number">10</span>) &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; currying_factorial(<span class="number">2</span>)(<span class="number">10</span>) &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; currying_factorial(<span class="number">3</span>)(<span class="number">10</span>) &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<ol start="4" type="1">
<li>lambda表达式整体可以被当做函数的参数或者返回值。 闭包和currying的例子就是将整个lambda表达式作为返回值。现在再举一个作为参数的例子：</li>
</ol>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;functional&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">operate</span><span class="params">(<span class="keyword">int</span> x, <span class="keyword">int</span> y, <span class="keyword">const</span> <span class="built_in">std</span>::function&lt;<span class="keyword">int</span>(<span class="keyword">int</span>, <span class="keyword">int</span>)&gt; &amp;op)</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> op(x, y);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">auto</span> add = [](<span class="keyword">int</span> x, <span class="keyword">int</span> y) &#123; <span class="keyword">return</span> x + y;&#125;;</span><br><span class="line">    <span class="keyword">auto</span> mul = [](<span class="keyword">int</span> x, <span class="keyword">int</span> y) &#123; <span class="keyword">return</span> x - y;&#125;;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; operate(<span class="number">10</span>, <span class="number">5</span>, add) &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; operate(<span class="number">10</span>, <span class="number">5</span>, mul) &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>运行的结果：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">15</span><br><span class="line">5</span><br></pre></td></tr></table></figure>
<p>其实函数也可以当参数传入的（函数指针），但是lambda表达式要更为直观和灵活一些。谁能一眼看出<code>int (*func(int))(int)</code>究竟是什么意思呢（这是一个函数的定义，输入的参数是int，返回值是一个函数指针，函数指针对应的函数的输入和输出类型都是int）。</p>
<h2 id="三lambda表达式的语法">三、Lambda表达式的语法</h2>
<p>看到前面的lambda表达式的各种有趣的功能，现在是不是非常迫切的想尝试一把？</p>
<p>ISO C++ 标准展示了作为第三个参数传递给 <code>std::sort()</code> 函数的简单 lambda：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;algorithm&gt;  </span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;cmath&gt;  </span></span></span><br><span class="line">  </span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">abssort</span><span class="params">(<span class="keyword">float</span>* x, <span class="keyword">unsigned</span> n)</span> </span>&#123;  </span><br><span class="line">    <span class="built_in">std</span>::sort(x, x + n,  </span><br><span class="line">        <span class="comment">// Lambda expression begins  </span></span><br><span class="line">        [](<span class="keyword">float</span> a, <span class="keyword">float</span> b) &#123;</span><br><span class="line">            <span class="keyword">return</span> (<span class="built_in">std</span>::<span class="built_in">abs</span>(a) &lt; <span class="built_in">std</span>::<span class="built_in">abs</span>(b));  </span><br><span class="line">        &#125; <span class="comment">// end of lambda expression  </span></span><br><span class="line">    );  </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>lambda表达式的组成部分见下图：</p>
<p><img data-src="http://cdn.miaoerduo.com/images/lambdaexpsyntax.png" alt="lambda expression syntax" /></p>
<ol type="1">
<li>Capture 子句（在 C++ 规范中也称为 lambda 引导。）</li>
<li>参数列表（可选）。 （也称为 lambda 声明符)</li>
<li>可变规范（可选）。</li>
<li>异常规范（可选）。</li>
<li>尾随返回类型（可选）。</li>
<li>“lambda 体”</li>
</ol>
<p>接下来我们需要学习这6个部分。</p>
<h3 id="capture-子句">1、Capture 子句</h3>
<p>我们知道，一般情况下，函数只能访问自己的参数和外部的全局变量。而lambda表达式却可以访问上下文的变量（参见闭包的例子）。那么如何指定要访问的变量，以及访问的方式（值或者引用）呢？这就是Capture 子句要解决的问题。</p>
<p><code>Lambda</code> 可在其主体中引入新的变量（用 C++14），它还可以访问（或“捕获”）周边范围内的变量。<code>Lambda</code> 以 <code>Capture</code> 子句（标准语法中的 <code>lambda</code> 引导）开头，它指定要捕获的变量以及是通过值还是引用进行捕获:</p>
<ul>
<li>有与号 (<code>&amp;</code>) 前缀的变量通过引用访问，没有该前缀的变量通过值访问。</li>
<li>空 <code>capture</code> 子句 <code>[ ]</code> 指示 lambda 表达式的主体不访问封闭范围中的变量。可以使用默认捕获模式（标准语法中的 <code>capture-default</code>）来指示如何捕获 <code>lambda</code> 中引用的任何外部变量：
<ul>
<li><code>[&amp;]</code> 表示通过引用捕获引用的所有变量</li>
<li><code>[=]</code> 表示通过值捕获它们。</li>
</ul></li>
<li>可以使用默认捕获模式，然后为特定变量显式指定相反的模式。</li>
</ul>
<p>例如，如果 <code>lambda</code> 体通过引用访问外部变量 <code>total</code> 并通过值访问外部变量 <code>factor</code>，则以下 <code>capture</code> 子句等效：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">[&amp;total, factor]</span><br><span class="line">[factor, &amp;total]</span><br><span class="line">[&amp;, factor]</span><br><span class="line">[factor, &amp;]</span><br><span class="line">[=, &amp;total]</span><br><span class="line">[&amp;total, =]</span><br></pre></td></tr></table></figure>
<p>我们之前的闭包中使用的就是通过值访问。</p>
<ul>
<li>使用 <code>capture-default</code> 时，只有 <code>lambda</code> 中提及的变量才会被捕获。</li>
<li>如果 capture 子句包含 <code>capture-default</code> <code>&amp;</code>，则该 capture 子句的 <code>identifier</code> 中没有任何 <code>capture</code> 可采用 <code>&amp; identifier</code> 形式。</li>
<li>同样，如果 capture 子句包含 <code>capture-default</code> <code>=</code>，则该 capture 子句的 <code>capture</code> 不能采用 <code>= identifier</code> 形式。</li>
<li>identifier 或 <code>this</code> 在 capture 子句中出现的次数不能超过一次。</li>
</ul>
<p>以下代码片段给出了一些示例。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">S</span> &#123;</span> <span class="function"><span class="keyword">void</span> <span class="title">f</span><span class="params">(<span class="keyword">int</span> i)</span></span>; &#125;;  </span><br><span class="line">  </span><br><span class="line"><span class="keyword">void</span> S::f(<span class="keyword">int</span> i) &#123;  </span><br><span class="line">    [&amp;, i]&#123;&#125;;    <span class="comment">// OK  </span></span><br><span class="line">    [=, &amp;i]&#123;&#125;;   <span class="comment">// OK</span></span><br><span class="line">    [&amp;, &amp;i]&#123;&#125;;   <span class="comment">// ERROR: i preceded by &amp; when &amp; is the default  </span></span><br><span class="line">    [=, <span class="keyword">this</span>]&#123;&#125;; <span class="comment">// ERROR: this when = is the default  </span></span><br><span class="line">    [i, i]&#123;&#125;;    <span class="comment">// ERROR: i repeated  </span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p><code>capture</code> 后跟省略号是包扩展，如以下可变参数模板<span class="exturl" data-url="aHR0cHM6Ly9tc2RuLm1pY3Jvc29mdC5jb20vemgtY24vbGlicmFyeS9kbjQzOTc3OS5hc3B4" title="https://msdn.microsoft.com/zh-cn/library/dn439779.aspx">[4]<i class="fa fa-external-link"></i></span>示例中所示：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span>&lt;<span class="class"><span class="keyword">class</span>... <span class="title">Args</span>&gt;  </span></span><br><span class="line"><span class="class"><span class="title">void</span> <span class="title">f</span>(<span class="title">Args</span>... <span class="title">args</span>) &#123;</span>  </span><br><span class="line">    <span class="keyword">auto</span> x = [args...] &#123; <span class="keyword">return</span> g(args...); &#125;;  </span><br><span class="line">    x();  </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>要在类方法的正文中使用 <code>lambda</code> 表达式，需要将 <code>this</code> 指针传递给 <code>Capture</code> 子句，以提供对封闭类的方法和数据成员的访问权限。</p>
<p>这里大家可能觉得有点奇怪，将this指针传给Capture子句？</p>
<p>其实我们常使用的成员函数也是用类似的方法实现的。我们知道，使用成员函数需要有一个类实例，但是调用类函数就不需要。这是因为成员函数的第一个参数是this，当然这个参数我们编写代码的时候不需要自己手动写出，而是默认的。使用像python这样的语言的时候就是需要显示的写出的。在使用类实例调用成员函数的时候，会默认将this指针传入。成员函数有这么一个参数，就可以访问类实例的各种变量和方法。而类函数是没有这个参数的，也就是没有this这个指针，因此它的调用并不需要类实例，当然也就不能访问类实例的变量。</p>
<p>在使用 capture 子句时，要记住以下几点（尤其是使用采取多线程的 lambda 时）：</p>
<ol type="1">
<li>引用捕获可用于修改外部变量，而值捕获却不能实现此操作。（mutable允许修改副本，而不能修改原始项。）</li>
<li>引用捕获会反映外部变量的更新，而值捕获却不会反映。</li>
<li>引用捕获引入生存期依赖项，而值捕获却没有生存期依赖项。当 lambda 以异步方式运行时，这一点尤其重要。 如果在异步 lambda 中通过引用捕获本地变量，该本地变量将很可能在 lambda 运行时消失，从而导致运行时访问冲突。</li>
</ol>
<p>通用捕获 (C++14) 在 C++14 中，可在 <code>Capture</code> 子句中引入并初始化新的变量，而无需使这些变量存在于 lambda 函数的封闭范围内。</p>
<p>初始化可以任何任意表达式表示；且将从该表达式生成的类型推导新变量的类型。此功能的一个好处是，在 C++14 中，可从周边范围捕获只移动的变量（例如 <code>std::unique_ptr</code>）并在 <code>lambda</code> 中使用它们。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">pNums = make_unique&lt;<span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt;&gt;(nums);  </span><br><span class="line"><span class="comment">//...  </span></span><br><span class="line"><span class="keyword">auto</span> a = [ptr = move(pNums)]()  </span><br><span class="line">&#123;  </span><br><span class="line">    <span class="comment">// use ptr  </span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<h3 id="参数列表">2、参数列表</h3>
<p>除了捕获变量，lambda 还可接受输入参数。 参数列表（在标准语法中称为 <em>lambda 声明符</em>）是可选的，它在大多数方面类似于函数的参数列表。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">auto</span> add = [] (<span class="keyword">int</span> first, <span class="keyword">int</span> second)  </span><br><span class="line">&#123;  </span><br><span class="line">    <span class="keyword">return</span> first + second;  </span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>在 C++14 中，如果参数类型是泛型，则可以使用 <code>auto</code> 关键字作为类型说明符。 这将告知编译器将函数调用运算符创建为模板。参数列表中的每个 <code>auto</code> 实例等效于一个不同的类型参数。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">auto</span> add = [] (<span class="keyword">auto</span> first, <span class="keyword">auto</span> second)  </span><br><span class="line">&#123;  </span><br><span class="line">    <span class="keyword">return</span> first + second;  </span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p><code>lambda 表达式</code>可以将另一个 <code>lambda 表达式</code>作为其参数。</p>
<p>由于参数列表是可选的，因此在不将参数传递到 lambda 表达式，并且其 <code>lambda-declarator:</code> 不包含 <code>exception-specification</code>、<code>trailing-return-type</code> 或 <code>mutable</code> 的情况下，可以省略空括号。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">[]&#123;&#125;; <span class="comment">// 这就是最简单的lambda表达式</span></span><br></pre></td></tr></table></figure>
<h3 id="可变规范">3、可变规范</h3>
<p>通常，<code>lambda</code> 的函数调用运算符为 <code>const-by-value</code>，但对 <code>mutable</code> 关键字的使用可将其取消。它不会生成可变的数据成员。利用可变规范，<code>lambda 表达式</code>的主体可以修改通过值捕获的变量。本文后面的一些示例将显示如何使用 <code>mutable</code>。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> n = <span class="number">10</span>;</span><br><span class="line">    <span class="keyword">auto</span> lambda1 = [n](<span class="keyword">int</span> x) &#123;</span><br><span class="line">        <span class="comment">/* ++ n; */</span> <span class="comment">// 这句编译会出错，错误信息如下：</span></span><br><span class="line">                    <span class="comment">// error: cannot assign to a variable captured</span></span><br><span class="line">                    <span class="comment">// by copy in a non-mutable lambda</span></span><br><span class="line">        <span class="keyword">return</span> x + n;</span><br><span class="line">    &#125;;</span><br><span class="line">    <span class="keyword">auto</span> lambda2 = [n](<span class="keyword">int</span> x) <span class="keyword">mutable</span> &#123;</span><br><span class="line">        ++ n;</span><br><span class="line">        <span class="keyword">return</span> x + n;</span><br><span class="line">    &#125;;</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; lambda1(<span class="number">5</span>) &lt;&lt; <span class="string">" "</span> &lt;&lt; n &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; lambda2(<span class="number">5</span>) &lt;&lt; <span class="string">" "</span> &lt;&lt; n &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>输出的结果是：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">15 10</span><br><span class="line">16 10</span><br></pre></td></tr></table></figure>
<p>可以看出n确实是通过值来访问，在<code>lambda1</code>中，我们运行<code>++ n</code>，在编译的时候会报错。使用<code>mutable</code>修饰之后，就可以修改参数（副本）的值。</p>
<h3 id="异常规范">4、异常规范</h3>
<p>你可以使用 <code>throw()</code> 异常规范来指示 <code>lambda 表达式</code>不会引发任何异常。与普通函数一样，如果 <code>lambda 表达式</code>声明 <span class="exturl" data-url="aHR0cHM6Ly9tc2RuLm1pY3Jvc29mdC5jb20vemgtY24vbGlicmFyeS84NGUyendoaC5hc3B4" title="https://msdn.microsoft.com/zh-cn/library/84e2zwhh.aspx">C4297<i class="fa fa-external-link"></i></span> 异常规范且 <code>lambda 体</code>引发异常，Visual C++ 编译器将生成警告 <code>throw()</code>，如下所示：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// throw\_lambda\_expression.cpp  </span></span><br><span class="line"><span class="comment">// compile with: /W4 /EHsc</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> <span class="comment">// C4297 expected  </span></span></span><br><span class="line"><span class="function"></span>&#123;  </span><br><span class="line">   []() <span class="keyword">throw</span>() &#123; <span class="keyword">throw</span> <span class="number">5</span>; &#125;();  </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>在MSDN的异常规范<span class="exturl" data-url="aHR0cHM6Ly9tc2RuLm1pY3Jvc29mdC5jb20vemgtY24vbGlicmFyeS93ZmEwZWR5cy5hc3B4" title="https://msdn.microsoft.com/zh-cn/library/wfa0edys.aspx">[5]<i class="fa fa-external-link"></i></span>中，明确指出异常规范是在 C++11 中弃用的 C++ 语言功能。因此这里不建议大家使用。</p>
<h3 id="返回类型">5、返回类型</h3>
<p>将自动推导 <code>lambda 表达式</code>的返回类型。无需使用 <code>auto</code> 关键字，除非指定<code>尾随返回类型</code>。<code>trailing-return-type</code> 类似于普通方法或函数的返回类型部分。但是，返回类型必须跟在参数列表的后面，你必须在返回类型前面包含 <code>trailing-return-type</code> 关键字 <code>-&gt;</code>。 如果 <code>lambda 体</code>仅包含一个返回语句或其表达式不返回值，则可以省略 <code>lambda 表达式</code>的<code>返回类型部分</code>。如果 <code>lambda 体</code>包含单个返回语句，编译器将从返回表达式的类型推导返回类型。否则，编译器会将返回类型推导为 <code>void</code>。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;typeinfo&gt;</span></span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">auto</span> lambda1 = [](<span class="keyword">int</span> i) &#123;<span class="keyword">return</span> i;&#125;;</span><br><span class="line">    <span class="keyword">auto</span> lambda2 = [](<span class="keyword">int</span> i) -&gt; <span class="keyword">bool</span> &#123;<span class="keyword">return</span> i;&#125;;</span><br><span class="line">    <span class="keyword">auto</span> lambda3 = [](<span class="keyword">int</span> i) -&gt; <span class="keyword">float</span> &#123;<span class="keyword">return</span> i;&#125;;</span><br><span class="line">    <span class="comment">/* auto lambda4 = []&#123; return &#123;1, 2&#125;; &#125;;*/</span> <span class="comment">// ERROR: return type is void</span></span><br><span class="line">                                              <span class="comment">// cannot deduce lambda return type</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">auto</span> x1 = lambda1(<span class="number">10</span>);</span><br><span class="line">    <span class="keyword">auto</span> x2 = lambda2(<span class="number">10</span>);</span><br><span class="line">    <span class="keyword">auto</span> x3 = lambda3(<span class="number">10</span>);</span><br><span class="line"></span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; x1 &lt;&lt; <span class="string">" "</span> &lt;&lt; <span class="keyword">typeid</span>(x1).name() &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; x2 &lt;&lt; <span class="string">" "</span> &lt;&lt; <span class="keyword">typeid</span>(x2).name() &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; x3 &lt;&lt; <span class="string">" "</span> &lt;&lt; <span class="keyword">typeid</span>(x3).name() &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>typeinfo的功能是获取一个变量的类型，由于它的实现依赖于编译器，所以在不同平台下的输出可能不完全一样。小喵这边的输出是：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">10 i</span><br><span class="line">1 b</span><br><span class="line">10 f</span><br></pre></td></tr></table></figure>
<p>可以看出，三个lambda的输出是不相同的。默认情况下，会返回一个最直接的类型。</p>
<h3 id="lambda体">6、lambda体</h3>
<p>lambda体其实和函数体几乎完全相同。 <code>lambda 表达式</code>的 <code>lambda 体</code>（标准语法中的 <code>compound-statement</code>）可包含普通方法或函数的主体可包含的任何内容。普通函数和 <code>lambda 表达式</code>的主体均可访问以下变量类型：</p>
<ul>
<li>从封闭范围捕获变量，如前所述（Capture）。</li>
<li>参数</li>
<li>本地声明变量</li>
<li>类数据成员（在类内部声明并且捕获 <code>this</code> 时）</li>
<li>具有静态存储持续时间的任何变量（例如，全局变量）</li>
</ul>
<p>这里要注意我们在<code>Capture 规范</code>中说到的值访问和引用访问的特点。</p>
<p>下面的例子都是MSDN上给出的。以下示例包含显式捕获变量 <code>n</code> 和引用隐式捕获变量 <code>m</code> 的 lambda 表达式：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// captures_lambda_expression.cpp  </span></span><br><span class="line"><span class="comment">// compile with: /W4 /EHsc</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;  </span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;  </span><br><span class="line">  </span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span>  </span></span><br><span class="line"><span class="function"></span>&#123;  </span><br><span class="line">   <span class="keyword">int</span> m = <span class="number">0</span>;  </span><br><span class="line">   <span class="keyword">int</span> n = <span class="number">0</span>;  </span><br><span class="line">   [&amp;, n] (<span class="keyword">int</span> a) <span class="keyword">mutable</span> &#123; m = ++n + a; &#125;(<span class="number">4</span>);  </span><br><span class="line">   <span class="built_in">cout</span> &lt;&lt; m &lt;&lt; <span class="built_in">endl</span> &lt;&lt; n &lt;&lt; <span class="built_in">endl</span>;  </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>输出结果：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">5</span><br><span class="line">0</span><br></pre></td></tr></table></figure>
<p>由于变量 <code>n</code> 是通过值捕获的，因此在调用 <code>lambda 表达式</code>后，变量的值仍保持 <code>0</code> 不变。  <code>mutable</code> 规范允许在 lambda 中修改 <code>n</code>。</p>
<p>尽管 <code>lambda 表达式</code>只能捕获具有自动存储持续时间的变量，但你可以在 <code>lambda 表达式</code>的主体中使用具有静态存储持续时间的变量。</p>
<p>以下示例使用 <code>generate</code> 函数和 <code>lambda 表达式</code>为 <code>vector</code> 对象中的每个元素赋值。 <code>lambda 表达式</code>将修改静态变量以生成下一个元素的值。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">fillVector</span><span class="params">(<span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt;&amp; v)</span>  </span></span><br><span class="line"><span class="function"></span>&#123;  </span><br><span class="line">    <span class="comment">// A local static variable.  </span></span><br><span class="line">    <span class="keyword">static</span> <span class="keyword">int</span> nextValue = <span class="number">1</span>;  </span><br><span class="line">  </span><br><span class="line">    <span class="comment">// The lambda expression that appears in the following call to  </span></span><br><span class="line">    <span class="comment">// the generate function modifies and uses the local static</span></span><br><span class="line">    <span class="comment">// variable nextValue.  </span></span><br><span class="line">    generate(v.begin(), v.end(), [] &#123; <span class="keyword">return</span> nextValue++; &#125;);</span><br><span class="line">    <span class="comment">//WARNING: this is not thread-safe and is shown for illustration only  </span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="四应用lambda的比较函数的编写">四、应用Lambda的比较函数的编写</h2>
<p>为什么要补充这一部分呢？因为我们在写程序的时候，往往最常用到lambda的地方就是数组的sort。 首先，我们知道std::sort默认是接受2个参数的，表示需要排序的序列的开始和结尾。对于一些复杂的数据类型，我们可以给它添加一个用来比较的函数 <code>operator &lt;</code>。但更多的是通过给sort添加第三个参数来实现。而这个参数就是一个比较器。</p>
<p>sort默认使用 <code>&lt;</code> 比较符来进行比较，排序的结果是升序。我们写的比较函数的功能就是代替 <code>&lt;</code>。记住这个特点，就不会在编写比较函数的时候理不清思路。</p>
<p>这里举一个小例子，给一组点坐标，按欧氏距离排序：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;algorithm&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;vector&gt;</span></span></span><br><span class="line"><span class="keyword">using</span> <span class="keyword">namespace</span> <span class="built_in">std</span>;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="built_in">vector</span>&lt; pair&lt;<span class="keyword">int</span>, <span class="keyword">int</span>&gt; &gt; arr;</span><br><span class="line">    arr.push_back(make_pair(<span class="number">1</span>, <span class="number">4</span>));</span><br><span class="line">    arr.push_back(make_pair(<span class="number">2</span>, <span class="number">3</span>));</span><br><span class="line">    arr.push_back(make_pair(<span class="number">5</span>, <span class="number">7</span>));</span><br><span class="line">    arr.push_back(make_pair(<span class="number">6</span>, <span class="number">2</span>));</span><br><span class="line"></span><br><span class="line">    sort(arr.begin(), arr.end(),</span><br><span class="line">        [](pair&lt;<span class="keyword">int</span>, <span class="keyword">int</span>&gt; left, pair&lt;<span class="keyword">int</span>, <span class="keyword">int</span>&gt; right) &#123;</span><br><span class="line">            <span class="keyword">int</span> d1 = left.first * left.first + left.second * left.second;</span><br><span class="line">            <span class="keyword">int</span> d2 = right.first * right.first + right.second * right.second;</span><br><span class="line">            <span class="keyword">return</span> d1 &lt; d2;</span><br><span class="line">        &#125;);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">auto</span> &amp;p: arr) &#123;</span><br><span class="line">        <span class="built_in">cout</span> &lt;&lt; <span class="string">"("</span> &lt;&lt; p.first &lt;&lt; <span class="string">", "</span> &lt;&lt; p.second &lt;&lt; <span class="string">")"</span> &lt;&lt; <span class="built_in">endl</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>输出结果：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">(2, 3)</span><br><span class="line">(1, 4)</span><br><span class="line">(6, 2)</span><br><span class="line">(5, 7)</span><br></pre></td></tr></table></figure>
<p>唯一需要注意的是，我们的比较函数取代的是 <code>&lt;</code>。</p>
<p>至此，关于<code>Lambda 表达式</code>的介绍也就结束了。说得不够详细的地方，请大家参考MSDN的官方文档，写的真心不错。小喵这里有一些地方都是直接照搬过来的。<span class="exturl" data-url="aHR0cHM6Ly9tc2RuLm1pY3Jvc29mdC5jb20vemgtY24vbGlicmFyeS9kZDI5MzYwOC5hc3B4" title="https://msdn.microsoft.com/zh-cn/library/dd293608.aspx">https://msdn.microsoft.com/zh-cn/library/dd293608.aspx<i class="fa fa-external-link"></i></span></p>
<h2 id="五补充">五、补充</h2>
<p>最近在类中使用了<code>lambda表达式</code>，结果出现奇怪的问题，故记录下来。<code>lambda表达式</code>可以允许捕获局部变量，但是类成员变量并不是局部变量，因此不能被lambda捕获。我们想要在lambda中使用类成员变量的话，只需要捕获<code>this</code>指针就可以。之后在lambda函数体中就可以随时使用成员变量了。对于this的捕获，永远是值传递的方式，即使指定了默认捕获的方式为引用，另外<code>[&amp;this]</code>这样的捕获方式是不允许的。</p>
<p><strong>转载请注明出处~</strong></p>
<ul>
<li>[1] <span class="exturl" data-url="aHR0cHM6Ly9tc2RuLm1pY3Jvc29mdC5jb20vemgtY24vbGlicmFyeS9kZDI5MzYwOC5hc3B4" title="https://msdn.microsoft.com/zh-cn/library/dd293608.aspx">https://msdn.microsoft.com/zh-cn/library/dd293608.aspx<i class="fa fa-external-link"></i></span></li>
<li>[2] <span class="exturl" data-url="aHR0cHM6Ly93d3cuemhpaHUuY29tL3F1ZXN0aW9uLzIwMTI1MjU2" title="https://www.zhihu.com/question/20125256">https://www.zhihu.com/question/20125256<i class="fa fa-external-link"></i></span></li>
<li>[3] <span class="exturl" data-url="aHR0cHM6Ly96aC53aWtpcGVkaWEub3JnL3dpa2kv5p+v6YeM5YyW" title="https://zh.wikipedia.org/wiki/柯里化">https://zh.wikipedia.org/wiki/柯里化<i class="fa fa-external-link"></i></span></li>
<li>[4] <span class="exturl" data-url="aHR0cHM6Ly9tc2RuLm1pY3Jvc29mdC5jb20vemgtY24vbGlicmFyeS9kbjQzOTc3OS5hc3B4" title="https://msdn.microsoft.com/zh-cn/library/dn439779.aspx">https://msdn.microsoft.com/zh-cn/library/dn439779.aspx<i class="fa fa-external-link"></i></span></li>
<li>[5] <span class="exturl" data-url="aHR0cHM6Ly9tc2RuLm1pY3Jvc29mdC5jb20vemgtY24vbGlicmFyeS93ZmEwZWR5cy5hc3B4" title="https://msdn.microsoft.com/zh-cn/library/wfa0edys.aspx">https://msdn.microsoft.com/zh-cn/library/wfa0edys.aspx<i class="fa fa-external-link"></i></span></li>
</ul>
]]></content>
      <categories>
        <category>C++</category>
      </categories>
  </entry>
  <entry>
    <title>一个简单粗暴的人脸认证标注工具的实现</title>
    <url>/2017/01/01/face-verify-annotate-tool/</url>
    <content><![CDATA[<blockquote>
<p>小喵的唠叨话：话说最近小喵也要开始写论文了，想了两周还是没有头绪，不知道该写些什么。恰好又被分配了一点标注数据的工作，于是乎想写点代码，休闲一下。结果也就是这篇博客。对了，小喵对GUI编程一窍不通，只知道Windows有MFC，Mac上的不知道。。。恰好听说过QT，而且知道这个界面库是跨平台的，也就选用了这个工具了。</p>
</blockquote>
<a id="more"></a>
<p>那么现在开始和小喵一起瞎猫似的捯饬QT吧~ 先看一眼效果图： <img data-src="http://cdn.miaoerduo.com/images/annotate-pro.png" alt="annotate pro" /></p>
<p>是不是乍一看还挺炫酷。功能上也还好，至少简单的标注工作都能完成了。那么让我们来一步一步的完成这个工具吧。</p>
<h2 id="一功能需求">一、功能需求</h2>
<p>这个程序主要的功能是完成一个人脸认证的标注工具。 具体来说，就是给定很多对人脸的图片，要标注一下这一对是不是同一个人。同时，每一对的图片的人脸一张是生活照，一张是证件照，需要同时标注出哪张是证件照，那张是生活照。照片都是经过检测和对齐的，这个工具只需要完成简单的显示、标注、保存记录的工作就可以。 当然考虑到有时候需要标注的list可能很大，可以加入跳转的功能。标注结果都保存在内存，用户可以随时更改，点击保存，则写入硬盘。</p>
<h2 id="二数据结构">二、数据结构</h2>
<p>那么是不是现在就可以动手写代码了呢？当然不是！ 小喵写这个软件一共用了3天的时间，第一天完成了一个超简单demo程序，熟悉了一下QT的事件添加，路径选择和显示图片的几个功能。之后又仔细的思考了一下各种数据的结构，才动手做了这一版工具。没有一个清晰的数据的概念，会造成许多的无用功。所以，大家在写程序的时候，要在准备阶段多花一点时间来思考，毕竟写代码才是最简单的事情不是吗？</p>
<ol type="1">
<li>输入数据格式：因为小喵的工作环境下，大家都对linux有一些了解，所以可以自行生成好图片的路径的list，这里统一要求，list必须是偶数行（2n行），代表n对，相邻的图片为一对。</li>
<li>标注数据存储：考虑到我们不仅需要标注是不是一对，还得标注哪张是证件照，所以不妨直接在读数据的时候就分成两份，这样就用两个 <code>std::vector&lt;std::string&gt;</code> 来存储就行了。</li>
<li>标注过程的状态：我们需要知道标注过程中的那些信息呢？主要应该有：总数据量，当前已标注的对数。</li>
<li>标注结果：每一对都有一组对应地 结果，考虑到有4中情况：未标注，不确定，不匹配，匹配这四种，我们定义一个枚举的状态表 <code>enum AnnoState</code> 就好。之后用一个 <code>std::vector&lt;enum AnnoState&gt;</code> 来存储标注结果。</li>
</ol>
<h2 id="三界面制作">三、界面制作</h2>
<p>GUI程序的界面一直是个很让人头疼的问题，记得在本科学习Java的时候，需要自己手写一个控件，使用new JButton()类似的方式创建按钮，然后添加到主界面上，位置什么的都得调用这个对象来设置，十分的繁琐。那么QT能不能简化这个过程呢？答案是肯定的。</p>
<p>创建项目-&gt;选择Application-&gt;Qt Widgets Application。然后项目名改成Anno Pro，其他全部默认设置，就创建好了一个项目了。这个初始的项目里面有3个文件夹：头文件，源文件和界面文件，以及一个.pro结尾的项目配置文件。</p>
<p><img data-src="http://cdn.miaoerduo.com/images/qt-create-project.png" alt="qt-create-project" /></p>
<p>既然需要编辑界面，我们自然会想查看一下界面文件了，双击 <code>MainWindow.ui</code> (我这里全部都是默认的名字)。出现的是一个充满各种控件的可视化界面编辑器。</p>
<p><img data-src="http://cdn.miaoerduo.com/images/qt-main-window-ui.png" alt="qt-main-window-ui" /></p>
<p>按照我们之前的界面样式，拖动左边的控件，就可以完成界面的编写了。小喵这里只用到了几种控件：</p>
<ul>
<li><code>QPushButton</code>：各种按钮</li>
<li><code>QLabel</code>：所以显示文字和图像的区域都是这这个控件</li>
<li><code>QFrame</code>：一个容器，小喵用它只是为了结构上更清晰</li>
<li><code>QSlider</code>：滑动条，小喵用的是水平滑动条</li>
<li><code>QStatusBar</code>：状态栏，这应该是自带的，如果删掉的话，在MainWindow控件点击右键就可以创建了</li>
</ul>
<p>拖动完成后，双击空间，就可以给空间设置文本，同时注意给每个控件起一个好听的名字（起名字很重要的！《代码大全》中甚至用一章，好几十页的篇幅介绍如何命名）。</p>
<p>至于其他的控件，大家可以自行研究。反正小喵现在的道行应该才是筑基。 那么我们就愉快的完成了界面的编写了~点击左右下的运行图标（三角形的那个），就可以看到自己的运行程序了！</p>
<h2 id="四数据定义与初始化">四、数据定义与初始化</h2>
<p>我们先前已经分析了我们需要的数据了，这部分开始使用代码的定义这些结构。打开我们头文件<code>mianwindow.h</code>，添加需要的变量，小喵就直接把自己的头文件复制下来了：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">ifndef</span> MAINWINDOW_H</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> MAINWINDOW_H</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;QMainWindow&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;vector&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;string&gt;</span></span></span><br><span class="line"><span class="keyword">namespace</span> Ui &#123;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">MainWindow</span>;</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">MainWindow</span> :</span> <span class="keyword">public</span> QMainWindow</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">enum</span> AnnoState &#123;</span><br><span class="line">        UNKNOWN = <span class="number">0</span>,  <span class="comment">// 未标注</span></span><br><span class="line">        YES = <span class="number">1</span>,      <span class="comment">// 匹配</span></span><br><span class="line">        NO = <span class="number">2</span>,       <span class="comment">// 不匹配</span></span><br><span class="line">        UNSURE = <span class="number">3</span>    <span class="comment">// 不确定</span></span><br><span class="line">    &#125;;</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="function"><span class="keyword">explicit</span> <span class="title">MainWindow</span><span class="params">(QWidget *parent = <span class="number">0</span>)</span></span>;</span><br><span class="line">    ~MainWindow();</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    Ui::MainWindow *ui; <span class="comment">// 自带的，ui界面的接口</span></span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">vector</span>&lt;<span class="built_in">std</span>::<span class="built_in">string</span>&gt; image_list_1;  <span class="comment">// 用来存放左边的图片的list</span></span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">vector</span>&lt;<span class="built_in">std</span>::<span class="built_in">string</span>&gt; image_list_2;  <span class="comment">// 用来存放右边的图片的list</span></span><br><span class="line">    <span class="keyword">int</span> current_idx;                        <span class="comment">// 当前图片对的id</span></span><br><span class="line">    <span class="keyword">int</span> total_pair_num;                     <span class="comment">// 总共的图片对的数目</span></span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">vector</span>&lt; AnnoState &gt; annotation_list;  <span class="comment">// 标注的结果</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">endif</span> <span class="comment">// MAINWINDOW_H</span></span></span><br></pre></td></tr></table></figure>
<p>可以看出，小喵添加了一个 <code>enum</code> 的类型，用来表示标注结果的类型。虽然只有4个状态，我们甚至可以直接约定几个 <code>int</code> 值来表示，但相信我，为这么4个状态定义一个枚举类型是完全有必要的。之后我们所有的成员变量都是 <code>private</code> 的。具体含义，注释中也有写明。</p>
<p>下一步就是初始化了。初始化的过程当然得写在构造函数里，这里，小喵在初始化的时候强迫用户选择一个标注的list，如果不这么做，会有很多的意外情况。请原谅小喵的怠惰。。。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">MainWindow::MainWindow(QWidget *parent) :</span><br><span class="line">    QMainWindow(parent),</span><br><span class="line">    ui(<span class="keyword">new</span> Ui::MainWindow)</span><br><span class="line">&#123;</span><br><span class="line">    ui-&gt;setupUi(<span class="keyword">this</span>);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 选择输入文件</span></span><br><span class="line">    <span class="keyword">while</span> (<span class="number">1</span>) &#123;</span><br><span class="line">        QString file_name = QFileDialog::getOpenFileName(<span class="keyword">this</span>, <span class="string">"choose a file to annotate"</span>, <span class="string">"."</span>);</span><br><span class="line">        <span class="keyword">if</span> (file_name.isEmpty()) &#123;</span><br><span class="line">            <span class="keyword">int</span> ok = QMessageBox::information(<span class="keyword">this</span>, <span class="string">"choose a file to annotate"</span>, <span class="string">"Don't want to work now?"</span>, QMessageBox::Ok | QMessageBox::Cancel);</span><br><span class="line">            <span class="keyword">if</span> (ok == QMessageBox::Ok) &#123;</span><br><span class="line">                <span class="built_in">exit</span>(<span class="number">0</span>);</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="built_in">std</span>::<span class="function">ifstream <span class="title">is</span><span class="params">(file_name.toStdString())</span></span>;</span><br><span class="line">        <span class="built_in">std</span>::<span class="built_in">string</span> image_name;</span><br><span class="line">        <span class="keyword">bool</span> is_odd = <span class="literal">true</span>;</span><br><span class="line">        <span class="keyword">while</span> (is &gt;&gt; image_name) &#123;</span><br><span class="line">            <span class="keyword">if</span> (is_odd) &#123;</span><br><span class="line">                <span class="keyword">this</span>-&gt;image_list_1.push_back(image_name);</span><br><span class="line">            &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">                <span class="keyword">this</span>-&gt;image_list_2.push_back(image_name);</span><br><span class="line">            &#125;</span><br><span class="line">            is_odd = !is_odd;</span><br><span class="line">        &#125;</span><br><span class="line">        is.close();</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (image_list_1.size() != image_list_2.size()) &#123;</span><br><span class="line">            QMessageBox::information(<span class="keyword">this</span>, <span class="string">"choose a file to annotate"</span>, <span class="string">"this image list is not even"</span>, QMessageBox::Ok);</span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (<span class="number">0</span> == image_list_1.size()) &#123;</span><br><span class="line">            QMessageBox::information(<span class="keyword">this</span>, <span class="string">"choose a file to annotate"</span>, <span class="string">"this image list is empty"</span>, QMessageBox::Ok);</span><br><span class="line">            <span class="keyword">continue</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    assert(image_list_1.size() == image_list_2.size());</span><br><span class="line">    <span class="comment">// 初始化其他参数</span></span><br><span class="line">    <span class="keyword">this</span>-&gt;total_pair_num = image_list_1.size();</span><br><span class="line">    <span class="keyword">this</span>-&gt;current_idx = <span class="number">0</span>;</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">vector</span>&lt;AnnoState&gt; annotation_list(<span class="keyword">this</span>-&gt;total_pair_num, AnnoState::UNKNOWN);</span><br><span class="line">    <span class="keyword">this</span>-&gt;annotation_list.swap(annotation_list);</span><br><span class="line"></span><br><span class="line">    display();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这里用了两个QT的组件：</p>
<ul>
<li><code>QFileDialog</code>：这个组件是一个文件对话框，其中有两个十分有用的函数：
<ul>
<li><code>getOpenFileName</code> 用于选择一个文件，并返回文件名；</li>
<li><code>getSaveFileName</code> 用于选择一个文件来保存数据，并返回一个文件名。</li>
<li>这两个函数的参数很多，小喵只用到了前面的3个，用到的参数依次是：父组件，标题，初始目录。其他的参数的功能，喵粉可以去官网查一下。</li>
</ul></li>
<li>QMessageBox::information，这个函数的功能是显示一个消息窗口。四个参数分别表示：父组件，标题，内容，按钮样式。</li>
</ul>
<p>相信大家懂一点点C++的知识的话，很容易看懂这段代码。 这里就是使用了一个循环，让用户选择文件，如果选择成功了，则读取数据到我们的list中，最终初始化了其他的参数，在调用display函数来显示。这个display函数是我们自己编写的，后面会说到。另外，assert函数是断言，他保证了断言的数据的合法性，如果不合法，程序会退出。想使用这个函数，需要包含头文件 <code>assert.h</code>。</p>
<h2 id="五添加事件响应">五，添加事件响应</h2>
<p>小喵之前了解到，QT使用的是一种信号和槽的事件机制，是一种十分高级的机制。那么有没有什么简单的方法，为我们的每个控件绑定自己的的事件呢？</p>
<p>在界面编辑界面下，<strong>右击</strong>需要添加事件的空间，然后选择<strong>转到槽</strong>。这时候会有很多选项，这里直接选择 <code>clicked</code> 就可以。然后你会发现我们的mainwindow类中，多了一个pivate slot的函数（也就是槽函数）。</p>
<p><img data-src="http://cdn.miaoerduo.com/images/qt-goto-slot.png" alt="qt-goto-slot" /></p>
<p>我们可以给每一个需要添加事件的函数都用这种方式来绑定事件，最终头文件中会出现这样的声明（函数名称的规则是：<code>on_控件名_信号类型</code>）：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> slots:</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">on_pushButton_save_clicked</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">on_pushButton_ok_clicked</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">on_pushButton_no_clicked</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">on_pushButton_unsure_clicked</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">on_pushButton_next_clicked</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">on_pushButton_prev_clicked</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">on_pushButton_switch_clicked</span><span class="params">()</span></span>;</span><br><span class="line">    <span class="function"><span class="keyword">void</span> <span class="title">on_horizontalSlider_progress_sliderReleased</span><span class="params">()</span></span>;</span><br></pre></td></tr></table></figure>
<p>在源文件中，也会生成空的函数定义。我们只需要自己完成函数定义就大功告成！</p>
<p>下面给出的是除了save的所有的函数的定义。主要工作是，给每个事件编写修改数据的代码，而不去负责任何界面相关的部分。各个控件可以通过 <code>this-&gt;ui</code> 来设置和获取。使用Qt Creator的时候，要充分利用智能提示。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * @brief MainWindow::on_pushButton_ok_clicked</span></span><br><span class="line"><span class="comment"> * 标注为"匹配"</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">void</span> MainWindow::on_pushButton_ok_clicked()</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">this</span>-&gt;annotation_list[<span class="keyword">this</span>-&gt;current_idx] = MainWindow::AnnoState::YES;</span><br><span class="line">    ++ <span class="keyword">this</span>-&gt;current_idx;</span><br><span class="line">    display();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * @brief MainWindow::on_pushButton_no_clicked</span></span><br><span class="line"><span class="comment"> * 标注为"不匹配"</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">void</span> MainWindow::on_pushButton_no_clicked()</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">this</span>-&gt;annotation_list[<span class="keyword">this</span>-&gt;current_idx] = MainWindow::AnnoState::NO;</span><br><span class="line">    ++ <span class="keyword">this</span>-&gt;current_idx;</span><br><span class="line">    display();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * @brief MainWindow::on_pushButton_unsure_clicked</span></span><br><span class="line"><span class="comment"> * 标注为"不确定"</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">void</span> MainWindow::on_pushButton_unsure_clicked()</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">this</span>-&gt;annotation_list[<span class="keyword">this</span>-&gt;current_idx] = MainWindow::AnnoState::UNSURE;</span><br><span class="line">    ++ <span class="keyword">this</span>-&gt;current_idx;</span><br><span class="line">    display();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * @brief MainWindow::on_pushButton_next_clicked</span></span><br><span class="line"><span class="comment"> * 移动到下一组</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">void</span> MainWindow::on_pushButton_next_clicked()</span><br><span class="line">&#123;</span><br><span class="line">    ++ <span class="keyword">this</span>-&gt;current_idx;</span><br><span class="line">    display();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * @brief MainWindow::on_pushButton_prev_clicked</span></span><br><span class="line"><span class="comment"> * 移动到上一组</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">void</span> MainWindow::on_pushButton_prev_clicked()</span><br><span class="line">&#123;</span><br><span class="line">    -- <span class="keyword">this</span>-&gt;current_idx;</span><br><span class="line">    display();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * @brief MainWindow::on_pushButton_switch_clicked</span></span><br><span class="line"><span class="comment"> * 交换两边的图片</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">void</span> MainWindow::on_pushButton_switch_clicked()</span><br><span class="line">&#123;</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">string</span> tmp = <span class="keyword">this</span>-&gt;image_list_1[<span class="keyword">this</span>-&gt;current_idx];</span><br><span class="line">    <span class="keyword">this</span>-&gt;image_list_1[<span class="keyword">this</span>-&gt;current_idx] = <span class="keyword">this</span>-&gt;image_list_2[<span class="keyword">this</span>-&gt;current_idx];</span><br><span class="line">    <span class="keyword">this</span>-&gt;image_list_2[<span class="keyword">this</span>-&gt;current_idx] = tmp;</span><br><span class="line">    display();</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * @brief MainWindow::on_horizontalSlider_progress_sliderReleased</span></span><br><span class="line"><span class="comment"> * 拖放进度条，控制进度</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">void</span> MainWindow::on_horizontalSlider_progress_sliderReleased()</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">int</span> pos = <span class="keyword">this</span>-&gt;ui-&gt;horizontalSlider_progress-&gt;value();</span><br><span class="line">    <span class="keyword">this</span>-&gt;current_idx = pos;</span><br><span class="line">    <span class="keyword">this</span>-&gt;display();</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>至此，我们的大体的功能逻辑就编写完了。 那么怎么让界面上显示我们的系统状态呢？注意到了我们上面的每一个函数都调用了display这个函数了吗？这个函数正式负责绘制界面的功能。 部分主要介绍三个函数：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> <span class="built_in">std</span>::<span class="built_in">string</span> UNSURE_FILE = <span class="string">":File/images/unsure.png"</span>;</span><br><span class="line"><span class="keyword">const</span> <span class="built_in">std</span>::<span class="built_in">string</span> YES_FILE = <span class="string">":File/images/yes.gif"</span>;</span><br><span class="line"><span class="keyword">const</span> <span class="built_in">std</span>::<span class="built_in">string</span> NO_FILE = <span class="string">":File/images/no.gif"</span>;</span><br><span class="line"><span class="keyword">const</span> <span class="built_in">std</span>::<span class="built_in">string</span> UNKNOWN_FILE = <span class="string">":File/images/unknown.png"</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * @brief set_image 将图像设置到label上，图像自动根据label的大小来缩放</span></span><br><span class="line"><span class="comment"> * @param label</span></span><br><span class="line"><span class="comment"> * @param image</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">set_image</span><span class="params">(QLabel *label, <span class="keyword">const</span> QPixmap &amp;image)</span> </span>&#123;</span><br><span class="line">    <span class="function"><span class="keyword">float</span> <span class="title">ratio</span><span class="params">(<span class="number">0.</span>)</span></span>;</span><br><span class="line">    ratio = <span class="number">1.</span> * label-&gt;width() / image.width();</span><br><span class="line">    ratio = fmin( <span class="number">1.</span> * label-&gt;height() / image.height(), ratio );</span><br><span class="line">    QPixmap m = image.scaled(<span class="keyword">static_cast</span>&lt;<span class="keyword">int</span>&gt;(image.width() * ratio), <span class="keyword">static_cast</span>&lt;<span class="keyword">int</span>&gt;(image.height() * ratio));</span><br><span class="line">    label-&gt;setPixmap(m);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">set_image</span><span class="params">(QLabel *label, <span class="keyword">const</span> <span class="built_in">std</span>::<span class="built_in">string</span> image_path)</span> </span>&#123;</span><br><span class="line">    <span class="function">QPixmap <span class="title">image</span><span class="params">(image_path.c_str())</span></span>;</span><br><span class="line">    set_image(label, image);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * @brief MainWindow::display</span></span><br><span class="line"><span class="comment"> * 根据系统中的所有的变量来设置当前界面中的各个部分的内容</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">void</span> MainWindow::display() &#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (<span class="keyword">this</span>-&gt;current_idx &gt;= <span class="keyword">this</span>-&gt;total_pair_num) &#123;</span><br><span class="line">        QMessageBox::information(<span class="keyword">this</span>, <span class="string">"annotation over"</span>, <span class="string">"Congratulations! You've finished all the job! Please save your work :)"</span>, QMessageBox::Ok);</span><br><span class="line">        <span class="keyword">this</span>-&gt;current_idx = <span class="keyword">this</span>-&gt;total_pair_num - <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (<span class="keyword">this</span>-&gt;current_idx &lt; <span class="number">0</span>) &#123;</span><br><span class="line">        QMessageBox::information(<span class="keyword">this</span>, <span class="string">"annotation warning"</span>, <span class="string">"You must start at 0 (not a negative position, I konw you wanna challenge this app) :)"</span>, QMessageBox::Ok);</span><br><span class="line">        <span class="keyword">this</span>-&gt;current_idx = <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 进度条</span></span><br><span class="line">    <span class="keyword">this</span>-&gt;ui-&gt;horizontalSlider_progress-&gt;setRange(<span class="number">0</span>, <span class="keyword">this</span>-&gt;total_pair_num - <span class="number">1</span>);</span><br><span class="line">    <span class="keyword">this</span>-&gt;ui-&gt;horizontalSlider_progress-&gt;setValue(<span class="keyword">this</span>-&gt;current_idx);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 状态栏</span></span><br><span class="line">    <span class="keyword">this</span>-&gt;ui-&gt;statusBar-&gt;showMessage(QString((<span class="built_in">std</span>::to_string(<span class="keyword">this</span>-&gt;current_idx + <span class="number">1</span>) + <span class="string">" / "</span> + <span class="built_in">std</span>::to_string(<span class="keyword">this</span>-&gt;total_pair_num)).c_str()));</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 文件名</span></span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">string</span> image_name_1 = <span class="keyword">this</span>-&gt;image_list_1[<span class="keyword">this</span>-&gt;current_idx];</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">string</span> image_base_name_1 = image_name_1.substr(image_name_1.find_last_of(<span class="string">"/"</span>) + <span class="number">1</span>);</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">string</span> image_name_2 = <span class="keyword">this</span>-&gt;image_list_2[<span class="keyword">this</span>-&gt;current_idx];</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">string</span> image_base_name_2 = image_name_2.substr(image_name_2.find_last_of(<span class="string">"/"</span>) + <span class="number">1</span>);</span><br><span class="line">    <span class="keyword">this</span>-&gt;ui-&gt;label_image_name_1-&gt;setText(image_base_name_1.c_str());</span><br><span class="line">    <span class="keyword">this</span>-&gt;ui-&gt;label_image_name_2-&gt;setText(image_base_name_2.c_str());</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 显示图像</span></span><br><span class="line">    set_image(<span class="keyword">this</span>-&gt;ui-&gt;label_image_view_1, image_name_1);</span><br><span class="line">    set_image(<span class="keyword">this</span>-&gt;ui-&gt;label_image_view_2, image_name_2);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 显示标注结果</span></span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">string</span> show_image_name = UNKNOWN_FILE;</span><br><span class="line">    <span class="keyword">switch</span> (<span class="keyword">this</span>-&gt;annotation_list[<span class="keyword">this</span>-&gt;current_idx]) &#123;</span><br><span class="line">    <span class="keyword">case</span> AnnoState::UNKNOWN:</span><br><span class="line">        show_image_name = UNKNOWN_FILE;</span><br><span class="line">        <span class="keyword">break</span>;</span><br><span class="line">    <span class="keyword">case</span> AnnoState::YES:</span><br><span class="line">        show_image_name = YES_FILE;</span><br><span class="line">        <span class="keyword">break</span>;</span><br><span class="line">    <span class="keyword">case</span> AnnoState::NO:</span><br><span class="line">        show_image_name = NO_FILE;</span><br><span class="line">        <span class="keyword">break</span>;</span><br><span class="line">    <span class="keyword">case</span> AnnoState::UNSURE:</span><br><span class="line">        show_image_name = UNSURE_FILE;</span><br><span class="line">        <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    set_image(<span class="keyword">this</span>-&gt;ui-&gt;label_image_compare_status, show_image_name);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>最开始我们定义了4个图片的路径。这可以是绝对路径或者相对路径。我们这里的路径设置的比较奇怪，在下面我们会讲到。<code>set_image</code> 负责将给定的图片绘制到 <code>QLabel</code> 上，为了显示的好看，图像会按照 <code>QLabel</code> 的尺寸来动态的缩放。这样就不会出现有个图像太大或太小的情况了。<code>display</code> 则是负责各个区域的绘制。</p>
<p>还差一步是保存结果：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> * @brief MainWindow::on_pushButton_save_clicked</span></span><br><span class="line"><span class="comment"> * 保存结果文件</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">void</span> MainWindow::on_pushButton_save_clicked()</span><br><span class="line">&#123;</span><br><span class="line">    QString file_name = QFileDialog::getSaveFileName(<span class="keyword">this</span>, <span class="string">"choose a file to save"</span>, <span class="string">"."</span>);</span><br><span class="line">    <span class="keyword">if</span> (file_name.isEmpty()) &#123;</span><br><span class="line">        QMessageBox::information(<span class="keyword">this</span>, <span class="string">"choose a file to save"</span>, <span class="string">"please enter a legal file name"</span>, QMessageBox::Ok);</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">std</span>::<span class="function">ofstream <span class="title">os</span><span class="params">(file_name.toStdString())</span></span>;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> idx = <span class="number">0</span>; idx &lt; <span class="keyword">static_cast</span>&lt;<span class="keyword">int</span>&gt;(<span class="keyword">this</span>-&gt;annotation_list.size()); ++ idx) &#123;</span><br><span class="line">        os &lt;&lt; <span class="keyword">this</span>-&gt;image_list_1[idx] &lt;&lt; <span class="string">" "</span> &lt;&lt; <span class="keyword">this</span>-&gt;image_list_2[idx] &lt;&lt; <span class="string">" "</span> &lt;&lt; <span class="keyword">this</span>-&gt;annotation_list[idx] &lt;&lt; <span class="string">"\n"</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    os.close();</span><br><span class="line">    QMessageBox::information(<span class="keyword">this</span>, <span class="string">"save"</span>, <span class="string">"save result success"</span>, QMessageBox::Ok);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="六添加资源">六、添加资源</h2>
<p>由于我们的程序是需要publish出去的，因此图片文件等资源，必须包含在程序中。那么Qt怎么添加文件资源呢？</p>
<p>在项目视图下，右键项目-&gt;添加新文件-&gt;Qt-&gt;Qt Resource File。就可以创建一个qrc文件了。</p>
<p><img data-src="http://cdn.miaoerduo.com/images/qt-create-resource-file.png" alt="qt-create-resource-file" /></p>
<p>我这里给这个文件取名为image。</p>
<p>之后，建议在项目的根目录里面新建一个文件夹，用来存放资源。小喵的结构是这个样子的：</p>
<p><img data-src="http://cdn.miaoerduo.com/images/annotate-tool-files.png" alt="annotate-tool-files" /></p>
<p>小喵的项目根目录新建了一个文件夹images，并将图像素材放入了这个文件夹。</p>
<p>之后回到QT，我们刚建好的image.qrc文件-&gt;Open in Editor。</p>
<p>先添加前缀，这里写上/File。之后点击新建的/File目录，再点击添加-&gt;添加文件，选择我们的素材文件。最终的效果图如下：</p>
<p><img data-src="http://cdn.miaoerduo.com/images/qt-resource-image-files.png" alt="qt-resource-image-files" /></p>
<p>之后，我们就可以在程序中直接访问这些资源了。这也就是我们之前的那四个奇怪的路径的由来了。</p>
<h2 id="七发布">七、发布</h2>
<p>此时此刻，相信每一个喵粉的程序都能在自己的电脑上愉快的玩耍了。这么有意思的程序，怎么分享给其他人呢？</p>
<p>和Windows上常用的VS类似，Qt Creator的左下角有个发布选项：</p>
<p><img data-src="http://cdn.miaoerduo.com/images/qt-project-publish.png" alt="qt-project-publish" /></p>
<p>选择Release，然后构建整个项目就可以了。之后找到我们的程序，双击就可以运行。</p>
<p>这时候你会愉快的把这个程序发给你的好伙伴，得到的反应一定是：这是啥！我打不开！ 为什么呢？ 虽然Qt是一个跨平台的界面库，但如果对方的电脑上没有安装Qt，那么就不能运行。不过不用失落，Qt中早已给出一个绝妙的解决办法。</p>
<p>小喵的电脑是Mac的，所以找到的解决方案也是Mac的，Windows和Linux上也有类似的办法，大家可以自行查找。</p>
<p><span class="exturl" data-url="aHR0cDovL3d3dy5jbmJsb2dzLmNvbS9FNzg2OEEvYXJjaGl2ZS8yMDEyLzEyLzAyLzI3OTgyMjUuaHRtbA==" title="http://www.cnblogs.com/E7868A/archive/2012/12/02/2798225.html">http://www.cnblogs.com/E7868A/archive/2012/12/02/2798225.html<i class="fa fa-external-link"></i></span></p>
<p>参考上述博客，我们使用 <code>macdeployqt</code> 这个工具来处理一下release的程序就搞定。这时候你会发现原本100k的程序变成了22M。但是直接发给别人的时候，是可以直接运行的！</p>
<p>至此，本次的博客结束了。 完整的项目在github上可以下载： <span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL21pYW9lcmR1by9Bbm5vX3Bybw==" title="https://github.com/miaoerduo/Anno_pro">https://github.com/miaoerduo/Anno_pro<i class="fa fa-external-link"></i></span></p>
<p><strong>转载请注明出处~</strong></p>
]]></content>
      <categories>
        <category>C++</category>
        <category>QT</category>
      </categories>
  </entry>
  <entry>
    <title>应用OpenMP的一个简单的设计模式</title>
    <url>/2016/11/15/openmp-tutorial/</url>
    <content><![CDATA[<blockquote>
<p>小喵的唠叨话：最近很久没写博客了，一是因为之前写的LSoftmax后馈一直没有成功，所以在等作者的源码。二是最近没什么想写的东西。前两天，在预处理图片的时候，发现处理200w张图片，跑了一晚上也才处理完一半。早上的时候，出于无奈，花半小时改写了一个简单调用OpenMP的处理程序，用了30个核心，然后一小时不到就处理完了。感慨在多核的时代，即使是简单的程序，如果能支持多核，应该都能节省不少时间。</p>
</blockquote>
<a id="more"></a>
<h2 id="一写在前面">一、写在前面</h2>
<p>对于OpenMP，小喵其实并不是了解很多，而且小喵本身也只用到了OpenMP的最简单的功能。在这里主要是分享一个自己常用的写简单的并行程序的思路。希望能帮助到大家。</p>
<p>这个设计模式的主要特点如下：</p>
<ol type="1">
<li>处理的任务是独立的；</li>
<li>可以在运行中输出结果，而不是最终才输出；</li>
<li>有限的资源占用；</li>
<li>在每次任务的执行时间不同的情况下，也能很好的工作；</li>
<li>在每次任务执行需要占用私有的数据时（依赖了线程不安全的库），也可以很好的工作；</li>
<li>输出是有序的</li>
</ol>
<p>注意：本文中仅介绍小喵自己用到的几个OpenMP的功能。既不深入也不完善。仅适合初学者。 小喵学习OpenMP主要是看了周明伟的博客： <span class="exturl" data-url="aHR0cDovL2Jsb2cuY3Nkbi5uZXQvZHJ6aG91d2VpbWluZy9hcnRpY2xlL2RldGFpbHMvNDA5MzYyNA==" title="http://blog.csdn.net/drzhouweiming/article/details/4093624">OpenMP编程指南<i class="fa fa-external-link"></i></span>。想要比较深入地学习的童鞋请看周老师的博客。对于什么是OpenMP，OpenMP有什么优点等的问题。周老师的博客也很详细的说明。这里小喵就不多废话了。直奔主题。</p>
<h2 id="二如何使用openmp">二、如何使用OpenMP</h2>
<p>小喵使用的开发环境是Linux，windows的童鞋可以看一下这个博客：<span class="exturl" data-url="aHR0cDovL3d3dy5jbmJsb2dzLmNvbS95YW5neWFuZ2N2L2FyY2hpdmUvMjAxMi8wMy8yMy8yNDEzMzM1Lmh0bWw=" title="http://www.cnblogs.com/yangyangcv/archive/2012/03/23/2413335.html">http://www.cnblogs.com/yangyangcv/archive/2012/03/23/2413335.html<i class="fa fa-external-link"></i></span>。MAC上的GCC实际上是Clang，想要使用OpenMP的话比较麻烦。要额外装一些东东，自己bing一下就有。</p>
<p>本喵的编译环境是CentOS 7， GCC 4.8.5。大多数系统和编译器都支持OpenMP了。</p>
<p>先举个小栗子： 不使用OpenMP：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> N 100000000</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">fun</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">int</span> a = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; N; ++ i) &#123;</span><br><span class="line">        a += i;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> a;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">100</span>; ++ i) &#123;</span><br><span class="line">        fun();</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">"finish"</span> &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>之后使用g++编译，并计时：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">g++ sample_without_omp.cpp -o sample_without_omp.bin</span><br><span class="line">time ./sample_with_omp.bin</span><br></pre></td></tr></table></figure>
<p>运行结果：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">.&#x2F;sample_without_omp.bin  24.42s user 0.00s system 100% cpu 24.417 total</span><br></pre></td></tr></table></figure>
<p>这里可以看到用了100%的cpu，总时间是24.417 s。</p>
<p>使用OpenMP，调用2个线程：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;omp.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> N 100000000</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">fun</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="keyword">int</span> a = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; N; ++ i) &#123;</span><br><span class="line">        a += i;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> a;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">pragma</span> omp parallel for num_threads(2) schedule(dynamic)</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">100</span>; ++ i) &#123;</span><br><span class="line">        fun();</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">"finish"</span> &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这里源码的差别是多了一个omp.h的头文件，和一个奇怪的语句： <code>#pragma omp parallel for num_threads(2) schedule(dynamic)</code></p>
<p>编译的时候，也有点小修改：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">g++ sample_with_omp.cpp -o sample_with_omp.bin -fopenmp</span><br><span class="line">time ./sample_with_omp.bin</span><br></pre></td></tr></table></figure>
<p>运行结果如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">.&#x2F;sample_with_omp.bin  24.32s user 0.01s system 199% cpu 12.182 total</span><br></pre></td></tr></table></figure>
<p>可以看出，user的时间几乎没变，这表示CPU总的运行时间没有变化。但是cpu的使用变成了199%，total的时间变成了12.182 s。这就表明了我们使用了2个cpu，使得运行时间成功减半了！是不是很愉快，我们只添加了1行代码，就使得程序的速度翻倍。可见OpenMP是多么的简洁实用。</p>
<p>那么，现在是不是不用小喵说，我们也知道怎么给程序加入OpenMP的支持了呢？</p>
<p>归纳一下，主要有三点：</p>
<ol type="1">
<li>加入OpenMP的头文件 <code>omp.h</code></li>
<li>使用合适的 <code>编译器指令</code> 修饰我们需要并行的部分（线程数、任务分配模式等等，后面会讲到）</li>
<li>编译的时候加入 <code>openmp</code> 的支持，编译的时候加入参数 <code>-fopenmp</code></li>
</ol>
<h2 id="三forkjoin的并行执行模式">三、fork/join的并行执行模式</h2>
<p>我们之前看到了一个简单的例子，可以看出，程序其实是有串行部分和并行部分两个部分组成的。</p>
<p>在程序刚启动的时候，只有一个主线程，当执行到并行部分的时候（上面的例子中就是pragma之后的for循环），并行的代码会通过派生其他线程来执行。只有当并行的所有代码执行完之后，才会继续执行串行的部分。</p>
<p>因此主要的运行流程是这个样子的：</p>
<p><img data-src="http://cdn.miaoerduo.com/images/openmp-pipeline.svg" alt="openmp pipeline" /></p>
<p>理解这个流程是相当重要的，可以避免很多的不必要的错误。一个常见的错误就是资源访问的冲突。比如文件，流对象等，如果<strong>在并行的代码部分随意访问这些资源，就可能会导致不可预见的错误</strong>。这在多线程编程中也是最常出现的错误，我们在下面会具体说到。</p>
<h2 id="四openmp的常用指令和库函数">四、OpenMP的常用指令和库函数</h2>
<p>在C/C++中，OpenMP的指令使用的格式如下：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">pragma</span> omp 指令 [子句 [子句] ...]</span></span><br></pre></td></tr></table></figure>
<p>指令用来指示下面的代码的运行模式。子句是给出一些额外的信息。</p>
<p>这里主要介绍两个指令：<code>parallel</code>，<code>for</code></p>
<ul>
<li><code>parallel</code>：用在代码段之前，表示下面的代码段使用多线程运行。</li>
<li><code>for</code>：用于 <code>for</code> 循环之前，将循环分配到多个线程中并行执行，必须保证每次循环之间无相关性。</li>
<li><code>parallel for</code>：<code>parallel</code> 和 <code>for</code> 语句的结合，也是用在一个 <code>for</code> 循环之前，表示 <code>for</code> 循环的代码将被多个线程并行执行。</li>
</ul>
<p>小喵使用的时候都是直接使用了 <code>parallel for</code> 这个组合指令。用来对紧接着的 <code>for</code> 循环的代码段进行并行。其他的指令请查阅之前提到的博客。</p>
<p>子句中主要是给出一些额外的设置，这里也主要介绍2个：<code>num_threads</code>，<code>schedule</code>。</p>
<ul>
<li>num_threads：指定线程的数目（不设置该参数似乎会使用和cpu核心数相同的线程数）。</li>
<li>schedule：指定如何调度for循环迭代。有4种模式：<code>static</code>、<code>dynamic</code>、<code>guided</code>、·，后面会专门讲到。</li>
</ul>
<p>这里，我们再回顾一下之前写的代码：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">pragma</span> omp parallel for num_threads(2) schedule(dynamic)</span></span><br></pre></td></tr></table></figure>
<p>是不是豁然开朗。这句话的意思是，使用OpenMP（<code>#pragma omp</code>），将下面的 <code>for</code> 循环使用多线程去执行（<code>parallel for</code>），线程数为2（<code>num_threads(2)</code>），任务调度方式使用 <code>dynamic</code> 模式（<code>schedule(dynamic)</code>）。 现在，让我们趁热打铁，学习for循环的写法。</p>
<p>这里，小喵直接复制了周老师的说法（解释得实在太好了）：</p>
<pre>
for 循环语句中，书写是需要按照一定规范来写才可以的，即for循环小括号内的语句要按照一定的规范进行书写，for语句小括号里共有三条语句
for( i = start; i < end; i++)

i = start; 是for循环里的第一条语句，必须写成 “变量＝初值” 的方式。如 i＝0
i < end; 是for循环里的第二条语句，这个语句里可以写成以下4种形式之一：
变量 < 边界值
变量 <= 边界值
变量 > 边界值
变量 >= 边界值
如 i>10 i<10 i>=10 i<=10 等等
最后一条语句i++可以有以下9种写法之一
i++
++i
i–
–i
i += inc
i -= inc
i = i + inc
i = inc + i
i = i – inc
例如i += 2; i -= 2；i = i + 2；i = i – 2；都是符合规范的写法。</pre>
<p>可见一般来说，我们的for循环的写法OpenMP是支持的。那么有没有OpenMP不支持的for循环呢？小喵没试过，不过可以猜想，<code>for (auto &amp;v: arr)</code> 这种写法是不支持的。使用迭代器的话，不知道能不能使用，小喵没有验证过。喵粉如果好奇的话，可以自行验证一下。</p>
<p>在介绍schedule之前，我们先学习几个常用的库函数，用来获取和设置OpenMP的各种运行时状态： - <code>omp_get_num_procs</code>, 返回运行本线程的多处理机的处理器个数。通常可以根据处理器的个数来合理设置并行的线程数。 - <code>omp_get_num_threads</code>, 返回当前并行区域中的活动线程个数。比如上面的例子，应该就会返回2。 - <code>omp_get_thread_num</code>, 返回线程号。并行区域的代码会被多个线程执行，而每个线程都有一个自己的ID，也就是线程号。如果我们设置使用N个线程，那么线程号会是0，1，2，...，N-1。 - <code>omp_set_num_threads</code>**`, 设置并行执行代码时的线程个数。和num_threads功能相同。</p>
<h2 id="五openmp中的任务调度">五、OpenMP中的任务调度</h2>
<p>那么接下来，我们开始学习任务调度的四种模式。使用的子句就是schedule。schedule的使用格式：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">schedule(type[, size])</span><br></pre></td></tr></table></figure>
<p>type主要有4种：<code>static</code>，<code>dynamic</code>，<code>guilded</code>，<code>runtime</code>。</p>
<h3 id="static静态调度">1、static（静态调度）</h3>
<p>表示静态调度，当不设置schedule的时候，多数编译器就是使用这种调度方式。它十分的简单。给定N个任务，启用t个线程，那么直接给每个线程分配N/t个任务，考虑到N可能不能整除t，所以每个线程的任务数会有极小的不同。</p>
<p>下面举个例子：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;omp.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">int</span> task_num = <span class="number">10</span>;</span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">pragma</span> omp parallel for num_threads(2) schedule(static)</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; task_num; ++ i) &#123;</span><br><span class="line">        <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">"i = "</span> &lt;&lt; i &lt;&lt; <span class="string">" thread_id = "</span> &lt;&lt; omp_get_thread_num() &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>输出结果如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">i &#x3D; 0 thread_id &#x3D; 0</span><br><span class="line">i &#x3D; 5 thread_id &#x3D; 1</span><br><span class="line">i &#x3D; 6 thread_id &#x3D; 1</span><br><span class="line">i &#x3D; 7 thread_id &#x3D; 1</span><br><span class="line">i &#x3D; 8 thread_id &#x3D; 1</span><br><span class="line">i &#x3D; 9 thread_id &#x3D; 1</span><br><span class="line">i &#x3D; 1 thread_id &#x3D; 0</span><br><span class="line">i &#x3D; 2 thread_id &#x3D; 0</span><br><span class="line">i &#x3D; 3 thread_id &#x3D; 0</span><br><span class="line">i &#x3D; 4 thread_id &#x3D; 0</span><br></pre></td></tr></table></figure>
<p>可以看出，0-4被分配给了0线程，5-9被分配给了1线程。由于是多线程，所以打印出来的顺序并不能保证。如果使用了<code>size</code>，则每次回分配给一个线程size次任务，依次迭代。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;omp.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">int</span> task_num = <span class="number">10</span>;</span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">pragma</span> omp parallel for num_threads(2) schedule(static, 2)</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; task_num; ++ i) &#123;</span><br><span class="line">        <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">"i = "</span> &lt;&lt; i &lt;&lt; <span class="string">" thread_id = "</span> &lt;&lt; omp_get_thread_num() &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>运行结果和上面稍有不同：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">i &#x3D; 2 thread_id &#x3D; 1</span><br><span class="line">i &#x3D; 0 thread_id &#x3D; 0</span><br><span class="line">i &#x3D; 3 thread_id &#x3D; 1</span><br><span class="line">i &#x3D; 6 thread_id &#x3D; 1</span><br><span class="line">i &#x3D; 7 thread_id &#x3D; 1</span><br><span class="line">i &#x3D; 1 thread_id &#x3D; 0</span><br><span class="line">i &#x3D; 4 thread_id &#x3D; 0</span><br><span class="line">i &#x3D; 5 thread_id &#x3D; 0</span><br><span class="line">i &#x3D; 8 thread_id &#x3D; 0</span><br><span class="line">i &#x3D; 9 thread_id &#x3D; 0</span><br></pre></td></tr></table></figure>
<p>可以看出，连续的2个任务会被分配到同一个线程。0、1给线程0，2、3给线程1，4、5给线程0，6、7给线程1 ……</p>
<p><code>static</code> 是一个十分简单的策略，但同时会带来一些问题。比如当任务的执行时间差异很大的时候，由于OpenMP的 <code>fork/join</code> 的机制，速度快的线程必须等待速度慢的线程，如果恰好分配的很不合理的话（耗时的任务集中在了某一个线程），其他的线程可能会等待较长的时间。这显然不利于我们充分利用多核资源。</p>
<h3 id="dynamic动态调度">2、dynamic（动态调度）</h3>
<p>动态调度会根据运行时的线程状态来决定下一次的迭代。当一个线程执行完自己的任务之后，会再去领取任务。不设置 <code>size</code> 的话，一个线程一次会分配一个任务，当执行完了，会再领取一个任务。如果设置了 <code>size</code> ，线程则一次领取 <code>size</code> 个任务。 <code>dynamic</code> 是小喵最爱的模式！是因为它和标准的生产者消费者模式很相似。这里生产者默认一次性生产所有的任务，然后每个线程都是一个消费者，当自己执行完了，会再次去领取任务。这样，任务的分配会更加的有弹性，更好的适应了任务时间不同的情况。</p>
<p>下面也是一个小栗子，不使用<code>size</code>：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;omp.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">int</span> task_num = <span class="number">10</span>;</span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">pragma</span> omp parallel for num_threads(2) schedule(dynamic)</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; task_num; ++ i) &#123;</span><br><span class="line">        <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">"i = "</span> &lt;&lt; i &lt;&lt; <span class="string">" thread_id = "</span> &lt;&lt; omp_get_thread_num() &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>运行结果：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">i &#x3D; 0 thread_id &#x3D; 0</span><br><span class="line">i &#x3D; 1 thread_id &#x3D; 1</span><br><span class="line">i &#x3D; 2 thread_id &#x3D; 0</span><br><span class="line">i &#x3D; 3 thread_id &#x3D; 0</span><br><span class="line">i &#x3D; 4 thread_id &#x3D; 1</span><br><span class="line">i &#x3D; 5 thread_id &#x3D; 1</span><br><span class="line">i &#x3D; 6 thread_id &#x3D; 1</span><br><span class="line">i &#x3D; 7 thread_id &#x3D; 1</span><br><span class="line">i &#x3D; 8 thread_id &#x3D; 1</span><br><span class="line">i &#x3D; 9 thread_id &#x3D; 1</span><br></pre></td></tr></table></figure>
<p>可以看出任务的分配是不均匀的。</p>
<p>使用<code>size</code>之后：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;omp.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">int</span> task_num = <span class="number">10</span>;</span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">pragma</span> omp parallel for num_threads(2) schedule(dynamic, 2)</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; task_num; ++ i) &#123;</span><br><span class="line">        <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">"i = "</span> &lt;&lt; i &lt;&lt; <span class="string">" thread_id = "</span> &lt;&lt; omp_get_thread_num() &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>运行结果如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">i &#x3D; 0 thread_id &#x3D; 0</span><br><span class="line">i &#x3D; 2 thread_id &#x3D; 1</span><br><span class="line">i &#x3D; 3 thread_id &#x3D; 1</span><br><span class="line">i &#x3D; 4 thread_id &#x3D; 1</span><br><span class="line">i &#x3D; 5 thread_id &#x3D; 1</span><br><span class="line">i &#x3D; 6 thread_id &#x3D; 1</span><br><span class="line">i &#x3D; 7 thread_id &#x3D; 1</span><br><span class="line">i &#x3D; 8 thread_id &#x3D; 1</span><br><span class="line">i &#x3D; 9 thread_id &#x3D; 1</span><br><span class="line">i &#x3D; 1 thread_id &#x3D; 0</span><br></pre></td></tr></table></figure>
<p>线程0先领取了任务0、1。线程1领取了2、3。线程1做完之后，又领取了4、5 ……</p>
<p>可以看出，每次的任务分配是以2个为单位的，分配的顺序视运行时状态动态调整。</p>
<h3 id="guided启发式调度">3、guided（启发式调度）</h3>
<p>采用启发式调度方法进行调度，每次分配给线程迭代次数不同，开始比较大，以后逐渐减小。 size表示每次分配的迭代次数的最小值，由于每次分配的迭代次数会逐渐减少，少到size时，将不再减少。如果不知道size的大小，那么默认size为1，即一直减少到1。具体采用哪一种启发式算法，需要参考具体的编译器和相关手册的信息。</p>
<h3 id="runtime">4、runtime</h3>
<p>runtime调用，并不是一个真的调度方式。它是根据环境变量的OMP_SCHEDULE来确定调度模式。最终仍然是上述三种方式之一。具体用法可以查看相关文档。</p>
<h2 id="六一个常用的设计模式">六、一个常用的设计模式</h2>
<p>在做了前5个部分的铺垫之后，相信喵粉们已经初步掌握了OpenMP的几个基本的知识。那么，现在就开始讲我们最重要的部分——小喵最常用的一个设计模式。 主要流程如下：</p>
<ol type="1">
<li>初始化
<ol type="1">
<li>定义线程数为<code>thread_num</code></li>
<li>定义平均每个线程上的任务数为<code>task_per_thread</code></li>
<li>初始化处理器对象（<code>handle_arr</code>）,大小为<code>thread_num</code></li>
<li>初始化任务空间（<code>task_arr</code>），大小为 <code>thread_num * task_per_thread</code></li>
<li>初始化结果空间（<code>result_arr</code>），大小为 <code>thread_num * task_per_thread</code></li>
</ol></li>
<li>读取任务（串行）
<ol type="1">
<li>读取 <code>thread_num * task_per_thread</code> 个任务，存入 <code>task_arr</code></li>
<li>记录读取任务的数目 <code>task_num (task_num &lt;= thread_num * task_per_thread)</code></li>
</ol></li>
<li>任务处理（并行）
<ol type="1">
<li>任务的 <code>task_id</code> 就是for循环的下标</li>
<li>通过 <code>omp_get_thread_num</code> 获取当前的线程id，根据线程id查找处理器对象。</li>
<li>使用处理器处理定义的 <code>task_id</code> 对应的任务 <code>task_arr[task_id]</code></li>
<li>将执行结果存入 <code>result_arr[task_id]</code> 的位置</li>
</ol></li>
<li>结果处理（串行）
<ol type="1">
<li>根据 <code>task_num</code>，处理完 <code>result_arr</code> 中的结果。</li>
</ol></li>
<li>程序状态判断
<ol type="1">
<li>判断 <code>task_num</code> 是否等于 <code>thread_num * task_per_thread</code>。
<ul>
<li>如果相等，说明任务队列没有执行完，继续 <code>&lt;2&gt;</code> 开始执行。</li>
<li>如果不相等，则说明任务队列全部处理完，程序执行结束 <code>&lt;6&gt;</code>。</li>
</ul></li>
</ol></li>
<li>enjoy your programming</li>
</ol>
<p>让我们来一步一步的理解这个模式。</p>
<h3 id="初始化">&lt;1&gt;初始化</h3>
<p>这里主要完成一些初始化的工作。</p>
<h4 id="thread_num-和-task_per_thread">1. <code>thread_num</code> 和 <code>task_per_thread</code></h4>
<p>可以看到，这里初始化了两个参数。那么为什么需要 <code>thread_num</code> 和 <code>task_per_thread</code> 这两个参数呢？</p>
<p>为了更好的利用和控制资源。</p>
<p>根据机器的不同，我们可以自己设置需要开启的线程数，这就是 <code>thread_num</code>。</p>
<blockquote>
<p>反派汪：我觉得你说的有问题。我们在程序中明明可以利用omp_get_num_procs获取机器的所有的处理器的数目，然后就启用这么多的线程的话，不就能最大限度的使用所有的计算能力了吗？</p>
<p>喵座：其实不然。假如服务器的处理器数目为40，按照你的思路，则会启用40个线程。这样一是会造成其他人不能正常的工作，二是当服务器本来就有其他的程序在run的时候，你的40个线程亦不能很好的工作。不如自己在运行之前设置一下需要的计算资源数，会更方便一点。</p>
</blockquote>
<p>那么为什么我们需要设置这个 <code>task_per_thread</code> 呢？</p>
<p>因为资源是有限的。</p>
<p>考虑到最高效的工作方式，就是让所有的线程不间断的工作。比如一次性读完所有的任务列表，然后使用 <code>dynamic</code> 做完所有的任务。这样在任务做完之前，每个线程都会无间歇的工作。</p>
<p>理想是完美的，现实是残酷的。如果任务非常多，比如小喵需要处理的200w条数据。很难一次性全部载入内存。而且，即使这么做了，也必须得任务全部做完，才能得到运行结果，时效性很差。</p>
<p>那么我们不设置 <code>thask_per_thread</code> 不行吗？或者就把这个设置成1。每次就读取线程数相同的任务数，这样代码编写不应该更简单吗？</p>
<p>这时候，让我们回顾一下OpenMP的调度机制。如果每次只读取 <code>thread_num</code> 这么多的个任务数，那么每次并行计算的时候，每个线程都会分配到一个任务。那么总的耗时将变成最慢的任务的执行时间。</p>
<p>举个简单的例子，比如有12个任务，耗时为2，1，2，1，2，1，2，1，2，1，2，1。我们使用2个线程。那么每处理2个任务，耗时都是2。总时间是12。</p>
<p>如果我们每6个一起执行，也是使用2个线程。需要的总时间会变成了10。执行过程看下图：</p>
<p><img data-src="http://cdn.miaoerduo.com/images/task-queue.svg" alt="任务队列" /></p>
<p>可以使用 <code>task_per_task</code> 这个策略，每次处理 <code>thread_num * task_per_task</code> 个任务的话，可以更好了利用多核的资源。（<code>task_per_task</code>设得越大，讲道理效果应该越好。小喵自己喜欢设成10或20）</p>
<p>另一个好处是，当我们处理完这些任务之后，可以立刻将结果写入结果文件。</p>
<h4 id="处理器对象">2. 处理器对象</h4>
<p>这是可选的。我们在实际处理任务的时候，有时候会使用到一些特殊的资源，而且必须保证这些资源是独占的（比如网络通信的套接字，文件对象，或是线程不安全的一些实例的对象）。最简单高效的方法就是为每个线程都初始化一个自己的处理器（或是资源）对象。这样在实际处理的时候，每个线程可以根据自己的线程id找到自己的处理器，从而避免了多线程中的各种问题。</p>
<h4 id="task_arr-和-result_arr">3. <code>task_arr</code> 和 <code>result_arr</code></h4>
<p>这两个空间是用来存放每次并行处理的任务和结果的。大小自然和每次并行的任务数（<code>thread_num * task_per_thread</code>）相等。考虑到每次并行都可以复用这些空间，所以提前申请好足够的空间可以提高运行效率。</p>
<h3 id="读取任务">&lt;2&gt;读取任务</h3>
<p>我们通常会将任务的内容保存在文件中。而文件的读取是不能并行的。因此我们需要提前按串行的方式将任务读取到任务队列 <code>task_arr</code> 中。每次读取 <code>thread_num * task_per_thread</code> 个。考虑到任务总数可能不是 <code>thread_num * task_per_thread</code> 的整数倍，因此最后一次读取的任务数会稍小一点。我们将每次读取的任务数记录下来，命名为 <code>task_num</code>。</p>
<h3 id="任务处理">&lt;3&gt;任务处理</h3>
<p>这里就是我们刚刚学习到的OpenMP的用武之地。</p>
<p>通常的写法是：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">pragma</span> omp parallel for num_threads(thread_num) schedule(dynamic)</span></span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">int</span> task_idx = <span class="number">0</span>; task_idx &lt; task_num; ++ task_idx) &#123;</span><br><span class="line">    <span class="keyword">int</span> thread_id = omp_get_thread_num();                      <span class="comment">// 获取当前的线程id</span></span><br><span class="line">    handle_type handle = handle_arr[thread_id];                <span class="comment">// 根据线程id，获取处理器</span></span><br><span class="line">    result_type result = handle-&gt;process(task_arr[task_idx]);  <span class="comment">// 处理指定的任务</span></span><br><span class="line">    result_arr[task_idx] = result;                             <span class="comment">// 在指定位置写回执行的结果</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>获取当前的线程号，然后获取处理器，然后处理对应的任务，并将结果存放进对应的位置。</p>
<p>注意，线程之间是独立的，不能读写同一个线程不安全的资源。而且在并行区域不保证任何的线程间的顺序。</p>
<p>这样，我们就能安全且高效的执行完每次的任务了。</p>
<h3 id="结果处理">&lt;4&gt;结果处理</h3>
<p>这部分十分简单，因为任务的结果已经按顺序存进了 <code>result_arr</code> 中，有效的 <code>result</code> 是前 <code>task_num</code> 个，之后想怎么处理都是喵粉自己的事情了。</p>
<h3 id="程序状态判断">&lt;5&gt;程序状态判断</h3>
<p>正如我们在 <code>&lt;2&gt;</code> 中说到的，我们每次处理一批任务，最后的一批任务的个数将不是 <code>thread_num * task_per_thread</code> 这么多。因此需要与 <code>task_num</code> 比较一下。如果相等，就可能是我们还没有处理完，回到 <code>&lt;2&gt;</code> 继续执行。如果不相等，那就说明我们处理完了所有的任务了！你可以坐下来喝杯caffe，然后enjoy多线程带来的快感了。</p>
<p>最后，附上一个简单的demo。使用多线程，从文本上读取图片的list，读取图片的大小，并将结果存入一个新的文本中。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;opencv2/opencv.hpp&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;fstream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;string&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> &#123;</span></span><br><span class="line">    <span class="keyword">int</span> width;</span><br><span class="line">    <span class="keyword">int</span> height;</span><br><span class="line">&#125; Size;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> **argv)</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (argc &lt; <span class="number">3</span>) &#123;</span><br><span class="line">        <span class="built_in">std</span>::<span class="built_in">cerr</span> &lt;&lt; <span class="string">"usage: get_size.bin input_list output_list"</span></span><br><span class="line">            <span class="string">" [thread_num] [task_per_thread]"</span> &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line">            <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">int</span> thread_num = (argc &gt; <span class="number">3</span>) ? atoi(argv[<span class="number">3</span>]):<span class="number">1</span>;</span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">int</span> task_per_thread = (argc &gt; <span class="number">4</span>) ? atoi(argv[<span class="number">4</span>]): <span class="number">10</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">int</span> total_task = thread_num * task_per_thread;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">std</span>::<span class="built_in">string</span> image_name_arr[total_task];    <span class="comment">// task arr</span></span><br><span class="line">    Size image_size_arr[total_task];           <span class="comment">// result arr</span></span><br><span class="line"></span><br><span class="line">    <span class="built_in">std</span>::<span class="function">ifstream <span class="title">is</span><span class="params">(argv[<span class="number">1</span>])</span></span>;</span><br><span class="line">    <span class="built_in">std</span>::<span class="function">ofstream <span class="title">os</span><span class="params">(argv[<span class="number">2</span>])</span></span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">int</span> count = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span> (<span class="number">1</span>) &#123;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 读取任务</span></span><br><span class="line">        <span class="keyword">int</span> task_num = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> task_idx = <span class="number">0</span>; task_idx &lt; total_task; ++ task_idx) &#123;</span><br><span class="line">            <span class="keyword">if</span> (!(is &gt;&gt; image_name_arr[task_idx])) <span class="keyword">break</span>;</span><br><span class="line">            ++ task_num;</span><br><span class="line">            ++ count;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 处理任务</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">pragma</span> omp parallel for num_threads(thread_num) schedule(dynamic)</span></span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> task_idx = <span class="number">0</span>; task_idx &lt; task_num; ++ task_idx) &#123;</span><br><span class="line">            cv::Mat image = cv::imread(image_name_arr[task_idx]);</span><br><span class="line">            image_size_arr[task_idx].width = image.cols;</span><br><span class="line">            image_size_arr[task_idx].height = image.rows;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="built_in">std</span>::<span class="built_in">cout</span> &lt;&lt; <span class="string">"process #"</span> &lt;&lt; count &lt;&lt; <span class="built_in">std</span>::<span class="built_in">endl</span>;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 处理结果</span></span><br><span class="line">        <span class="keyword">for</span> (<span class="keyword">int</span> task_idx = <span class="number">0</span>; task_idx &lt; task_num; ++ task_idx) &#123;</span><br><span class="line">            os &lt;&lt; image_name_arr[task_idx] &lt;&lt; <span class="string">" "</span></span><br><span class="line">                &lt;&lt; image_size_arr[task_idx].width &lt;&lt; <span class="string">" "</span></span><br><span class="line">                &lt;&lt; image_size_arr[task_idx].height &lt;&lt; <span class="string">"\n"</span>;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 状态判断</span></span><br><span class="line">        <span class="keyword">if</span> (task_num != total_task) <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>编译和执行：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">g++ get_image_size_with_omp.cpp -o get_image_size_with_omp -fopenmp -I/path/to/opencv/include -L/path/to/opencv/lib -lopencv_core -lopencv_highgui</span><br><span class="line">./get_image_size_with_omp /path/to/image_list /path/to/save/result 2 20</span><br></pre></td></tr></table></figure>
<p>怎么样，使用这种模式来实现简单的多线程程序是不是很简单？</p>
<p><strong>转载请注明出处~</strong></p>
]]></content>
      <categories>
        <category>C++</category>
        <category>OpenMP</category>
      </categories>
  </entry>
  <entry>
    <title>基于Caffe的Large Margin Softmax Loss的实现（二）</title>
    <url>/2016/10/08/caffe-large-margin-softmax-2/</url>
    <content><![CDATA[<blockquote>
<p>小喵的唠叨话：前一篇博客，我们做完了L-Softmax的准备工作。而这一章，我们开始进行前馈的研究。</p>
</blockquote>
<a id="more"></a>
<h2 id="四前馈">四、前馈</h2>
<p>还记得上一篇博客，小喵给出的三个公式吗？不记得也没关系。</p>
<p>这次，我们要一点一点的通过代码来实现这些公式。小喵主要是GPU上实现前后馈的代码，因为这个层只是用来训练，GPU速度应该会快一点。</p>
<p>我们首先要进行一般的FC层的前馈，因为LM_FC的前馈只是修改了一般的FC中的若干个值，而大部分的值都是没有修改过的。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">const</span> Dtype* bottom_data = bottom[<span class="number">0</span>]-&gt;gpu_data();</span><br><span class="line"><span class="keyword">const</span> Dtype* label_data = bottom[<span class="number">1</span>]-&gt;gpu_data();</span><br><span class="line">Dtype* top_data = top[<span class="number">0</span>]-&gt;mutable_gpu_data();</span><br><span class="line"><span class="keyword">const</span> Dtype* weight = <span class="keyword">this</span>-&gt;blobs_[<span class="number">0</span>]-&gt;gpu_data();</span><br><span class="line"><span class="comment">// 普通fc层的计算</span></span><br><span class="line"><span class="keyword">if</span> (M_ == <span class="number">1</span>) &#123;</span><br><span class="line">  caffe_gpu_gemv&lt;Dtype&gt;(CblasNoTrans, N_, K_, (Dtype)<span class="number">1.</span>,</span><br><span class="line">                       weight, bottom_data, (Dtype)<span class="number">0.</span>, top_data);</span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">  caffe_gpu_gemm&lt;Dtype&gt;(CblasNoTrans,</span><br><span class="line">                        transpose_ ? CblasNoTrans : CblasTrans,</span><br><span class="line">                        M_, N_, K_, (Dtype)<span class="number">1.</span>,</span><br><span class="line">                        bottom_data, weight, (Dtype)<span class="number">0.</span>, top_data);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这样就计算完了一个普通的FC的前馈。 之后是一些具体的实现。</p>
<h3 id="余弦">1，余弦</h3>
<p><span class="math display">\[
\cos(\theta_j)=\frac{W_j^Tx_i}{\|W_j\|\|x_i\|}
\]</span></p>
<p>这是要求出label为 <span class="math inline">\(j\)</span> 的weight的权值和feature之间的余弦值。公式大家在高中应该就学过了。这样需要出三部分：<span class="math inline">\(W_j^Tx_i\)</span>，<span class="math inline">\(\|W_j\|\)</span> 和 <span class="math inline">\(\|x_i\|\)</span>。这里 <span class="math inline">\(i\)</span> 表示feature的序号，因为一个mini batch中有很多张图片。<span class="math inline">\(j\)</span> 表示正确的label值。</p>
<p><span class="math inline">\(W_j^Tx_i\)</span> 的计算非常简单，因为FC层的前馈计算出来的就是这个值。因此我们可以直接从FC的前馈结果中直接复制对应位置的结果。<span class="math inline">\(\|W_j\|\)</span> 和 <span class="math inline">\(\|x_i\|\)</span> 是比较简单的模值的计算，使用 <code>caffe_cpu_dot</code> 很容易就可以求得（为什么不使用 <code>caffe_gpu_dot</code> 呢？因为小喵在使用 <code>caffe_gpu_dot</code> 的时候，caffe会报一个奇怪的错误，不知道是不是因为GPU的显存不能随意访问的）。</p>
<p>最后的余弦值带入到上面的式子，就一下子搞定~</p>
<p>这里用到了几个变量：</p>
<ul>
<li>M: batch size</li>
<li>N: class num</li>
<li>K: feature length</li>
</ul>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// w * x</span></span><br><span class="line"><span class="comment">// 直接从前馈的结果中复制</span></span><br><span class="line">Dtype *wx_data = <span class="keyword">this</span>-&gt;wx_.mutable_gpu_data();</span><br><span class="line">copy_label_score&lt;Dtype&gt;&lt;&lt;&lt;CAFFE_GET_BLOCKS(M_), CAFFE_CUDA_NUM_THREADS&gt;&gt;&gt;(M_, N_, label_data, top_data, wx_data);</span><br><span class="line"></span><br><span class="line"><span class="comment">// w * w</span></span><br><span class="line">Dtype *abs_w_data = <span class="keyword">this</span>-&gt;abs_w_.mutable_cpu_data();</span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">int</span> m = <span class="number">0</span>; m &lt; M_; ++ m) &#123;</span><br><span class="line">  abs_w_data[m] = caffe_cpu_dot&lt;Dtype&gt;(</span><br><span class="line">    K_,</span><br><span class="line">    <span class="keyword">this</span>-&gt;blobs_[<span class="number">0</span>]-&gt;cpu_data() + <span class="keyword">static_cast</span>&lt;<span class="keyword">int</span>&gt;(label_cpu_data[m]) * K_,</span><br><span class="line">    <span class="keyword">this</span>-&gt;blobs_[<span class="number">0</span>]-&gt;cpu_data() + <span class="keyword">static_cast</span>&lt;<span class="keyword">int</span>&gt;(label_cpu_data[m]) * K_</span><br><span class="line">    );</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// x * x</span></span><br><span class="line">Dtype *abs_x_data = <span class="keyword">this</span>-&gt;abs_x_.mutable_cpu_data();</span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">int</span> m = <span class="number">0</span>; m &lt; M_; ++ m) &#123;</span><br><span class="line">  abs_x_data[m] = caffe_cpu_dot&lt;Dtype&gt;(</span><br><span class="line">    K_, </span><br><span class="line">    bottom[<span class="number">0</span>]-&gt;cpu_data() + m * K_,</span><br><span class="line">    bottom[<span class="number">0</span>]-&gt;cpu_data() + m * K_</span><br><span class="line">    );</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// abs_w, abs_x</span></span><br><span class="line">caffe_gpu_powx&lt;Dtype&gt;(M_, <span class="keyword">this</span>-&gt;abs_w_.mutable_gpu_data(), <span class="number">0.5</span>, <span class="keyword">this</span>-&gt;abs_w_.mutable_gpu_data());</span><br><span class="line">caffe_gpu_powx&lt;Dtype&gt;(M_, <span class="keyword">this</span>-&gt;abs_x_.mutable_gpu_data(), <span class="number">0.5</span>, <span class="keyword">this</span>-&gt;abs_x_.mutable_gpu_data());</span><br><span class="line"></span><br><span class="line"><span class="comment">// cos_t = wx / (|x| * |w|)</span></span><br><span class="line">Dtype *cos_t_data = <span class="keyword">this</span>-&gt;cos_t_.mutable_gpu_data();</span><br><span class="line">caffe_gpu_div&lt;Dtype&gt;(M_, wx_data, <span class="keyword">this</span>-&gt;abs_x_.gpu_data(), cos_t_data);</span><br><span class="line">caffe_gpu_div&lt;Dtype&gt;(M_, cos_t_data, <span class="keyword">this</span>-&gt;abs_w_.gpu_data(), cos_t_data);</span><br></pre></td></tr></table></figure>
<p>其中 <code>copy_label_score</code> 是我们自己编写的用来复制结果的核函数（如何编写Cuda程序就是另一门学科了）：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">typename</span> Dtype&gt;</span><br><span class="line">__<span class="function">global__ <span class="keyword">void</span> <span class="title">copy_label_score</span><span class="params">(<span class="keyword">const</span> <span class="keyword">int</span> M, <span class="keyword">const</span> <span class="keyword">int</span> N, <span class="keyword">const</span> Dtype *label_data, <span class="keyword">const</span> Dtype *top_data, Dtype *wx_data)</span> </span>&#123;</span><br><span class="line">  CUDA_KERNEL_LOOP(index, M) &#123;</span><br><span class="line">    wx_data[index] = top_data[index * N + <span class="keyword">static_cast</span>&lt;<span class="keyword">int</span>&gt;(label_data[index])];</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>相信机智如你的喵粉，看到这几行代码，一定可以轻松理解。</p>
<p>这里，小喵想多介绍一点东西。</p>
<p>我们知道Caffe里面的数据都是通过Blob结构来存储的，比如这里的bottom_data，其实就是一个blob，默认形状是(n, c, h, w)，n表示的就是batch size，c是channel数，h，w分贝表示高和宽。而且blob中的内存的存储顺序，也和一般的C语言中的数组一样。因此我们这里计算feature的模的时候，是直接每K_个数值计算一次点乘。</p>
<p>同理，weight是存储在 <code>this-&gt;blobs[0]</code> 中的，那么weight的形状又是什么样子的呢？这里非常碰巧的是，如果我们在prototxt中设置的transpose为false的话，weight的形状是 <code>N * K</code>，也就是说，我们可以将weight看成一个矩阵，它的每一行都与feature直接点乘，得到输出，也就是说weight的每一行都是我们需要计算模值的 <span class="math inline">\(W_j\)</span>，所以我们计算weight的模的时候，用的计算方法和计算feature模时很相似。我们这里强制设置transpose为false，因为这样计算会比较简单。如果你设成了true，那就必须自己写个求模的函数了。</p>
<h3 id="m倍余弦">2，m倍余弦</h3>
<p><span class="math display">\[
\cos(m\theta_i)=\sum_n(-1)^n{C_m^{2n}\cos^{m-2n}(\theta_i)\cdot(1-\cos(\theta_i)^2)^n}, (2n\leq m)
\]</span></p>
<p>我们在(1)中求出了 <span class="math inline">\(\cos(\theta)\)</span>，对于给定的margin，只需要代入公式就可以求出<span class="math inline">\(\cos(m\theta)\)</span>的值了。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">typename</span> Dtype&gt;</span><br><span class="line">__<span class="function">global__ <span class="keyword">void</span> <span class="title">cal_cos_mt</span><span class="params">(<span class="keyword">const</span> <span class="keyword">int</span> count, <span class="keyword">const</span> <span class="keyword">unsigned</span> <span class="keyword">int</span> margin, <span class="keyword">const</span> <span class="keyword">int</span> *C_M_N, <span class="keyword">const</span> Dtype *cos_t_data, Dtype *cos_mt_data)</span> </span>&#123;</span><br><span class="line">  CUDA_KERNEL_LOOP(index, count) &#123;</span><br><span class="line">    Dtype <span class="keyword">cos_t</span> = cos_t_data[index];</span><br><span class="line">    Dtype sin_t_2 = <span class="number">1</span> - <span class="keyword">cos_t</span> * <span class="keyword">cos_t</span>;</span><br><span class="line">    Dtype cos_mt = <span class="number">0.</span>;</span><br><span class="line">    <span class="keyword">int</span> flag = <span class="number">-1</span>;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> n = <span class="number">0</span>; n &lt;= (margin / <span class="number">2</span>); ++ n) &#123;</span><br><span class="line">      flag *= <span class="number">-1</span>;</span><br><span class="line">      cos_mt += flag * C_M_N[<span class="number">2</span> * n] * powf(<span class="keyword">cos_t</span>, (margin - <span class="number">2</span> * n)) * powf(sin_t_2, n);</span><br><span class="line">    &#125;</span><br><span class="line">    cos_mt_data[index] = cos_mt;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>上面是用来计算 <span class="math inline">\(\cos(m\theta)\)</span> 的cuda函数，调用也十分的简单：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// cos(mt)</span></span><br><span class="line">cal_cos_mt&lt;Dtype&gt;&lt;&lt;&lt;CAFFE_GET_BLOCKS(M_), CAFFE_CUDA_NUM_THREADS&gt;&gt;&gt;(</span><br><span class="line">  M_, <span class="keyword">this</span>-&gt;margin, <span class="keyword">this</span>-&gt;C_M_N_.gpu_data(), <span class="keyword">this</span>-&gt;cos_t_.mutable_gpu_data(), <span class="keyword">this</span>-&gt;cos_mt_-&gt;mutable_gpu_data());</span><br></pre></td></tr></table></figure>
<h3 id="前馈">3，前馈</h3>
<p><span class="math display">\[
f_{y_{i}}=(-1)^k\cdot\|W_{y_{i}}\|\|x_{i}\|\cos(m\theta_i)-2k\cdot\|W_{y_i}\|\|x_i\|
\]</span></p>
<p>严格上来说，我们需要求的并不是这个式子，而是：</p>
<p><span class="math display">\[
f_{y_i}=\frac{\lambda\|W_{y_i}\|\|x_i\|\cos(\theta_{y_i})+\|W_{y_i}\|\|x_i\|\varphi(\theta_{y_i})}{1+\lambda}
\]</span></p>
<p><span class="math display">\[
\varphi(\theta)=(-1)^k\cos(m\theta)-2k, \theta\in[\frac{k\pi}{m}, \frac{(k+1)\pi}{m}]
\]</span></p>
<p>可以看出，当 <span class="math inline">\(\lambda\)</span> 为0的时候，这两个式子就退化成前面的一个式子了。</p>
<p>k的求法十分简单，只需要将 <span class="math inline">\(\cos(\theta)\)</span> 与各个区间进行比较就可以得到。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// k</span></span><br><span class="line"><span class="keyword">int</span> *k_cpu_data = <span class="keyword">this</span>-&gt;k_.mutable_cpu_data();</span><br><span class="line"><span class="keyword">const</span> Dtype *cos_t_cpu_data = <span class="keyword">this</span>-&gt;cos_t_.cpu_data();</span><br><span class="line"><span class="keyword">for</span> (<span class="keyword">int</span> m = <span class="number">0</span>; m &lt; M_; ++ m) &#123;</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">int</span> _k = <span class="number">0</span>; _k &lt; <span class="keyword">this</span>-&gt;cos_theta_bound_.count(); ++ _k) &#123;</span><br><span class="line">    <span class="keyword">if</span> (<span class="keyword">this</span>-&gt;cos_theta_bound_.cpu_data()[_k] &lt; cos_t_cpu_data[m]) &#123;</span><br><span class="line">      k_cpu_data[m] = _k - <span class="number">1</span>;</span><br><span class="line">      <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>最后一步就是计算出真正的前馈值了！按照公式容易编写程序：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">typename</span> Dtype&gt;</span><br><span class="line">__<span class="function">global__ <span class="keyword">void</span> <span class="title">LMForward</span><span class="params">(</span></span></span><br><span class="line"><span class="function"><span class="params">  <span class="keyword">const</span> <span class="keyword">int</span> M, <span class="keyword">const</span> <span class="keyword">int</span> N, <span class="keyword">const</span> <span class="keyword">float</span> lambda,</span></span></span><br><span class="line"><span class="function"><span class="params">  <span class="keyword">const</span> Dtype *label_data, <span class="keyword">const</span> Dtype *cos_mt_data, <span class="keyword">const</span> <span class="keyword">int</span> *k_data,</span></span></span><br><span class="line"><span class="function"><span class="params">  <span class="keyword">const</span> Dtype *abs_w_data, <span class="keyword">const</span> Dtype *abs_x_data, Dtype *top_data)</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">  CUDA_KERNEL_LOOP(index, M) &#123;</span><br><span class="line">    Dtype cos_mt = cos_mt_data[index];</span><br><span class="line">    <span class="keyword">int</span> k = k_data[index];</span><br><span class="line">    <span class="keyword">int</span> label = <span class="keyword">static_cast</span>&lt;<span class="keyword">int</span>&gt;(label_data[index]);</span><br><span class="line">    Dtype abs_w = abs_w_data[index];</span><br><span class="line">    Dtype abs_x = abs_x_data[index];</span><br><span class="line">    top_data[N * index + label] =  (lambda * top_data[N * index + label] + abs_w * abs_x * ( powf(<span class="number">-1</span>, k) * cos_mt - <span class="number">2</span> * k )) / (<span class="number">1</span> + lambda);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>调用也十分简单：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// y</span></span><br><span class="line">LMForward&lt;Dtype&gt;&lt;&lt;&lt;CAFFE_GET_BLOCKS(M_), CAFFE_CUDA_NUM_THREADS&gt;&gt;&gt;(</span><br><span class="line">  M_, N_, <span class="keyword">this</span>-&gt;lambda,</span><br><span class="line">  label_data, <span class="keyword">this</span>-&gt;cos_mt_-&gt;gpu_data(), <span class="keyword">this</span>-&gt;k_.gpu_data(),</span><br><span class="line">  <span class="keyword">this</span>-&gt;abs_w_.gpu_data(), <span class="keyword">this</span>-&gt;abs_x_.gpu_data(), top[<span class="number">0</span>]-&gt;mutable_gpu_data());</span><br></pre></td></tr></table></figure>
<p>最后附上，完整的前馈代码（省略头文件和caffe的名字空间）：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">typename</span> Dtype&gt;</span><br><span class="line">__<span class="function">global__ <span class="keyword">void</span> <span class="title">copy_label_score</span><span class="params">(<span class="keyword">const</span> <span class="keyword">int</span> M, <span class="keyword">const</span> <span class="keyword">int</span> N, <span class="keyword">const</span> Dtype *label_data, <span class="keyword">const</span> Dtype *top_data, Dtype *wx_data)</span> </span>&#123;</span><br><span class="line">  CUDA_KERNEL_LOOP(index, M) &#123;</span><br><span class="line">    wx_data[index] = top_data[index * N + <span class="keyword">static_cast</span>&lt;<span class="keyword">int</span>&gt;(label_data[index])];</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">typename</span> Dtype&gt;</span><br><span class="line">__<span class="function">global__ <span class="keyword">void</span> <span class="title">cal_cos_mt</span><span class="params">(<span class="keyword">const</span> <span class="keyword">int</span> count, <span class="keyword">const</span> <span class="keyword">unsigned</span> <span class="keyword">int</span> margin, <span class="keyword">const</span> <span class="keyword">int</span> *C_M_N, <span class="keyword">const</span> Dtype *cos_t_data, Dtype *cos_mt_data)</span> </span>&#123;</span><br><span class="line">  CUDA_KERNEL_LOOP(index, count) &#123;</span><br><span class="line">    Dtype <span class="keyword">cos_t</span> = cos_t_data[index];</span><br><span class="line">    Dtype sin_t_2 = <span class="number">1</span> - <span class="keyword">cos_t</span> * <span class="keyword">cos_t</span>;</span><br><span class="line">    Dtype cos_mt = <span class="number">0.</span>;</span><br><span class="line">    <span class="keyword">int</span> flag = <span class="number">-1</span>;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> n = <span class="number">0</span>; n &lt;= (margin / <span class="number">2</span>); ++ n) &#123;</span><br><span class="line">      flag *= <span class="number">-1</span>;</span><br><span class="line">      cos_mt += flag * C_M_N[<span class="number">2</span> * n] * powf(<span class="keyword">cos_t</span>, (margin - <span class="number">2</span> * n)) * powf(sin_t_2, n);</span><br><span class="line">    &#125;</span><br><span class="line">    cos_mt_data[index] = cos_mt;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">typename</span> Dtype&gt;</span><br><span class="line">__<span class="function">global__ <span class="keyword">void</span> <span class="title">LMForward</span><span class="params">(</span></span></span><br><span class="line"><span class="function"><span class="params">  <span class="keyword">const</span> <span class="keyword">int</span> M, <span class="keyword">const</span> <span class="keyword">int</span> N, <span class="keyword">const</span> <span class="keyword">float</span> lambda,</span></span></span><br><span class="line"><span class="function"><span class="params">  <span class="keyword">const</span> Dtype *label_data, <span class="keyword">const</span> Dtype *cos_mt_data, <span class="keyword">const</span> <span class="keyword">int</span> *k_data,</span></span></span><br><span class="line"><span class="function"><span class="params">  <span class="keyword">const</span> Dtype *abs_w_data, <span class="keyword">const</span> Dtype *abs_x_data, Dtype *top_data)</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">  CUDA_KERNEL_LOOP(index, M) &#123;</span><br><span class="line">    Dtype cos_mt = cos_mt_data[index];</span><br><span class="line">    <span class="keyword">int</span> k = k_data[index];</span><br><span class="line">    <span class="keyword">int</span> label = <span class="keyword">static_cast</span>&lt;<span class="keyword">int</span>&gt;(label_data[index]);</span><br><span class="line">    Dtype abs_w = abs_w_data[index];</span><br><span class="line">    Dtype abs_x = abs_x_data[index];</span><br><span class="line">    top_data[N * index + label] =  (lambda * top_data[N * index + label] + abs_w * abs_x * ( powf(<span class="number">-1</span>, k) * cos_mt - <span class="number">2</span> * k )) / (<span class="number">1</span> + lambda);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">typename</span> Dtype&gt;</span><br><span class="line"><span class="keyword">void</span> LargeMarginInnerProductLayer&lt;Dtype&gt;::Forward_gpu(<span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; bottom,</span><br><span class="line">    <span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; top) &#123;</span><br><span class="line">  <span class="keyword">const</span> Dtype* bottom_data = bottom[<span class="number">0</span>]-&gt;gpu_data();</span><br><span class="line">  <span class="keyword">const</span> Dtype* label_data = bottom[<span class="number">1</span>]-&gt;gpu_data();</span><br><span class="line">  Dtype* top_data = top[<span class="number">0</span>]-&gt;mutable_gpu_data();</span><br><span class="line">  <span class="keyword">const</span> Dtype* weight = <span class="keyword">this</span>-&gt;blobs_[<span class="number">0</span>]-&gt;gpu_data();</span><br><span class="line"></span><br><span class="line">  <span class="comment">// 普通fc层的计算</span></span><br><span class="line">  <span class="keyword">if</span> (M_ == <span class="number">1</span>) &#123;</span><br><span class="line">    caffe_gpu_gemv&lt;Dtype&gt;(CblasNoTrans, N_, K_, (Dtype)<span class="number">1.</span>,</span><br><span class="line">                         weight, bottom_data, (Dtype)<span class="number">0.</span>, top_data);</span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    caffe_gpu_gemm&lt;Dtype&gt;(CblasNoTrans,</span><br><span class="line">                          transpose_ ? CblasNoTrans : CblasTrans,</span><br><span class="line">                          M_, N_, K_, (Dtype)<span class="number">1.</span>,</span><br><span class="line">                          bottom_data, weight, (Dtype)<span class="number">0.</span>, top_data);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">const</span> Dtype* label_cpu_data = bottom[<span class="number">1</span>]-&gt;cpu_data();</span><br><span class="line"></span><br><span class="line">  <span class="comment">// w * x</span></span><br><span class="line">  <span class="comment">// 直接从前馈的结果中复制</span></span><br><span class="line">  Dtype *wx_data = <span class="keyword">this</span>-&gt;wx_.mutable_gpu_data();</span><br><span class="line">  copy_label_score&lt;Dtype&gt;&lt;&lt;&lt;CAFFE_GET_BLOCKS(M_), CAFFE_CUDA_NUM_THREADS&gt;&gt;&gt;(M_, N_, label_data, top_data, wx_data);</span><br><span class="line"></span><br><span class="line">  <span class="comment">// w * w</span></span><br><span class="line">  Dtype *abs_w_data = <span class="keyword">this</span>-&gt;abs_w_.mutable_cpu_data();</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">int</span> m = <span class="number">0</span>; m &lt; M_; ++ m) &#123;</span><br><span class="line">    abs_w_data[m] = caffe_cpu_dot&lt;Dtype&gt;(</span><br><span class="line">      K_,</span><br><span class="line">      <span class="keyword">this</span>-&gt;blobs_[<span class="number">0</span>]-&gt;cpu_data() + <span class="keyword">static_cast</span>&lt;<span class="keyword">int</span>&gt;(label_cpu_data[m]) * K_,</span><br><span class="line">      <span class="keyword">this</span>-&gt;blobs_[<span class="number">0</span>]-&gt;cpu_data() + <span class="keyword">static_cast</span>&lt;<span class="keyword">int</span>&gt;(label_cpu_data[m]) * K_</span><br><span class="line">      );</span><br><span class="line">  &#125;</span><br><span class="line">  </span><br><span class="line">  <span class="comment">// x * x</span></span><br><span class="line">  Dtype *abs_x_data = <span class="keyword">this</span>-&gt;abs_x_.mutable_cpu_data();</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">int</span> m = <span class="number">0</span>; m &lt; M_; ++ m) &#123;</span><br><span class="line">    abs_x_data[m] = caffe_cpu_dot&lt;Dtype&gt;(</span><br><span class="line">      K_,</span><br><span class="line">      bottom[<span class="number">0</span>]-&gt;cpu_data() + m * K_,</span><br><span class="line">      bottom[<span class="number">0</span>]-&gt;cpu_data() + m * K_</span><br><span class="line">      );</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// abs_w, abs_x</span></span><br><span class="line">  caffe_gpu_powx&lt;Dtype&gt;(M_, <span class="keyword">this</span>-&gt;abs_w_.mutable_gpu_data(), <span class="number">0.5</span>, <span class="keyword">this</span>-&gt;abs_w_.mutable_gpu_data());</span><br><span class="line">  caffe_gpu_powx&lt;Dtype&gt;(M_, <span class="keyword">this</span>-&gt;abs_x_.mutable_gpu_data(), <span class="number">0.5</span>, <span class="keyword">this</span>-&gt;abs_x_.mutable_gpu_data());</span><br><span class="line"></span><br><span class="line">  <span class="comment">// cos_t = wx / (|x| * |w|)</span></span><br><span class="line">  Dtype *cos_t_data = <span class="keyword">this</span>-&gt;cos_t_.mutable_gpu_data();</span><br><span class="line">  caffe_gpu_div&lt;Dtype&gt;(M_, wx_data, <span class="keyword">this</span>-&gt;abs_x_.gpu_data(), cos_t_data);</span><br><span class="line">  caffe_gpu_div&lt;Dtype&gt;(M_, cos_t_data, <span class="keyword">this</span>-&gt;abs_w_.gpu_data(), cos_t_data);</span><br><span class="line"></span><br><span class="line">  <span class="comment">// cos(mt)</span></span><br><span class="line">  cal_cos_mt&lt;Dtype&gt;&lt;&lt;&lt;CAFFE_GET_BLOCKS(M_), CAFFE_CUDA_NUM_THREADS&gt;&gt;&gt;(</span><br><span class="line">    M_, <span class="keyword">this</span>-&gt;margin,</span><br><span class="line">    <span class="keyword">this</span>-&gt;C_M_N_.gpu_data(),</span><br><span class="line">    <span class="keyword">this</span>-&gt;cos_t_.gpu_data(),</span><br><span class="line">    <span class="keyword">this</span>-&gt;cos_mt_.mutable_gpu_data()</span><br><span class="line">    );</span><br><span class="line"></span><br><span class="line">  <span class="comment">// k</span></span><br><span class="line">  <span class="keyword">int</span> *k_cpu_data = <span class="keyword">this</span>-&gt;k_.mutable_cpu_data();</span><br><span class="line">  <span class="keyword">const</span> Dtype *cos_t_cpu_data = <span class="keyword">this</span>-&gt;cos_t_.cpu_data();</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">int</span> m = <span class="number">0</span>; m &lt; M_; ++ m) &#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> _k = <span class="number">0</span>; _k &lt; <span class="keyword">this</span>-&gt;cos_theta_bound_.count(); ++ _k) &#123;</span><br><span class="line">      <span class="keyword">if</span> (<span class="keyword">this</span>-&gt;cos_theta_bound_.cpu_data()[_k] &lt; cos_t_cpu_data[m]) &#123;</span><br><span class="line">        k_cpu_data[m] = _k - <span class="number">1</span>;</span><br><span class="line">        <span class="keyword">break</span>;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// y</span></span><br><span class="line">  LMForward&lt;Dtype&gt;&lt;&lt;&lt;CAFFE_GET_BLOCKS(M_), CAFFE_CUDA_NUM_THREADS&gt;&gt;&gt;(</span><br><span class="line">    M_, N_, <span class="keyword">this</span>-&gt;lambda,</span><br><span class="line">    label_data, <span class="keyword">this</span>-&gt;cos_mt_.gpu_data(), <span class="keyword">this</span>-&gt;k_.gpu_data(),</span><br><span class="line">    <span class="keyword">this</span>-&gt;abs_w_.gpu_data(), <span class="keyword">this</span>-&gt;abs_x_.gpu_data(), top[<span class="number">0</span>]-&gt;mutable_gpu_data());</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>那么，这样关于large margin softmax loss的前馈我们就轻松的实现了。下一篇，我们要讲最复杂的后馈的实现了。</p>
<p>如果您觉得本文对您有帮助，那请小喵喝杯茶吧~~O(∩_∩)O~~ 再次感慨 <span class="math inline">\(\LaTeX\)</span> 大法好。</p>
<p><strong>转载</strong> 请注明出处~</p>
<p>关于Large margin softmax loss的后馈的部分，小喵一直实现的有问题，现在作者也把代码开源出来了，建议还是直接看作者的代码吧。</p>
]]></content>
      <categories>
        <category>Deep Learning</category>
      </categories>
  </entry>
  <entry>
    <title>基于Caffe的Large Margin Softmax Loss的实现（一）</title>
    <url>/2016/10/01/caffe-large-margin-softmax-1/</url>
    <content><![CDATA[<blockquote>
<p>小喵的唠叨话：在写完上一次的博客之后，已经过去了2个月的时间，小喵在此期间，做了大量的实验工作，最终在使用的DeepID2的方法之后，取得了很不错的结果。这次呢，主要讲述一个比较新的论文中的方法，L-Softmax，据说单model在LFW上能达到98.71%的等错误率。更重要的是，小喵觉得这个方法和DeepID2并不冲突，如果二者可以互补，或许单model达到99%+将不是梦想。</p>
</blockquote>
<a id="more"></a>
<p>和上一篇博客一样，小喵对读者做了如下的假定：</p>
<ol type="1">
<li>了解Deep Learning的基本知识。</li>
<li>仔细阅读过L-Softmax的论文，了解其中的数学推导。</li>
<li>使用Caffe作为训练框架。</li>
<li>即使不满足上述3条，也能持之以恒的学习。</li>
</ol>
<p>L-Softmax的论文：Large-Margin Softmax Loss for Convolutional Neutral Networks Google 一下，第一条应该就是论文的地址，鉴于大家时间有限，小喵把原文地址也贴出来了，但不保证长期有效。<span class="exturl" data-url="aHR0cDovL2ptbHIub3JnL3Byb2NlZWRpbmdzL3BhcGVycy92NDgvbGl1ZDE2LnBkZg==" title="http://jmlr.org/proceedings/papers/v48/liud16.pdf">http://jmlr.org/proceedings/papers/v48/liud16.pdf<i class="fa fa-external-link"></i></span> 这里我们也将整个系列分几部分来讲。</p>
<h2 id="一margin与lambda">一、margin与lambda</h2>
<p>margin和lambda这两个参数是我们这篇博客的重点。也是整篇论文的重点。对于分类的任务，每个样本都会有N的输出的分数（N的类别），如果在训练中，人为的使正确类别的得分变小，也就是说加大了区分正确类别的难度，那么网络就会学习出更有区分能力的特征，并且加大类间的距离。作者选用的加大难度的方式就是改变最后一个FC层中的weight和特征之间的角度值，角度增大的倍数就是margin，从而使特定类别的得分变小。而第二个参数lambda是为了避免网络不收敛而设定的，我们之后会讲到。</p>
<p>为了实现这个效果，我们需要设计一个新的层，<code>large_margin_inner_product_layer</code>。这个层和一般的 <code>inner_product_layer</code> 很相似，但是多了特定类别削弱的功能。</p>
<p>考虑到这个层是有参数的，我们需要在 <code>caffe.proto</code>（<code>caffe_home/src/caffe/proto/caffe.proto</code>）中做一些修改。这里的定义是按照 <code>protobuf</code> 的语法写的，简单的修改只要照着其他的参数来改写就好。</p>
<p>首先定义我们的这个层的参数。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">message LargeMarginInnerProductParameter &#123;</span><br><span class="line">  optional uint32 num_output &#x3D; 1; &#x2F;&#x2F; The number of outputs for the layer</span><br><span class="line">  optional bool bias_term &#x3D; 2 [default &#x3D; true]; &#x2F;&#x2F; whether to have bias terms</span><br><span class="line">  optional FillerParameter weight_filler &#x3D; 3; &#x2F;&#x2F; The filler for the weight</span><br><span class="line">  optional FillerParameter bias_filler &#x3D; 4; &#x2F;&#x2F; The filler for the bias</span><br><span class="line"></span><br><span class="line">  &#x2F;&#x2F; The first axis to be lumped into a single inner product computation;</span><br><span class="line">  &#x2F;&#x2F; all preceding axes are retained in the output.</span><br><span class="line">  &#x2F;&#x2F; May be negative to index from the end (e.g., -1 for the last axis).</span><br><span class="line">  optional int32 axis &#x3D; 5 [default &#x3D; 1];</span><br><span class="line">  &#x2F;&#x2F; Specify whether to transpose the weight matrix or not.</span><br><span class="line">  &#x2F;&#x2F; If transpose &#x3D;&#x3D; true, any operations will be performed on the transpose</span><br><span class="line">  &#x2F;&#x2F; of the weight matrix. The weight matrix itself is not going to be transposed</span><br><span class="line">  &#x2F;&#x2F; but rather the transfer flag of operations will be toggled accordingly.</span><br><span class="line">  optional bool transpose &#x3D; 6 [default &#x3D; false];</span><br><span class="line">  optional uint32 margin &#x3D; 7 [default &#x3D; 1];</span><br><span class="line">  optional float lambda &#x3D; 8 [default &#x3D; 0];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>参数的定义和 <code>InnerProductParameter</code> 非常相似，只是多了两个参数 <code>margin</code> 和 <code>lambda</code>。</p>
<p>之后在 <code>LayerParameter</code> 添加一个可选参数（照着 <code>InnerProductParameter</code> 写就好）。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">optional LargeMarginInnerProductParameter large_margin_inner_product_param &#x3D; 147;</span><br></pre></td></tr></table></figure>
<p>这时，喵粉可能很在意这个147是怎么回事。其实呢，在protobuf中，每个结构中的变量都需要一个id，只要保证不重复即可。我们在LayerParameter的最开始可以看到这么一行注释：</p>
<p><img data-src="http://cdn.miaoerduo.com/images/next-availabel-layer-id.jpg" alt="next-availabel-layer-id" /></p>
<p>说明下一个有效的id是147。这里我们新加的参数就果断占用了这个id。修改之后，建议把注释改一下（不要人为的挖坑）：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">LayerParameter next available layer-specific ID: 148 (last added: large_margin_inner_product_param)</span><br></pre></td></tr></table></figure>
<p>避免之后再新加层的时候出问题。 工作完毕，我们就可以在 <code>train_val.prototxt</code> 中用这种方式使用这个新层了（具体的使用，后面再说）：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">layer &#123;</span><br><span class="line">  name: &quot;fc2&quot;</span><br><span class="line">  type: &quot;LargeMarginInnerProduct&quot;</span><br><span class="line">  bottom: &quot;fc1&quot;</span><br><span class="line">  bottom: &quot;label&quot;</span><br><span class="line">  top: &quot;fc2&quot;</span><br><span class="line">  param &#123;</span><br><span class="line">    lr_mult: 1</span><br><span class="line">    decay_mult: 1</span><br><span class="line">  &#125;</span><br><span class="line">  param &#123;</span><br><span class="line">    lr_mult: 0</span><br><span class="line">    decay_mult: 0</span><br><span class="line">  &#125;</span><br><span class="line">  large_margin_inner_product_param &#123;</span><br><span class="line">    num_output: 10000</span><br><span class="line">    margin: 2</span><br><span class="line">    lambda: 0</span><br><span class="line">    weight_filler &#123;</span><br><span class="line">      type: &quot;xavier&quot;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h2 id="二运筹帷幄之成员变量">二，运筹帷幄之成员变量</h2>
<p>我们刚刚在 <code>caffe.proto</code> 中，添加了新参数的定义。而事实上，我们还没有这个层的具体实现。这部分，主要介绍我们需要的临时变量。</p>
<p>首先，我们要理清整个计算的流程。</p>
<p>先看前馈。</p>
<p>第一步，需要求出W和x的夹角的余弦值：</p>
<p><span class="math display">\[
cos(\theta_j)=\frac{W_j^Tx_i}{\|W_j\|\|x_i\|} 
\]</span></p>
<p>第二步，计算m倍角度的余弦值：</p>
<p><span class="math display">\[
\cos(m\theta_i)=\sum_n(-1)^n{C_m^{2n}\cos^{m-2n}(\theta_i)\cdot(1-\cos(\theta_i)^2)^n}, (2n\leq m)
\]</span></p>
<p>第三步，计算前馈：</p>
<p><span class="math display">\[
f_{y_{i}}=(-1)^k\cdot\|W_{y_{i}}\|\|x_{i}\|\cos(m\theta_i)-2k\cdot\|W_{y_i}\|\|x_i\|
\]</span></p>
<p>k是根据 <span class="math inline">\(\cos(\theta)\)</span> 的取值决定的。</p>
<p>后馈比前馈要复杂一些，不过使用的变量也是一样的。因此我们可以编写自己的头文件了。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">ifndef</span> CAFFE_LARGE_MARGIN_INNER_PRODUCT_LAYER_HPP_</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> CAFFE_LARGE_MARGIN_INNER_PRODUCT_LAYER_HPP_</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;vector&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"caffe/blob.hpp"</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"caffe/layer.hpp"</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"caffe/proto/caffe.pb.h"</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">namespace</span> caffe &#123;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">typename</span> Dtype&gt;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">LargeMarginInnerProductLayer</span> :</span> <span class="keyword">public</span> Layer&lt;Dtype&gt; &#123;</span><br><span class="line"> <span class="keyword">public</span>:</span><br><span class="line">  <span class="function"><span class="keyword">explicit</span> <span class="title">LargeMarginInnerProductLayer</span><span class="params">(<span class="keyword">const</span> LayerParameter&amp; param)</span></span></span><br><span class="line">      : Layer&lt;Dtype&gt;(param) &#123;&#125;</span><br><span class="line">  <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">LayerSetUp</span><span class="params">(<span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; bottom,</span></span></span><br><span class="line"><span class="function"><span class="params">      <span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; top)</span></span>;</span><br><span class="line">  <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">Reshape</span><span class="params">(<span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; bottom,</span></span></span><br><span class="line"><span class="function"><span class="params">      <span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; top)</span></span>;</span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">virtual</span> <span class="keyword">inline</span> <span class="keyword">const</span> <span class="keyword">char</span>* <span class="title">type</span><span class="params">()</span> <span class="keyword">const</span> </span>&#123; <span class="keyword">return</span> <span class="string">"LargeMarginInnerProduct"</span>; &#125;</span><br><span class="line">  <span class="comment">// edited by miao</span></span><br><span class="line">  <span class="comment">// LM_FC层有两个bottom</span></span><br><span class="line">  <span class="function"><span class="keyword">virtual</span> <span class="keyword">inline</span> <span class="keyword">int</span> <span class="title">ExactNumBottomBlobs</span><span class="params">()</span> <span class="keyword">const</span> </span>&#123; <span class="keyword">return</span> <span class="number">2</span>; &#125;</span><br><span class="line">  <span class="comment">// end edited</span></span><br><span class="line">  <span class="function"><span class="keyword">virtual</span> <span class="keyword">inline</span> <span class="keyword">int</span> <span class="title">ExactNumTopBlobs</span><span class="params">()</span> <span class="keyword">const</span> </span>&#123; <span class="keyword">return</span> <span class="number">1</span>; &#125;</span><br><span class="line"></span><br><span class="line"> <span class="keyword">protected</span>:</span><br><span class="line">  <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">Forward_cpu</span><span class="params">(<span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; bottom,</span></span></span><br><span class="line"><span class="function"><span class="params">      <span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; top)</span></span>;</span><br><span class="line">  <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">Forward_gpu</span><span class="params">(<span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; bottom,</span></span></span><br><span class="line"><span class="function"><span class="params">      <span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; top)</span></span>;</span><br><span class="line">  <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">Backward_cpu</span><span class="params">(<span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; top,</span></span></span><br><span class="line"><span class="function"><span class="params">      <span class="keyword">const</span> <span class="built_in">vector</span>&lt;<span class="keyword">bool</span>&gt;&amp; propagate_down, <span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; bottom)</span></span>;</span><br><span class="line">  <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">Backward_gpu</span><span class="params">(<span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; top,</span></span></span><br><span class="line"><span class="function"><span class="params">      <span class="keyword">const</span> <span class="built_in">vector</span>&lt;<span class="keyword">bool</span>&gt;&amp; propagate_down, <span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; bottom)</span></span>;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">int</span> M_;</span><br><span class="line">  <span class="keyword">int</span> K_;</span><br><span class="line">  <span class="keyword">int</span> N_;</span><br><span class="line">  <span class="keyword">bool</span> bias_term_;</span><br><span class="line">  Blob&lt;Dtype&gt; bias_multiplier_;</span><br><span class="line">  <span class="keyword">bool</span> transpose_;  <span class="comment">///&lt; if true, assume transposed weights</span></span><br><span class="line"></span><br><span class="line">  <span class="comment">// added by miao</span></span><br><span class="line"></span><br><span class="line">  <span class="comment">// 一些常数</span></span><br><span class="line">  Blob&lt;Dtype&gt; cos_theta_bound_;   <span class="comment">// 区间边界的cos值</span></span><br><span class="line">  Blob&lt;<span class="keyword">int</span>&gt; k_;                   <span class="comment">// 当前角度theta所在的区间的位置</span></span><br><span class="line">  Blob&lt;<span class="keyword">int</span>&gt; C_M_N_;               <span class="comment">// 组合数</span></span><br><span class="line">  <span class="keyword">unsigned</span> <span class="keyword">int</span> margin;            <span class="comment">// margin</span></span><br><span class="line">  <span class="keyword">float</span> lambda;                   <span class="comment">// lambda</span></span><br><span class="line"></span><br><span class="line">  Blob&lt;Dtype&gt; wx_;                <span class="comment">// wjT * xi</span></span><br><span class="line">  Blob&lt;Dtype&gt; abs_w_;             <span class="comment">// ||wj||</span></span><br><span class="line">  Blob&lt;Dtype&gt; abs_x_;             <span class="comment">// ||xi||</span></span><br><span class="line">  Blob&lt;Dtype&gt; cos_t_;             <span class="comment">// cos(theta)</span></span><br><span class="line">  Blob&lt;Dtype&gt; cos_mt_;            <span class="comment">// cos(margin * theta)</span></span><br><span class="line"></span><br><span class="line">  Blob&lt;Dtype&gt; dydw_;              <span class="comment">// 输出对w的导数</span></span><br><span class="line">  Blob&lt;Dtype&gt; dydx_;              <span class="comment">// 输出对x的导数</span></span><br><span class="line">  <span class="comment">// end added</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">&#125;  <span class="comment">// namespace caffe</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">endif</span>  <span class="comment">// CAFFE_LARGE_MARGIN_INNER_PRODUCT_LAYER_HPP_</span></span></span><br></pre></td></tr></table></figure>
<p>这里主要是复制了 <code>inner_product_layer.hpp</code>，然后做了一点修改。具体是增加了几个成员变量，同时改了 <code>ExactNumBottomBlobs</code> 的返回值，因为我们的这个层磁带 <code>bottom</code> 需要两个，前一层的 <code>feature</code> 和样本的 <code>label</code>。</p>
<h2 id="三内存和常量的初始化">三、内存和常量的初始化</h2>
<p>这部分，主要给我们的各个成员变量分配内存，同时给几个常量进行初始化。这里也是照着 <code>inner_product_layer.cpp</code> 来写的，在 <code>setup</code> 的时候，增加了一些用于初始化的代码，并删除了 <code>forward_cpu</code> 和 <code>backwark_cpu</code> 的具体实现。</p>
<p>修改之后的代码如下：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;vector&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;cmath&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"caffe/filler.hpp"</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"caffe/layers/large_margin_inner_product_layer.hpp"</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"caffe/util/math_functions.hpp"</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> PI 3.14159265</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">namespace</span> caffe &#123;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">factorial</span><span class="params">(<span class="keyword">int</span> n)</span> </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (<span class="number">0</span> == n) <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">  <span class="keyword">int</span> f = <span class="number">1</span>;</span><br><span class="line">  <span class="keyword">while</span> (n) &#123;</span><br><span class="line">    f *= n;</span><br><span class="line">    -- n;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> f;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">typename</span> Dtype&gt;</span><br><span class="line"><span class="keyword">void</span> LargeMarginInnerProductLayer&lt;Dtype&gt;::LayerSetUp(<span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; bottom,</span><br><span class="line">      <span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; top) &#123;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">const</span> <span class="keyword">int</span> axis = bottom[<span class="number">0</span>]-&gt;CanonicalAxisIndex(</span><br><span class="line">      <span class="keyword">this</span>-&gt;layer_param_.large_margin_inner_product_param().axis());</span><br><span class="line">  <span class="comment">// added by miao</span></span><br><span class="line">  <span class="built_in">std</span>::<span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt; wx_shape(<span class="number">1</span>);</span><br><span class="line">  wx_shape[<span class="number">0</span>] = bottom[<span class="number">0</span>]-&gt;shape(<span class="number">0</span>);</span><br><span class="line">  <span class="keyword">this</span>-&gt;wx_.Reshape(wx_shape);</span><br><span class="line">  <span class="keyword">this</span>-&gt;abs_w_.Reshape(wx_shape);</span><br><span class="line">  <span class="keyword">this</span>-&gt;abs_x_.Reshape(wx_shape);</span><br><span class="line">  <span class="keyword">this</span>-&gt;k_.Reshape(wx_shape);</span><br><span class="line">  <span class="keyword">this</span>-&gt;cos_t_.Reshape(wx_shape);</span><br><span class="line">  <span class="keyword">this</span>-&gt;cos_mt_.Reshape(wx_shape);</span><br><span class="line"></span><br><span class="line">  <span class="built_in">std</span>::<span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt; cos_theta_bound_shape(<span class="number">1</span>);</span><br><span class="line">  <span class="keyword">this</span>-&gt;margin = <span class="keyword">static_cast</span>&lt;<span class="keyword">unsigned</span> <span class="keyword">int</span>&gt;(<span class="keyword">this</span>-&gt;layer_param_.large_margin_inner_product_param().margin());</span><br><span class="line">  cos_theta_bound_shape[<span class="number">0</span>] = <span class="keyword">this</span>-&gt;margin + <span class="number">1</span>;</span><br><span class="line">  <span class="keyword">this</span>-&gt;cos_theta_bound_.Reshape(cos_theta_bound_shape);</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">int</span> k = <span class="number">0</span>; k &lt;= <span class="keyword">this</span>-&gt;margin; ++ k) &#123;</span><br><span class="line">    <span class="keyword">this</span>-&gt;cos_theta_bound_.mutable_cpu_data()[k] = <span class="built_in">std</span>::<span class="built_in">cos</span>(PI * k / <span class="keyword">this</span>-&gt;margin);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">this</span>-&gt;C_M_N_.Reshape(cos_theta_bound_shape);</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">int</span> n = <span class="number">0</span>; n &lt;= <span class="keyword">this</span>-&gt;margin; ++ n) &#123;</span><br><span class="line">    <span class="keyword">this</span>-&gt;C_M_N_.mutable_cpu_data()[n] = factorial(<span class="keyword">this</span>-&gt;margin) / factorial(<span class="keyword">this</span>-&gt;margin - n) / factorial(n);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// d size</span></span><br><span class="line">  <span class="built_in">std</span>::<span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt; d_shape(<span class="number">2</span>);</span><br><span class="line">  d_shape[<span class="number">0</span>] = bottom[<span class="number">0</span>]-&gt;shape(<span class="number">0</span>);</span><br><span class="line">  d_shape[<span class="number">1</span>] = bottom[<span class="number">0</span>]-&gt;count(axis);</span><br><span class="line">  <span class="keyword">this</span>-&gt;dydw_.Reshape(d_shape);</span><br><span class="line">  <span class="keyword">this</span>-&gt;dydx_.Reshape(d_shape);</span><br><span class="line"></span><br><span class="line">  <span class="keyword">this</span>-&gt;lambda = <span class="keyword">this</span>-&gt;layer_param_.large_margin_inner_product_param().lambda();</span><br><span class="line">  <span class="comment">// end added</span></span><br><span class="line"></span><br><span class="line">  transpose_ = <span class="literal">false</span>; <span class="comment">// 坚决不转置！</span></span><br><span class="line"></span><br><span class="line">  <span class="keyword">const</span> <span class="keyword">int</span> num_output = <span class="keyword">this</span>-&gt;layer_param_.large_margin_inner_product_param().num_output();</span><br><span class="line">  bias_term_ = <span class="keyword">this</span>-&gt;layer_param_.large_marin_inner_product_param().bias_term();</span><br><span class="line">  N_ = num_output;</span><br><span class="line">  </span><br><span class="line">  <span class="comment">// Dimensions starting from "axis" are "flattened" into a single</span></span><br><span class="line">  <span class="comment">// length K_ vector. For example, if bottom[0]'s shape is (N, C, H, W),</span></span><br><span class="line">  <span class="comment">// and axis == 1, N inner products with dimension CHW are performed.</span></span><br><span class="line">  K_ = bottom[<span class="number">0</span>]-&gt;count(axis);</span><br><span class="line">  <span class="comment">// Check if we need to set up the weights</span></span><br><span class="line">  <span class="keyword">if</span> (<span class="keyword">this</span>-&gt;blobs_.size() &gt; <span class="number">0</span>) &#123;</span><br><span class="line">    LOG(INFO) &lt;&lt; <span class="string">"Skipping parameter initialization"</span>;</span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (bias_term_) &#123;</span><br><span class="line">      <span class="keyword">this</span>-&gt;blobs_.resize(<span class="number">2</span>);</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      <span class="keyword">this</span>-&gt;blobs_.resize(<span class="number">1</span>);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// Initialize the weights</span></span><br><span class="line">    <span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt; weight_shape(<span class="number">2</span>);</span><br><span class="line">    <span class="keyword">if</span> (transpose_) &#123;</span><br><span class="line">      weight_shape[<span class="number">0</span>] = K_;</span><br><span class="line">      weight_shape[<span class="number">1</span>] = N_;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      weight_shape[<span class="number">0</span>] = N_;</span><br><span class="line">      weight_shape[<span class="number">1</span>] = K_;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">this</span>-&gt;blobs_[<span class="number">0</span>].reset(<span class="keyword">new</span> Blob&lt;Dtype&gt;(weight_shape));</span><br><span class="line">    <span class="comment">// fill the weights</span></span><br><span class="line">    <span class="built_in">shared_ptr</span>&lt;Filler&lt;Dtype&gt; &gt; weight_filler(GetFiller&lt;Dtype&gt;(</span><br><span class="line">        <span class="keyword">this</span>-&gt;layer_param_.large_margin_inner_product_param().weight_filler()));</span><br><span class="line">    weight_filler-&gt;Fill(<span class="keyword">this</span>-&gt;blobs_[<span class="number">0</span>].get());</span><br><span class="line">    <span class="comment">// If necessary, intiialize and fill the bias term</span></span><br><span class="line">    <span class="keyword">if</span> (bias_term_) &#123;</span><br><span class="line">      <span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt; bias_shape(<span class="number">1</span>, N_);</span><br><span class="line">      <span class="keyword">this</span>-&gt;blobs_[<span class="number">1</span>].reset(<span class="keyword">new</span> Blob&lt;Dtype&gt;(bias_shape));</span><br><span class="line">      <span class="built_in">shared_ptr</span>&lt;Filler&lt;Dtype&gt; &gt; bias_filler(GetFiller&lt;Dtype&gt;(</span><br><span class="line">          <span class="keyword">this</span>-&gt;layer_param_.large_margin_inner_product_param().bias_filler()));</span><br><span class="line">      bias_filler-&gt;Fill(<span class="keyword">this</span>-&gt;blobs_[<span class="number">1</span>].get());</span><br><span class="line">    &#125;   </span><br><span class="line"></span><br><span class="line">  &#125;  <span class="comment">// parameter initialization</span></span><br><span class="line">  <span class="keyword">this</span>-&gt;param_propagate_down_.resize(<span class="keyword">this</span>-&gt;blobs_.size(), <span class="literal">true</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">typename</span> Dtype&gt;</span><br><span class="line"><span class="keyword">void</span> LargeMarginInnerProductLayer&lt;Dtype&gt;::Reshape(<span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; bottom,</span><br><span class="line">      <span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; top) &#123;</span><br><span class="line">  <span class="comment">// Figure out the dimensions</span></span><br><span class="line">  <span class="keyword">const</span> <span class="keyword">int</span> axis = bottom[<span class="number">0</span>]-&gt;CanonicalAxisIndex(</span><br><span class="line">      <span class="keyword">this</span>-&gt;layer_param_.large_margin_inner_product_param().axis());</span><br><span class="line">  <span class="keyword">const</span> <span class="keyword">int</span> new_K = bottom[<span class="number">0</span>]-&gt;count(axis);</span><br><span class="line">  CHECK_EQ(K_, new_K)</span><br><span class="line">      &lt;&lt; <span class="string">"Input size incompatible with large margin inner product parameters."</span>;</span><br><span class="line">  <span class="comment">// The first "axis" dimensions are independent inner products; the total</span></span><br><span class="line">  <span class="comment">// number of these is M_, the product over these dimensions.</span></span><br><span class="line">  M_ = bottom[<span class="number">0</span>]-&gt;count(<span class="number">0</span>, axis);</span><br><span class="line">  <span class="comment">// The top shape will be the bottom shape with the flattened axes dropped,</span></span><br><span class="line">  <span class="comment">// and replaced by a single axis with dimension num_output (N_).</span></span><br><span class="line">  <span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt; top_shape = bottom[<span class="number">0</span>]-&gt;shape();</span><br><span class="line">  top_shape.resize(axis + <span class="number">1</span>);</span><br><span class="line">  top_shape[axis] = N_;</span><br><span class="line">  top[<span class="number">0</span>]-&gt;Reshape(top_shape);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">typename</span> Dtype&gt;</span><br><span class="line"><span class="keyword">void</span> LargeMarginInnerProductLayer&lt;Dtype&gt;::Forward_cpu(<span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; bottom,</span><br><span class="line">    <span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; top) &#123;</span><br><span class="line">  <span class="comment">// not implement</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">typename</span> Dtype&gt;</span><br><span class="line"><span class="keyword">void</span> LargeMarginInnerProductLayer&lt;Dtype&gt;::Backward_cpu(<span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; top,</span><br><span class="line">    <span class="keyword">const</span> <span class="built_in">vector</span>&lt;<span class="keyword">bool</span>&gt;&amp; propagate_down,</span><br><span class="line">    <span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; bottom) &#123;</span><br><span class="line">  <span class="comment">// not implement</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">ifdef</span> CPU_ONLY</span></span><br><span class="line">STUB_GPU(LargeMarginInnerProductLayer);</span><br><span class="line"><span class="meta">#<span class="meta-keyword">endif</span></span></span><br><span class="line"></span><br><span class="line">INSTANTIATE_CLASS(LargeMarginInnerProductLayer);</span><br><span class="line">REGISTER_LAYER_CLASS(LargeMarginInnerProduct);</span><br><span class="line"></span><br><span class="line">&#125;  <span class="comment">// namespace caffe</span></span><br></pre></td></tr></table></figure>
<p>至此，<code>large_margin_inner_product_layer</code> 的准备工作就做完了。</p>
<p>下一篇博客，我们来详细的讨论前馈的具体实现。</p>
<p>如果您觉得本文对您有帮助，</p>
<p><strong>转载</strong> 请注明出处~</p>
]]></content>
      <categories>
        <category>Deep Learning</category>
      </categories>
  </entry>
  <entry>
    <title>基于Caffe的DeepID2实现（下）</title>
    <url>/2016/07/15/deepid2-3/</url>
    <content><![CDATA[<blockquote>
<p>小喵的唠叨话：这次的博客，真心累伤了小喵的心。但考虑到知识需要巩固和分享，小喵决定这次把剩下的内容都写完。</p>
</blockquote>
<a id="more"></a>
<h2 id="四数据的重整简单的划分">四、数据的重整，简单的划分</h2>
<p>前面的Data层用于生成成对的输入数据，Normalization层，用于将feature归一化，那么之后是不是就可以使用ContrastiveLoss层进行训练了呢？</p>
<p>且慢，还差一步。</p>
<p>ContrastiveLoss层要求有3个bottom：feature1、feature2以及表示对位的feature是否为同一个identity的label。</p>
<p>我们现在得到的feature却是所有的都在一起，data层直接得到的label也和这里要求的label不同。因此务必要对数据进行一次重整。</p>
<p>一个简单的规则就是按照奇偶，将feature划分成两部分。这样得到的两部分正好就是相同位置为一对。对于label的重整，也可以用类似的方法。小喵这里只对feature进行重整，而label的处理则是通过改ContrastiveLoss层来实现。</p>
<p>feature的重整本质上就是一个切片的操作，这里命名为id2_slice_layer，实现方法就是按照奇偶把bottom的数据复制到top。后馈的时候，也就是将两部分的feature的diff都直接复制到对应位置的bottom_diff中，具体实现如下：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// created by miao</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">ifndef</span> CAFFE_ID2_SLICE_LAYER_HPP_</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> CAFFE_ID2_SLICE_LAYER_HPP_</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;vector&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"caffe/blob.hpp"</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"caffe/layer.hpp"</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"caffe/proto/caffe.pb.h"</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">namespace</span> caffe &#123;</span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * @brief Takes a Blob and slices it along either the num or channel dimension,</span></span><br><span class="line"><span class="comment"> *        outputting multiple sliced Blob results.</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * TODO(dox): thorough documentation for Forward, Backward, and proto params.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">typename</span> Dtype&gt;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Id2SliceLayer</span> :</span> <span class="keyword">public</span> Layer&lt;Dtype&gt; &#123;</span><br><span class="line"> <span class="keyword">public</span>:</span><br><span class="line">  <span class="function"><span class="keyword">explicit</span> <span class="title">Id2SliceLayer</span><span class="params">(<span class="keyword">const</span> LayerParameter&amp; param)</span></span></span><br><span class="line">      : Layer&lt;Dtype&gt;(param) &#123;&#125;</span><br><span class="line">  <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">LayerSetUp</span><span class="params">(<span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; bottom,</span></span></span><br><span class="line"><span class="function"><span class="params">      <span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; top)</span></span>;</span><br><span class="line">  <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">Reshape</span><span class="params">(<span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; bottom,</span></span></span><br><span class="line"><span class="function"><span class="params">      <span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; top)</span></span>;</span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">virtual</span> <span class="keyword">inline</span> <span class="keyword">const</span> <span class="keyword">char</span>* <span class="title">type</span><span class="params">()</span> <span class="keyword">const</span> </span>&#123; <span class="keyword">return</span> <span class="string">"Id2Slice"</span>; &#125;</span><br><span class="line">  <span class="function"><span class="keyword">virtual</span> <span class="keyword">inline</span> <span class="keyword">int</span> <span class="title">ExactNumBottomBlobs</span><span class="params">()</span> <span class="keyword">const</span> </span>&#123; <span class="keyword">return</span> <span class="number">1</span>; &#125;</span><br><span class="line">  <span class="function"><span class="keyword">virtual</span> <span class="keyword">inline</span> <span class="keyword">int</span> <span class="title">MinTopBlobs</span><span class="params">()</span> <span class="keyword">const</span> </span>&#123; <span class="keyword">return</span> <span class="number">1</span>; &#125;</span><br><span class="line"></span><br><span class="line"> <span class="keyword">protected</span>:</span><br><span class="line">  <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">Forward_cpu</span><span class="params">(<span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; bottom,</span></span></span><br><span class="line"><span class="function"><span class="params">      <span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; top)</span></span>;</span><br><span class="line">  <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">Forward_gpu</span><span class="params">(<span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; bottom,</span></span></span><br><span class="line"><span class="function"><span class="params">      <span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; top)</span></span>;</span><br><span class="line">  <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">Backward_cpu</span><span class="params">(<span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; top,</span></span></span><br><span class="line"><span class="function"><span class="params">      <span class="keyword">const</span> <span class="built_in">vector</span>&lt;<span class="keyword">bool</span>&gt;&amp; propagate_down, <span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; bottom)</span></span>;</span><br><span class="line">  <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">Backward_gpu</span><span class="params">(<span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; top,</span></span></span><br><span class="line"><span class="function"><span class="params">      <span class="keyword">const</span> <span class="built_in">vector</span>&lt;<span class="keyword">bool</span>&gt;&amp; propagate_down, <span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; bottom)</span></span>;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">&#125;  <span class="comment">// namespace caffe</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">endif</span>  <span class="comment">// CAFFE_ID2_SLICE_LAYER_HPP_</span></span></span><br></pre></td></tr></table></figure>
<p>头文件，巨简单。。。</p>
<p>Cpp的代码，也非常简单，要注意id2_slice层的top有两个，每个的形状都是bottom的一半。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// created by miao</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;algorithm&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;vector&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"caffe/layers/id2_slice_layer.hpp"</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"caffe/util/math_functions.hpp"</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">namespace</span> caffe &#123;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">typename</span> Dtype&gt;</span><br><span class="line"><span class="keyword">void</span> Id2SliceLayer&lt;Dtype&gt;::LayerSetUp(<span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; bottom,</span><br><span class="line">      <span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; top) &#123;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">typename</span> Dtype&gt;</span><br><span class="line"><span class="keyword">void</span> Id2SliceLayer&lt;Dtype&gt;::Reshape(<span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; bottom,</span><br><span class="line">      <span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; top) &#123;</span><br><span class="line">    <span class="built_in">vector</span>&lt;<span class="keyword">int</span>&gt; top_shape = bottom[<span class="number">0</span>]-&gt;shape();</span><br><span class="line">    top_shape[<span class="number">0</span>] /= <span class="number">2</span>;</span><br><span class="line">    top[<span class="number">0</span>]-&gt;Reshape(top_shape);</span><br><span class="line">    top[<span class="number">1</span>]-&gt;Reshape(top_shape);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">typename</span> Dtype&gt;</span><br><span class="line"><span class="keyword">void</span> Id2SliceLayer&lt;Dtype&gt;::Forward_cpu(<span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; bottom,</span><br><span class="line">      <span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; top) &#123;</span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">int</span> feature_size = bottom[<span class="number">0</span>]-&gt;count(<span class="number">1</span>);</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> n = <span class="number">0</span>; n &lt; bottom[<span class="number">0</span>]-&gt;num(); ++ n) &#123;</span><br><span class="line">        caffe_copy(</span><br><span class="line">                feature_size, </span><br><span class="line">                bottom[<span class="number">0</span>]-&gt;cpu_data() + n * feature_size, </span><br><span class="line">                top[n &amp; <span class="number">1</span>]-&gt;mutable_cpu_data() + (n / <span class="number">2</span>) * feature_size</span><br><span class="line">                );</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">typename</span> Dtype&gt;</span><br><span class="line"><span class="keyword">void</span> Id2SliceLayer&lt;Dtype&gt;::Backward_cpu(<span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; top,</span><br><span class="line">      <span class="keyword">const</span> <span class="built_in">vector</span>&lt;<span class="keyword">bool</span>&gt;&amp; propagate_down, <span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; bottom) &#123;</span><br><span class="line">    <span class="keyword">const</span> <span class="keyword">int</span> feature_size = bottom[<span class="number">0</span>]-&gt;count(<span class="number">1</span>);</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> n = <span class="number">0</span>; n &lt; bottom[<span class="number">0</span>]-&gt;num(); ++ n) &#123;</span><br><span class="line">        caffe_copy(</span><br><span class="line">                feature_size,</span><br><span class="line">                top[n &amp; <span class="number">1</span>]-&gt;cpu_diff() + (n / <span class="number">2</span>) * feature_size,</span><br><span class="line">                bottom[<span class="number">0</span>]-&gt;mutable_cpu_diff() + n * feature_size</span><br><span class="line">                );</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">ifdef</span> CPU_ONLY</span></span><br><span class="line">STUB_GPU(Id2SliceLayer);</span><br><span class="line"><span class="meta">#<span class="meta-keyword">endif</span></span></span><br><span class="line"></span><br><span class="line">INSTANTIATE_CLASS(Id2SliceLayer);</span><br><span class="line">REGISTER_LAYER_CLASS(Id2Slice);</span><br><span class="line"></span><br><span class="line">&#125;  <span class="comment">// namespace caffe</span></span><br></pre></td></tr></table></figure>
<p>GPU上的实现，为了简单起见，也是直接调用了CPU的前馈函数。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// created by miao</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;vector&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"caffe/layers/id2_slice_layer.hpp"</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"caffe/util/math_functions.hpp"</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">namespace</span> caffe &#123;</span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">typename</span> Dtype&gt;</span><br><span class="line"><span class="keyword">void</span> Id2SliceLayer&lt;Dtype&gt;::Forward_gpu(<span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; bottom,</span><br><span class="line">      <span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; top) &#123;</span><br><span class="line">    <span class="keyword">this</span>-&gt;Forward_cpu(bottom, top);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">typename</span> Dtype&gt;</span><br><span class="line"><span class="keyword">void</span> Id2SliceLayer&lt;Dtype&gt;::Backward_gpu(<span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; top,</span><br><span class="line">      <span class="keyword">const</span> <span class="built_in">vector</span>&lt;<span class="keyword">bool</span>&gt;&amp; propagate_down, <span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; bottom) &#123;</span><br><span class="line">    <span class="keyword">this</span>-&gt;Backward_cpu(top, propagate_down, bottom);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">INSTANTIATE_LAYER_GPU_FUNCS(Id2SliceLayer);</span><br><span class="line"></span><br><span class="line">&#125;  <span class="comment">// namespace caffe</span></span><br></pre></td></tr></table></figure>
<p>这样就完成了feature的重整。由于没有用到新的参数，因此也不需要修改caffe.proto。</p>
<p>亲可以仿照这个方法对label来做类似的操作。鉴于小喵比较懒。。。这里就只是简单的改ContrastiveLoss层的代码了。</p>
<p>第一步，在ContrastiveLossLayer中新增一个用于记录feature pair是否是同一个identity的成员变量，取代原本的第三个bottom的功能。这样只需要在前馈的时候提前算好，就可以代替之前的第三个bottom来使用，而不需要再修改别的地方的代码。</p>
<p>为了大家使用的方便，小喵直接把修改之后的头文件粘贴出来（删掉注释）。新增的行，用“added by miao”这个注释标注出来。头文件只加了一行。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">ifndef</span> CAFFE_CONTRASTIVE_LOSS_LAYER_HPP_</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> CAFFE_CONTRASTIVE_LOSS_LAYER_HPP_</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;vector&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"caffe/blob.hpp"</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"caffe/layer.hpp"</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"caffe/proto/caffe.pb.h"</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"caffe/layers/loss_layer.hpp"</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">namespace</span> caffe &#123;</span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">typename</span> Dtype&gt;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ContrastiveLossLayer</span> :</span> <span class="keyword">public</span> LossLayer&lt;Dtype&gt; &#123;</span><br><span class="line"> <span class="keyword">public</span>:</span><br><span class="line">  <span class="function"><span class="keyword">explicit</span> <span class="title">ContrastiveLossLayer</span><span class="params">(<span class="keyword">const</span> LayerParameter&amp; param)</span></span></span><br><span class="line">      : LossLayer&lt;Dtype&gt;(param), diff_() &#123;&#125;</span><br><span class="line">  <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">LayerSetUp</span><span class="params">(<span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; bottom,</span></span></span><br><span class="line"><span class="function"><span class="params">      <span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; top)</span></span>;</span><br><span class="line"></span><br><span class="line">  <span class="function"><span class="keyword">virtual</span> <span class="keyword">inline</span> <span class="keyword">int</span> <span class="title">ExactNumBottomBlobs</span><span class="params">()</span> <span class="keyword">const</span> </span>&#123; <span class="keyword">return</span> <span class="number">3</span>; &#125;</span><br><span class="line">  <span class="function"><span class="keyword">virtual</span> <span class="keyword">inline</span> <span class="keyword">const</span> <span class="keyword">char</span>* <span class="title">type</span><span class="params">()</span> <span class="keyword">const</span> </span>&#123; <span class="keyword">return</span> <span class="string">"ContrastiveLoss"</span>; &#125;</span><br><span class="line">  <span class="function"><span class="keyword">virtual</span> <span class="keyword">inline</span> <span class="keyword">bool</span> <span class="title">AllowForceBackward</span><span class="params">(<span class="keyword">const</span> <span class="keyword">int</span> bottom_index)</span> <span class="keyword">const</span> </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> bottom_index != <span class="number">2</span>;</span><br><span class="line">  &#125;</span><br><span class="line"> <span class="keyword">protected</span>:</span><br><span class="line">  <span class="comment">/// @copydoc ContrastiveLossLayer</span></span><br><span class="line">  <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">Forward_cpu</span><span class="params">(<span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; bottom,</span></span></span><br><span class="line"><span class="function"><span class="params">      <span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; top)</span></span>;</span><br><span class="line">  <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">Forward_gpu</span><span class="params">(<span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; bottom,</span></span></span><br><span class="line"><span class="function"><span class="params">      <span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; top)</span></span>;</span><br><span class="line">  <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">Backward_cpu</span><span class="params">(<span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; top,</span></span></span><br><span class="line"><span class="function"><span class="params">      <span class="keyword">const</span> <span class="built_in">vector</span>&lt;<span class="keyword">bool</span>&gt;&amp; propagate_down, <span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; bottom)</span></span>;</span><br><span class="line">  <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">Backward_gpu</span><span class="params">(<span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; top,</span></span></span><br><span class="line"><span class="function"><span class="params">      <span class="keyword">const</span> <span class="built_in">vector</span>&lt;<span class="keyword">bool</span>&gt;&amp; propagate_down, <span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; bottom)</span></span>;</span><br><span class="line"></span><br><span class="line">  Blob&lt;Dtype&gt; diff_;  <span class="comment">// cached for backward pass</span></span><br><span class="line">  Blob&lt;Dtype&gt; dist_sq_;  <span class="comment">// cached for backward pass</span></span><br><span class="line">  Blob&lt;Dtype&gt; diff_sq_;  <span class="comment">// tmp storage for gpu forward pass</span></span><br><span class="line">  Blob&lt;Dtype&gt; summer_vec_;  <span class="comment">// tmp storage for gpu forward pass</span></span><br><span class="line">  Blob&lt;Dtype&gt; is_same_; <span class="comment">// added by miao</span></span><br><span class="line">&#125;;</span><br><span class="line">&#125;  <span class="comment">// namespace caffe</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">endif</span>  <span class="comment">// CAFFE_CONTRASTIVE_LOSS_LAYER_HPP_</span></span></span><br></pre></td></tr></table></figure>
<p>源文件的修改也十分简单，这里只贴出来Cuda的部分。源文件，修改了与原来的bottom3相关的地方。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;algorithm&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;vector&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;iostream&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"caffe/layers/contrastive_loss_layer.hpp"</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"caffe/util/math_functions.hpp"</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">namespace</span> caffe &#123;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">typename</span> Dtype&gt;</span><br><span class="line"><span class="keyword">void</span> ContrastiveLossLayer&lt;Dtype&gt;::Forward_gpu(</span><br><span class="line">    <span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; bottom, <span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; top) &#123;</span><br><span class="line">  <span class="keyword">const</span> <span class="keyword">int</span> count = bottom[<span class="number">0</span>]-&gt;count();</span><br><span class="line">  caffe_gpu_sub(</span><br><span class="line">      count,</span><br><span class="line">      bottom[<span class="number">0</span>]-&gt;gpu_data(),  <span class="comment">// a</span></span><br><span class="line">      bottom[<span class="number">1</span>]-&gt;gpu_data(),  <span class="comment">// b</span></span><br><span class="line">      diff_.mutable_gpu_data());  <span class="comment">// a_i-b_i</span></span><br><span class="line">  caffe_gpu_powx(</span><br><span class="line">      count,</span><br><span class="line">      diff_.mutable_gpu_data(),  <span class="comment">// a_i-b_i</span></span><br><span class="line">      Dtype(<span class="number">2</span>),</span><br><span class="line">      diff_sq_.mutable_gpu_data());  <span class="comment">// (a_i-b_i)^2</span></span><br><span class="line">  caffe_gpu_gemv(</span><br><span class="line">      CblasNoTrans,</span><br><span class="line">      bottom[<span class="number">0</span>]-&gt;num(),</span><br><span class="line">      bottom[<span class="number">0</span>]-&gt;channels(),</span><br><span class="line">      Dtype(<span class="number">1.0</span>),</span><br><span class="line">      diff_sq_.gpu_data(),  <span class="comment">// (a_i-b_i)^2</span></span><br><span class="line">      summer_vec_.gpu_data(),</span><br><span class="line">      Dtype(<span class="number">0.0</span>),</span><br><span class="line">      dist_sq_.mutable_gpu_data());  <span class="comment">// Sum (a_i-b_i)^2</span></span><br><span class="line">  Dtype margin = <span class="keyword">this</span>-&gt;layer_param_.contrastive_loss_param().margin();</span><br><span class="line">  <span class="keyword">bool</span> legacy_version =</span><br><span class="line">      <span class="keyword">this</span>-&gt;layer_param_.contrastive_loss_param().legacy_version();</span><br><span class="line">  <span class="function">Dtype <span class="title">loss</span><span class="params">(<span class="number">0.0</span>)</span></span>;</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; bottom[<span class="number">0</span>]-&gt;num(); ++i) &#123;</span><br><span class="line">    <span class="comment">// added by miao</span></span><br><span class="line">    is_same_.mutable_cpu_data()[i] = (bottom[<span class="number">2</span>]-&gt;cpu_data()[<span class="number">2</span> * i] == bottom[<span class="number">2</span>]-&gt;cpu_data()[<span class="number">2</span> * i + <span class="number">1</span>])? <span class="number">1</span>:<span class="number">0</span>;</span><br><span class="line">    <span class="keyword">if</span> (is_same_.cpu_data()[i] == <span class="number">1</span>) &#123;  <span class="comment">// similar pairs</span></span><br><span class="line">      loss += dist_sq_.cpu_data()[i];</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;  <span class="comment">// dissimilar pairs</span></span><br><span class="line">      <span class="keyword">if</span> (legacy_version) &#123;</span><br><span class="line">        loss += <span class="built_in">std</span>::max(margin - dist_sq_.cpu_data()[i], Dtype(<span class="number">0.0</span>));</span><br><span class="line">      &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        Dtype dist = <span class="built_in">std</span>::max(margin - <span class="built_in">sqrt</span>(dist_sq_.cpu_data()[i]),</span><br><span class="line">                              Dtype(<span class="number">0.0</span>));</span><br><span class="line">        loss += dist*dist;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  loss = loss / <span class="keyword">static_cast</span>&lt;Dtype&gt;(bottom[<span class="number">0</span>]-&gt;num()) / Dtype(<span class="number">2</span>);</span><br><span class="line">  top[<span class="number">0</span>]-&gt;mutable_cpu_data()[<span class="number">0</span>] = loss;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">typename</span> Dtype&gt;</span><br><span class="line">__<span class="function">global__ <span class="keyword">void</span> <span class="title">CLLBackward</span><span class="params">(<span class="keyword">const</span> <span class="keyword">int</span> count, <span class="keyword">const</span> <span class="keyword">int</span> channels,</span></span></span><br><span class="line"><span class="function"><span class="params">    <span class="keyword">const</span> Dtype margin, <span class="keyword">const</span> <span class="keyword">bool</span> legacy_version, <span class="keyword">const</span> Dtype alpha,</span></span></span><br><span class="line"><span class="function"><span class="params">    <span class="keyword">const</span> Dtype* y, <span class="keyword">const</span> Dtype* diff, <span class="keyword">const</span> Dtype* dist_sq,</span></span></span><br><span class="line"><span class="function"><span class="params">    Dtype *bottom_diff)</span> </span>&#123;</span><br><span class="line">  CUDA_KERNEL_LOOP(i, count) &#123;</span><br><span class="line">    <span class="keyword">int</span> n = i / channels;  <span class="comment">// the num index, to access y and dist_sq</span></span><br><span class="line">    <span class="keyword">if</span> (<span class="keyword">static_cast</span>&lt;<span class="keyword">int</span>&gt;(y[n])) &#123;  <span class="comment">// similar pairs</span></span><br><span class="line">      bottom_diff[i] = alpha * diff[i];</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;  <span class="comment">// dissimilar pairs</span></span><br><span class="line">      Dtype mdist(<span class="number">0.0</span>);</span><br><span class="line">      <span class="function">Dtype <span class="title">beta</span><span class="params">(<span class="number">0.0</span>)</span></span>;</span><br><span class="line">      <span class="keyword">if</span> (legacy_version) &#123;</span><br><span class="line">        mdist = (margin - dist_sq[n]);</span><br><span class="line">        beta = -alpha;</span><br><span class="line">      &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        Dtype dist = <span class="built_in">sqrt</span>(dist_sq[n]);</span><br><span class="line">        mdist = (margin - dist);</span><br><span class="line">        beta = -alpha * mdist / (dist + Dtype(<span class="number">1e-4</span>)) * diff[i];</span><br><span class="line">      &#125;</span><br><span class="line">      <span class="keyword">if</span> (mdist &gt; <span class="number">0.0</span>) &#123;</span><br><span class="line">        bottom_diff[i] = beta;</span><br><span class="line">      &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        bottom_diff[i] = <span class="number">0</span>;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">typename</span> Dtype&gt;</span><br><span class="line"><span class="keyword">void</span> ContrastiveLossLayer&lt;Dtype&gt;::Backward_gpu(<span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; top,</span><br><span class="line">    <span class="keyword">const</span> <span class="built_in">vector</span>&lt;<span class="keyword">bool</span>&gt;&amp; propagate_down, <span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; bottom) &#123;</span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; <span class="number">2</span>; ++i) &#123;</span><br><span class="line">    <span class="keyword">if</span> (propagate_down[i]) &#123;</span><br><span class="line">      <span class="keyword">const</span> <span class="keyword">int</span> count = bottom[<span class="number">0</span>]-&gt;count();</span><br><span class="line">      <span class="keyword">const</span> <span class="keyword">int</span> channels = bottom[<span class="number">0</span>]-&gt;channels();</span><br><span class="line">      Dtype margin = <span class="keyword">this</span>-&gt;layer_param_.contrastive_loss_param().margin();</span><br><span class="line">      <span class="keyword">const</span> <span class="keyword">bool</span> legacy_version =</span><br><span class="line">          <span class="keyword">this</span>-&gt;layer_param_.contrastive_loss_param().legacy_version();</span><br><span class="line">      <span class="keyword">const</span> Dtype sign = (i == <span class="number">0</span>) ? <span class="number">1</span> : <span class="number">-1</span>;</span><br><span class="line">      <span class="keyword">const</span> Dtype alpha = sign * top[<span class="number">0</span>]-&gt;cpu_diff()[<span class="number">0</span>] /</span><br><span class="line">          <span class="keyword">static_cast</span>&lt;Dtype&gt;(bottom[<span class="number">0</span>]-&gt;num());</span><br><span class="line">      <span class="comment">// NOLINT_NEXT_LINE(whitespace/operators)</span></span><br><span class="line">      CLLBackward&lt;Dtype&gt;&lt;&lt;&lt;CAFFE_GET_BLOCKS(count), CAFFE_CUDA_NUM_THREADS&gt;&gt;&gt;(</span><br><span class="line">          count, channels, margin, legacy_version, alpha,</span><br><span class="line">          is_same_.gpu_data(),  <span class="comment">// pair similarity 0 or 1  added by miao</span></span><br><span class="line">          diff_.gpu_data(),  <span class="comment">// the cached eltwise difference between a and b</span></span><br><span class="line">          dist_sq_.gpu_data(),  <span class="comment">// the cached square distance between a and b</span></span><br><span class="line">          bottom[i]-&gt;mutable_gpu_diff());</span><br><span class="line">      CUDA_POST_KERNEL_CHECK;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">INSTANTIATE_LAYER_GPU_FUNCS(ContrastiveLossLayer);</span><br><span class="line"></span><br><span class="line">&#125;  <span class="comment">// namespace caffe</span></span><br></pre></td></tr></table></figure>
<p>需要注意的时候，前馈和后馈都需要做一点代码上的修改，虽说十分的简单，但也要小心。 至此，基于Caffe的DeepID2的修改全部完成。</p>
<p>最后，十分感谢小喵的师弟，胖喵的审查，每一篇博客都由胖喵第一手的阅读并提出意见。  </p>
<p><strong>转载</strong>请注明出处~</p>
]]></content>
      <categories>
        <category>Deep Learning</category>
      </categories>
  </entry>
  <entry>
    <title>基于Caffe的DeepID2实现（中）</title>
    <url>/2016/07/15/deepid2-2/</url>
    <content><![CDATA[<blockquote>
<p>小喵的唠叨话：我们在上一篇博客里面，介绍了Caffe的Data层的编写。有了Data层，下一步则是如何去使用生成好的训练数据。也就是这一篇的内容。  </p>
</blockquote>
<a id="more"></a>
<h2 id="二精髓deepid2-loss层">二、精髓，DeepID2 Loss层</h2>
<p>DeepID2这篇论文关于verification signal的部分，给出了一个用于监督verification的loss。</p>
<p><img data-src="http://cdn.miaoerduo.com/images/verification_loss.jpg" alt="verification_loss" /></p>
<p>其中，<span class="math inline">\(f_i\)</span> 和 <span class="math inline">\(f_j\)</span> 是归一化之后的特征。</p>
<p>当 <span class="math inline">\(f_i\)</span> 和<span class="math inline">\(f_j\)</span> 属于同一个identity的时候，也就是 <span class="math inline">\(y_{ij}=1\)</span> 时，loss是二者的L2距离，约束使得特征更为相近。</p>
<p>当 <span class="math inline">\(f_i\)</span> 和 <span class="math inline">\(f_j\)</span> 不属于同一个identity的时候，即 <span class="math inline">\(y_{ij}=-1\)</span> ，这时的loss表示什么呢？参数m又表示什么？</p>
<p>m在这里是margin的意思，是一个可以自行设置的参数，表示期望的不同identity的feature之间的距离。当两个feature的大于margin时，说明网络已经可以很好的区分这两个特征，因此这是loss为0，当feature间的距离小于margin时，loss则为<span class="math inline">\((m-|f_i - f_j|)^2\)</span>，表示还需要两个特征能够更好的区分。因此这个loss函数比较好的反应了我们的需求，也就是DeepID2的算法思想。</p>
<p>这个Loss层实现起来似乎并不麻烦，前馈十分的简单。至于后馈，求导也非常简单。但是Caffe加入新层，需要在caffe.proto文件中，做一些修改，这也是最困扰小喵的地方。</p>
<p>不过有个好消息就是：Caffe官网增加了ContrastiveLossLayer这个层！和我们的需要是一样的。因此我们不需要自己实现这个层。</p>
<p>喜大普奔之余，小喵也专门看了Caffe的文档，以及这里提到了siamese network，发现这个网络使用ContrastiveLossLayer的方式比较独特，Caffe项目中的examples中有例子，感兴趣可以看看。</p>
<p>ContrastiveLossLayer的输入，也就是bottom有三部分，feature1、feature2、label，feature1和feature2是分别对应的两组feature，而label则表示该对feature是否是属于同一个identity，是的话，则为1，不是则为0。而且该层还提供一个参数margin，也就是论文的公式里面的m。</p>
<p>最终的结论就是，虽然我们不需要自己写Loss层，但是还是必须增加一些额外的层。</p>
<p>主要有2个，用于将特征归一化的NormalizationLayer以及用于将feature层转换成ContrastiveLossLayer的输入的层，不妨命名为ID2SliceLayer。</p>
<h2 id="三小问题大智慧之normalization-layer">三、小问题，大智慧之Normalization Layer</h2>
<p>这个归一化的层用于将输入的feature map进行归一化。Caffe官网并没有提供相关的层，因此我们必须自己实现（或者从网上找），这里我们还是选择自己来实现，顺便学习一下Caffe加层的技巧。</p>
<p>Normalization层的前馈非常的简单，输入为一个向量x，输出为归一化之后的向量：</p>
<p><span class="math display">\[f(\vec x)=\frac{\vec x}{\left\| \vec x \right \|}\]</span></p>
<p>至于后馈，需要求导，计算稍微有点复杂，小喵在推导4遍之后才给出如下表达式：</p>
<p><span class="math display">\[\frac{\partial \vec f}{\partial \vec x}=-\frac{1}{\left\| \vec x \right\|}*{\vec x}*{\vec x^T}+\frac{1}{\left\| \vec x \right\|}\]</span></p>
<p>其中x为输入的特征向量，为列向量。这里是将整个feature map看做一个列向量。</p>
<p>知道了前馈后馈的计算规则，那么很容易编写自己的层了，这里小喵建议大家找个Caffe已经有了的内容相近的层，照着改写。比如这个Normalization层，没有任何层的参数，所以照着ReLU类似的层就很好编写。</p>
<p>之后就祭出我们的code：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// create by miao</span></span><br><span class="line"><span class="comment">// 主要实现了feature的归一化</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">ifndef</span> CAFFE_NORMALIZATION_LAYER_HPP_</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> CAFFE_NORMALIZATION_LAYER_HPP_</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;vector&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"caffe/blob.hpp"</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"caffe/layer.hpp"</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"caffe/proto/caffe.pb.h"</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"caffe/layers/neuron_layer.hpp"</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">namespace</span> caffe &#123;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">typename</span> Dtype&gt;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">NormalizationLayer</span> :</span> <span class="keyword">public</span> NeuronLayer&lt;Dtype&gt; &#123;</span><br><span class="line"> <span class="keyword">public</span>:</span><br><span class="line">  <span class="function"><span class="keyword">explicit</span> <span class="title">NormalizationLayer</span><span class="params">(<span class="keyword">const</span> LayerParameter&amp; param)</span></span></span><br><span class="line">      : NeuronLayer&lt;Dtype&gt;(param) &#123;&#125;</span><br><span class="line">  <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">LayerSetUp</span><span class="params">(<span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; bottom,</span></span></span><br><span class="line"><span class="function"><span class="params">      <span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; top)</span></span>;</span><br><span class="line">  <span class="function"><span class="keyword">virtual</span> <span class="keyword">inline</span> <span class="keyword">const</span> <span class="keyword">char</span>* <span class="title">type</span><span class="params">()</span> <span class="keyword">const</span> </span>&#123; <span class="keyword">return</span> <span class="string">"Normalization"</span>; &#125;</span><br><span class="line">  <span class="function"><span class="keyword">virtual</span> <span class="keyword">inline</span> <span class="keyword">int</span> <span class="title">ExactNumBottomBlobs</span><span class="params">()</span> <span class="keyword">const</span> </span>&#123; <span class="keyword">return</span> <span class="number">1</span>; &#125;</span><br><span class="line">  <span class="function"><span class="keyword">virtual</span> <span class="keyword">inline</span> <span class="keyword">int</span> <span class="title">ExactNumTopBlobs</span><span class="params">()</span> <span class="keyword">const</span> </span>&#123; <span class="keyword">return</span> <span class="number">1</span>; &#125;</span><br><span class="line">  </span><br><span class="line"> <span class="keyword">protected</span>:</span><br><span class="line">  <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">Forward_cpu</span><span class="params">(<span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; bottom,</span></span></span><br><span class="line"><span class="function"><span class="params">      <span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; top)</span></span>;</span><br><span class="line">  <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">Forward_gpu</span><span class="params">(<span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; bottom,</span></span></span><br><span class="line"><span class="function"><span class="params">      <span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; top)</span></span>;</span><br><span class="line">  <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">Backward_cpu</span><span class="params">(<span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; top,</span></span></span><br><span class="line"><span class="function"><span class="params">      <span class="keyword">const</span> <span class="built_in">vector</span>&lt;<span class="keyword">bool</span>&gt;&amp; propagate_down, <span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; bottom)</span></span>;</span><br><span class="line">  <span class="function"><span class="keyword">virtual</span> <span class="keyword">void</span> <span class="title">Backward_gpu</span><span class="params">(<span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; top,</span></span></span><br><span class="line"><span class="function"><span class="params">      <span class="keyword">const</span> <span class="built_in">vector</span>&lt;<span class="keyword">bool</span>&gt;&amp; propagate_down, <span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; bottom)</span></span>;</span><br><span class="line">  Blob&lt;Dtype&gt; norm_val_; <span class="comment">// 记录每个feature的模</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line">&#125;  <span class="comment">// namespace caffe</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">endif</span>  <span class="comment">// CAFFE_NORMALIZATION_LAYER_HPP_</span></span></span><br></pre></td></tr></table></figure>
<p>这个层的头文件异常的简单，和ReLU的仅有的区别就是类的名字不一样，而且多了个成员变量norm_val_，用来记录每个feature的模值。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// create by miao</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;vector&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;cmath&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"caffe/layers/normalization_layer.hpp"</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"caffe/util/math_functions.hpp"</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">namespace</span> caffe &#123;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">typename</span> Dtype&gt;</span><br><span class="line"><span class="keyword">void</span> NormalizationLayer&lt;Dtype&gt;::LayerSetUp(<span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; bottom,</span><br><span class="line">        <span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; top) &#123;</span><br><span class="line">    NeuronLayer&lt;Dtype&gt;::LayerSetUp(bottom, top);</span><br><span class="line">    CHECK_NE(top[<span class="number">0</span>], bottom[<span class="number">0</span>]) &lt;&lt; <span class="keyword">this</span>-&gt;type() &lt;&lt; <span class="string">" Layer does not "</span></span><br><span class="line">        <span class="string">"allow in-place computation."</span>;</span><br><span class="line">    norm_val_.Reshape(bottom[<span class="number">0</span>]-&gt;shape(<span class="number">0</span>), <span class="number">1</span>, <span class="number">1</span>, <span class="number">1</span>); <span class="comment">// 申请norm的内存</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">typename</span> Dtype&gt; </span><br><span class="line"><span class="keyword">void</span> NormalizationLayer&lt;Dtype&gt;::Forward_cpu(</span><br><span class="line">    <span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; bottom, <span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; top) &#123;</span><br><span class="line"></span><br><span class="line">    Dtype *norm_val_cpu_data = norm_val_.mutable_cpu_data();</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> n = <span class="number">0</span>; n &lt; bottom[<span class="number">0</span>]-&gt;shape(<span class="number">0</span>); ++ n) &#123;</span><br><span class="line">        <span class="comment">// 计算每个c * h * w的区域的模</span></span><br><span class="line">        norm_val_cpu_data[n] = <span class="built_in">std</span>::<span class="built_in">sqrt</span>(<span class="keyword">static_cast</span>&lt;<span class="keyword">float</span>&gt;(</span><br><span class="line">                    caffe_cpu_dot&lt;Dtype&gt;(</span><br><span class="line">                        bottom[<span class="number">0</span>]-&gt;count(<span class="number">1</span>), </span><br><span class="line">                        bottom[<span class="number">0</span>]-&gt;cpu_data() + bottom[<span class="number">0</span>]-&gt;offset(n), </span><br><span class="line">                        bottom[<span class="number">0</span>]-&gt;cpu_data() + bottom[<span class="number">0</span>]-&gt;offset(n)</span><br><span class="line">                        )</span><br><span class="line">                    ));</span><br><span class="line">        <span class="comment">// 将每个bottom归一化，输出到top</span></span><br><span class="line">        caffe_cpu_scale&lt;Dtype&gt;(</span><br><span class="line">                top[<span class="number">0</span>]-&gt;count(<span class="number">1</span>), </span><br><span class="line">                <span class="number">1.</span> / norm_val_cpu_data[n], </span><br><span class="line">                bottom[<span class="number">0</span>]-&gt;cpu_data() + bottom[<span class="number">0</span>]-&gt;offset(n), </span><br><span class="line">                top[<span class="number">0</span>]-&gt;mutable_cpu_data() + top[<span class="number">0</span>]-&gt;offset(n)</span><br><span class="line">                );</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">typename</span> Dtype&gt;</span><br><span class="line"><span class="keyword">void</span> NormalizationLayer&lt;Dtype&gt;::Backward_cpu(</span><br><span class="line">    <span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; top, </span><br><span class="line">    <span class="keyword">const</span> <span class="built_in">vector</span>&lt;<span class="keyword">bool</span>&gt;&amp; propagate_down,</span><br><span class="line">    <span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; bottom) &#123;</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">const</span> Dtype *norm_val_cpu_data = norm_val_.cpu_data();</span><br><span class="line">    <span class="keyword">const</span> Dtype *top_diff = top[<span class="number">0</span>]-&gt;cpu_diff();</span><br><span class="line">    Dtype *bottom_diff = bottom[<span class="number">0</span>]-&gt;mutable_cpu_diff();</span><br><span class="line">    <span class="keyword">const</span> Dtype *bottom_data = bottom[<span class="number">0</span>]-&gt;cpu_data();</span><br><span class="line"></span><br><span class="line">    caffe_copy(top[<span class="number">0</span>]-&gt;count(), top_diff, bottom_diff);</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> n = <span class="number">0</span>; n &lt; top[<span class="number">0</span>]-&gt;shape(<span class="number">0</span>); ++ n) &#123;</span><br><span class="line">        Dtype a = - <span class="number">1.</span>/(norm_val_cpu_data[n] * norm_val_cpu_data[n] * norm_val_cpu_data[n]) * caffe_cpu_dot&lt;Dtype&gt;(</span><br><span class="line">                top[<span class="number">0</span>]-&gt;count(<span class="number">1</span>),</span><br><span class="line">                top_diff + top[<span class="number">0</span>]-&gt;offset(n),</span><br><span class="line">                bottom_data + bottom[<span class="number">0</span>]-&gt;offset(n)</span><br><span class="line">                );</span><br><span class="line">        Dtype b = <span class="number">1.</span> / norm_val_cpu_data[n];</span><br><span class="line">        caffe_cpu_axpby&lt;Dtype&gt;(</span><br><span class="line">                top[<span class="number">0</span>]-&gt;count(<span class="number">1</span>),</span><br><span class="line">                a,</span><br><span class="line">                bottom_data + bottom[<span class="number">0</span>]-&gt;offset(n),</span><br><span class="line">                b,</span><br><span class="line">                bottom_diff + top[<span class="number">0</span>]-&gt;offset(n)</span><br><span class="line">                );</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"><span class="meta">#<span class="meta-keyword">ifdef</span> CPU_ONLY</span></span><br><span class="line">STUB_GPU(NormalizationLayer);</span><br><span class="line"><span class="meta">#<span class="meta-keyword">endif</span></span></span><br><span class="line"></span><br><span class="line">INSTANTIATE_CLASS(NormalizationLayer);</span><br><span class="line">REGISTER_LAYER_CLASS(Normalization);</span><br><span class="line"></span><br><span class="line">&#125; <span class="comment">// namespace caffe</span></span><br></pre></td></tr></table></figure>
<p>最后就是GPU部分的代码，如果不在乎性能的话，直接在CUDA的前后馈里面调用CPU版的前后馈就行。当然如果了解CUDA的话，完全可以写一份GPU版的代码。小喵这里就偷懒了一下。。。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// create by miao</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;vector&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;cmath&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"caffe/layers/normalization_layer.hpp"</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">"caffe/util/math_functions.hpp"</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">namespace</span> caffe &#123;</span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">typename</span> Dtype&gt; </span><br><span class="line"><span class="keyword">void</span> NormalizationLayer&lt;Dtype&gt;::Forward_gpu(</span><br><span class="line">    <span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; bottom, <span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; top) &#123;</span><br><span class="line">    <span class="keyword">this</span>-&gt;Forward_cpu(bottom, top);</span><br><span class="line">&#125;   </span><br><span class="line"></span><br><span class="line"><span class="keyword">template</span> &lt;<span class="keyword">typename</span> Dtype&gt;</span><br><span class="line"><span class="keyword">void</span> NormalizationLayer&lt;Dtype&gt;::Backward_gpu(</span><br><span class="line">    <span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; top, </span><br><span class="line">    <span class="keyword">const</span> <span class="built_in">vector</span>&lt;<span class="keyword">bool</span>&gt;&amp; propagate_down,</span><br><span class="line">    <span class="keyword">const</span> <span class="built_in">vector</span>&lt;Blob&lt;Dtype&gt;*&gt;&amp; bottom) &#123;</span><br><span class="line">    <span class="keyword">this</span>-&gt;Backward_cpu(top, propagate_down, bottom);</span><br><span class="line">&#125;</span><br><span class="line">INSTANTIATE_LAYER_GPU_FUNCS(NormalizationLayer);</span><br><span class="line">&#125; <span class="comment">// namespace caffe</span></span><br></pre></td></tr></table></figure>
<p>这样，我们就写完了Normalization层的所有代码。</p>
<p>对于比较老版本的Caffe，还需要修改/caffe_root/src/caffe/caffe.proto文件。而新版的Caffe只要在新增参数的情况下才需要修改。我们的这个Normalization层并没有用到新的参数，因此并不需要修改caffe.proto文件。</p>
<p>至于新版的Caffe为什么这么智能，原因其实就在这两行代码：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">INSTANTIATE_CLASS(NormalizationLayer);</span><br><span class="line">REGISTER_LAYER_CLASS(Normalization);</span><br></pre></td></tr></table></figure>
<p>宏INSTANTIATE_CLASS在/caffe_root/include/caffe/common.hpp中定义。</p>
<p>宏REGISTER_LAYER_CLASS在/caffe_root/include/caffe/layer_factory.hpp中定义。</p>
<p>感兴趣可以自行查阅。</p>
<div style="background-color: #ddd; padding: 20px">
<p>重要更新:</br> 小喵最近训练的时候实际上已经不使用Normalization层了，而是将Contrastive Loss直接接在feature层的后面，同时由于训练数据都是正样本对，那么margin就没有意义了。不过比较麻烦的是loss weight的选取。</p>
</div>
<p><strong>转载</strong>请注明出处~</p>
]]></content>
      <categories>
        <category>Deep Learning</category>
      </categories>
  </entry>
  <entry>
    <title>基于Caffe的DeepID2实现（上）</title>
    <url>/2016/07/13/deepid2-1/</url>
    <content><![CDATA[<blockquote>
<p>小喵的唠叨话：小喵最近在做人脸识别的工作，打算将汤晓鸥前辈的DeepID，DeepID2等算法进行实验和复现。DeepID的方法最简单，而DeepID2的实现却略微复杂，并且互联网上也没有比较好的资源。因此小喵在试验之后，确定了实验结果的正确性之后，才准备写这篇博客，分享给热爱Deep Learning的小伙伴们。</p>
</blockquote>
<p>能够看到这篇博客的小伙伴们，相信已经对Deep Learning有了比较深入的了解。因此，小喵对亲作了如下的假定：</p>
<ol type="1">
<li>了解Deep Learning的基本知识</li>
<li>目前在做人脸识别的相关工作</li>
<li>仔细阅读过DeepID和DeepID2的论文</li>
<li>使用Caffe作为训练框架</li>
<li>即使不满足上述4个条件，也会持之以恒的学习</li>
</ol>
<a id="more"></a>
<p>如果亲发现对上述的条件都不满足的话，那么这篇博文可能内容还是略显枯涩乏味，你可以从了解Caffe开始，慢慢学习。</p>
<p>相关资源：</p>
<ul>
<li>DeepID：<span class="exturl" data-url="aHR0cDovL21tbGFiLmllLmN1aGsuZWR1LmhrL3BkZi9ZaVN1bl9DVlBSMTQucGRm" title="http://mmlab.ie.cuhk.edu.hk/pdf/YiSun_CVPR14.pdf">http://mmlab.ie.cuhk.edu.hk/pdf/YiSun_CVPR14.pdf<i class="fa fa-external-link"></i></span></li>
<li>DeepID2：<span class="exturl" data-url="aHR0cDovL3BhcGVycy5uaXBzLmNjL3BhcGVyLzU0MTYtYW5hbG9nLW1lbW9yaWVzLWluLWEtYmFsYW5jZWQtcmF0ZS1iYXNlZC1uZXR3b3JrLW9mLWUtaS1uZXVyb25z" title="http://papers.nips.cc/paper/5416-analog-memories-in-a-balanced-rate-based-network-of-e-i-neurons">http://papers.nips.cc/paper/5416-analog-memories-in-a-balanced-rate-based-network-of-e-i-neurons<i class="fa fa-external-link"></i></span></li>
<li>Caffe：<span class="exturl" data-url="aHR0cDovL2NhZmZlLmJlcmtlbGV5dmlzaW9uLm9yZy8=" title="http://caffe.berkeleyvision.org/">http://caffe.berkeleyvision.org/<i class="fa fa-external-link"></i></span></li>
</ul>
<p>由于篇幅较大，这里会分成几个部分，依次讲解。</p>
<h2 id="一设计我们独特的data层">一、设计我们独特的Data层</h2>
<p>在DeepID2中，有两种监督信号。一是<strong>Identity signal</strong>，这和DeepID中的实现方法一样，用给定label的人脸数据，进行分类的训练，这里使用 <code>softmax_with_loss</code> 层来实现(<code>softmax + cross-entropy loss</code>)。这里不再介绍。另一种就是<strong>verification signal</strong>，也就是人脸比对的监督。这里要求，输入的数据时成对存在，每一对都有一个公共的label，是否是同一个类别。如果是同一个identity，则要求他们的特征更接近，如果是不同的identity，则要求他们的特征尽可能远离。</p>
<p>不论最终怎么实现，我们的第一步是确定的，构造合适的数据。</p>
<p>使用Caffe训练的时候，第一步是打Batch，将训练数据写入LMDB或者LevelDB数据库中，训练的时候Caffe会从数据库中读取图片，因此一个简单的实现方法就是构造许多的pair，然后打Batch的时候就能保证每对图片都是相连的，然后在训练的时候做一些小Trick就可以实现。</p>
<p>但是就如上面所说，打Batch的同时，图片的顺序就已经是确定的了，因此网络输入的图片pair也是固定的，这样似乎就缺乏了一些灵活性。</p>
<p>那么如何动态的构造我们的训练数据呢？</p>
<p><strong>设计我们独特的data层。</strong></p>
<p>这里为了方便，使用Python来拓展Caffe的功能。Python是一门简洁的语言，非常适合做这种工作。不过Caffe中如果使用了Python的层，那么就不能使用多GPU了，这点需要注意（希望以后能增加这个支持）。</p>
<h3 id="让你的caffe支持python拓展">1）让你的Caffe支持Python拓展</h3>
<p>在Caffe根目录的Makefile.config中，有这么一句话。</p>
<p><img data-src="http://cdn.miaoerduo.com/images/caffe_with_python_layer.png" alt="with_python_layer" /></p>
<p>我们需要使用Python层，因此需要取消这个注释。之后Make一下你的Caffe和pycaffe。这样Caffe就支持Python层了。</p>
<h3 id="编写data层">2）编写data层</h3>
<p>基于Python的data层的编写，Caffe是给了一个简单的例子的，在<code>/caffe_home/examples/pycaffe/layers/</code>中。我们简单的照着这个例子来写。</p>
<p>首先，我们定义自己需要的参数。这里，我们需要：</p>
<ol type="1">
<li>batch_size: batch的大小，没什么好解释的，要求这个数是大于0的偶数</li>
<li>mean_file：图像的均值文件的路径</li>
<li>scale：图像减均值后变换的尺度</li>
<li>image_root_dir：训练数据的根目录</li>
<li>source：训练数据的list路径</li>
<li>crop_size：图像crop的大小</li>
<li>ratio：正样本所占的比例（0~1）</li>
</ol>
<p>caffe在train.prototxt中定义网络结构的时候，可以传入这些参数。我们目前只需要知道，这些参数一定可以获取到，就可以了。另外，source表示训练数据的list的文件地址，这里用到的训练数据的格式和Caffe打batch的数据一样。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">file_path1 label1</span><br><span class="line">file_path2 label2</span><br></pre></td></tr></table></figure>
<p>这样的格式。</p>
<p>Data层的具体实现，首先需要继承caffe.Layer这个类，之后实现setup, forward, backward和reshape，不过data层并不需要backward和reshape。setup主要是为了初始化各种参数，并且设置top的大小。对于Data层来说，forward则是生成数据和label。</p>
<p>闲话少说，代码来见。</p>
<figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment">#-*- encoding: utf-8 -*-</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> sys</span><br><span class="line"><span class="keyword">import</span> caffe</span><br><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"><span class="keyword">import</span> os</span><br><span class="line"><span class="keyword">import</span> os.path <span class="keyword">as</span> osp</span><br><span class="line"><span class="keyword">import</span> random</span><br><span class="line"><span class="keyword">import</span> cv2</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">id2_data_layer</span><span class="params">(caffe.Layer)</span>:</span></span><br><span class="line">    <span class="string">"""</span></span><br><span class="line"><span class="string">    这个python的data layer用于动态的构造训练deepID2的数据</span></span><br><span class="line"><span class="string">    每次forward会产生多对数据，每对数据可能是相同的label或者不同的label</span></span><br><span class="line"><span class="string">    """</span></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">setup</span><span class="params">(self, bottom, top)</span>:</span></span><br><span class="line">        self.top_names = [<span class="string">'data'</span>, <span class="string">'label'</span>]</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 读取输入的参数</span></span><br><span class="line">        params = eval(self.param_str)</span><br><span class="line">        <span class="keyword">print</span> <span class="string">"init data layer"</span></span><br><span class="line">        <span class="keyword">print</span> params</span><br><span class="line"></span><br><span class="line">        self.batch_size = params[<span class="string">'batch_size'</span>]  <span class="comment"># batch_size</span></span><br><span class="line">        self.ratio = float(params[<span class="string">'ratio'</span>])</span><br><span class="line">        self.scale = float(params[<span class="string">'scale'</span>])</span><br><span class="line">        <span class="keyword">assert</span> self.batch_size &gt; <span class="number">0</span> <span class="keyword">and</span> self.batch_size % <span class="number">2</span> == <span class="number">0</span>, <span class="string">"batch size must be 2X"</span></span><br><span class="line">        <span class="keyword">assert</span> self.ratio &gt; <span class="number">0</span> <span class="keyword">and</span> self.ratio &lt; <span class="number">1</span>, <span class="string">"ratio must be in (0, 1)"</span></span><br><span class="line">        self.image_root_dir = params[<span class="string">'image_root_dir'</span>]</span><br><span class="line">        self.mean_file = params[<span class="string">'mean_file'</span>]       </span><br><span class="line">        self.source = params[<span class="string">'source'</span>]</span><br><span class="line">        self.crop_size = params[<span class="string">'crop_size'</span>]</span><br><span class="line"></span><br><span class="line">        top[<span class="number">0</span>].reshape(self.batch_size, <span class="number">3</span>, params[<span class="string">'crop_size'</span>], params[<span class="string">'crop_size'</span>])</span><br><span class="line">        top[<span class="number">1</span>].reshape(self.batch_size, <span class="number">1</span>)</span><br><span class="line">        self.batch_loader = BatchLoader(self.image_root_dir, self.mean_file, self.scale, self.source, self.batch_size, self.ratio)</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">forward</span><span class="params">(self, bottom, top)</span>:</span></span><br><span class="line">        blob, label_list = self.batch_loader.get_mini_batch()</span><br><span class="line">        top[<span class="number">0</span>].data[...] = blob</span><br><span class="line">        top[<span class="number">1</span>].data[...] = label_list</span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">backward</span><span class="params">(self, bottom, top)</span>:</span></span><br><span class="line">        <span class="keyword">pass</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">reshape</span><span class="params">(self, bottom, top)</span>:</span></span><br><span class="line">        <span class="keyword">pass</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">BatchLoader</span><span class="params">(object)</span>:</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">__init__</span><span class="params">(self, root_dir, mean_file, scale, image_list_path, batch_size, ratio)</span>:</span></span><br><span class="line">        <span class="keyword">print</span> <span class="string">"init batch loader"</span></span><br><span class="line">        self.batch_size = batch_size</span><br><span class="line">        self.ratio = ratio <span class="comment"># true pair / false pair</span></span><br><span class="line">        self.image2label = &#123;&#125;   <span class="comment"># key:image_name    value:label</span></span><br><span class="line">        self.label2images = &#123;&#125;  <span class="comment"># key:label         value: image_name array</span></span><br><span class="line">        self.images = []        <span class="comment"># store all image_name</span></span><br><span class="line">        self.mean = np.load(mean_file)</span><br><span class="line">        self.scale = scale</span><br><span class="line">        self.root_dir = root_dir</span><br><span class="line">        <span class="keyword">with</span> open(image_list_path) <span class="keyword">as</span> fp:</span><br><span class="line">            <span class="keyword">for</span> line <span class="keyword">in</span> fp:</span><br><span class="line">                data = line.strip().split()</span><br><span class="line">                image_name = data[<span class="number">0</span>]</span><br><span class="line">                label = data[<span class="number">-1</span>]</span><br><span class="line">                self.images.append(image_name)</span><br><span class="line">                self.image2label[image_name] = label</span><br><span class="line">                <span class="keyword">if</span> label <span class="keyword">not</span> <span class="keyword">in</span> self.label2images:</span><br><span class="line">                    self.label2images[label] = []</span><br><span class="line">                self.label2images[label].append(image_name)</span><br><span class="line">        self.labels = self.label2images.keys()</span><br><span class="line">        self.label_num = len(self.labels)</span><br><span class="line">        self.image_num = len(self.image2label)</span><br><span class="line">        <span class="keyword">print</span> <span class="string">"init batch loader over"</span></span><br><span class="line"></span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">get_mini_batch</span><span class="params">(self)</span>:</span></span><br><span class="line">        image_list, label_list = self._get_batch(self.batch_size / <span class="number">2</span>)</span><br><span class="line">        cv_image_list = map(<span class="keyword">lambda</span> image_name: (self.scale * (cv2.imread(os.path.join(self.root_dir, image_name)).astype(np.float32, copy=<span class="literal">False</span>).transpose((<span class="number">2</span>, <span class="number">0</span>, <span class="number">1</span>)) - self.mean)), image_list)</span><br><span class="line">        blob = np.require(cv_image_list)</span><br><span class="line">        label_blob = np.require(label_list, dtype=np.float32).reshape((self.batch_size, <span class="number">1</span>))</span><br><span class="line">        <span class="keyword">return</span> blob, label_blob</span><br><span class="line">    </span><br><span class="line">    <span class="function"><span class="keyword">def</span> <span class="title">_get_batch</span><span class="params">(self, pair_num)</span>:</span></span><br><span class="line">        image_list = []</span><br><span class="line">        label_list = []</span><br><span class="line">        <span class="keyword">for</span> pair_idx <span class="keyword">in</span> xrange(pair_num):</span><br><span class="line">            <span class="keyword">if</span> random.random() &lt; self.ratio: <span class="comment"># true pair</span></span><br><span class="line">                <span class="keyword">while</span> <span class="literal">True</span>:</span><br><span class="line">                    label_idx = random.randint(<span class="number">0</span>, self.label_num - <span class="number">1</span>)</span><br><span class="line">                    label = self.labels[label_idx]</span><br><span class="line">                    <span class="keyword">if</span> len(self.label2images[label]) &gt; <span class="number">5</span>:</span><br><span class="line">                        <span class="keyword">break</span></span><br><span class="line">                first_idx = random.randint(<span class="number">0</span>, len(self.label2images[label]) - <span class="number">1</span>)</span><br><span class="line">                second_id = random.randint(<span class="number">0</span>, len(self.label2images[label]) - <span class="number">2</span>)</span><br><span class="line">                <span class="keyword">if</span> second_id &gt;= first_idx:</span><br><span class="line">                    second_id += <span class="number">1</span></span><br><span class="line">                image_list.append(self.label2images[label][first_idx])</span><br><span class="line">                image_list.append(self.label2images[label][second_id])</span><br><span class="line">                label_list.append(int(label))</span><br><span class="line">                label_list.append(int(label))</span><br><span class="line">            <span class="keyword">else</span>:                   <span class="comment"># false pair</span></span><br><span class="line">                <span class="keyword">for</span> i <span class="keyword">in</span> xrange(<span class="number">2</span>):</span><br><span class="line">                    image_id = random.randint(<span class="number">0</span>, self.image_num - <span class="number">1</span>)</span><br><span class="line">                    image_name = self.images[image_id]</span><br><span class="line">                    label = self.image2label[image_name]</span><br><span class="line">                    image_list.append(image_name)</span><br><span class="line">                    label_list.append(int(label))</span><br><span class="line">        <span class="keyword">return</span> image_list, label_list</span><br></pre></td></tr></table></figure>
<p>上述的代码可以根据给定的list，batch size，ratio等参数生成符合要求的data和label。这里还有一些问题需要注意：</p>
<ol type="1">
<li>对输入的参数没有检验。</li>
<li>没有对读取图像等操作做异常处理。因此如果很不幸地读到的图片路径不合法，那么程序突然死掉都是有可能的。。。小喵的数据都是可以读的，所以木有问题。</li>
<li>在选取正负样本对的时候，对于正样本对，只有样本对应的label中的图片数大于5的时候，才选正样本（小喵的训练数据每个人都有至少几十张图片，所以木有出现问题），如果样本比较少的话，可以更改这个数（特别是有测试集的时候，测试集通常数目都很少，小喵训练的时候都是不用测试集的，因为会死循环。。。）。对于选取负样本对的时候，只是随便选了两张图片，而并没有真的保证这一对是不同label，这里考虑到训练数据是比较多的，所以不大可能选中同一个label的样本，因此可以近似代替负样本对。</li>
<li>这里有个减均值的操作，这个均值文件是经过特殊转换求出的numpy的数组。Caffe生成的均值文件是不能直接用的，但是可以通过仿照Caffe中Classifier中的写法来代替（caffe.io.Transformer工具）。另外这里的图片数据和均值文件是一样大小的，但实际上可能并不一定相等。如果需要对输入图片做各种随机化的操作，还需要自己修改代码。</li>
</ol>
<p>至此，我们就完成了一个简单的Data层了。</p>
<p>那么怎么调用自己的data层呢？</p>
<p>这里有一个十分简单的写法。在我们用来训练的prototxt中，将Data层的定义改成如下的方式：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">layer &#123;</span><br><span class="line">    name: &quot;data&quot;</span><br><span class="line">    type: &quot;Python&quot;</span><br><span class="line">    top: &quot;data&quot;</span><br><span class="line">    top: &quot;label&quot;</span><br><span class="line">    include &#123;</span><br><span class="line">        phase: TRAIN</span><br><span class="line">    &#125;    </span><br><span class="line">    python_param &#123;</span><br><span class="line">        module: &quot;id2_data_layer&quot;</span><br><span class="line">        layer: &quot;id2_data_layer&quot;</span><br><span class="line">        param_str: &quot;&#123;&#39;crop_size&#39; : 128, &#39;batch_size&#39; : 96, &#39;mean_file&#39;: &#39;&#x2F;your&#x2F;data&#x2F;root&#x2F;mean.npy&#39;, &#39;scale&#39;: 0.0078125, &#39;source&#39;: &#39;&#x2F;path&#x2F;to&#x2F;your&#x2F;train_list&#39;, &#39;image_root_dir&#39;: &#39;&#x2F;path&#x2F;to&#x2F;your&#x2F;image_root&#x2F;&#39;&#125;&quot;    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>python_param中的这三个参数需要注意：</p>
<p><code>module</code>：模块名，我们先前编写的data层，本身就是一个文件，也就是一个模块，因此模块名就是文件名。</p>
<p><code>layer</code>：层的名字，我们在文件中定义的类的名字。这里比较巧合，module和layer的名字相同。</p>
<p><code>param_str</code>：所有的需要传给data层的参数都通过这个参数来传递。这里简单的使用了Python字典的格式的字符串，在data层中使用eval来执行（o(╯□╰)o  这其实并不是一个好习惯），从而获取参数，当然也可以使用别的方式来传递，比如json或者xml等。</p>
<p>最后，你在训练的时候可能会报错，说找不到你刚刚的层，或者找不到caffe，只需要把这个层的代码所在的文件夹的路径加入到PYTHONPATH中即可。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">export</span> PYTHONPATH=PYTHONPATH:/path/to/your/layer/:/path/to/caffe/python</span><br></pre></td></tr></table></figure>
<p>这样就完成了我们的Data层的编写，是不是非常简单？</p>
<div style="background-color: #ddd; padding: 20px">
<p>重要更新：<br/> 1，小喵最近发现直接在image_data_layer.cpp中进行修改，可以更好的实现这个目标，而且支持多GPU。<br/> 2，训练的数据可以只用正样本对，因为identity signal已经十分强调不同identity的feature之间的距离，因此verification signal只需要强调相同的identity的feature相近就好。<br/> 3，小喵新的训练数据，构造pair的方式也做了修改。每次使用所有的数据构造pair，然后用来训练，每个epoch后都重新生成一次list。这样可以保证identity signal能够每次训练所有的图片，而verification signal也能每次训练不同的样本对。</p>
</div>
<p><strong>转载</strong>请注明出处~</p>
]]></content>
      <categories>
        <category>Deep Learning</category>
      </categories>
  </entry>
  <entry>
    <title>三、Redis基本操作——List</title>
    <url>/2016/06/11/redis-list/</url>
    <content><![CDATA[<blockquote>
<p>小喵的唠叨话：前面我们介绍了Redis的string的数据结构的原理和操作。当时我们提到Redis的键值对不仅仅是字符串。而这次我们就要介绍Redis的第二个数据结构了，List（链表）。由于List在原理上的实现并不是特别的复杂，我们在这里将原理和具体的命令都放在一起介绍。</p>
</blockquote>
<a id="more"></a>
<h2 id="redis-list-原理">Redis List 原理</h2>
<p>学习过数据结构的同学，一定对链表（Linked List）十分的熟悉。相信我们自己也曾经使用过这种数据结构。</p>
<p>链表分为很多种：单向链表，双向链表，循环链表，块状链表等等。</p>
<p>链表的作用也有很多。首先，链表可以存放数据。其次链表可以模拟队列、堆栈等其他的数据结构。</p>
<p>链表的实现也有多种，以C语言为例，最常见的是构造节点node，node中又有指针，用于指向下一个node，这样就构成了单向链表，如果node中有两个指针，分别指向前一个和后一个node，则构造了双向链表，再如果链表是首尾相连的，那么就是循环链表。链表的具体算法，是数据结构里面的内容，我们这里就不专门介绍啦。</p>
<p>那么，Redis内部的List是什么样子的链表呢？又是怎么来维护的呢？</p>
<p>我们慢慢的剖析。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">listNode</span> &#123;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 前置节点</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">listNode</span> *<span class="title">prev</span>;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 后置节点</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">listNode</span> *<span class="title">next</span>;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 节点的值</span></span><br><span class="line">    <span class="keyword">void</span> *value;</span><br><span class="line">&#125; listNode;</span><br></pre></td></tr></table></figure>
<p>上述就是Redis的节点的定义。</p>
<p>可以看出，这是一个双向的链表。比较有意思的是，节点内存的值，是一个void *的指针，这就说明节点内部可以存放任何的内容。</p>
<div style="background-color:#ddd; padding: 20px">
<p>反派汪：我不能理解为什么value是void <em>的指针就能存任何内容？我甚至不知道为什么还有要void </em>这么奇怪的数据类型。<br/> 喵太：其实很多人也有这个疑问。不过先问问你自己，指针到底是什么？int <em>，float </em>和char <em>的这几种指针有什么区别？<br/> 反派汪：指针里面存的就是地址，通过地址就能访问到指定的内存。以int </em>指针为例，int <em>说明，指针指向的是一个由int型的数据组成的内存。通过指针运算符</em>，我们就能取到对应位置的内存中的值。通过对指针的+和-，我们就能移动到相邻的位置。<br/> 喵太：嗯，说的不错，但是你只说对了一半。指针里面存放的是地址，无论任何类型的指针，里面存放的都只是一个32位的无符号整数。指针的类型，其实只是决定了对内存进行的操作。int <em>的指针，每次加1的时候，其实移动了4个字节，char </em>则移动1个。使用指针运算符<em>其实也是一样。int </em>会得到连续4个字节的内容，并且存进一个int变量里面，而char <em>则会得到一个字节的内容，并存进一个char里面。因此，任何类型的指针其实本质上是一样的。<br/> 反派汪：那你的意思是，所有类型的指针都是一样的了。那么我还是不能理解void </em>的指针究竟能做什么。<br/> 喵太：void <em>的指针是C语言中最简单的一种指针，它存放的是一个地址，并且没有给出任何操作上的提示。但是任何类型的指针都能赋给void </em>的指针。void <em>的指针也能强制转换成任何类型的指针。这里使用void </em>的指针，只是在说明，value对应的是一块内存。开发者，可以自行转换指针的类型，来做相应的操作。如果你熟悉C语言的话，malloc函数的返回值也是一个void <em>的指针，而我们通常会把它强制转换成我们需要的任何数据类型。<br/> 反派汪：这样啊，那原则上这里的指针用什么类型的都是可以的了？反正就是一个地址而已。只是void </em>更纯粹一点，也最符合要求，毕竟它只是指向数据的地址，没有做任何的限制。<br/> 喵太：孺狗可教也。</p>
</div>
<p>现在，我们有了listNode这个节点的数据结构，就可以构造我们的链表了。</p>
<p> <img data-src="http://cdn.miaoerduo.com/images/redis-list.jpg" alt="list" /></p>
<center>
图一 链表的结构
</center>
<p>依照上图的示意图，我们就可以使用listNode构造一个双向链表了。</p>
<p>Redis中，为了更好地管理链表，定义了一个list的数据结构，作为链表的封装。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">list</span> &#123;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 头节点</span></span><br><span class="line">    listNode *head;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 尾节点</span></span><br><span class="line">    listNode *tail;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 链表中的节点数</span></span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">int</span> len;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 节点值复制函数</span></span><br><span class="line">    <span class="keyword">void</span> *(*dup) (<span class="keyword">void</span> *ptr);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 节点值释放函数</span></span><br><span class="line">    <span class="keyword">void</span> (*<span class="built_in">free</span>) (<span class="keyword">void</span> *ptr);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 节点值对比函数</span></span><br><span class="line">    <span class="keyword">int</span> (*match) (<span class="keyword">void</span> *ptr, <span class="keyword">void</span> *key);</span><br><span class="line">&#125; <span class="built_in">list</span>;</span><br></pre></td></tr></table></figure>
<p>list结构记录了链表的头指针，尾指针，链表的节点数。dup，free和match三个成员则表示对节点的值进行复制，释放和比较的函数。由于这里的节点的value的内容是任意的，复制和释放并不一定能用类似于memcpy和free的函数来处理（不太理解的话，可以google一下浅拷贝和深拷贝）。</p>
<p>因此，我们得到的最终的链表结构是这个样子的：</p>
<p><img data-src="http://cdn.miaoerduo.com/images/redis-list2.jpg" alt="list" /></p>
<center>
图二 链表的结构
</center>
<p>Redis的链表的实现的主要特性如下：</p>
<ol type="1">
<li>双端：链表节点都有prev和next指针，这样获取一个节点的前置节点和后置节点的算法复杂度都为O(1)。</li>
<li>无环：list的第一个节点（头节点）的prev和最后一个节点（尾节点）的next都指向NULL。</li>
<li>带表头指针和表尾指针：通过list的head和tail两个指针，可以随意的从链表的头和尾进行操作。</li>
<li>带链表长度计数器：可以通过len成员来获取链表的节点的个数，复杂度O(1)。</li>
<li>多态：链表使用void *指针来保存value，并且可以通过dup，free，match来操控节点的value值，因此，该链表可以保存任意类型的值。</li>
</ol>
<p>小喵看到这个多态的实现也是惊呆了，原来C语言也能实现多态，也第一次知道了函数指针居然可以这么用。</p>
<p>由此想到之前看过的一句话：高手用树叶也能杀人。希望每个人都能成为传说中的高手~</p>
<p>关于链表的操作，是数据结构课程中的基础，无外乎是增、删、改、查这四个操作，这里不再介绍。聪明的你，bing一下就OK了！</p>
<h2 id="redis-list-实战">Redis List 实战</h2>
<p>现在，我们已经基本了解了Redis的List结构的底层实现。那么，Redis提供了哪些可以供我们调用的接口呢？请听小喵慢慢道来。</p>
<p>以下指令根据类别和使用的频率进行排序。</p>
<p>指令清单：</p>
<ul>
<li>BLPOP</li>
<li>BRPOP</li>
<li>BRPOPLPUSH</li>
<li>LINDEX</li>
<li>LINSERT</li>
<li>LLEN</li>
<li>LPOP</li>
<li>LPUSH</li>
<li>LPUSHX</li>
<li>LRANGE</li>
<li>LREM</li>
<li>LSET</li>
<li>LTRIM</li>
<li>RPOP</li>
<li>RPOPLPUSH</li>
<li>RPUSH</li>
<li>RPUSHX</li>
</ul>
<h3 id="一push操作">一、PUSH操作</h3>
<h4 id="rpush-key-value-value-...">1，RPUSH key value [value ...]</h4>
<p>从队列的右边入队一个元素或多个元素，复杂度O(1)。</p>
<p>将所有指定的值插入存于key的列表的尾部（从右侧插入）。如果key不存在，那么PUSH之前，会先自动创建一个空的列表。如果key对应的值不是一个list的话，则会返回一个错误。如果同时push多个值的话，值会依次从左到右PUSH从尾部进入list。</p>
<p>那么这个命令有什么作用呢？</p>
<p>作用大大滴！</p>
<p>PUSH和POP操作，其实是队列的基本操作。Redis的list就是一个极其强大的队列系统。我们在哪些地方会用到队列呢？下面，我们说两个例子：</p>
<h5 id="a评论系统">a，评论系统</h5>
<p>逛过微博的筒子们应该都对评论系统有了解。我们在看完一条微博之后，常常会评论一番，或者看看其他人的吐槽。每条评论的记录都是按照时间顺序排序的。我们读的时候也是这个顺序。这时，队列就是一个很好的存储结构。每提交一次评论，都向list的末尾添加一个新的节点。</p>
<p>当然，博客本身也可以是这样的结构。</p>
<h5 id="b并行转串行">b，并行转串行</h5>
<p>我们做后台开发的筒子们应该都遇到过类似的情景。用户每时每刻都可能发出请求，而且请求的频率经常变化。这时，后台的程序不可能立刻响应每一个用户的请求，尤其是请求特别占资源的服务的时候（双11的时候，你有没有看到404页面？）。有什么好的办法来解决这个问题呢？我们需要一个排队系统。根据用户的请求时间，将用户的请求放入队列中，后台程序依次从队列中获取任务，处理并将结果返回到结果队列。</p>
<p>这其实也是一个生产者消费者模型。通过队列，我们将并行的请求转换成串行的任务队列，之后依次处理（当然后台的程序也可以多核并行处理）。</p>
<p>那么，这么强大的功能，我们不马上试试吗？</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">redis&gt; rpush queue a</span><br><span class="line">(integer) 1</span><br><span class="line">redis&gt; rpush queue b</span><br><span class="line">(integer) 2</span><br><span class="line">redis&gt; rpush queue c</span><br><span class="line">(integer) 3</span><br><span class="line">redis&gt; rpush queue d e f</span><br><span class="line">(integer) 6</span><br><span class="line">redis&gt; lrange queue 0 -1</span><br><span class="line">1) &quot;a&quot;</span><br><span class="line">2) &quot;b&quot;</span><br><span class="line">3) &quot;c&quot;</span><br><span class="line">4) &quot;d&quot;</span><br><span class="line">5) &quot;e&quot;</span><br><span class="line">6) &quot;f&quot;</span><br></pre></td></tr></table></figure>
<p>lrange这个命令是获取指定范围的list中的数据，我们下面再具体介绍。</p>
<p>这个例子中，我们依次将a、b、c、d、e、f、g从尾部(右侧)追加到queue中，最后通过lrange查看queue中的数据的顺序。</p>
<h4 id="lpush-key-value-value-...">2，LPUSH key value [value ...]</h4>
<p>从队列的左边入队一个或多个元素，复杂度O(1)。</p>
<p>这个指令和RPUSH几乎一样，只是插入节点的方向相反了，是从list的头部(左侧)进行插入的。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">redis&gt; del queue</span><br><span class="line">(integer) 1</span><br><span class="line">redis&gt; lpush queue a</span><br><span class="line">(integer) 1</span><br><span class="line">redis&gt; lpush queue b</span><br><span class="line">(integer) 2</span><br><span class="line">redis&gt; lpush queue c</span><br><span class="line">(integer) 3</span><br><span class="line">redis&gt; lpush queue e f g</span><br><span class="line">(integer) 6</span><br><span class="line">redis&gt; lrange queue 0 -1</span><br><span class="line">1) &quot;g&quot;</span><br><span class="line">2) &quot;f&quot;</span><br><span class="line">3) &quot;e&quot;</span><br><span class="line">4) &quot;c&quot;</span><br><span class="line">5) &quot;b&quot;</span><br><span class="line">6) &quot;a&quot;</span><br></pre></td></tr></table></figure>
<p>可以看出，结果正好和RPUSH相反。</p>
<h4 id="rpushx-key-value">3，RPUSHX key value</h4>
<p>从队列的右边入队一个元素，仅队列存在时有效。当队列不存在时，不进行任何操作。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line"># 之前queue已经存在，且有a、b、c、d、e、f、g这6个元素</span><br><span class="line">redis&gt; rpushx queue z</span><br><span class="line">(integer) 7</span><br><span class="line">redis&gt; del queue</span><br><span class="line">(integer) 1</span><br><span class="line">redis&gt; rpushx queue z</span><br><span class="line">(integer) 0</span><br></pre></td></tr></table></figure>
<p>可以看出，最开始rpushx向queue中新增了一个节点，但当我们删掉了queue时，再rpushx，就没有插入成功（返回值为0）。</p>
<h4 id="lpushx-key-value">4，LPUSHX key value</h4>
<p>从队列的左边入队一个元素，仅队列存在时有效。当队列不存在时，不进行任何操作。</p>
<p>参考RPUSHX。</p>
<h3 id="二pop操作">二、POP操作</h3>
<p>PUSH操作，是从队列头部和尾部增加节点的操作。而POP是获取并删除头尾节点的操作。</p>
<h4 id="lpop-key">1，LPOP key</h4>
<p>从队列的左边出队一个元素，复杂度O(1)。如果list为空，则返回nil。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">redis&gt; del queue</span><br><span class="line">(integer) 0</span><br><span class="line">redis&gt; rpush queue a b c d e f</span><br><span class="line">(integer) 6</span><br><span class="line">redis&gt; lrange queue 0 -1</span><br><span class="line">1) &quot;a&quot;</span><br><span class="line">2) &quot;b&quot;</span><br><span class="line">3) &quot;c&quot;</span><br><span class="line">4) &quot;d&quot;</span><br><span class="line">5) &quot;e&quot;</span><br><span class="line">6) &quot;f&quot;</span><br><span class="line">redis&gt; lpop queue</span><br><span class="line">&quot;a&quot;</span><br><span class="line">redis&gt; lpop queue</span><br><span class="line">&quot;b&quot;</span><br><span class="line">redis&gt; lrange queue 0 -1</span><br><span class="line">1) &quot;c&quot;</span><br><span class="line">2) &quot;d&quot;</span><br><span class="line">3) &quot;e&quot;</span><br><span class="line">4) &quot;f&quot;</span><br><span class="line">redis&gt; rpop queue</span><br><span class="line">&quot;f&quot;</span><br><span class="line">redis&gt; rpop queue</span><br><span class="line">&quot;e&quot;</span><br><span class="line">redis&gt; lrange queue 0 -1</span><br><span class="line">1) &quot;c&quot;</span><br><span class="line">2) &quot;d&quot;</span><br></pre></td></tr></table></figure>
<p>我们首先向空的list中添加了a、b、c、d、e、f这6个值，之后从左边POP(LPOP)出两个值，再从右侧POP(RPOP)出两个值。</p>
<h4 id="rpop-key">2，RPOP key</h4>
<p>从队列的右边出队一个元素，复杂度O(1)。如果list为空，则返回nil。</p>
<p>见LPOP。</p>
<h4 id="blpop-key-key-...-timeout">3，BLPOP key [key ...] timeout</h4>
<p>删除，并获得该列表中的第一元素，或阻塞，直到有一个可用。</p>
<p>这是LPOP的阻塞版本。在LPOP的时候，如果队列中没有值，则会返回一个nil。而BLPOP则会等待一段时间，如果list中有值（等待的时候，被添加的），则返回对应值；如果在给定时间内仍没有得到结果，则返回nil。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">redis&gt; lrange queue 0 -1</span><br><span class="line">1) &quot;c&quot;</span><br><span class="line">2) &quot;d&quot;</span><br><span class="line">redis&gt; BLPOP queue 1</span><br><span class="line">1) &quot;queue&quot;</span><br><span class="line">2) &quot;c&quot;</span><br><span class="line">redis&gt; BLPOP queue 1</span><br><span class="line">1) &quot;queue&quot;</span><br><span class="line">2) &quot;d&quot;</span><br><span class="line">redis&gt; BLPOP queue 1</span><br><span class="line">(nil)</span><br><span class="line">(1.10s)</span><br><span class="line">redis&gt; LPOP queue</span><br><span class="line">(nil)</span><br></pre></td></tr></table></figure>
<p>我们仍接着上面的实验继续，这时queue里面只有2个元素了，我们使用BLPOP取值，前两次都成功地得到了值，效果和LPOP一样。但第三次的时候，由于list已经为空，但是BLPOP并没有立刻返回nil，而是阻塞了一点时间(timeout的时间)，之后才返回了nil。最后，我们试验了一下LPOP，证实了LPOP是立刻返回结果的。</p>
<p>timeout表示等待的时间，单位是秒。当设为0时，表示永远阻塞，非0时，表示等待的最长时间。</p>
<p>要注意的是，LBPOP支持多个key，也就是说可以同时监听多个list，并按照key的顺序，依次检查list是否为空，如果不为空，则返回最优先的list中的值。如果都为空，则阻塞，直到有一个list不为空，那么返回这个list对应的值。这里进行试验不是特别的方便，更具体的介绍可以查看中文官网的文档：<span class="exturl" data-url="aHR0cDovL3JlZGlzLmNuL2NvbW1hbmRzL2JscG9wLmh0bWw=" title="http://redis.cn/commands/blpop.html">http://redis.cn/commands/blpop.html<i class="fa fa-external-link"></i></span>。</p>
<h4 id="brpop-key-key-...-timeout">4，BRPOP key [key ...] timeout</h4>
<p>删除，并获得该列表中的最后一个元素，或阻塞，直到有一个可用。</p>
<p>参考BLPOP。</p>
<h3 id="三pop-and-push">三、POP and PUSH</h3>
<h4 id="rpoplpush-source-destination">1，RPOPLPUSH source destination</h4>
<p>删除列表中的最后一个元素，将其追加到另一个列表。</p>
<p>这个命令可以原子性地返回并删除source对应的列表的最后一个元素（尾部元素），并将钙元素放入destination对应的列表的第一个元素位置（列表头部）。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">redis&gt; rpush q1 1 2 3 4 5</span><br><span class="line">(integer) 5</span><br><span class="line">redis&gt; lrange q1 0 -1</span><br><span class="line">1) &quot;1&quot;</span><br><span class="line">2) &quot;2&quot;</span><br><span class="line">3) &quot;3&quot;</span><br><span class="line">4) &quot;4&quot;</span><br><span class="line">5) &quot;5&quot;</span><br><span class="line">redis&gt; rpoplpush q1 q2</span><br><span class="line">&quot;5&quot;</span><br><span class="line">redis&gt; rpoplpush q1 q2</span><br><span class="line">&quot;4&quot;</span><br><span class="line">redis&gt; lrange q1 0 -1</span><br><span class="line">1) &quot;1&quot;</span><br><span class="line">2) &quot;2&quot;</span><br><span class="line">3) &quot;3&quot;</span><br><span class="line">redis&gt; lrange q2 0 -1</span><br><span class="line">1) &quot;4&quot;</span><br><span class="line">2) &quot;5&quot;</span><br><span class="line">redis&gt;</span><br></pre></td></tr></table></figure>
<p>我们简单的看一下上述的例子，首先我们初始化一个q1，内容是{1, 2, 3, 4, 5}。这是q2没有定义，可以理解是一个空的list {}。</p>
<p>之后使用rpoplpush，从q1右边pop出一个元素5，然后在q2左侧push进。则现在的q1为{1, 2, 3, 4}，q2为{5}。</p>
<p>再进行一次rpoplpush，从q1右边pop出一个元素4，然后在q2左侧push进。则现在的q1为{1, 2, 3}，q2为{4, 5}。</p>
<p>那么，这个有意思的命令有什么实际的用处呢？</p>
<p>redis的官网给出了两个有意思的案例（因为写的很详细，所以直接照搬下来了）<span class="exturl" data-url="aHR0cDovL3d3dy5yZWRpcy5jbi9jb21tYW5kcy9ycG9wbHB1c2guaHRtbA==" title="http://www.redis.cn/commands/rpoplpush.html">2<i class="fa fa-external-link"></i></span>：</p>
<p>模式：安全的队列</p>
<p>Redis通常都被用做一个处理各种后台工作或消息任务的消息服务器。一个简单的队列模式就是：生产者把消息放入一个列表中，等待消息的消费者用 <span class="exturl" data-url="aHR0cDovL3d3dy5yZWRpcy5jbi9jb21tYW5kcy9ycG9wLmh0bWw=" title="http://www.redis.cn/commands/rpop.html">RPOP<i class="fa fa-external-link"></i></span> 命令（用轮询方式），或者用 BRPOP 命令（如果客户端使用阻塞操作会更好）来得到这个消息。</p>
<p>然而，因为消息有可能会丢失，所以这种队列并是不安全的。例如，当接收到消息后，出现了网络问题或者消费者端崩溃了，那么这个消息就丢失了。</p>
<p>RPOPLPUSH (或者其阻塞版本的 <span class="exturl" data-url="aHR0cDovL3d3dy5yZWRpcy5jbi9jb21tYW5kcy9icnBvcGxwdXNoLmh0bWw=" title="http://www.redis.cn/commands/brpoplpush.html">BRPOPLPUSH<i class="fa fa-external-link"></i></span>）提供了一种方法来避免这个问题：消费者端取到消息的同时把该消息放入一个正在处理中的列表。 当消息被处理了之后，该命令会使用 LREM 命令来移除正在处理中列表中的对应消息。</p>
<p>另外，可以添加一个客户端来监控这个正在处理中列表，如果有某些消息已经在这个列表中存在很长时间了（即超过一定的处理时限），那么这个客户端会把这些超时消息重新加入到队列中。</p>
<p>模式：循环列表</p>
<p>RPOPLPUSH 命令的 source 和 destination 是相同的话，那么客户端在访问一个拥有n个元素的列表时，可以在 O(N) 时间里一个接一个获取列表元素，而不用像 <span class="exturl" data-url="aHR0cDovL3d3dy5yZWRpcy5jbi9jb21tYW5kcy9scmFuZ2UuaHRtbA==" title="http://www.redis.cn/commands/lrange.html">LRANGE<i class="fa fa-external-link"></i></span> 那样需要把整个列表从服务器端传送到客户端。</p>
<p>上面这种模式即使在以下两种情况下照样能很好地工作：</p>
<ul>
<li>有多个客户端同时对同一个列表进行旋转（rotating）：它们会取得不同的元素，直到列表里所有元素都被访问过，又从头开始这个操作。</li>
<li>有其他客户端在往列表末端加入新的元素。</li>
</ul>
<p>这个模式让我们可以很容易地实现这样一个系统：有 N 个客户端，需要连续不断地对一批元素进行处理，而且处理的过程必须尽可能地快。 一个典型的例子就是服务器上的监控程序：它们需要在尽可能短的时间内，并行地检查一批网站，确保它们的可访问性。</p>
<p>值得注意的是，使用这个模式的客户端是易于扩展（scalable）且安全的（reliable），因为即使客户端把接收到的消息丢失了， 这个消息依然存在于队列中，等下次迭代到它的时候，由其他客户端进行处理。</p>
<h4 id="brpoplpush-source-destination-timeout">2，BRPOPLPUSH source destination timeout</h4>
<p>弹出一个列表的值，将它推到另一个列表，并返回它;或阻塞，直到有一个可用。</p>
<p>RPOPLPUSH的阻塞版本。timeout的单位是秒，当timeout为0的时候，表示无限期阻塞。</p>
<p>具体使用参考RPOPLPUSH。</p>
<h3 id="四其他">四、其他</h3>
<h4 id="llen-key">1，LLEN key</h4>
<p>获得队列(List)的长度</p>
<p>和名字一样易懂，这里不再过多解释。</p>
<h4 id="lrange-key-start-stop">2，LRANGE key start stop</h4>
<p>从列表中获取指定返回的元素</p>
<p>我们在前面用到了很多次。LRANGE可以获取list的指定范围的值。范围用start和stop表示。负数表示从右向左数。</p>
<p>需要注意的是，超出范围的下标不会产生错误：如果start&gt;end，会得到空列表，如果end超过队尾，则Redis会将其当做列表的最后一个元素。</p>
<h4 id="lindex-key-index">3，LINDEX key index</h4>
<p>获取一个元素，通过其索引列表</p>
<p>我们之前介绍的操作都是对list的两端进行的，所以算法复杂度都只有O(1)。而这个操作是指定位置来进行的，每次操作，list都得找到对应的位置，因此算法复杂度为O(N)。list的下表是从0开始的，index为负的时候是从右向左数。-1表示最后一个元素。当下标超出的时候，会返回nul。所以不用像操作数组一样担心范围越界的情况。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">redis&gt; rpush q1 a b c d f e g</span><br><span class="line">(integer) 7</span><br><span class="line">redis&gt; lrange q1 0 -1</span><br><span class="line">1) &quot;a&quot;</span><br><span class="line">2) &quot;b&quot;</span><br><span class="line">3) &quot;c&quot;</span><br><span class="line">4) &quot;d&quot;</span><br><span class="line">5) &quot;f&quot;</span><br><span class="line">6) &quot;e&quot;</span><br><span class="line">7) &quot;g&quot;</span><br><span class="line">redis&gt; lindex q1 0</span><br><span class="line">&quot;a&quot;</span><br><span class="line">redis&gt; lindex q1 3</span><br><span class="line">&quot;d&quot;</span><br><span class="line">redis&gt; lindex q1 4</span><br><span class="line">&quot;f&quot;</span><br><span class="line">redis&gt; lindex q1 -1</span><br><span class="line">&quot;g&quot;</span><br><span class="line">redis&gt; lindex q1 100</span><br><span class="line">(nil)</span><br></pre></td></tr></table></figure>
<h4 id="lset-key-index-value">4，LSET key index value</h4>
<p>设置队列里面一个元素的值</p>
<p>这个操作和LINDEX类似，只不过LINDEX是读，而LSET是写。下标的用法和LINDX相同。不过当index越界的时候，这里会报异常。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">redis&gt; rpush q1 a b c d f e g</span><br><span class="line">(integer) 7</span><br><span class="line">redis&gt; lrange q1 0 -1</span><br><span class="line">1) &quot;a&quot;</span><br><span class="line">2) &quot;b&quot;</span><br><span class="line">3) &quot;c&quot;</span><br><span class="line">4) &quot;d&quot;</span><br><span class="line">5) &quot;f&quot;</span><br><span class="line">6) &quot;e&quot;</span><br><span class="line">7) &quot;g&quot;</span><br><span class="line">redis&gt; lset q1 0 1</span><br><span class="line">OK</span><br><span class="line">redis&gt; lset q1 3 3</span><br><span class="line">OK</span><br><span class="line">redis&gt; lset q1 -1 0</span><br><span class="line">OK</span><br><span class="line">redis&gt; lset q1 100 0</span><br><span class="line">(error) ERR index out of range</span><br><span class="line">redis&gt; lrange q1 0 -1</span><br><span class="line">1) &quot;1&quot;</span><br><span class="line">2) &quot;b&quot;</span><br><span class="line">3) &quot;c&quot;</span><br><span class="line">4) &quot;3&quot;</span><br><span class="line">5) &quot;f&quot;</span><br><span class="line">6) &quot;e&quot;</span><br><span class="line">7) &quot;0&quot;</span><br></pre></td></tr></table></figure>
<h4 id="lrem-key-count-value">5，LREM key count value</h4>
<p>从列表中删除元素</p>
<p>该命令用于从key对应的list中，移除前count次出现 的值为value的元素。count参数有三种情况：</p>
<p>count &gt; 0: 表示从头向尾（左到右）移除值为value的元素。</p>
<p>count &lt; 0: 表示从尾向头（右向左）移除值为value的元素。</p>
<p>count = 0: 表示移除所有值为value的元素。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">redis&gt; rpush q 1 0 1 1 2 1 0 1 1</span><br><span class="line">(integer) 9</span><br><span class="line">redis&gt; lrange q 0 -1</span><br><span class="line">1) &quot;1&quot;</span><br><span class="line">2) &quot;0&quot;</span><br><span class="line">3) &quot;1&quot;</span><br><span class="line">4) &quot;1&quot;</span><br><span class="line">5) &quot;2&quot;</span><br><span class="line">6) &quot;1&quot;</span><br><span class="line">7) &quot;0&quot;</span><br><span class="line">8) &quot;1&quot;</span><br><span class="line">9) &quot;1&quot;</span><br><span class="line">redis&gt; lrem q 2 1</span><br><span class="line">(integer) 2</span><br><span class="line">redis&gt; lrange q 0 -1</span><br><span class="line">1) &quot;0&quot;</span><br><span class="line">2) &quot;1&quot;</span><br><span class="line">3) &quot;2&quot;</span><br><span class="line">4) &quot;1&quot;</span><br><span class="line">5) &quot;0&quot;</span><br><span class="line">6) &quot;1&quot;</span><br><span class="line">7) &quot;1&quot;</span><br><span class="line">redis&gt; lrem q -2 1</span><br><span class="line">(integer) 2</span><br><span class="line">redis&gt; lrange q 0 -1</span><br><span class="line">1) &quot;0&quot;</span><br><span class="line">2) &quot;1&quot;</span><br><span class="line">3) &quot;2&quot;</span><br><span class="line">4) &quot;1&quot;</span><br><span class="line">5) &quot;0&quot;</span><br><span class="line">redis&gt; lrem q 0 1</span><br><span class="line">(integer) 2</span><br><span class="line">redis&gt; lrange q 0 -1</span><br><span class="line">1) &quot;0&quot;</span><br><span class="line">2) &quot;2&quot;</span><br><span class="line">3) &quot;0&quot;</span><br></pre></td></tr></table></figure>
<h4 id="ltrim-key-start-stop">6，LTRIM key start stop</h4>
<p>修剪到指定范围内的清单</p>
<p>这个命令和LRANGE十分相似，LRANGE会将指定范围的元素返回给客户端，而LTRIM会对list进行修剪，使其只包含指定范围的元素。start和stop表示范围。</p>
<p>超出范围的下标不会产生错误：如果start&gt;end，会得到空列表，如果end超过队尾，则Redis会将其当做列表的最后一个元素。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">redis&gt; rpush q a b c d e f g</span><br><span class="line">(integer) 7</span><br><span class="line">redis&gt; lrange q 0 -1</span><br><span class="line">1) &quot;a&quot;</span><br><span class="line">2) &quot;b&quot;</span><br><span class="line">3) &quot;c&quot;</span><br><span class="line">4) &quot;d&quot;</span><br><span class="line">5) &quot;e&quot;</span><br><span class="line">6) &quot;f&quot;</span><br><span class="line">7) &quot;g&quot;</span><br><span class="line">redis&gt; ltrim q 1 4</span><br><span class="line">OK</span><br><span class="line">redis&gt; lrange q 0 -1</span><br><span class="line">1) &quot;b&quot;</span><br><span class="line">2) &quot;c&quot;</span><br><span class="line">3) &quot;d&quot;</span><br><span class="line">4) &quot;e&quot;</span><br></pre></td></tr></table></figure>
<h4 id="linsert-key-beforeafter-pivot-value">7，LINSERT key BEFORE|AFTER pivot value</h4>
<p>在列表中的另一个元素之前或之后插入一个元素</p>
<p>该命令将value插入值key对应的列表的基准值pivot的前面或是后面。</p>
<p>当key不存在时，这个list被视为空列表，任何操作都不会发生。</p>
<p>当key存在，但保存的不是list，则会报error。</p>
<p>该命令会返回修改之后的list的长度，如果找不到pivot，则会返回-1。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">redis&gt; rpush q a b c d e</span><br><span class="line">(integer) 5</span><br><span class="line">redis&gt; lrange q 0 -1</span><br><span class="line">1) &quot;a&quot;</span><br><span class="line">2) &quot;b&quot;</span><br><span class="line">3) &quot;c&quot;</span><br><span class="line">4) &quot;d&quot;</span><br><span class="line">5) &quot;e&quot;</span><br><span class="line">redis&gt; linsert q before c 0</span><br><span class="line">(integer) 6</span><br><span class="line">redis&gt; linsert q after c 0</span><br><span class="line">(integer) 7</span><br><span class="line">redis&gt; linsert q before 0 1</span><br><span class="line">(integer) 8</span><br><span class="line">redis&gt; linsert q after 0 1</span><br><span class="line">(integer) 9</span><br><span class="line">redis&gt; lrange q 0 -1</span><br><span class="line">1) &quot;a&quot;</span><br><span class="line">2) &quot;b&quot;</span><br><span class="line">3) &quot;1&quot;</span><br><span class="line">4) &quot;0&quot;</span><br><span class="line">5) &quot;1&quot;</span><br><span class="line">6) &quot;c&quot;</span><br><span class="line">7) &quot;0&quot;</span><br><span class="line">8) &quot;d&quot;</span><br><span class="line">9) &quot;e&quot;</span><br><span class="line">redis&gt; linsert q after w 1</span><br><span class="line">(integer) -1</span><br></pre></td></tr></table></figure>
<p>这里，我们构造了一个list{a, b, c, d, e}，先在c的前后插入了0，再在0前后插入了1。可以看出，linsert只会对第一个匹配pivot的前后进行操作。</p>
<p>最后，我们在w前面插入1，但是list中并没有w，所以返回了-1。</p>
<p><strong>转载</strong>请注明出处。</p>
]]></content>
      <categories>
        <category>C++</category>
        <category>Redis</category>
      </categories>
  </entry>
  <entry>
    <title>二、Redis基本操作——String(实战篇)</title>
    <url>/2016/05/01/redis-string-practice/</url>
    <content><![CDATA[<blockquote>
<p>小喵的唠叨话：上一周因为实验室要用GIT作为版本控制器，导致小喵花了一周的时间研究GIT，Redis这边，就没来得及继续研究。前几天看到有人搜索Redis怎么向数组里面添加内容，搜到了我的上一篇文章，可惜上一篇只讲到了String的原理，所以小喵痛定思痛，决定还是爬起来写这篇博客，免得搜索String操作的小伙伴又空手而归。</p>
</blockquote>
<a id="more"></a>
<p>上一篇博客，我们详细的介绍了Redis中String数据类型的底层实现，相信大家已经在原理上掌握的相当不错了。这次，我们就介绍Redis的命令行操作。当然，我们实际开发的时候可能不会太经常直接用到Redis的命令，而是通过一些数据库的封装来操控（就像我们虽然会使用SQL，但是实际开发的时候，总是有一些特别好用的库，既安全又方便）。但是，学习最基础的Redis命令，是针对各种语言的以不变应万变的法宝，毕竟库是针对于语言的。同时，学会了Redis的基本命令，将来对各种库的掌握也能更迅速。</p>
<p>关于Redis的命令，主要参考Redis的官方文档(<span class="exturl" data-url="aHR0cDovL3JlZGlzLmlvL2NvbW1hbmRzI3N0cmluZw==" title="http://redis.io/commands#string">http://redis.io/commands#string<i class="fa fa-external-link"></i></span>)。小喵这里主要像是翻译一下，并且给出一些实用的例子。排版上，小喵按功能重新组织了一下指令，并将比较常用的指令放在了最前面。同时也增加了锚点，方便大家查阅。小喵这里的Redis版本是3.0.7，应该是目前的最新版。</p>
<p>注：redis的命令（SET，GET等）是不区分大小写的（KEY和VALUE区分的），为了方便所以小喵的操作可能都是小写的。</p>
<p>指令清单:</p>
<ul>
<li>APPEND</li>
<li>BITCOUNT</li>
<li>BITOP</li>
<li>BITPOS</li>
<li>DECR</li>
<li>DECRBY</li>
<li>GET</li>
<li>GETBIT</li>
<li>GETRANGE</li>
<li>GETSET</li>
<li>INCR</li>
<li>INCRBY</li>
<li>INCRBYFLOAT</li>
<li>MGET</li>
<li>MSET</li>
<li>MSETNX</li>
<li>SET</li>
<li>SETBIT</li>
<li>SETEX</li>
<li>SETNX</li>
<li>SETRANGE</li>
<li>STRLEN</li>
</ul>
<h3 id="一set的相关操作">一，SET的相关操作</h3>
<h4 id="set-key-value-ex-seconds-px-milliseconds-nxxx">1，SET key value [EX seconds] [PX milliseconds] [NX|XX]</h4>
<p>Set the string value of a key</p>
<p>将键key设置成指定的字符串value。</p>
<p>我们知道Redis是一种key-value数据库（当然这么说可能不够严谨，因为Redis支持很多高级的数据结构）。因此所有的数据都是通过key来访问，key就是键。</p>
<p>SET指令可以将字符串的value和key绑定在一起。</p>
<p>如果不加额外的参数。当key中已经保存了一个值的话，这个值会被覆盖成新的值，并且忽略掉原始类型（一个key对应的不一定是一个字符串，也可以是List等其他的数据结构，这些结构在后面的文章里面会陆续介绍）；如果key不存在，那么则会在数据库中新增一个key，对应的值就是你刚刚设置的。</p>
<p>例子：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">redis&gt; get name</span><br><span class="line">(nil)</span><br><span class="line">redis&gt; set name miaoerduo</span><br><span class="line">OK</span><br><span class="line">redis&gt; get name</span><br><span class="line">&quot;miaoerduo&quot;</span><br><span class="line">redis&gt; set name miao</span><br><span class="line">OK</span><br><span class="line">redis&gt; get name</span><br><span class="line">&quot;miao&quot;</span><br></pre></td></tr></table></figure>
<p>GET命令用于查看key对应的值，我们下面会讲到。</p>
<p>第一个get，由于name这个key不存在，因此我们得到的结果是nil，也就是说空。</p>
<p>之后我们设置一次，然后就get到我们的结果了。之后再设置一次，会发现结果已经被覆盖。</p>
<p>这就是set命令的基本用法。</p>
<p>我们看到文档的后面有一些可选的参数。下面依次来介绍：</p>
<p><strong>EX seconds</strong>：设置key的过时时间，单位为秒。</p>
<p><strong>PX milliseconds</strong>：设置key的过期时间，单位为毫秒。</p>
<p>以ex为例：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">redis&gt; set name miaoerduo ex 10</span><br><span class="line">OK</span><br><span class="line">redis&gt; get name</span><br><span class="line">&quot;miaoerduo&quot;</span><br><span class="line">redis&gt; get name</span><br><span class="line">(nil)</span><br></pre></td></tr></table></figure>
<p>我们设置了name的过期时间为10秒。</p>
<p>在设置完name之后，立刻获取name的值，可以得到正确的输出。但是过了10s之后，发现只能得到一个nil。说明这个key已经被清空了。</p>
<p>那么这个设置有什么作用呢？</p>
<p>我们说几个常见的应用场景：</p>
<h5 id="a验证码">a，验证码</h5>
<p>我们经常在登录一些网站或是进行付款等操作的时候，都会收到一些验证码，并且说10min后失效。</p>
<p>实际上就可以通过下面一条指令来实现：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">set phone_num code ex 600</span><br></pre></td></tr></table></figure>
<p>用手机号作为key，验证码作为值，超时6min。这样当你输入好验证码，提交的时候，后台就可以了先 <code>get phone_num</code>，再比较你的输入和数据库里面存的值，从而完成身份的验证。</p>
<h5 id="bsession">b，session</h5>
<p>早前，网站通过cookie来保存用户的用户名和密码，之后出现了很多的安全隐患，因此就提出了session的机制。</p>
<p>用户登陆成功之后，本地的cookie会保留一个较长的随机码，而网站后的后台则存储了这个随机码和你的用户id的对应关系。在你第二次登录的时候，cookie会传输到后台，而后台则根据你的随机码，获取你的用户信息，如果符合，则自动登录。这样，即使网站上有不法分子获取了你的cookie，也得不到你的任何信息，因为你的真实的有用的信息都存储在网站的后台。</p>
<p>我们在登录邮箱的时候，通常都会有一个选项，7天内自动登录。这其实就是给后台存的session设置了一个超时。聪明的你是不是已经会自己实现了呢？</p>
<p><strong>NX</strong>：（if Not eXist）只有键key不存在的时候才会设置key的值</p>
<p><strong>XX</strong>：只有键key存在的时候才会设置key的值 NX通常用于实现锁机制，XX的功能，小喵暂时木有头绪。。。想到应用场景的筒子们可以留言告诉小喵，小喵看到了一定会更新的。</p>
<p>举个例子：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">redis&gt; del name</span><br><span class="line">(integer) 0</span><br><span class="line">redis&gt; get name</span><br><span class="line">(nil)</span><br><span class="line">redis&gt; set name miaoerduo nx</span><br><span class="line">OK</span><br><span class="line">redis&gt; set name miaoerduo nx</span><br><span class="line">(nil)</span><br><span class="line">redis&gt; get name</span><br><span class="line">&quot;miaoerduo&quot;</span><br><span class="line">redis&gt; set name miao xx</span><br><span class="line">OK</span><br><span class="line">redis&gt; get name</span><br><span class="line">&quot;miao&quot;</span><br><span class="line">redis&gt; del name</span><br><span class="line">(integer) 1</span><br><span class="line">redis&gt; set name miao xx</span><br><span class="line">(nil)</span><br></pre></td></tr></table></figure>
<p>首先我们使用del指令删除了name（这个指令后面会介绍）。之后使用nx模式设置name，第一次成功了，而第二次就失败了，因为name已经存在了。之后使用xx模式修改，发现确实可以。del name之后，再使用xx模式，就失败了，因为此时的name已经不存在了。</p>
<p>以上，就是set的所有的用法。</p>
<h4 id="setrange-key-offset-value">2，SETRANGE key offset value</h4>
<p>Overwrite part of a string at key starting at the specified offset</p>
<p>这个命令可以覆盖掉key对应的string的一部分。offset表示需要覆盖的字符串的起始位置，之后会用value的值，覆盖掉原始string的对应位置的数据。</p>
<p>这里有一些比较有意思的操作：如果原始key不存在，则默认为一个长度为0的字符串。如果offset超过原始string的长度，那么会在之前的string后面补充0以达到offset。如果value的长度超过了原始string后面可以覆盖的部分，则Redis内部会重新申请内存，完成数据的追加（还记得上一章的内容吗？），这时候数据库由于需要分配内存，可能会出现阻塞（需要分配的内存越大，阻塞时间越长）。</p>
<p>例子：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">redis&gt; set str &quot;hello world&quot;</span><br><span class="line">OK</span><br><span class="line">redis&gt; setrange str 6 redis</span><br><span class="line">(integer) 11</span><br><span class="line">redis&gt; get str</span><br><span class="line">&quot;hello redis&quot;</span><br><span class="line">redis&gt; setrange str 15 aha</span><br><span class="line">(integer) 18</span><br><span class="line">redis&gt; get str</span><br><span class="line">&quot;hello redis\x00\x00\x00\x00aha&quot;</span><br><span class="line">redis&gt; del str</span><br><span class="line">(integer) 1</span><br><span class="line">redis&gt; setrange str 5 &quot;hello miao&quot;</span><br><span class="line">(integer) 15</span><br><span class="line">redis&gt; get str</span><br><span class="line">&quot;\x00\x00\x00\x00\x00hello miao&quot;</span><br></pre></td></tr></table></figure>
<p>在这个实验中，我们先新建了一个str，内容是"hello world"，之后，从位置6开始写入字符串"redis"，则得到了"hello redis"。之后我们在15的位置，写入"aha"，这是offset已经比字符串的长度要大了，则Redis会默认填充0（\x00是0的16进制表达），之后再追加字符串。最后，我们给一个不存在的key使用setrange设置了一个值，结果表现得和空字符串一样。</p>
<h4 id="mset-key-value-key-value-...">3，MSET key value [key value ...]</h4>
<p>Set multiple keys to multiple values</p>
<p>一次性设置多个key-value。如果key的值已存在，则会直接覆盖。 相当与同时调用多次SET命令。不过要注意的是，这个操作是原子的。也就是说，所有的keys都一次性设置的。如果你同时运行两个MSET来设置相同的keys，那么操作的结果也只会是两次MSET中的后一次的结果，而不会是混杂的结果。</p>
<p>例子：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">redis&gt; mset name1 miaoerduo name2 miao name3 love</span><br><span class="line">OK</span><br><span class="line">redis&gt; get name1</span><br><span class="line">&quot;miaoerduo&quot;</span><br><span class="line">redis&gt; get name2</span><br><span class="line">&quot;miao&quot;</span><br><span class="line">redis&gt; get name3</span><br><span class="line">&quot;love&quot;</span><br></pre></td></tr></table></figure>
<h4 id="msetnx-key-value-key-value-...">4，MSETNX key value [key value ...]</h4>
<p>Set multiple keys to multiple values, only if none of the keys exist</p>
<p>一次性设置多个key-value。如果存在任何一个key已经存在，那么这个操作都不会执行。所以，当使用MSETNX的时候，要么全部key都被修改，要么全部都不被修改。</p>
<p>当然这个操作也是原子的。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">redis&gt; get name3</span><br><span class="line">&quot;love&quot;</span><br><span class="line">redis&gt; msetnx name3 miaoerduo name4 miao</span><br><span class="line">(integer) 0</span><br><span class="line">redis&gt; get name4</span><br><span class="line">(nil)</span><br><span class="line">redis&gt; msetnx name4 miaoerduo name5 miao</span><br><span class="line">(integer) 1</span><br><span class="line">redis&gt; get name4</span><br><span class="line">&quot;miaoerduo&quot;</span><br><span class="line">redis&gt; get name5</span><br><span class="line">&quot;miao&quot;</span><br></pre></td></tr></table></figure>
<p>当name3已经存在的时候，我们设置name3和name4，发现，连name4都没有创建。当设置name4和name5的时候，由于这两个key之前都不存在，因此设置成功了。</p>
<h4 id="setex-key-seconds-value">5，SETEX key seconds value</h4>
<p>Set the value and expiration of a key</p>
<p>设置key的值和超时。和前面的set key value ex seconds一样。</p>
<h4 id="psetex-key-milliseconds-value">6，PSETEX key milliseconds value</h4>
<p>Set the value and expiration in milliseconds of a key</p>
<p>设置key的值和超时。和前面的set key value px milliseconds一样。</p>
<h4 id="setnx-key-value">7，SETNX key value</h4>
<p>Set the value of a key, only if the key does not exist</p>
<p>当key不存在的时候，设置key的值。和set key value nx一样。</p>
<h3 id="二get的相关操作">二、GET的相关操作</h3>
<h4 id="get-key">1，GET key</h4>
<p>Get the value of a key</p>
<p>根据给定的key，获取value值。这个操作，我们之前已经使用了很多次。</p>
<p>如果key不存在，会返回nil。如果key对应的值不是string(List，Set等)，则会报错，因为GET只能处理string类型的value。</p>
<p>演示如下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">redis&gt; get name</span><br><span class="line">(nil)</span><br><span class="line">redis&gt; set name &quot;miaoerduo&quot;</span><br><span class="line">OK</span><br><span class="line">redis&gt; get name</span><br><span class="line">&quot;miaoerduo&quot;</span><br><span class="line">redis&gt; lpush arr 1 2 3 4 5</span><br><span class="line">(integer) 5</span><br><span class="line">redis&gt; get arr</span><br><span class="line">(error) WRONGTYPE Operation against a key holding the wrong kind of value</span><br></pre></td></tr></table></figure>
<p>注意，lpush是List部分的内容，arr的值是一个List的结构。这里只需要知道key不是string类型的时候，get的时候就会报错。</p>
<h4 id="mget-key-key-...">2，MGET key [key ...]</h4>
<p>Get the values of all the given keys</p>
<p>还记得我们之前可以同时设置多个key-value吗？其实我们也可以一次性获取多个key的值。如果key不存在，则对应的地方返回nil。</p>
<p>那么一次性获取多个值和单独一次一次GET有什么区别呢？</p>
<p>小喵认为，主要有两点，原子操作和查询效率。</p>
<p>比如博客上会统计评论数和访问数。如果我们依次读取这两部分的值，那么可能得到的两个值并不是同一时刻的。而如果使用MGET，则得到的一定是同一时刻的。这就是原子操作的威力（注，官方文档只介绍了SET和GET的操作是原子的，并没有说MGET是不是，这里小喵果断的说MGET是原子操作也是不合理的。但考虑到使用MGET的时候，是把查询指令一次性传输到后台来执行，所以应该原子操作。无论如何，在上面的例子中，使用MGET总是比两次GET要合理的）。</p>
<p>另外，如果数据库的查询，都分为三个过程，传输查询指令，执行指令，输出结果。</p>
<p>如果分多次GET的话，在传输指令和输出结果的这两个部分就要重复很多次，效率大打折扣。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">redis&gt; set key1 a</span><br><span class="line">OK</span><br><span class="line">redis&gt; set key2 b</span><br><span class="line">OK</span><br><span class="line">redis&gt; mget key1 key2 key3</span><br><span class="line">1) &quot;a&quot;</span><br><span class="line">2) &quot;b&quot;</span><br><span class="line">3) (nil)</span><br></pre></td></tr></table></figure>
<p>我们只创建了key1和key2，使用MGET获取的时候，由于没有key3，对应的位置返回了nil。</p>
<h3 id="getrange-key-start-end">3，GETRANGE key start end</h3>
<p>Get a substring of the string stored at a key</p>
<p>该指令只要用于获取字符串的字串，在Redis2.0版本之前，叫做SUBSTR。strat和end是字串的起始和结束的位置，可以用负数来表示距离string尾部的未知的下标。-1是最后一个字符，-2是底数第二个字符。这个表达方式和Python的获取list的子list非常相似。</p>
<p>需要注意的有两点：</p>
<p>字串包括了start和end这两个位置的字符。在Python中是不包含end的。</p>
<p>当给出的start和end超出了string的范围时，指令只会返回在string内的结果。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">redis&gt; set str &quot;hello world&quot;</span><br><span class="line">OK</span><br><span class="line">redis&gt; getrange str 6 -1</span><br><span class="line">&quot;world&quot;</span><br><span class="line">redis&gt; getrange str -5 -1</span><br><span class="line">&quot;world&quot;</span><br><span class="line">redis&gt; getrange str 0 4</span><br><span class="line">&quot;hello&quot;</span><br><span class="line">redis&gt; getrange str 0 100</span><br><span class="line">&quot;hello world&quot;</span><br></pre></td></tr></table></figure>
<p>上述例子是几种情况下的输出。</p>
<h4 id="getset-key-value">4，GETSET key value</h4>
<p>Set the string value of a key and return its old value</p>
<p>设置key的值，并返回之前的值。如果之前key的值不是string，则会报错。</p>
<p>这个指令相当于先GET，再SET。</p>
<p>这个指令可以用来配合INCR指令一起使用支持重置的技术功能（INCR我们后面会讲到）。先设置count为0，每次INCR使得count加1。等到需要获取计数的时候，使用GETSET count 0，就能获取计数的值，并且把计数器重置了。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">redis&gt; set str &quot;hello&quot;</span><br><span class="line">OK</span><br><span class="line">redis&gt; getset str &quot;world&quot;</span><br><span class="line">&quot;hello&quot;</span><br><span class="line">redis&gt; get str</span><br><span class="line">&quot;world&quot;</span><br></pre></td></tr></table></figure>
<p>第一行设置str为"hello"</p>
<p>第二行获取了str原先的值，并把str设置成"world"。</p>
<p>第三行GET的时候，就是修改之后的值了。</p>
<h3 id="三string的修改操作">三、string的修改操作</h3>
<h4 id="strlen-key">1，STRLEN key</h4>
<p>Get the length of the value stored in a key</p>
<p>返回key对应的string的长度，如果key对应的不是string，则报错。如果key不存在，则返回0（还是把key对应的看成空字符串）。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">redis&gt; set str &quot;hello world&quot;</span><br><span class="line">OK</span><br><span class="line">redis&gt; strlen str</span><br><span class="line">(integer) 11</span><br><span class="line">redis&gt; get nokey</span><br><span class="line">(nil)</span><br><span class="line">redis&gt; strlen nokey</span><br><span class="line">(integer) 0</span><br></pre></td></tr></table></figure>
<h4 id="append-key-value">2，APPEND key value</h4>
<p>Append a value to a key</p>
<p>如果key已经存在，且值为string，则将value追加到值的后面，如果key不存在，则会创建一个空的字符串的key，然后执行追加操作。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">redis&gt; set str &quot;hello&quot;</span><br><span class="line">OK</span><br><span class="line">redis&gt; append str &quot; world&quot;</span><br><span class="line">(integer) 11</span><br><span class="line">redis&gt; get str</span><br><span class="line">&quot;hello world&quot;</span><br><span class="line">redis&gt; del str</span><br><span class="line">(integer) 1</span><br><span class="line">redis&gt; append str &quot;hello&quot;</span><br><span class="line">(integer) 5</span><br><span class="line">redis&gt; get str</span><br><span class="line">&quot;hello&quot;</span><br></pre></td></tr></table></figure>
<p>这个例子中，我们先向已有值的str中append了一个字符串。然后向不存在的key，也添加了字符串。</p>
<h4 id="incr-key">3，INCR key</h4>
<p>Increment the integer value of a key by one</p>
<p>对存储在key的整数值进行原子的加1操作。</p>
<p>如果key不存在，则会设置默认值0，再加1。</p>
<p>如果key存在，但是存储的值不是字符串，或者存储的字符串不能表示整数，则执行该操作时，会报错。</p>
<p>这个操作仅限于64位的有符号的整型数据。</p>
<p>比较有意思的是，虽然这个key存储的值是个字符串，但是该操作的效果和对相应的数字进行操作一样。而且，Redis中，在存储这类字符串的时候，底层上其实存储的就是一个整数，因此不存在存储上的浪费。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">redis&gt; set count 0</span><br><span class="line">OK</span><br><span class="line">redis&gt; incr count</span><br><span class="line">(integer) 1</span><br><span class="line">redis&gt; incr count</span><br><span class="line">(integer) 2</span><br><span class="line">redis&gt; del count</span><br><span class="line">(integer) 1</span><br><span class="line">redis&gt; get count</span><br><span class="line">(nil)</span><br><span class="line">redis&gt; incr count</span><br><span class="line">(integer) 1</span><br><span class="line">redis&gt; incr count</span><br><span class="line">(integer) 2</span><br></pre></td></tr></table></figure>
<p>值得注意的是，该操作是原子操作，即使有多个请求传输到Redis，count执行的结果都不会错误，所以我们可以大胆放心的用这个功能实现多线程的计数功能。</p>
<h4 id="decr-key">4，DECR key</h4>
<p>Decrement the integer value of a key by one 对存储在key的整数值进行原子的减1操作。</p>
<p>注意事项和INCR一样。</p>
<h4 id="incrby-key-increment">5，INCRBY key increment</h4>
<p>Increment the integer value of a key by the given amount</p>
<p>对存储在key的整数值进行原子的加操作，加increment。</p>
<p>如果key不存在，操作之前，key就会被置为0。如果key的value类型错误或者是个不能表示成数字的字符串，就返回错误。这个操作最多支持64位有符号的整型数字。基本上和INCR一样。</p>
<h4 id="decrby-key-decrement">6，DECRBY key decrement</h4>
<p>Decrement the integer value of a key by the given number</p>
<p>对存储在key的整数值进行原子的减操作，减increment。</p>
<p>其他和INCR一样。</p>
<h4 id="incrbyfloat-key-increment">7，INCRBYFLOAT key increment</h4>
<p>Increment the float value of a key by the given amount</p>
<p>对存储造key中的浮点数进行原子的加操作，加increment。</p>
<p>如果key不存在，操作之前，key就会被置为0。如果key的value类型错误或者是个不能表示成浮点数的字符串，就返回错误。</p>
<p>我们并没有DECRBYFLOAT这个操作，因此想要实现减操作，只需要把increment设成负的就可以。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">redis&gt; set a 1.5</span><br><span class="line">OK</span><br><span class="line">redis&gt; incrbyfloat a 10.1</span><br><span class="line">&quot;11.6&quot;</span><br><span class="line">redis&gt; incrbyfloat a 10.1</span><br><span class="line">&quot;21.7&quot;</span><br><span class="line">redis&gt; incrbyfloat a -10.1</span><br><span class="line">&quot;11.6&quot;</span><br><span class="line">redis&gt; incrbyfloat a -1.5e2</span><br><span class="line">&quot;-138.39999999999999999&quot;</span><br></pre></td></tr></table></figure>
<p>浮点数可以用一般的小数和科学计数法表示。</p>
<h3 id="四二进制操作">四、二进制操作</h3>
<h4 id="setbit-key-offset-value">1，SETBIT key offset value</h4>
<p>Sets or clears the bit at offset in the string value stored at key</p>
<p>设置或者清空key的value(字符串)在offset处的bit值。这里将string看成由bit组成的数组。</p>
<p>指定位置的bit可以被设置，或者被清空，这个由value（只能是0或者1）来决定。当key不存在的时候，就创建一个新的字符串value。要确保这个字符串足够长到在offset处有bit值。参数offset需要大于等于0，并且小于2^32(限制bitmap大小为512)。当key对应的字符串增大的时候，新增的部分bit值都是设置为0。</p>
<p>该操作返回value原来的offset位置的bit值。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">redis&gt; setbit a 0 1</span><br><span class="line">(integer) 0</span><br><span class="line">redis&gt; setbit a 1 1</span><br><span class="line">(integer) 0</span><br><span class="line">redis&gt; setbit a 2 1</span><br><span class="line">(integer) 0</span><br><span class="line">redis&gt; setbit a 3 1</span><br><span class="line">(integer) 0</span><br><span class="line">redis&gt; get a</span><br><span class="line">&quot;\xf0&quot;</span><br></pre></td></tr></table></figure>
<p>a最开始不存在，使用setbit操作，将a的前4位都设置成1。最终就得到了\xf0，这是16进制表示的结果，前4位都是1，其他都是0。</p>
<h4 id="getbit-key-offset">2，GETBIT key offset</h4>
<p>Returns the bit value at offset in the string value stored at key</p>
<p>获取key对应的string在offset处的bit值。当offset超出了字符串长度的时候，这个字符串就被假定为由0比特填充的连续空间。当key不存在的时候，它就认为是一个空字符串，所以offset总是超出范围，然后value也被认为是由0比特填充的连续空间。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">redis&gt; setbit a 7 1</span><br><span class="line">(integer) 0</span><br><span class="line">redis&gt; getbit a 0</span><br><span class="line">(integer) 0</span><br><span class="line">redis&gt; getbit a 7</span><br><span class="line">(integer) 1</span><br><span class="line">redis&gt; getbit a 100</span><br><span class="line">(integer) 0</span><br></pre></td></tr></table></figure>
<h4 id="bitcount-key-start-end">3，BITCOUNT key [start end]</h4>
<p>Count set bits in a string</p>
<p>统计key的string的二进制中1的个数。 start和end分别表示string的起始和结束位置，含义和GETRANGE中一样。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">redis&gt; setbit mykey 0 1</span><br><span class="line">(integer) 0</span><br><span class="line">redis&gt; setbit mykey 10 1</span><br><span class="line">(integer) 0</span><br><span class="line">redis&gt; setbit mykey 5 1</span><br><span class="line">(integer) 0</span><br><span class="line">redis&gt; bitcount mykey</span><br><span class="line">(integer) 3</span><br></pre></td></tr></table></figure>
<h4 id="bitop-operation-destkey-key-key-...">4，BITOP operation destkey key [key ...]</h4>
<p>Perform bitwise operations between strings</p>
<p>对string进行bit级别的操作。具体操作有4种。AND，OR，XOR，NOT。</p>
<p>用法如下：</p>
<p>BITOP AND destkey srckey1 srckey2 srckey3 ... srckeyN</p>
<p>BITOP OR destkey srckey1 srckey2 srckey3 ... srckeyN</p>
<p>BITOP XOR destkey srckey1 srckey2 srckey3 ... srckeyN</p>
<p>BITOP NOT destkey srckey NOT</p>
<p>操作后面只有一个目标key和srckey，是因为NOT操作是一元的。</p>
<p>对于AND，OR和XOR操作，Redis会将srckey1，srckey2，...，srckeyN这些字符串对位进行相关操作，之后将结果存入destkey中。</p>
<p>如果srckey的length不相等的话，Redis内部会将短的字符串补齐，并填充上0。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">redis&gt; set key1 &quot;\xf0\xf0&quot;</span><br><span class="line">OK</span><br><span class="line">redis&gt; set key2 &quot;\x0f\x0f&quot;</span><br><span class="line">OK</span><br><span class="line">redis&gt; set key3 &quot;\xff\xff&quot;</span><br><span class="line">OK</span><br><span class="line">redis&gt; bitop and destkey key1 key2 key3</span><br><span class="line">(integer) 2</span><br><span class="line">redis&gt; get destkey</span><br><span class="line">&quot;\x00\x00&quot;</span><br><span class="line">redis&gt; bitop or destkey key1 key2 key3</span><br><span class="line">(integer) 2</span><br><span class="line">redis&gt; get destkey</span><br><span class="line">&quot;\xff\xff&quot;</span><br><span class="line">redis&gt; bitop xor destkey key1 key2 key3</span><br><span class="line">(integer) 2</span><br><span class="line">redis&gt; get destkey</span><br><span class="line">&quot;\x00\x00&quot;</span><br><span class="line">redis&gt; bitop not destkey key1</span><br><span class="line">(integer) 2</span><br><span class="line">redis&gt; get destkey</span><br><span class="line">&quot;\x0f\x0f&quot;</span><br></pre></td></tr></table></figure>
<h4 id="bitpos-key-bit-start-end">5，BITPOS key bit [start] [end]</h4>
<p>Find first bit set or clear in a string</p>
<p>返回string的二进制中第一个0或1的位置。</p>
<p>这里将string看成一个有许多bit组成的数组。其中start和end表示string的一个间隔，如果指定了start和end，则BITPOS只会查询这个区间。注意，start和end表示的字符的位置，不是bit的位置。</p>
<p>即使指定了start和end，BITPOS这个操作也只会这个目标bit的绝对地址。</p>
<p>有几点需要注意：</p>
<p>在没有指定查询区间或只指定start的时候，查询bit位为0的位置时，如果string中没有该位，则会返回string的bit位的总数。比如在\xff\xff\xff直接查询bit为0的位置，Redis默认该字符串后面都是0，因此，返回的结果就是12（下标从0开始数的）。</p>
<p>如果指定了查询区间，无论查询0或是1，在没查询到的时候只会返回-1。</p>
<p>在没有指定查询区间时，查询bit位为1的位置时，如果string中没有该位，则会返回-1，表示未查询到。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">redis&gt; set bits &quot;\x00\xff\x00\xff&quot;</span><br><span class="line">OK</span><br><span class="line">redis&gt; strlen bits</span><br><span class="line">(integer) 4</span><br><span class="line">redis&gt; bitpos bits 0</span><br><span class="line">(integer) 0</span><br><span class="line">redis&gt; bitpos bits 1</span><br><span class="line">(integer) 8</span><br><span class="line">redis&gt; bitpos bits 0 1</span><br><span class="line">(integer) 16</span><br><span class="line">redis&gt; bitpos bits 0 1 -1</span><br><span class="line">(integer) 16</span><br><span class="line">redis&gt; bitpos bits 1 1 -1</span><br><span class="line">(integer) 8</span><br><span class="line">redis&gt; bitpos bits 0 3</span><br><span class="line">(integer) 32</span><br><span class="line">redis&gt; bitpos bits 0 3 3</span><br><span class="line">(integer) -1</span><br></pre></td></tr></table></figure>
<p>看上面的例子，bits的初始设置的二进制表示为：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">00000000 11111111 00000000 11111111</span><br></pre></td></tr></table></figure>
<p>直接获取0（bitpos bits 0）的位置为0，获取1（bitpos bits 1）的位置为8。</p>
<p>指定开始位置为1（start = 1）的时候，0第一次出现（bitpos bits 1）的位置为16。</p>
<p>指定起始位置为3（start = 3）的时候，0第一次出现（bitpos bits 0 3）的位置为32。</p>
<p>这是因为Redis在查询字符串的时候查询到了字符串的末尾，之后默认末尾的后面都是0，因此得到了32这的个位置。</p>
<p>最后，当指定必须在第3个字节（从0开始数）查询0的时候，由于查不到0，因此只返回了-1。</p>
<p>这篇博客的内容，小喵自己也感觉有点多，每个指令小喵都是参考了官方的文档，然后都自己动手做了实验。可能有些地方，小喵没有理解的很好，这时就需要大家多多指教了。</p>
<p><strong>转载</strong>请注明出处~</p>
]]></content>
      <categories>
        <category>C++</category>
        <category>Redis</category>
      </categories>
  </entry>
  <entry>
    <title>一、Redis基本操作——String(原理篇)</title>
    <url>/2016/04/25/redis-string-principles/</url>
    <content><![CDATA[<blockquote>
<p>小喵的唠叨话：最近京东图书大减价，小喵手痒了就买了本《Redis设计与实现》[<span class="exturl" data-url="aHR0cDovL3JlZGlzYm9vay5jb20v" title="http://redisbook.com/">1<i class="fa fa-external-link"></i></span>]来看看。这里权当小喵看书的笔记啦。这一系列的模式，主要是先介绍Redis的实现原理（可能很大一部分会直接照搬原作者的描述），加上小喵自己的想法，之后配合Redis官网上的各种相关的操作命令（原书上貌似没有很多的介绍命令）。</p>
</blockquote>
<p>本章介绍Redis中最常用到的字符串(String)。</p>
<a id="more"></a>
<h2 id="redis的字符串string的实现">Redis的字符串(String)的实现</h2>
<p>小喵之前有看到过《Redis设计与实现》的一部分章节。这是第一章的内容，小喵也是因为看了这一章的内容，才决定要买本仔细研究的。</p>
<p>首先，我们知道Redis是由C语言编写的，以高效和轻量著称。而C语言中的字符串是怎么实现的呢？字符数组。 比如一个简单的字符串"hello world"，其实是一个如下的字符的数组：<code>['h', 'e', 'l', 'l', 'o', ' ', 'w', 'o', 'r', 'l', 'd', '\0']</code>，最后的一个'\0'是空字符，表示字符串的结尾。</p>
<p>Redis由于各种原因，并没有直接使用了C语言的字符串结构，而是对其做了一些封装，得到了自己的简单动态字符串(simple dynamic string, SDS)的抽象类型。Redis中，默认以SDS作为自己的字符串表示。只有在一些字符串不可能出现变化的地方使用C字符串。</p>
<p><strong>SDS的定义</strong>如下：</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">sdshdr</span> &#123;</span></span><br><span class="line">    <span class="comment">// 用于记录buf数组中使用的字节的数目</span></span><br><span class="line">    <span class="comment">// 和SDS存储的字符串的长度相等</span></span><br><span class="line">    <span class="keyword">int</span> len;</span><br><span class="line">    <span class="comment">// 用于记录buf数组中没有使用的字节的数目</span></span><br><span class="line">    <span class="keyword">int</span> <span class="built_in">free</span>;</span><br><span class="line">    <span class="comment">// 字节数组，用于储存字符串</span></span><br><span class="line">    <span class="keyword">char</span> buf\[\];</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure>
<p>可以看出来，SDS的结构并不复杂:</p>
<ul>
<li>buf是一块可用的内存空间，通常大小会大于等于需要存储的字符串的大小（大于？为什么要大于呢？读者可以思考一下）。</li>
<li>len表示字符串的长度，也表示buf中已经被使用的空间的大小。</li>
<li>free表示buf中没有被使用的空间的大小。</li>
<li>要注意的是，buf的大小等于len+free+1，其中多余的1个字节是用来存储'\0'的。</li>
</ul>
<p>那么这么封装到底有什么好处呢？我们一点一点剖析。</p>
<h3 id="常数复杂度获取字符串长度">1，常数复杂度获取字符串长度</h3>
<p>在C语言中的字符串只是简单的字符的数组，当使用strlen获取字符串长度的时候，C语言内部其实是直接顺序遍历数组的内容，找到对应的<code>'\0'</code>对应的字符，从而计算出字符串的长度。显然这个算法复杂度和字符串的长度成正比，即<code>O(N)</code>。而对于SDS来说，只需要访问SDS的len属性就能得到字符串的长度，复杂度为<code>O(1)</code>。这样，获取字符串长度的操作就不会成为Redis的瓶颈（当然len的作用不止这么简单，后面还会介绍别的）。</p>
<h3 id="杜绝缓冲区溢出">2，杜绝缓冲区溢出</h3>
<p>我们知道<code>C++</code>里面的字符串使用了<code>STL</code>的<code>string</code>类型，我们开发者不太需要关注内存的分配和释放的过程。但是Redis是C语言编写的，并没有这么方便的数据类型。对于字符串的拼接、复制等操作，C语言开发者必须确保目标字符串的空间足够大，不然就会出现溢出的情况。</p>
<figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">char</span> a[<span class="number">10</span>] = <span class="string">"hello"</span>;</span><br><span class="line"><span class="built_in">strcat</span>(a, <span class="string">" world"</span>);</span><br><span class="line"><span class="built_in">strcpy</span>(a, <span class="string">"hello world"</span>);</span><br></pre></td></tr></table></figure>
<p>上面的三句代码，就是C语言的字符串拼接和复制的使用，但是明显出现了缓冲区溢出的问题。字符数组a的长度是10，而<code>"hello world"</code>字符串的长度为11，则需要12个字节的空间来存储（不要忘记了'\0'）。</p>
<p>然后，我们看看Redis的SDS是怎么处理字符串修改的这种情况。</p>
<p>当使用SDS的API对字符串进行修改的时候，API内部第一步会检测字符串的大小是否满足。如果空间已经满足要求，那么就像C语言一样操作即可。如果不满足，则拓展buf的空间，使得满足操作的需求，之后再进行操作。每次操作之后，len和free的值会做相应的修改。</p>
<p>这就是SDS的全部的高明之处了吗？当然不！</p>
<p>当API发现SDS的buf的容量不够的时候，并不是简单申请正好适合的大小，而是额外申请了一倍的空间！我们以sds的API <code>sdscat</code> 函数为例，该函数实现了sds的拼接的功能。</p>
<p>下面的例子是<code>"hello"</code> 和<code>" world"</code>的拼接的过程。</p>
<p><img data-src="http://cdn.miaoerduo.com/images/sds-before.png" alt="修改之前的sds" /></p>
<center>
图1 sdscat执行之前的sds
</center>
<p><img data-src="http://cdn.miaoerduo.com/images/sds-after.png" alt="修改之后的sds" /></p>
<center>
图2 sdscat执行之后的sds
</center>
<p>这里的buf的容量是23（free + len + 1）。为什么要这么做呢？耐心向下看吧。</p>
<h3 id="减少修改字符串时带来的内存重分配次数">3，减少修改字符串时带来的内存重分配次数</h3>
<p>我们之前说到，对于一个N长的字符串，C语言中底层是一个N+1长的字符数组（有一个字节存放空字符）。C字符串的长度和底层数组之间的长度存在着这样的关系，因此当进行字符串的操作而导致字符串长度发生变化的时候，需要对内存进行重新分配。</p>
<ul>
<li>如果操作会增长字符串，那么在执行之前，就需要进行内存分配扩充底层数组的大小。</li>
<li>如果是缩短字符串的操作，则需要释放额外的内存</li>
</ul>
<p>注：这是书中的意思，但小喵觉得如果字符串缩小的话，其实并不用立刻释放内存，如果字符串是malloc出来的话，需要释放的直接free就可以，也不需要给定空间的大小，所以不会出现内存泄露。当然，也可能Redis里面是用别的方式实现，这样小喵就不懂了）。</p>
<p>对于一般的程序而言，如果修改字符串的操作并不是特别常出现，那么每次修改都重新分配一下内存也是可以接受的。但是Redis作为一个数据库，其读写速度，数据修改频率都被要求达到很高的效率。因此这种低效的方式并不适合Redis。</p>
<p>为了避免C字符串的这些弊端，SDS通过<strong>未使用空间</strong>解除了字符串长度和底层数组长度之间的关系。也就是之前说的buf的长度为len和free之和（再加1）。数字里面可以包含未使用的空间，大小用free表示。</p>
<p>Redis主要通过以下两种策略来处理内存问题：</p>
<h4 id="i-空间预分配">i) 空间预分配</h4>
<p>这种方式用于处理字符串长度增加的问题。如果对字符串的修改使得字符串的长度增加，API首先会判断buf的空间大小是否满足，如果满足则直接操作，如果不满足，则进行如下操作：</p>
<p>如果对SDS进行修改之后的，SDS的长度（即len的值）小于1MB。程序将额外分配和len一样大小的未使用空间。以上面的"hello" + " world"的操作为例。在这个例子中"hello"的len是5（不考虑'\0')，修改之后的字符串"hello world"长度为11，那么新的SDS的buf的容量就是11*2+1。其中len和free都是11，多余的1字节用来存储'\0'。</p>
<p>如果对SDS修改之后的长度大于1MB，那么程序会分配1MB的未使用空间。比如原数据是5MB，修改之后需要6MB的空间，进行修改的操作后，buf的实际空间应该是7MB，其中len为6MB，free为1MB。</p>
<p>那么这些未使用空间能够做什么呢？为什么根据SDS的修改会的大小会有两种不同的分配原则呢？</p>
<p>小喵是这么认为的，如果数据被更改，则说明这个数据很可能会被再次更改，如果能够提前分配多余的空间，那么下一次变化的时候很可能就不需要再次分配空间了。如果数据比较小（&lt;1MB）的时候，可以分配等大的未使用空间。但是如果数据已经很大的时候（&gt;1MB），再分配同等大小的内存会显得十分浪费，毕竟不能确保这个字符串一定会被再次修改，所以只额外分配1MB的空间。</p>
<p>通过这种策略，SDS可以做到N次修改，最多进行N次内存分配。而C字符串在N次修改则一定要进行N次内存分配。一个是至多N次，一个是一定N次。用小喵的脑袋想，也觉得SDS这个策略简单、粗暴、高效。</p>
<h4 id="ii-惰性空间释放">ii) 惰性空间释放</h4>
<p>当执行字符串长度缩短的操作的时候，SDS并不直接重新分配多出来的字节，而是修改len和free的值（len相应减小，free相应增大，buf的空间大小不变化）。通过惰性空间释放，可以很好的避免缩短字符串需要的内存重分配的情况。而且多余的空间也可以为将来可能有的字符串增长的操作做优化。</p>
<p>当然，SDS也提供直接释放未使用空间的API，在需要的时候，也能真正的释放掉多余的空间。</p>
<h3 id="二进制安全">4，二进制安全</h3>
<p>C字符串中的字符必须符合某种编码（比如ASCII），并且字符串除了末尾之外不能出现空字符，否则会被程序认为是字符串的结尾。这就使得C字符串只能存储文本数据，而不能保存图像，音频等二进制数据。（这里，小喵的观点是不同的，小喵本人是做图像的，opencv等的库，都是使用unsigned char*来存储图像的数据。我们完全可以把字符数组看成一堆内存，存放任何数据都可以）</p>
<p>使用SDS就不需要依赖控制符，而是用len来指定存储数据的大小。同时所有的SDS操作的API都是二进制安全的（binary-safe），所有的SDS API都会以处理二进制的方式来处理SDS的buf的数据。程序不会对buf的数据做任何限制、过滤或假设，数据写入的时候是什么，读取的时候依然不变。</p>
<p>这也是我们将SDS的buf属性程序字节数组的原因，Redis不是使用这个数组来保存字符，而是储存一系列二进制数据。</p>
<h3 id="兼容部分c字符串函数">5，兼容部分C字符串函数</h3>
<p>由于SDS的buf的定义和C字符串完全相同，因此很多的C字符串的操作都是适用于SDS-&gt;buf的。比如当buf里面存的是文本字符串的时候，printf函数，也完全可以试用。这样，Redis就不需要为所有的字符串的处理编写自己的函数，大多数通过调用C语言的函数就可以。</p>
<h2 id="总结">总结</h2>
<table>
<thead>
<tr class="header">
<th>C字符串</th>
<th>SDS</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>获取字符串长度的复杂度为O(N)</td>
<td>获取字符串长度的复杂度为O(1)</td>
</tr>
<tr class="even">
<td>API是不安全的，可能会造成缓冲区溢出</td>
<td>API是安全的，不会造成缓冲区溢出</td>
</tr>
<tr class="odd">
<td>修改字符串长度N次必然需要执行N次内存重分配</td>
<td>修改字符串长度N次最多需要执行N次内存重分配</td>
</tr>
<tr class="even">
<td>只能保存文本数据</td>
<td>可以保存文本或者二进制数据</td>
</tr>
<tr class="odd">
<td>可以使用所有库中的函数</td>
<td>可以使用一部分库的函数</td>
</tr>
</tbody>
</table>
<p>以上则是Redis的string结构的原理部分。下一章我们会介绍一些string操作的redis命令。</p>
<p><strong>转载请注明出处。</strong></p>
<p>参考： [1] <span class="exturl" data-url="aHR0cDovL3JlZGlzYm9vay5jb20v" title="http://redisbook.com/">http://redisbook.com/<i class="fa fa-external-link"></i></span></p>
]]></content>
      <categories>
        <category>C++</category>
        <category>Redis</category>
      </categories>
  </entry>
  <entry>
    <title>Hello CUDA - CUDA程序简单入门</title>
    <url>/2016/02/07/hello-cuda/</url>
    <content><![CDATA[<blockquote>
<p>小喵的唠叨话：通过之前的对于GPU和CUDA的学习，我们应该简单的了解了什么是GPU，它能做什么，以及GPU的硬件结构。那么，大家是不是迫不及待的想要写出CUDA程序了呢？</p>
</blockquote>
<a id="more"></a>
<p>本章就要通过最简单的几个理解，教会大家CUDA程序的写法，并理解其工作方式。 下面就是第一个程序：</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">"Hello CUDA!\\n"</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>将上述代码保存成文件<code>hello_cuda.cu</code>，使用nvcc进行编译。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">nvcc hello_cuda.cu -o hello_cuda.out</span><br></pre></td></tr></table></figure>
<p>便可以得到可执行文件<code>hello_cuda.out</code>，运行该可执行文件，便可得到如下的结果（实验的环境为CentOS 7）:</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">Hello CUDA!</span><br></pre></td></tr></table></figure>
<p>这就是第一个CUDA程序。</p>
<div style="color: gray">
旁观者小汪：这不就是C语言的Hello World程序嘛，和GPU有什么关系？
</div>
<p>是的，这个程序确实和一般的CPU程序没什么两样，写这个程序主要是告诉大家，其实CUDA的语法本身并不难，我们可以像写CPU程序一样编写GPU上的程序。</p>
<p>现在我们简单分析一下这个程序，代码内容就不用分析了吧。首先，源码文件是以.cu结尾，这有别于C和C++的.c,.cc,.cpp等后缀。其次，编译的时候使用nvcc这个编译器，对于C/C++的程序，我们常用的编译器是gcc。NVIDIA公司通过提供额外的关键字（后面会说到）来拓展了C/C++语言，重写了gcc为nvcc，支持了整个CUDA程序的编译过程。nvcc的使用可以使用nvcc -h来查询。这不是本章的重点。 下面，我们看一个稍微复杂一点的程序。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">function_call3</span><span class="params">()</span> </span>&#123;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">__<span class="function">device__ <span class="keyword">void</span> <span class="title">function_call2</span><span class="params">()</span> </span>&#123;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">__<span class="function">global__ <span class="keyword">void</span> <span class="title">function_call1</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    function_call2();               <span class="comment">// success</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line">    function_call1&lt;&lt;&lt;<span class="number">1</span>,<span class="number">1</span>&gt;&gt;&gt;();      <span class="comment">// success</span></span><br><span class="line">    <span class="comment">// function_call2&lt;&lt;&lt;1, 1&gt;&gt;&gt;();  // error: a __device__ function call cannot be configured</span></span><br><span class="line">    function_call3();               <span class="comment">// success</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>很遗憾，这个程序既不会完成出色的算法，也不会有任何的输出，更不会为您煮出香喷喷的咖啡。</p>
<p>但这里我们可以惊奇的发现两个特殊的关键字<code>__global__</code>和<code>__device__</code>，这就是我们之前提到过的对<code>C/C++</code>的拓展的关键字。</p>
<p>CUDA程序主要有两个部分，CPU上运行的程序和GPU上的程序。这些特殊的关键字就是为了说明后面定义的函数（这些关键字也可以修饰变量）在何处运行。main函数作为入口函数，之前没有任何的修饰，是很正常的CPU程序。同理<code>function_call3</code>也是CPU的程序(CPU上的程序也可以用<code>__host__</code>来修饰)。<code>function_call1</code>被<code>__global__</code>修饰，意思是该程序可以被任何程序所调用（CPU或GPU），但是是在GPU上运行。<code>function_call2</code>被<code>__device__</code>修饰，表示该程序是GPU上的程序并且只能被GPU程序所调用。理解调用规则了吗？<code>&lt;&lt;&lt;1, 1&gt;&gt;&gt;</code>这个符号，这也是对C/C++的拓展，这两个数字分别表示<code>block_num</code>和<code>thread_num</code>，用来指示GPU程序的线程块和块内线程的数目。我们暂时不用理解。</p>
<p>现在，整个程序是不是就变得很清晰？这其实就是一个验证CPU和GPU上的函数调用的例子。</p>
<p>通过上面的这个例子，我们已经知道了CPU和GPU程序的调用规则。并编写出了两个_什么都不做的_CUDA程序。</p>
<p>最后，我们看一个更为复杂的程序，一个向量的加法程序。程序中可能有一些我们没有介绍过的东西，只需意会即可。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;cuda_runtime.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;assert.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> CUDA_CALL(x) &#123; const cudaError_t a = (x); <span class="meta-keyword">if</span> (a!= cudaSuccess) &#123; printf(<span class="meta-string">"\nCUDA Error: %s(err_num=%d)\n"</span>, cudaGetErrorString(a), a); cudaDeviceReset(); assert(0);&#125;&#125;</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> VECTOR_LENGTH 1024</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> BLOCK_NUM 2</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> THREAD_NUM 256</span></span><br><span class="line"></span><br><span class="line">__<span class="function">global__ <span class="keyword">void</span> <span class="title">vector_add</span><span class="params">(<span class="keyword">int</span> *v1, <span class="keyword">int</span> *v2, <span class="keyword">int</span> *r)</span> </span>&#123;</span><br><span class="line">    <span class="comment">// 线程号</span></span><br><span class="line">    <span class="keyword">unsigned</span> <span class="keyword">int</span> tid = blockDim.x * blockIdx.x + threadIdx.x;</span><br><span class="line">    <span class="keyword">while</span> (tid &lt; VECTOR_LENGTH) &#123;</span><br><span class="line">        r[tid] = v1[tid] + v2[tid];</span><br><span class="line">        tid += BLOCK_NUM * THREAD_NUM;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span> </span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// CPU内存</span></span><br><span class="line">    <span class="keyword">int</span> *host_v1 = (<span class="keyword">int</span> *) <span class="built_in">malloc</span>(VECTOR_LENGTH * <span class="keyword">sizeof</span>(<span class="keyword">int</span>));</span><br><span class="line">    <span class="keyword">int</span> *host_v2 = (<span class="keyword">int</span> *) <span class="built_in">malloc</span>(VECTOR_LENGTH * <span class="keyword">sizeof</span>(<span class="keyword">int</span>));</span><br><span class="line">    <span class="keyword">int</span> *host_result = (<span class="keyword">int</span> *) <span class="built_in">malloc</span>(VECTOR_LENGTH * <span class="keyword">sizeof</span>(<span class="keyword">int</span>));</span><br><span class="line">    <span class="keyword">int</span> *gpu_result = (<span class="keyword">int</span> *) <span class="built_in">malloc</span>(VECTOR_LENGTH * <span class="keyword">sizeof</span>(<span class="keyword">int</span>));</span><br><span class="line"></span><br><span class="line">    <span class="comment">// GPU内存</span></span><br><span class="line">    <span class="keyword">int</span> *device_v1;</span><br><span class="line">    <span class="keyword">int</span> *device_v2;</span><br><span class="line">    <span class="keyword">int</span> *device_result;</span><br><span class="line">    CUDA_CALL(cudaMalloc((<span class="keyword">void</span> **)&amp;device_v1, VECTOR_LENGTH * <span class="keyword">sizeof</span>(<span class="keyword">int</span>)));</span><br><span class="line">    CUDA_CALL(cudaMalloc((<span class="keyword">void</span> **)&amp;device_v2, VECTOR_LENGTH * <span class="keyword">sizeof</span>(<span class="keyword">int</span>)));</span><br><span class="line">    CUDA_CALL(cudaMalloc((<span class="keyword">void</span> **)&amp;device_result, VECTOR_LENGTH * <span class="keyword">sizeof</span>(<span class="keyword">int</span>)));</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 初始化CPU上的数据</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> idx = <span class="number">0</span>; idx &lt; VECTOR_LENGTH; ++ idx) &#123;</span><br><span class="line">        host_v1[idx] = idx;</span><br><span class="line">        host_v2[idx] = idx;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// CPU计算结果</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> idx = <span class="number">0</span>; idx &lt; VECTOR_LENGTH; ++ idx) &#123;</span><br><span class="line">        host_result[idx] = host_v1[idx] + host_v2[idx];</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 将CPU数据复制到GPU上</span></span><br><span class="line">    CUDA_CALL(cudaMemcpy(device_v1, host_v1, VECTOR_LENGTH * <span class="keyword">sizeof</span>(<span class="keyword">int</span>), cudaMemcpyHostToDevice));</span><br><span class="line">    CUDA_CALL(cudaMemcpy(device_v2, host_v2, VECTOR_LENGTH * <span class="keyword">sizeof</span>(<span class="keyword">int</span>), cudaMemcpyHostToDevice));</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 调用核函数,GPU计算结果</span></span><br><span class="line">    vector_add&lt;&lt;&lt;BLOCK_NUM, THREAD_NUM&gt;&gt;&gt;(device_v1, device_v2, device_result);</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 将GPU计算结果复制到CPU上</span></span><br><span class="line">    CUDA_CALL(cudaMemcpy(gpu_result, device_result, VECTOR_LENGTH * <span class="keyword">sizeof</span>(<span class="keyword">int</span>), cudaMemcpyDeviceToHost));</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 检查计算结果</span></span><br><span class="line">    <span class="keyword">int</span> is_match = <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> idx = <span class="number">0</span>; idx &lt; VECTOR_LENGTH; ++ idx) &#123;</span><br><span class="line">        <span class="comment">// printf("%d %d\\n", host_result[idx], gpu_result[idx]);</span></span><br><span class="line">        <span class="keyword">if</span> (host_result[idx] != gpu_result[idx]) &#123;</span><br><span class="line">            is_match = <span class="number">0</span>;</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (is_match) &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">"match\\n"</span>);</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">"not match\\n"</span>);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// 释放资源</span></span><br><span class="line">    <span class="built_in">free</span>(host_v1);</span><br><span class="line">    <span class="built_in">free</span>(host_v2);</span><br><span class="line">    <span class="built_in">free</span>(host_result);</span><br><span class="line">    <span class="built_in">free</span>(gpu_result);</span><br><span class="line"></span><br><span class="line">    CUDA_CALL(cudaFree(device_v1));</span><br><span class="line">    CUDA_CALL(cudaFree(device_v2));</span><br><span class="line">    CUDA_CALL(cudaFree(device_result));</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这个程序看起来稍微有点复杂。我们来一点点的分析和理解。<code>vector_add</code>被<code>__global__</code>修饰，看来是个GPU的程序。主函数里分别用CPU和GPU两种方式计算了两个向量的加法，最后也做了比较。还记得我们之前说过的显存的概念吗？其实，CPU和GPU的程序只能访问对应的内存，即CPU访问主存，GPU访问显存，不能互相访问，因此我们不能让<code>vector_add</code>这个函数直接对CPU上的数据进行计算(<code>malloc</code>得到的内存数据)，而是将CPU数据在GPU上做一次拷贝，之后再计算。</p>
<p><img data-src="http://cdn.miaoerduo.com/images/heterogeneous-programming.png" alt="heterogeneous-programming" /></p>
<center>
图1 Host和Device程序运行示意图
</center>
<p>*注：上图中顺序执行的代码（Series Code）是在Host上运行，（Parallel Code）是在Device上运行。</p>
<p>参考上图，我们简单总结一下CUDA程序的运行方式：我们在下面使用<strong>主机(host)</strong>表示CPU，使用<strong>设备(device)</strong>表示GPU。因此对于一个计算问题，我们需要在主机上准备好数据，然后复制到设备上，之后调用设备上的核函数(kernel function)进行计算，最终将设备上的计算结果复制到主机上。从而完成一次计算功能。对于复杂的任务，可以多次进行CPU-&gt;GPU-&gt;CPU-&gt;GPU-&gt;...的操作。</p>
<p>现在对于这个向量加法的程序是不是有点理解了？<code>cudaMalloc</code>的功能和<code>malloc</code>类似，但是是在GPU上申请显存，第一个参数是个指针的地址（不是一个指针），第二个参数是需要申请的内存的大小。申请完显存之后，会把显存的地址存入第一个参数指向的内存中，并返回一个状态码。但是，CPU上不能访问显存，因此这个指针在CPU上并没有什么实际意义，虽然我们可以把其中的地址打印出来，但是并不能访问其指向的存储空间。<code>cudaMemcpy</code>的功能与<code>memcpy</code>类似，参数分别表示：目的存储的地址，源存储地址，复制的数据大小，数据拷贝方向。数据拷贝方向有两种，<code>Host-&gt;Device</code>和<code>Device-&gt;Host</code>，分别用<code>cudaMemcpyHostToDevice</code>和<code>cudaMemcpyDeviceToHost</code>表示。 <code>cudaFree</code>的功能和<code>free</code>类似，用于释放GPU的显存。关于<code>vector_add</code>这个函数，它的参数是两个向量和结果的指针，这样就可以访问想要相加的向量的数据和结果的数据了。但是怎么知道当前需要计算向量的第几位的数值呢？参数中并没有给出。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">unsigned</span> <span class="keyword">int</span> tid = blockDim.x * blockIdx.x + threadIdx.x;</span><br></pre></td></tr></table></figure>
<p>之前我们提到过<code>&lt;&lt;&lt;block_num, thread_num&gt;&gt;&gt;</code>表示指示核函数的工作方式，在这里<code>vector_add</code>这个函数实际上是被多个SP调用（实际上有<code>block_num * thread_num</code>这么多线程，GPU程序运行成千上万的线程都是很正常的，这和CPU程序有很大的不同），即同时被运行很多次。通过上述的代码是用来计算每个被运行的核函数的线程的id（具体的意思下一章会有介绍）。这样每个线程能够根据自己的线程id，读取自己应该处理的数据，计算并将结果写回显存。</p>
<figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">while</span> (tid &lt; VECTOR_LENGTH) &#123;</span><br><span class="line">    r[tid] = v1[tid] + v2[tid];</span><br><span class="line">    tid += BLOCK_NUM * THREAD_NUM;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>这个循环使得这个程序对于任意长度的向量都可以做加法（可能需要把vector_length当成变量传入）。<code>tid &lt; VECTOR_LENGTH</code>这个判断是为了防止线程访问越界。</p>
<p>最后需要介绍的是CUDA的状态码，<code>cudaMalloc</code>，<code>cudaMemcpy</code>，<code>cudaFree</code>都有返回函数执行的状态码，但如果每次都检查这些状态码，则会有太多的重复代码，所以这里写了一个<code>CUDA_CALL</code>的宏定义，用来对函数调用的返回值进行检查。</p>
<p>至此，相信读者以及能够很清楚的理解这个向量加法的程序了，也能照这样子写出简单的CUDA程序了。现在我们唯一不了解的可能就是<code>&lt;&lt;&lt;block_num, thread_num&gt;&gt;&gt;</code>这种写法的具体意义和里面的参数的具体作用和设置。</p>
<p>下一章，小喵为您深入浅出CUDA线程模型。定让客官们不虚此行！</p>
<p><strong>转载请注明出处。</strong></p>
]]></content>
      <categories>
        <category>C++</category>
        <category>CUDA</category>
      </categories>
  </entry>
  <entry>
    <title>GPU硬件结构</title>
    <url>/2016/02/05/gpu-hardware/</url>
    <content><![CDATA[<blockquote>
<p>小喵的唠叨话：没想到研究生的生活这么繁忙，居然还有考试。小喵都快累成狗了。终于稍微闲下来一点了，开始继续写博客。这次的题目，其实是差不多一个月之前就想好的，但是一直碰到许多的事情，就耽搁了。</p>
</blockquote>
<p>这片文章主要介绍一下GPU的硬件结构。</p>
<a id="more"></a>
<p>首先，给大家看一张示意图（图片资源来自网上）。</p>
<p><img data-src="http://cdn.miaoerduo.com/images/GPU_hardware.jpg" alt="GPU（G80/GT200）卡的组成模块图" /></p>
<center>
图1 GPU（G80/GT200）卡的组成模块图
</center>
<p><img data-src="http://cdn.miaoerduo.com/images/GPU_hardware.jpg" alt="GPU硬件结构示意图" /></p>
<center>
图2 GPU结构示意图
</center>
<p>这里，我们主要了解需要有以下几个认识：</p>
<h3 id="一个主机可以有多个gpu">1，一个主机可以有多个GPU**</h3>
<p>像我们一般的PC机，就大多数有集显和独显。一般的运算服务器，也是可以一个机器上有多显卡。不同的主板上的有不同数目的显卡的插槽，需要强大的计算性能就可以考虑多卡的结构。当然电源和散热可能就会成为新的问题。</p>
<h3 id="smstreaming-multiprocessors-and-spstream-processor">2，SM(Streaming Multiprocessors) and SP(Stream Processor)</h3>
<p>GPU是一种<strong>SPMD</strong>（single program, multiple data）的工作方式是<strong>SIMD</strong>（Single Instruction Multiple Data）的一个升华。在同一时间，多个处理器运行同一套程序，但是处理的数据可以不同。这样带来的效果就是，如果简单的重复的任务特别多的话（比如向量的加法），GPU可以在同一时间处理多条数据。而且每个处理器需要处理的程序是相同的，因此只需要一次指令的取操作，所有处理器可以共同执行，效率上大大提高了。 流处理器簇（SM）的概念与CPU的核心类似，GPU的程序的执行单位就是SM，GPU的SM的数目通常有15-16个，一个SM中包含了多个SP，通常是32个，SP是实际的执行单元。对于一堆任务，GPU会按照以32个为一个单位分配到每个SM中执行。 举个例子，对于向量的加法，假设有两个向量长度分别为1024，每个SP都执行一个加法，那么一共会有1024/32=32批任务，如果每个GPU有16个SM，则每个SM被调用了32/16=2次。<strong>不考虑内存读写，指令优化，或其他的延时，只考虑计算时间</strong>。那么可以理解该次计算只需要2个计算的周期（切记，这里有取指令，取数据，运算和写数据的各种时间）。而对于一个四核的CPU机器而言，需要1024/4=256次计算周期。100倍的差距。</p>
<h3 id="内存">3，内存</h3>
<p>GPU的内存通常称为显存，是GPU上高速读写的存储介质，功能上和PC的内存类似。但GPU上的内存分为多种，具体有：寄存器内存，共享内存，全局内存，纹理内存，常量内存。看到这么多种内存，有没有和小喵一样目瞪口呆？</p>
<p>下面表格里是内存的带宽和延时</p>
**
<center>
Access Time by Memory Type
</center>
<p>**</p>
<table>
<colgroup>
<col style="width: 15%" />
<col style="width: 29%" />
<col style="width: 18%" />
<col style="width: 19%" />
<col style="width: 16%" />
</colgroup>
<thead>
<tr class="header">
<th>Storage Type</th>
<th>Registers Shared Memory</th>
<th>Texture Memory</th>
<th>Constant Memory</th>
<th>Global Memory</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>Bandwidth</td>
<td>~8 TB/s ~1.5 TB/s</td>
<td>~200 MB/s</td>
<td>~200 MB/s</td>
<td>~200 MB/s</td>
</tr>
<tr class="even">
<td>Latency</td>
<td>1 cycle</td>
<td>1 to 32 cycles</td>
<td>~400 to 600</td>
<td>~400 to 600</td>
</tr>
</tbody>
</table>
<h4 id="寄存器内存">寄存器内存</h4>
<p>读写最高效的内存，在CPU上，可用的寄存器内存微乎其微。</p>
<p>于是小喵专门Google了一下：</p>
<blockquote>
<p>Registers are memories located within the Central Processing Unit (CPU). They are few in number (there are rarely more than 64 registers) and also small in size, typically a register is less than <strong>64 bits</strong> in size.[1]</p>
</blockquote>
<p>但是GPU上，每个SM就提供了至少8192个寄存器！也就是说，如果一个SM上只运行32个线程，平均每个线程可以得到8192/32=256个寄存器（虽然通常不会只运行这么少的线程）。</p>
<p>GPU为了能够高效的工作，在粗细两个粒度对内存和任务进行管理。合理的使用各种内存可以使自己的程序更加高效。</p>
<h4 id="共享内存">共享内存</h4>
<p>顾名思义，是指数据能够共享的一块内存。该内存主要在每个SM内部，同一个SM中的所有的SP都可以访问该块内存。而且读写速度仅次于寄存器内存，因此在线程间进行通信用共享内存最适合不过了。</p>
<h4 id="全局内存">全局内存</h4>
<p>在整个GPU上，任何部件都能访问的内存，我们平时说的显存1G等等的就是说的全局内存。全局内存是GPU上最大的内存，也是我们最常打交道的内存。相比于寄存器内存和共享内存，全局内存的读写速度慢了几个数量级。所以我们在设计GPU的程序的时候，要尽量充分利用寄存器内存和共享内存，才能设计出高效的程序。</p>
<h4 id="常量内存">常量内存</h4>
<p>存放常量，大小通常是32K（本质上和L2缓存共享了内存，可以通过API修改模式）。</p>
<h4 id="纹理内存">纹理内存</h4>
<p>这个现在的作用比较少了，但功能很特殊，在后面会详细介绍。 以上就是GPU的硬件结构的简单介绍。感兴趣的同学可以访问NVIDIA的官网，获取更多的信息。</p>
<p>[1]<span class="exturl" data-url="aHR0cDovL3d3dy5kb2MuaWMuYWMudWsvfmVlZHdhcmRzL2NvbXBzeXMvbWVtb3J5Lw==" title="http://www.doc.ic.ac.uk/~eedwards/compsys/memory/">http://www.doc.ic.ac.uk/~eedwards/compsys/memory/<i class="fa fa-external-link"></i></span></p>
]]></content>
      <categories>
        <category>C++</category>
        <category>CUDA</category>
      </categories>
  </entry>
  <entry>
    <title>从0到1，手把手教你配置Caffe环境</title>
    <url>/2016/01/26/tutorial-to-build-caffe/</url>
    <content><![CDATA[<blockquote>
<p>小喵的唠叨话：之前小喵也说过，自己主要是学习图像方面的深度学习算法的，深度学习可以理解为神经网络的拓展，是近年来最火的一项技术。小喵学得不好，就不在这里卖弄了。图像中，最常用到的是CNN模型，也就是卷积神经网络（Convolutional Neural Networks）。实现CNN有许多框架，Caffe，MXNet，Tensor Flow，ConvNet等。这次主要教给大家怎么从0开始，配置自己的Caffe环境。</p>
</blockquote>
<a id="more"></a>
<p>由于是从0开始安装，如果有什么软件是大家已经装好的，就可以自行跳过。小喵的电脑是CentOS7,64位。主要按照下列的软件顺序进行安装：</p>
<ol type="1">
<li>GMP</li>
<li>MPFR</li>
<li>MPC</li>
<li>GCC 4.7.4</li>
<li>CMAKE</li>
<li>Python</li>
<li>Boost</li>
<li>Opencv：Python-Opencv</li>
<li>Glog</li>
<li>Gflags：Python-gflags</li>
<li>Google-apputils</li>
<li>Protobuf</li>
<li>Snappy</li>
<li>LevelDB：Pyhton-leveldb</li>
<li>Lmdb</li>
<li>OpenBlas</li>
<li>CUDA</li>
<li>cuDNN</li>
<li>Caffe</li>
</ol>
<p>第一步，更新系统的 <code>GCC</code> 如果是在个人的PC上进行更新，可能只需要使用 <code>yum</code> 或者 <code>apt-get</code> 等工具。小喵的实验环境是服务器，因此自己并没有root权限，还是需要按部就班的安装。</p>
<p><code>GCC</code> 依赖 <code>GMP</code>，<code>MPFR</code> 和 <code>MPC</code> 这三个库，它们主要是提供了一些数值计算等的函数。</p>
<p><strong>Tips: 编译的时候使用make命令的时候，可以加入参数-jn，可以多核一起编译，速度会快很多。</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">make -j4</span><br></pre></td></tr></table></figure>
<p>则表示4个核一起编译。</p>
<h3 id="gmp">1，GMP</h3>
<p><code>GMP</code>，<span class="exturl" data-url="aHR0cHM6Ly9nbXBsaWIub3JnLw==" title="https://gmplib.org/">https://gmplib.org/<i class="fa fa-external-link"></i></span>，一个高精度计算的库。从官网上下载得到安装文件，小喵下载的是6.1.0的版本。解压之后，使用命令:</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cd</span> /download/path/to/gmp/</span><br><span class="line">./configure --prefix=/path/to/install/</span><br><span class="line">make</span><br><span class="line">make install</span><br></pre></td></tr></table></figure>
<p>完成安装(/path/to/install/表示你希望安装的位置)。注意prefix前面是两个<code>'-'</code>。</p>
<p>安装完成之后，修改根目录中的 <code>.bash_profile</code>。将 <code>gmp</code> 安装后的文件夹中的lib的路径加入到 <code>LD_LIBRARY_PATH</code> 中。</p>
<p><strong>注意：之后每步操作都检查安装后的文件夹中，是否有lib目录或lib64目录，如果有，则加入 <code>LD_LIBRARY_PATH</code> 中。（lib64是64位的系统的库，如果两个目录都存在，将lib64加入到 <code>LD_LIBRARY_PATH</code> 就可以）。如果发现有bin目录，则将它加入PATH中，bin目录放置的都是可执行程序，只有加进PATH里面，系统才能够找到（大家都不会希望用ls命令的时候，都写成/bin/ls吧）。配置完后，可以source一下自己的 <code>.bash_profile</code> 文件，确保当前的环境是最新的。</strong></p>
<h3 id="mpfr">2，MPFR</h3>
<p><code>MPFR</code>，<span class="exturl" data-url="aHR0cDovL3d3dy5tcGZyLm9yZy8=" title="http://www.mpfr.org/">http://www.mpfr.org/<i class="fa fa-external-link"></i></span>，一个浮点数计算的库，该库对 <code>GMP</code> 有依赖，所以要在 <code>GMP</code> 之后安装。下载安装文件，小喵的是3.1.3版本。解压，使用命令。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cd</span> /download/path/to/mpfr/</span><br><span class="line">./configure --prefix=/path/to/install --with-gmp=/path/to/gmp/</span><br><span class="line">make</span><br><span class="line">make install</span><br></pre></td></tr></table></figure>
<p>这里的/path/to/gmp/表示上一步的gmp安装的位置。</p>
<h3 id="mpc">3，MPC</h3>
<p><code>MPC</code>，<span class="exturl" data-url="aHR0cDovL3d3dy5tdWx0aXByZWNpc2lvbi5vcmcv" title="http://www.multiprecision.org/">http://www.multiprecision.org/<i class="fa fa-external-link"></i></span>，一个复数的代数库，依赖 <code>GMP</code> 和 <code>MPFR</code>。下载安装包，版本1.0.3。解压。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cd</span> /download/path/to/mpc/</span><br><span class="line">./configure --prefix=/path/to/install/ --with-gmp=/path/to/gmp/ --with-mpfr=/path/to/mpfr/</span><br><span class="line">make</span><br><span class="line">make install</span><br></pre></td></tr></table></figure>
<p>/path/to/gmp/，/path/to/mpfr/分别表示gmp和mpfr的安装路径，以下的说明都是这种形式，不再特意强调。</p>
<h3 id="gcc">4，GCC</h3>
<p>完成上述的三个代数运算库的安装之后，则可以开始GCC的升级工作。小喵的电脑上是CentOS 7，GCC默认版本是4.4。4.4版本的GCC只能编译到4.7版本或以下的GCC，所以如果需要编译更高版本的GCC，则需要先编译较低版本的GCC，再使用新安装的GCC编译更高版本的。另外GCC编译时间较旧，可以考虑多核一起编译（还记得make -j4这个命令了吗？:-D）。</p>
<p><code>GCC</code>，<span class="exturl" data-url="aHR0cHM6Ly9nY2MuZ251Lm9yZy8=" title="https://gcc.gnu.org/">https://gcc.gnu.org/<i class="fa fa-external-link"></i></span>，一个Linux上的强大的编译环境，提供对各种语言：<code>C/C++</code>，<code>Fortran</code>，<code>Java</code>等的编译支持。下载安装程序，这里选择4.7.4版本（不要选择更高的版本，低版本的GCC不一定能编译）。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cd</span> /download/path/to/gcc/</span><br><span class="line">./configure --prefix=/path/to/install/ --<span class="built_in">enable</span>-threads=posix --<span class="built_in">disable</span>-checking --<span class="built_in">disable</span>-multilib --<span class="built_in">enable</span>-languages=c,c++,fortran --with-gmp=/path/to/gmp/ --with-mpfr=/path/to/mpfr/ --with-mpc=/path/to/mpc/</span><br><span class="line">make</span><br><span class="line">make install</span><br></pre></td></tr></table></figure>
<p>安装完成，不要忘记把bin目录和lib64目录分别加入 <code>PATH</code> 和 <code>LD_LIBRARY_PATH</code>。source一下后，使用 <code>gcc --version</code>，你会发现gcc的版本已经变成4.7.4了。</p>
<h3 id="cmake">5，CMAKE</h3>
<p><code>CMAKE</code>，<span class="exturl" data-url="aHR0cHM6Ly9jbWFrZS5vcmcv" title="https://cmake.org/">https://cmake.org/<i class="fa fa-external-link"></i></span>，是一个跨平台的安装（编译）工具，可以用简单的语句来描述所有平台的安装(编译过程)。版本3.4.1。解压。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cd</span> /download/path/to/cmake/</span><br><span class="line">./configure --prefix=/path/to/install/</span><br><span class="line">make</span><br><span class="line">make install</span><br></pre></td></tr></table></figure>
<h3 id="python">6，Python</h3>
<p><code>Python</code>，<span class="exturl" data-url="aHR0cHM6Ly93d3cucHl0aG9uLm9yZy8=" title="https://www.python.org/">https://www.python.org/<i class="fa fa-external-link"></i></span>，一种超好用的编程语言，有着胶水语言的美称。不仅语言简洁，相关的工具库也异常丰富。用 <code>Python</code> 可以很快的编写出测试原型，而且 <code>Caffe</code> 本身也提供了 <code>Python</code> 的接口。这里选择2.7.*版本。注意Python2.7.<em>和3.5.</em>并不完全兼容，目前主流的 <code>Python</code> 还是2.7，而且相关库的支持也比较成熟。由于还需要安装许多依赖的计算方面的库，不建议直接从官网下载。推荐下载 <code>Anaconda</code> 这个集成好的 <code>Python</code>环境。<span class="exturl" data-url="aHR0cHM6Ly93d3cuY29udGludXVtLmlvLw==" title="https://www.continuum.io/">https://www.continuum.io/<i class="fa fa-external-link"></i></span>，记住，2.7.*版本。小喵这里安装好的是2.7.10。</p>
<p>下载好之后，直接运行，然后跟个提示就刻轻松安装。</p>
<p>安装完成后，需要在 <code>.bash_profile</code> 中进行基础修改。</p>
<p>假定 <code>ANACONDA_ROOT</code> 表示安装的目录。将 <code>$ANACONDA/bin/</code> 加入 <code>PATH</code>，将 <code>$ANACONDA/lib/</code> 加入 <code>LD_LIBRARY_PATH</code> 加入新变量 <code>PYTHONPATH</code>，并 <code>export</code> 出来。<code>export PYTHONPATH=$ANACONDA/lib/python2.7/site-packages</code>，source一下则配置完成。</p>
<p>如果是安装官网的 <code>Python</code>，之后可能需要再安装 <code>Numpy</code> 这个库，否则不能正确调用 <code>OpenCV</code>。<code>Anaconda</code> 已经集成了，所以我们这里不用再次安装。</p>
<h3 id="boost">7，Boost</h3>
<p><code>Boost</code>，<span class="exturl" data-url="aHR0cDovL3d3dy5ib29zdC5vcmcv" title="http://www.boost.org/">http://www.boost.org/<i class="fa fa-external-link"></i></span>，又称 <code>C++准标准库</code>。传言，如果希望自己的库可以进入C++标准库，那么可以让它成为Boost的一部分，久而久之，Boost中的精华就会融进C++标准库。如果比较关注C++标准库的发展，重看每次更新的部分，应该大多都能看到Boost的身影。Boost中提供了很多强大又稳定的库，包括智能指针，各种算法库等等。这些在Caffe中都有用到。</p>
<p>这里下载1.58.0版本，Caffe对最新的1.60.0并不完全兼容。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cd</span> /download/path/to/boost/</span><br><span class="line">./bootstrap.sh --prefix=/path/to/install/</span><br><span class="line">./b2 install</span><br></pre></td></tr></table></figure>
<p>加入环境后，则完成安装。</p>
<h3 id="opencv">8，OpenCV</h3>
<p><code>OpenCV</code>，<span class="exturl" data-url="aHR0cDovL29wZW5jdi5vcmcv" title="http://opencv.org/">http://opencv.org/<i class="fa fa-external-link"></i></span>，一个开源的图像处理相关的库，不仅能够进行图像的各种处理（缩放，翻转，模糊等等），还内置了许多的机器学习的算法（比如 <code>AdaBoost</code> 的人脸检测）。图像处理必备库。这里下载版本2.4.11（小喵试了2.4.9和2.4.11两个版本，都可以正确安装）。3.0或以上的版本安装可能会有问题。</p>
<p><code>OpenCV</code> 的安装需要使用 <code>cmake</code> 工具来完成 <code>configure</code> 工作。请确保之前的 <code>cmake</code> 已经安装，并加入到 <code>PATH</code>。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cd</span> /download/path/to/opencv/</span><br><span class="line">mkdir build                    <span class="comment"># 创建一个临时目录</span></span><br><span class="line"><span class="built_in">cd</span> build</span><br><span class="line">ccmake ..                      <span class="comment"># 这里使用ccmake，比cmake界面更友好</span></span><br></pre></td></tr></table></figure>
<p>刚进入的时候，显示 <code>EMPTY CACHE</code>，根据提示，按 <code>c</code>，进行 <code>configure</code> 之后便可以看到配置界面，有许多的变量和其对应的值。我们主要检查以下几项 <code>PYTHON_NUMPY_INCLUDE_DIR</code>， <code>PYTHON_PACKAGES_PATH</code>， <code>SPHINX_BUILD</code> 如果之前的步骤都完成的话，这几项参数应该可以从系统中直接读取，如果发现没有读取，可能是没有加入环境变量或者安装错误。检查结束后，修改 <code>CMAKE_INSTALL_PREFIX</code> 的值，改成自己期望安装的位置。</p>
<p>在界面中，可以看到几个类似 <code>BUILD_PNG</code>， <code>BUILD_JPEG</code> 的参数，它们是图像处理的相关的库。如果系统中之前没有安装过这些库，而你需要 <code>OpenCV</code> 能够处理这些类型的文件，就把这些变量设成 <code>ON</code>，这样就会把这些库一起编译。</p>
<p>界面中有类似 <code>WITH_PNG</code>，<code>WITH_JPEG</code> 的变量，如果选成 <code>ON</code>，则 <code>OpenCV</code> 会安装对这些类型图片的支持。 <strong>Warning: 小喵在安装的时候就犯了一个错误，把 <code>WITH_PNG</code> 设成了 <code>ON</code>，但是把 <code>BUILD_PNG</code> 设成了 <code>OFF</code>，导致编译的时候找不到 <code>libpng</code> 的一些.so文件。所以如果大家需要对特殊类型图片的支持，要记得把前后都设成 <code>ON</code>。</strong></p>
<p>最后根据提示进行操作，就会生成对于的 <code>Makefile</code> 文件，则可进行下面的步骤：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">make</span><br><span class="line">make install</span><br></pre></td></tr></table></figure>
<p>记得把bin和lib分别加入到环境变量。至此，<code>OpenCV</code> 也就装完了。</p>
<p>接下来，要让 <code>Python</code> 能够调用 <code>OpenCV</code>。 方法很简单，将 <code>%OPENCV_INSTALL_PATH%/lib/python2.7/site-packages</code> 下的文件（应该只有两个），拷贝到 <code>%ANACONDA_INSTALL_PATH%/lib/python2.7/site-packages</code> 中，即可。</p>
<p>为了保险起见，你可以打开 <code>python</code>，输入 <code>import cv2</code>，看看有没有异常，没有的话，应该就是配置成功。</p>
<h3 id="glog">9，glog</h3>
<p><code>glog</code>，<code>google-log</code>，<span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL2dvb2dsZS9nbG9n" title="https://github.com/google/glog">https://github.com/google/glog<i class="fa fa-external-link"></i></span>，是google做的一项开源的项目，是一个C++的记录日志的工具。<code>Caffe</code>中有用到。可以下载源码，然后自己编译（貌似必须再装三个google的软件才可以），小喵安装的时候直接下载了release版本的程序 <span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL2dvb2dsZS9nbG9nL3JlbGVhc2Vz" title="https://github.com/google/glog/releases">https://github.com/google/glog/releases<i class="fa fa-external-link"></i></span>。解压，执行：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cd</span> /download/path/to/glog/</span><br><span class="line">./configure --prefix=/path/to/install/</span><br><span class="line">make</span><br><span class="line">make install</span><br></pre></td></tr></table></figure>
<p>加入lib目录。安装完成。</p>
<h3 id="gflags">10，gflags</h3>
<p><code>gflags</code>，<span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL2dmbGFncy9nZmxhZ3M=" title="https://github.com/gflags/gflags">https://github.com/gflags/gflags<i class="fa fa-external-link"></i></span>，貌似是一个处理命令行的C++工具，Caffe需要用到。该工具的安装也需要cmake。解压后执行：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cd</span> /download/path/to/gflags/</span><br><span class="line">mkdir build</span><br><span class="line"><span class="built_in">cd</span> build</span><br><span class="line">ccmake ..</span><br><span class="line"><span class="comment"># 设置安装路径CMAKE_INSTALL_PREFIX</span></span><br><span class="line"><span class="comment"># 把BUILD_SHARED_LIBS设成ON</span></span><br><span class="line">make</span><br><span class="line">make install</span><br></pre></td></tr></table></figure>
<p>把lib目录加入 <code>LD_LIBRARY_PATH</code>，则完成安装。使用 <code>Python</code> 调 <code>Caffe</code> 的时候，也会用到这个库，所以需要给 <code>Python</code> 也安装对于的库。这里可以用源码安装，也可以使用 <code>pip</code> 这个工具。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">pip install python-gflags</span><br></pre></td></tr></table></figure>
<h3 id="google-apputils">11，google-apputils</h3>
<p>下载源码安装，或者使用 <code>pip</code> 安装</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">pip install google-apputils</span><br></pre></td></tr></table></figure>
<h3 id="protobuf">12，Protobuf</h3>
<p><code>Protobuf</code>，<span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL2dvb2dsZS9wcm90b2J1Zg==" title="https://github.com/google/protobuf">https://github.com/google/protobuf<i class="fa fa-external-link"></i></span>，又是一个google出品的软件。可以通过简单的方式定义各种语言下数据结构。Caffe中有用到。</p>
<p>下载realse版本的程序来安装，<span class="exturl" data-url="aHR0cHM6Ly9kZXZlbG9wZXJzLmdvb2dsZS5jb20vcHJvdG9jb2wtYnVmZmVycy9kb2NzL2Rvd25sb2Fkcw==" title="https://developers.google.com/protocol-buffers/docs/downloads">https://developers.google.com/protocol-buffers/docs/downloads<i class="fa fa-external-link"></i></span>。解压，执行：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cd</span> /download/path/to/protobuf/</span><br><span class="line">./configure --prefix=/path/to/install/</span><br><span class="line">make</span><br><span class="line">make install</span><br></pre></td></tr></table></figure>
<p>增加python的支持：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">pip install protobuf</span><br></pre></td></tr></table></figure>
<h3 id="snappy">13，Snappy</h3>
<p><code>Snappy</code>，<span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL2dvb2dsZS9zbmFwcHk=" title="https://github.com/google/snappy">https://github.com/google/snappy<i class="fa fa-external-link"></i></span>。可以通过源码安装，也可以下载release版本，<span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL2dvb2dsZS9zbmFwcHkvcmVsZWFzZXMv" title="https://github.com/google/snappy/releases/">https://github.com/google/snappy/releases/<i class="fa fa-external-link"></i></span>。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cd</span> /download/path/to/snappy/</span><br><span class="line">./configure --prefix=/path/to/install/</span><br><span class="line">make</span><br><span class="line">make install</span><br></pre></td></tr></table></figure>
<h3 id="leveldb">14，leveldb</h3>
<p><code>leveldb</code>，<span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL2dvb2dsZS9sZXZlbGRi" title="https://github.com/google/leveldb">https://github.com/google/leveldb<i class="fa fa-external-link"></i></span>，一个轻量级的数据库。直接下载源码，解压。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cd</span> /download/path/to/leveldb/</span><br><span class="line">make</span><br></pre></td></tr></table></figure>
<p>之后直接生成了库文件。建议把生成的库文件和include文件夹都放置到合适的目录，并将库所在的目录加入 <code>LD_LIBRARY_PATH</code>。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">pip install leveldb</span><br></pre></td></tr></table></figure>
<p>增加python的支持。</p>
<h3 id="lmdb">15，LMDB</h3>
<p><code>LMDB</code>，<span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL2dseWNlcmluZS9saWJsbWRi" title="https://github.com/glycerine/liblmdb">https://github.com/glycerine/liblmdb<i class="fa fa-external-link"></i></span>，也是一个轻量级的数据库。解压之后，修改里面的 <code>Makefile</code> 文件的 <code>prefix</code> 为需要安装的位置。</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">make</span><br><span class="line">make install</span><br></pre></td></tr></table></figure>
<h3 id="openblas">16，OpenBlas</h3>
<p><code>OpenBlas</code>，<span class="exturl" data-url="aHR0cDovL3d3dy5vcGVuYmxhcy5uZXQv" title="http://www.openblas.net/">http://www.openblas.net/<i class="fa fa-external-link"></i></span>，一个代数库，同为代数库的还有 <code>MKL</code>，<code>ATLAS</code>。解压，执行：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">make</span><br><span class="line">make install PREFIX=/path/to/install/</span><br></pre></td></tr></table></figure>
<p>完成安装。</p>
<h3 id="cuda">17，CUDA</h3>
<p><code>CUDA</code>，<span class="exturl" data-url="aHR0cHM6Ly9kZXZlbG9wZXIubnZpZGlhLmNvbS9jdWRhLWRvd25sb2Fkcw==" title="https://developer.nvidia.com/cuda-downloads">https://developer.nvidia.com/cuda-downloads<i class="fa fa-external-link"></i></span>，GPU支持。小喵下载的是 <code>cuda_7.0.28_linux.run</code> 这个版本的安装程序。官网说明的安装过程需要用root执行：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sudo sh cuda_7.0.28_linux.run</span><br></pre></td></tr></table></figure>
<p>但事实上，不需要 <code>sudo</code> 也可以执行安装。非root的话，可能不能进行显卡驱动的安装，我这里已经有了驱动，所以不需要root安装。</p>
<p>另外驱动和cuda的版本必须一致，不然后出现cuda不能启动的错误。直接:</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">sh cuda_7.0.28_linux.run</span><br></pre></td></tr></table></figure>
<p>之后根据提示安装即可。</p>
<h3 id="cudnn">18，CUDNN</h3>
<p><code>cuDNN</code>，<span class="exturl" data-url="aHR0cHM6Ly9kZXZlbG9wZXIubnZpZGlhLmNvbS9jdWRubg==" title="https://developer.nvidia.com/cudnn">https://developer.nvidia.com/cudnn<i class="fa fa-external-link"></i></span>，是CUDA的加速程序。解压之后貌似就算成功。CUDA7.0建议使用CuDNN v3。</p>
<h3 id="caffe">19，CAFFE</h3>
<p><code>Caffe</code>，<span class="exturl" data-url="aHR0cDovL2NhZmZlLmJlcmtlbGV5dmlzaW9uLm9yZy8=" title="http://caffe.berkeleyvision.org/">http://caffe.berkeleyvision.org/<i class="fa fa-external-link"></i></span>，卷积神经网络的训练框架。在 <code>github</code> 上下载源码，<span class="exturl" data-url="aHR0cHM6Ly9naXRodWIuY29tL0JWTEMvY2FmZmU=" title="https://github.com/BVLC/caffe">https://github.com/BVLC/caffe<i class="fa fa-external-link"></i></span>。修改 <code>Makefile.json</code> 文件, 这里需要修改很多地方，需要自对照着修改。最后需要执行:</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">make</span><br><span class="line">make pycaffe</span><br><span class="line"><span class="comment"># 如果需要验证是否安装成功，使用下面的命令</span></span><br><span class="line"><span class="comment"># make test</span></span><br></pre></td></tr></table></figure>
<p>则完成安装！</p>
<p>以上便是整套Caffe环境的安装教程。如果有任何错误，请大家指正。</p>
<p><strong>转载请注明出处。</strong></p>
]]></content>
      <categories>
        <category>Deep Learning</category>
      </categories>
  </entry>
  <entry>
    <title>关于CUDA</title>
    <url>/2015/12/18/about-cuda/</url>
    <content><![CDATA[<blockquote>
<p>小喵的唠叨话：小喵的研究生生涯做的事图像识别相关的内容，主要使用的技术是深度学习。由于研究需要，不免会用到一些开源的程序，比如caffe（一个卷积神经网络的训练框架）等。使用卷积神经网络，可以很好的解决各类问题，但同时也面临着高昂的计算的问题，而GPU机器，就是一个很好的超级计算机。虽然小喵很少直接编写GPU的程序，但是通过对GPU的更深入理解，可以帮助很好的理解各种框架，以及做必要的修改。 好啦，闲话少说，我们言归正传。 本文中，我们主要介绍以下几个问题：</p>
</blockquote>
<a id="more"></a>
<ol type="1">
<li><strong>什么是GPU</strong></li>
<li><strong>什么是CUDA</strong></li>
<li><strong>如何学习CUDA</strong></li>
</ol>
<h2 id="一我们每个人都拥有自己的超级计算机">一、我们每个人都拥有自己的超级计算机</h2>
<p>现在，主流的电脑都会配置一个或多个GPU，也叫做显卡。我们都知道，玩大型游戏少不了一块好显卡，做视频编辑也需要好显卡，现在我要说，做科学计算更需要一块好显卡。 那么显卡具体有什么功能呢？ 答案是：计算 是的，显卡的实际功能就是计算，尤其是简单的大量的计算。 我们在玩游戏的时候，会有很多华丽的场景，特效，然而只有好的显卡才能让我们开启全特效。场景的渲染其实就是一个计算的过程。比如，游戏中人物的影子，精细的游戏中的影子并不是简单的贴图，而是根据光照，物体的空间分布和大小形状，来计算出物体在地面或者其他物体上的投影，所以才能达到以假乱真的强烈视觉体验。 视频编辑也是如此，尤其是渲染动画的时候，同样是通过GPU强大的计算能力，才能将各种指令设置，变成我们所见到的各种形形色色的特效。 最后，我们知道科研过程的关键有一点就是计算，既然GPU有这如此强大的能力（有多强大，我们稍后介绍），如果可以用于科研，岂不是很强大的工具？ 除此之外，GPU还可以用于轨道模拟，3D建模等等，总之只要是需要计算的地方，都很可能可以通过使用GPU来优化。</p>
<p>那么GPU的计算能力有多强呢？这里引用NVIDIA的一张图像来说明。</p>
<p><img data-src="http://cdn.miaoerduo.com/images/012516_1253_CUDA.png" alt="cuda" /></p>
<center>
图1 CPU和GPU的每秒钟的浮点数处理次数
</center>
<p>这是一张摘自NVIDIA官网的一张图，有木有很吃惊。随着时间的推进，GPU的发展速度远超过CPU，目前GPU的计算能力已然超过CPU几十倍！ 单从构造上说，一个GPU通常会有16个SM（可以理解为CPU的核，目前CPU常见的是双核和四核），每个SM又同时存在32个SP（现在应该变成了更多数目的CUDA core），即可以同时处理32个数（CPU一个核一次只能处理一个数），另外GPU拥有远多于CPU的寄存器和运算单元。除此之外，GPU的指令模式（SPMD）和CPU（MIMD）完全不同，内存管理也最大化的显示的交给程序员来掌控。如果设计合理，将程序加速100倍也毫不过分。</p>
<p>这就是GPU，隐藏在我们电脑中的超级计算器。</p>
<h2 id="二我们如何使用gpu">二、我们如何使用GPU</h2>
<p>早期的GPU主要用于图像处理。而一群脑洞大开的科学家发现GPU的具有强大的计算潜能，如果合理挖掘，定前途无可限量。于是开始了早期的GPU用于科学计算的编程。由于GPU本身主要用于处理对象，因此访问GPU，必须通过相关的图像接口。但是只要，将待处理的数据和运算构造成GPU中定义的图像和操作的形式，那么GPU就可以求解。</p>
<p>从而就出现了两个问题：</p>
<ol type="1">
<li>早期的GPU只支持OpenGL等图像处理接口，因此，在用于计算之前，我们不得不先学习图像操作的这些知识。</li>
<li>我们掌握了图像接口的编程以后，还要把我们要处理的问题合理设计成能够用图像处理方式解决的问题。</li>
</ol>
<p>从各种意义上来说，这都不是一件很容易的事情。 那么，既然我们对GPU的计算能力如此垂涎，为什么不能创造出适合普通程序员编写GPU程序的方法呢？</p>
<p>当然可以！</p>
<p>这时就不得不提NVIDIA这家公司。我们的显卡通常分为两类，A卡和N卡。A是AMD，N就是NVIDIA（最近似乎AMD也开始支持CUDA了）。NVIDIA为了让GPU变得更通用，便选取了当时最流行的C语言作为基本语言（现在也很流行），通过对C语言进行拓展（增加了一些关键字等），实现了使用C语言编写GPU程序的壮举，同时很好的封装了GPU的图像接口，抽象出独立于图像的程序和内存模型，让程序员们在不了解图像接口的情况下，也能编写出高效的GPU程序，受过广大程序员，科研人员的宠爱！这就是CUDA！一个我们最熟悉的全新的语言！</p>
<p>目前CUDA已经支持C，C++和Python。并且有了很多快速运算的代数库，可以满足大多数的应用的需求。</p>
<h2 id="三cuda学习会不会很难">三、CUDA学习会不会很难</h2>
<p>最后，当然就要说到如何学习CUDA。</p>
<p>之前也提到过，CUDA语言就是拓展版的C语言，语法上没有什么难度。我们只需要知道几个拓展修饰关键字，以及工作方式即可。之后我们需要学习CUDA的硬件结构与线程网格模型。再然后是最重要的主要的内存（GPU上的内存，通常也称为显存）的知识。GPU有寄存器内存，共享内存，全局内存，常量内存，纹理内存五种，每种都有其独特的特性。熟悉各种内存的特性，会对之后的GPU编程有极大地帮助。最后了解CUDA编程的常用的算法策略，熟悉CUDA的代数库，多显卡编程等。</p>
<p>小喵会一直持续更新该类别的博客，学习并分享。</p>
<p>以上便是这篇博客的主要内容。</p>
<p><strong>最后，转载请注明出处。</strong></p>
<p>欢迎转载和分享~</p>
]]></content>
      <categories>
        <category>C++</category>
        <category>CUDA</category>
      </categories>
  </entry>
  <entry>
    <title>新浪微博爬虫实现</title>
    <url>/2015/12/18/sina-crawler/</url>
    <content><![CDATA[<blockquote>
<p>小喵的唠叨话：由于项目需要（其实是个小兼职），需要抓取新浪微博的用户的微博数据。小喵在努力研究了一晚上之后，初步掌握的抓取的方法。所谓聪明的喵不吃独食，特来与大家分享。作为小喵的第一篇博客，小喵努力写了很久，如果写作上有什么不清晰的地方，也欢迎大家指正。</p>
</blockquote>
<a id="more"></a>
<h2 id="一分析新浪微博的网页">一、分析新浪微博的网页</h2>
<p>这里选择手机版的新浪微博，即：<span class="exturl" data-url="aHR0cDovL20ud2VpYm8uY24=" title="http://m.weibo.cn">http://m.weibo.cn<i class="fa fa-external-link"></i></span>。原因如下：手机版的微博内容和电脑版的一样，但是信息比较直接，并且手机网页的验证等操作也要较电脑版简单。故为了便于分析，选择手机微博是一个明智的选择。</p>
<p>登录进微博，随便打开一个关注的微博，查看其主页网址（例如：<span class="exturl" data-url="aHR0cDovL20ud2VpYm8uY24vdS8xNjMwNDYxNzU0" title="http://m.weibo.cn/u/1630461754">http://m.weibo.cn/u/1630461754<i class="fa fa-external-link"></i></span>）。</p>
<p>对比可以分析出：</p>
<ol type="1">
<li>每个用户应该有唯一的网址（当然有的用户有自己的个性域名），而这个网址的最后面的数字很可能是用户的唯一标示。（在后面，我们也验证了该数字确实是用户的uid）</li>
<li>所有的微博主页都是以<code>http://m.weibo.cn/u/{uid}</code>的形式（这不绝对，因为实验中确实发现的别的类型的网址，但大多数都是这个样子）。</li>
</ol>
<p>之后，主页中只会显示用户的最新微博信息，这并不是我们期望的。我们点击"查看更多微博"，这样跳转到一个只有微博的页面。</p>
<p><img data-src="http://cdn.miaoerduo.com/images/2016-04-18_113632.png" alt="2016-04-18_113632" /></p>
<center>
图1 手机微博页面
</center>
<p>在我这里，出现的网址是: <span class="exturl" data-url="aHR0cDovL20ud2VpYm8uY24vcGFnZS90cGw/Y29udGFpbmVyaWQ9MTAwNTA1MTYzMDQ2MTc1NF8tX1dFSUJPX1NFQ09ORF9QUk9GSUxFX1dFSUJPJmFtcDtpdGVtaWQ9JmFtcDt0aXRsZT0lRTUlODUlQTglRTklODMlQTglRTUlQkUlQUUlRTUlOEQlOUE=" title="http://m.weibo.cn/page/tpl?containerid=1005051630461754_-_WEIBO_SECOND_PROFILE_WEIBO&amp;itemid=&amp;title=%E5%85%A8%E9%83%A8%E5%BE%AE%E5%8D%9A">http://m.weibo.cn/page/tpl?containerid=1005051630461754_-_WEIBO_SECOND_PROFILE_WEIBO&amp;itemid=&amp;title=%E5%85%A8%E9%83%A8%E5%BE%AE%E5%8D%9A<i class="fa fa-external-link"></i></span></p>
<p>具体含义我们暂时不需要理解。</p>
<p>别走开，真正的黑科技要出现了。通过滚动页面，可以发现，页面在动态加载。看来为了用户体验，微博做了很多功夫。学习过一段时间的网页开发的同学都应该知道，网页不刷新，内容却发生改变，这很可能是使用Ajax技术。通过Ajax，对额外的微博数据进行加载，之后通过Dom操作，追加到页面的微博数据的最下面。</p>
<p>从上面的分析可知，如果我们能够查询到追加的数据的获取方式，就很可能实现微博内容的获取。</p>
<p>这里介绍一个常用的分析方法。打开chrome的控制台（事实上，firefox，IE都支持这个功能），在network下，查看数据加载情况。这里建议清空所有数据，然后滚动屏幕，这样很容易就能获得最新加载的数据了。</p>
<p>这里是我的截图：</p>
<p><img data-src="http://cdn.miaoerduo.com/images/2016-04-18_113843.png" alt="2016-04-18_113843" /></p>
<center>
图2 浏览器抓包
</center>
<p>很容易注意到名为json的这个数据包。复制并打开他的连接。这时候在浏览器中，出现了许多乱七八糟的东西 （这里的网址是：<code>http://m.weibo.cn/page/json?containerid=1005051630461754_-\_WEIBO\_SECOND\_PROFILE\_WEIBO&amp;page=3）</code>。这时候需要我们仔细辨认，你会发现，除了文字很奇怪之外，其他的东西，很明显包含了我们需要的东西。</p>
<p><img data-src="http://cdn.miaoerduo.com/images/2016-04-18_113958.png" alt="2016-04-18_113958" /></p>
<center>
图3 JSON包
</center>
<p>对！这就是我们所期望的！Ajax动态加载的数据！</p>
<p>对这个url做简单的实验和分析。可以知道：</p>
<ol type="1">
<li><code>http://m.weibo.cn/page/json</code>，这是数据的请求源，是固定的。用来区分资源的是后面的参数。</li>
<li><code>containerid=1005051630461754_-\_WEIBO\_SECOND\_PROFILE\_WEIBO</code>，这个形式也是几乎不变的。<code>1005051630461754</code>这个用来标志用户，试验了几次之后，我才发现了其中的规律：<code>1005051630461754 = 100505 + uid</code>！也就是说，只要知道用户的uid，很容易构造这个字段。</li>
<li><code>page=3</code>，明显用来表示请求的微博的页面。试了一下，如果把参数设为1，就是最新的微博的数据。聪明的你一定想到了通过改变page的数值，就可以轻松地抓取用户的所有微博！</li>
</ol>
<p>总结，我们知道，通过访问<code>http://m.weibo.cn/page/json?containerid=100505{uid}_-\_WEIBO\_SECOND\_PROFILE\_WEIBO&amp;page={page_num}</code>，就可以得到指定用户的指定页面的微博。</p>
<p>到此，我们简单的分析的微博的页面，并知道了数据的来源。之后便可以较为顺利的爬取用户的数据了。</p>
<p><strong><em>但是，难道到这里就结束了吗？</em></strong></p>
<p>不会这么简单。程序员的本质就是将一切做到极致。</p>
<h2 id="二获得用户的uid">二、获得用户的uid</h2>
<p>为了更机械化的抓取用户数据，我们可以将获取uid的任务，也编程实现。这里主要用三种方法：</p>
<ol type="1">
<li>知道用户的主页地址，比如：<span class="exturl" data-url="aHR0cDovL20ud2VpYm8uY24vdS8xNjMwNDYxNzU0" title="http://m.weibo.cn/u/1630461754">http://m.weibo.cn/u/1630461754<i class="fa fa-external-link"></i></span>，可以直接得到<code>uid</code>。</li>
<li>知道用户的昵称，通过新浪的api，<span class="exturl" data-url="aHR0cDovL29wZW4ud2VpYm8uY29tL3dpa2kvMi91c2Vycy9zaG93" title="http://open.weibo.com/wiki/2/users/show">http://open.weibo.com/wiki/2/users/show<i class="fa fa-external-link"></i></span>，可以获取用户的数据，包括<code>uid</code>。</li>
<li>知道用户的个性域名，通过新浪的api，<span class="exturl" data-url="aHR0cDovL29wZW4ud2VpYm8uY29tL3dpa2kvMi91c2Vycy9kb21haW5fc2hvdw==" title="http://open.weibo.com/wiki/2/users/domain_show">http://open.weibo.com/wiki/2/users/domain_show<i class="fa fa-external-link"></i></span>，即可获取。</li>
</ol>
<p>新浪的api需要，自己创建应用，然后授权等等。可以参考新浪微博官网的文档<span class="exturl" data-url="aHR0cDovL29wZW4ud2VpYm8uY29tL3dpa2kv5b6u5Y2aQVBJ" title="http://open.weibo.com/wiki/微博API">http://open.weibo.com/wiki/微博API<i class="fa fa-external-link"></i></span>，这里不再累述。</p>
<h2 id="三数据抓取解析">三、数据抓取解析</h2>
<blockquote>
<p>数据的抓取可以使用任意的支持HTTP通信的语言，注意抓取的频率就好，太快了可能会被系统认识到是爬虫 <em>虽然我们确实是</em> ，建议至少间隔1~2s。前面两步得到的数据都是JSON格式的，<strong>JSON</strong>（<strong>J</strong>ava<strong>S</strong>cript <strong>O</strong>bject <strong>N</strong>otation）是一种由轻量级的数据交换语言。基本上每种语言都对JSON有很好的支持。因此只要对JSON稍作了解，都可以快速解析。可以查看wiki的相关介绍：<span class="exturl" data-url="aHR0cHM6Ly96aC53aWtpcGVkaWEub3JnL3dpa2kvSlNPTg==" title="https://zh.wikipedia.org/wiki/JSON">https://zh.wikipedia.org/wiki/JSON<i class="fa fa-external-link"></i></span>。</p>
</blockquote>
<p>以上，便是微博的数据的抓取思路。这里仅供参考。有许多东西理解的可能还不够透彻。比如：100505是怎么来的，获取uid的没考虑到的情况（这个确实碰到了一个，但是暂时还没有研究）等等，如果以后有新的发现，一定会再次补充。</p>
<p><strong>最后，转载请注明出处。</strong></p>
<p>欢迎转载和分享~</p>
]]></content>
      <categories>
        <category>Miscellanea</category>
      </categories>
  </entry>
</search>
